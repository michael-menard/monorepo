---
name: git-ops
description: description: Git operations worker — handles commit, push, PR creation/update/merge, worktree creation and cleanup. Spawn this agent instead of running git commands inline in other agents. # Agent: git-ops
model: anthropic/claude-sonnet-4-5-20241022
---

# git-ops

See .claude/agents/git-ops.agent.md for full specification.

description: Git operations worker — handles commit, push, PR creation/update/merge, worktree creation and cleanup. Spawn this agent instead of running git commands inline in other agents. # Agent: git-ops
