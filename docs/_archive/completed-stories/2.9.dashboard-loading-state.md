# Story 2.9: Dashboard Loading State

## Status

Approved

## Story

**As a** user,
**I want** skeleton loaders while dashboard loads,
**so that** I see feedback during data fetch.

## Acceptance Criteria

1. ⬜ DashboardSkeleton component created
2. ⬜ Skeleton for stats cards
3. ⬜ Skeleton for recent MOCs grid
4. ⬜ Smooth appearance animation
5. ⬜ Matches layout of loaded content

## Tasks / Subtasks

- [ ] **Task 1: Stats Skeleton**
  - [ ] 3 card-shaped skeletons
  - [ ] Animated pulse effect

- [ ] **Task 2: MOCs Grid Skeleton**
  - [ ] 5 card-shaped skeletons
  - [ ] Same size as real cards

- [ ] **Task 3: Integration**
  - [ ] Show while isLoading
  - [ ] Transition to real content

## Dev Notes

### Component Structure

```typescript
export function DashboardSkeleton() {
  return (
    <div className="space-y-8">
      {/* Stats Skeleton */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {[1, 2, 3].map((i) => (
          <Skeleton key={i} className="h-24 rounded-lg" />
        ))}
      </div>

      {/* Recent MOCs Skeleton */}
      <div>
        <Skeleton className="h-6 w-32 mb-4" />
        <div className="grid grid-cols-5 gap-4">
          {[1, 2, 3, 4, 5].map((i) => (
            <Skeleton key={i} className="h-40 rounded-lg" />
          ))}
        </div>
      </div>
    </div>
  )
}
```

### Usage

```typescript
if (isLoading) {
  return <DashboardSkeleton />
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
