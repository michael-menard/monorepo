# Story 5.9: Wishlist Grid View

## Status

Approved

## Story

**As a** user,
**I want** a grid view of my wishlist,
**so that** I can see more items at once.

## Acceptance Criteria

1. ⬜ WishlistGridView component created
2. ⬜ Responsive grid (2/3/4 columns)
3. ⬜ Consistent card sizes
4. ⬜ Handles empty state
5. ⬜ Same actions as list view

## Tasks / Subtasks

- [ ] **Task 1: Create Component**
  - [ ] Create WishlistGridView.tsx
  - [ ] Responsive grid layout

## Dev Notes

```typescript
interface WishlistGridViewProps {
  items: WishlistItem[]
  onEdit: (item: WishlistItem) => void
  onDelete: (item: WishlistItem) => void
}

export function WishlistGridView({ items, onEdit, onDelete }: WishlistGridViewProps) {
  if (items.length === 0) {
    return <WishlistEmptyState />
  }

  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
      {items.map((item) => (
        <WishlistItemCard
          key={item.id}
          item={item}
          onEdit={() => onEdit(item)}
          onDelete={() => onDelete(item)}
          compact
        />
      ))}
    </div>
  )
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
