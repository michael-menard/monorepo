# Story 4.3: Create MOC Endpoint

## Status

Approved

## Story

**As a** user,
**I want** to create new MOCs via API,
**so that** I can add to my collection.

## Acceptance Criteria

1. ⬜ `useCreateMocMutation` hook available
2. ⬜ POST /mocs with MOC data
3. ⬜ Returns created MOC with ID
4. ⬜ Invalidates Mocs cache
5. 🔄 Backend: POST /mocs endpoint required

## Tasks / Subtasks

- [ ] **Task 1: Define Mutation**
  - [ ] Add createMoc mutation
  - [ ] Define input type
  - [ ] Configure invalidation

## Dev Notes

```typescript
createMoc: builder.mutation<MocDetail, CreateMocInput>({
  query: (data) => ({
    url: '/mocs',
    method: 'POST',
    body: data,
  }),
  invalidatesTags: ['Mocs'],
}),

interface CreateMocInput {
  title: string
  description?: string
  theme: string
  tags: string[]
  pieceCount?: number
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
