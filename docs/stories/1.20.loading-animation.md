# Story 1.20: Loading Animation

## Status

Approved

## Story

**As a** user,
**I want** a branded loading animation,
**so that** I know the app is working during loads.

## Acceptance Criteria

1. ✅ LoadingPage component exists
2. ⬜ LEGO-themed loading animation
3. ⬜ Centered in viewport
4. ⬜ Optional loading text
5. ⬜ Smooth fade-in animation
6. ⬜ Accessible (aria-busy, role)

## Tasks / Subtasks

- [ ] **Task 1: Verify Component Exists** (AC: 1)
  - [x] Check routes/pages/LoadingPage.tsx exists
  - [ ] Review current implementation

- [ ] **Task 2: Create LEGO Animation** (AC: 2)
  - [ ] Design spinning LEGO brick or building animation
  - [ ] Use CSS animation or Framer Motion
  - [ ] Use brand colors (primary red, accent yellow)

- [ ] **Task 3: Layout & Animation** (AC: 3, 5)
  - [ ] Center in viewport (flex/grid)
  - [ ] Add fade-in on mount
  - [ ] Consider pulse or bounce effect

- [ ] **Task 4: Loading Text** (AC: 4)
  - [ ] Accept optional message prop
  - [ ] Default to "Loading..."
  - [ ] Animate text (dots, fade)

- [ ] **Task 5: Accessibility** (AC: 6)
  - [ ] Add role="status"
  - [ ] Add aria-busy="true"
  - [ ] Add aria-live="polite"

## Dev Notes

### Existing Implementation

**File:** `apps/web/main-app/src/routes/pages/LoadingPage.tsx`

### Component Structure

```typescript
interface LoadingPageProps {
  message?: string
}

export function LoadingPage({ message = 'Loading...' }: LoadingPageProps) {
  return (
    <div
      className="flex flex-col items-center justify-center min-h-screen"
      role="status"
      aria-busy="true"
      aria-live="polite"
    >
      <div className="animate-spin">
        {/* LEGO brick icon or animation */}
      </div>
      <p className="mt-4 text-muted-foreground">{message}</p>
    </div>
  )
}
```

### LEGO Brick Animation Ideas

1. **Spinning brick:** Single brick rotating
2. **Stacking bricks:** Bricks building up
3. **Pulsing logo:** Logo with pulse effect
4. **Bouncing studs:** LEGO studs bouncing

### CSS Animation Example

```css
@keyframes brick-spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.loading-brick {
  animation: brick-spin 1s linear infinite;
}
```

### Testing

- Test component renders
- Test custom message displays
- Test accessibility attributes
- Visual test animation

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
