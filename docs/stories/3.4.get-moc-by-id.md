# Story 3.4: Get MOC by ID Endpoint

## Status

Approved

## Story

**As a** user,
**I want** to fetch a single MOC's details,
**so that** I can view its full information.

## Acceptance Criteria

1. ⬜ `useGetMocByIdQuery` hook available
2. ⬜ Fetches GET /mocs/:id
3. ⬜ Returns full MOC with files
4. ⬜ Cache by ID
5. 🔄 Backend: /mocs/:id endpoint required

## Tasks / Subtasks

- [ ] **Task 1: Define Endpoint**
  - [ ] Add getMocById query
  - [ ] Accept id param
  - [ ] Define full MOC type

## Dev Notes

```typescript
getMocById: builder.query<MocDetail, string>({
  query: (id) => `/mocs/${id}`,
  providesTags: (result, error, id) => [{ type: 'MocDetail', id }],
}),

interface MocDetail {
  id: string
  title: string
  description: string
  tags: string[]
  theme: string
  pieceCount: number
  images: MocImage[]
  files: MocFile[]
  createdAt: string
  updatedAt: string
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
