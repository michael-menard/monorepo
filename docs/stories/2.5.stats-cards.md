# Story 2.5: Stats Cards Component

## Status

Ready for Review

## Story

**As a** user,
**I want** to see stats displayed as cards,
**so that** I get a quick overview of my collection.

## Acceptance Criteria

1. ✅ StatsCards component created
2. ✅ Displays: Total MOCs, Wishlist Items, Themes
3. ✅ Each card has icon, label, value
4. ✅ Uses LEGO brand colors
5. ✅ Responsive grid layout (1/2/3 columns)

## Tasks / Subtasks

- [x] **Task 1: Create Component**
  - [x] Create StatsCards.tsx
  - [x] Accept stats data as props

- [x] **Task 2: Card Design**
  - [x] Use @repo/ui Card
  - [x] Add Lucide icons
  - [x] Apply brand styling

- [x] **Task 3: Responsive Layout**
  - [x] Mobile: 1 column
  - [x] Tablet: 2 columns
  - [x] Desktop: 3 columns

## Dev Notes

### Component Structure

```typescript
interface StatsCardsProps {
  stats: DashboardStats
  isLoading?: boolean
  error?: Error | null
}

export function StatsCards({ stats, isLoading = false, error = null }: StatsCardsProps) {
  // Animated counters with framer-motion
  // Loading skeleton state
  // Error state display
  // Empty state when all values are zero
  // Responsive grid: 1 col (mobile) / 2 cols (tablet) / 3 cols (desktop)
}
```

### Features Implemented

- Animated counter with framer-motion (smooth count-up animation)
- Loading skeleton state
- Error state with message display
- Empty state when all stats are zero
- LEGO brand colors (red, blue, yellow)
- Accessible with ARIA labels
- Hover effects with scale transform

## Dev Agent Record

### File List

| File | Action |
| ---- | ------ |
| `packages/core/ui/src/stats-cards.tsx` | Created |
| `packages/core/ui/src/__tests__/stats-cards.test.tsx` | Created |
| `packages/core/ui/src/index.ts` | Modified |
| `apps/web/main-app/src/components/Dashboard/StatsCards.tsx` | Modified (wrapper) |
| `apps/web/main-app/src/components/Dashboard/__tests__/StatsCards.test.tsx` | Modified |
| `apps/web/main-app/src/test/fixtures/dashboard.ts` | Created |
| `apps/web/main-app/src/test/fixtures/index.ts` | Created |
| `apps/web/main-app/src/test/setup.ts` | Modified |

### Debug Log References

None

### Completion Notes

- Created generic `StatsCards` component in `@repo/ui` package
- Component accepts `StatItem[]` with configurable icons, labels, values, and colors
- Supports loading, error, and empty states with customizable messages
- Animated counters using framer-motion
- Accessible with ARIA labels
- Responsive grid layout (1/2/3 columns)
- Dashboard wrapper component uses LEGO-specific branding (red/blue/yellow)
- 16 tests in @repo/ui, 12 tests in main-app wrapper - all passing
- Created reusable test fixtures for dashboard tests

### Agent Model Used

claude-opus-4-5-20250929

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
| 2025-11-30 | 1.0     | Implementation complete with animations, states, tests | Dev Agent |
