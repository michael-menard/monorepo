# Story 2.5: Stats Cards Component

## Status

Approved

## Story

**As a** user,
**I want** to see stats displayed as cards,
**so that** I get a quick overview of my collection.

## Acceptance Criteria

1. ⬜ StatsCards component created
2. ⬜ Displays: Total MOCs, Wishlist Items, Themes
3. ⬜ Each card has icon, label, value
4. ⬜ Uses LEGO brand colors
5. ⬜ Responsive grid layout (1/2/3 columns)

## Tasks / Subtasks

- [ ] **Task 1: Create Component**
  - [ ] Create StatsCards.tsx
  - [ ] Accept stats data as props

- [ ] **Task 2: Card Design**
  - [ ] Use @repo/ui Card
  - [ ] Add Lucide icons
  - [ ] Apply brand styling

- [ ] **Task 3: Responsive Layout**
  - [ ] Mobile: 1 column
  - [ ] Tablet: 2 columns
  - [ ] Desktop: 3 columns

## Dev Notes

### Component Structure

```typescript
interface StatsCardsProps {
  stats: DashboardStats
}

export function StatsCards({ stats }: StatsCardsProps) {
  const items = [
    { icon: Blocks, label: 'Total MOCs', value: stats.totalMocs },
    { icon: Heart, label: 'Wishlist', value: stats.wishlistCount },
    { icon: Palette, label: 'Themes', value: stats.themeCount },
  ]

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {items.map((item) => (
        <Card key={item.label}>
          <item.icon className="h-8 w-8 text-primary" />
          <p className="text-2xl font-bold">{item.value}</p>
          <p className="text-muted-foreground">{item.label}</p>
        </Card>
      ))}
    </div>
  )
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
