# Story 3.25: Gallery Loading States

## Status

Approved

## Story

**As a** user,
**I want** skeleton loaders while gallery loads,
**so that** I see visual feedback during fetches.

## Acceptance Criteria

1. ⬜ GallerySkeleton for grid view
2. ⬜ MocDetailSkeleton for detail page
3. ⬜ Animated pulse effect
4. ⬜ Matches actual layout dimensions
5. ⬜ Smooth transitions

## Tasks / Subtasks

- [ ] **Task 1: Grid Skeleton**
  - [ ] Create GallerySkeleton.tsx
  - [ ] Multiple card skeletons

- [ ] **Task 2: Detail Skeleton**
  - [ ] Create MocDetailSkeleton.tsx
  - [ ] Header, image, files areas

## Dev Notes

```typescript
export function GallerySkeleton() {
  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} className="space-y-2">
          <Skeleton className="aspect-[4/3] rounded-lg" />
          <Skeleton className="h-4 w-3/4" />
          <Skeleton className="h-3 w-1/2" />
        </div>
      ))}
    </div>
  )
}

export function MocDetailSkeleton() {
  return (
    <div className="space-y-6">
      <Skeleton className="h-8 w-1/2" />
      <Skeleton className="h-4 w-1/4" />
      <Skeleton className="aspect-[4/3] rounded-lg" />
      <div className="flex gap-2">
        {Array.from({ length: 5 }).map((_, i) => (
          <Skeleton key={i} className="h-16 w-16 rounded" />
        ))}
      </div>
    </div>
  )
}
```

## Change Log

| Date       | Version | Description   | Author   |
| ---------- | ------- | ------------- | -------- |
| 2025-11-28 | 0.1     | Initial draft | SM Agent |
