# Production Environment Configuration
# This file contains non-sensitive configuration values for the production environment

# Frontend Infrastructure (Update these when you deploy production)
S3_BUCKET_NAME=lego-moc-frontend-production-213351177820
CLOUDFRONT_DISTRIBUTION_ID=EXAMPLE_PROD_DISTRIBUTION_ID
FRONTEND_URL=https://app.yourdomain.com

# API Configuration (when backend services are deployed)
VITE_AUTH_API_URL=https://auth-api.yourdomain.com/api/auth
VITE_LEGO_API_URL=https://api.yourdomain.com
VITE_API_URL=https://api.yourdomain.com

# Build Configuration
VITE_ENVIRONMENT=production
NODE_ENV=production

# AWS Configuration
AWS_REGION=us-east-1
CDK_DEFAULT_REGION=us-east-1
