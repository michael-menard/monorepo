deferred_at: "2026-02-26T00:00:00Z"
reason: "psql not available in current shell environment during PM story generation"
retry_query: |
  INSERT INTO stories (
    story_id, feature, title, story_dir, story_file, story_type,
    points, priority, state, touches_backend, touches_frontend,
    touches_database, touches_infra
  ) VALUES (
    'KFMB-4020',
    'kb-first-migration',
    'Stage Directory Elimination — precondition-check, context-init, and Script State Detection',
    'plans/future/platform/kb-first-migration/backlog/KFMB-4020',
    'plans/future/platform/kb-first-migration/backlog/KFMB-4020/KFMB-4020.md',
    'tech_debt',
    5,
    'medium',
    'backlog',
    true,
    false,
    false,
    false
  )
  ON CONFLICT (story_id) DO UPDATE SET
    title = EXCLUDED.title,
    story_dir = EXCLUDED.story_dir,
    story_file = EXCLUDED.story_file,
    story_type = EXCLUDED.story_type,
    points = EXCLUDED.points,
    priority = EXCLUDED.priority,
    state = EXCLUDED.state,
    touches_backend = EXCLUDED.touches_backend,
    touches_frontend = EXCLUDED.touches_frontend,
    touches_database = EXCLUDED.touches_database,
    touches_infra = EXCLUDED.touches_infra;
connection: "postgresql://kbuser:TestPassword123!@localhost:5433/knowledgebase"

# TTL stamp — added by import-deferred-writes
processed_at: "2026-03-08T23:55:28.324Z"
imported: true
