deferred_at: "2026-02-26T00:00:00Z"
reason: "KB MCP tool unavailable — nested Claude Code session prevents claude -p subprocess calls; psql binary not installed"
retry_instructions: >
  Run the following SQL against the KB PostgreSQL at port 5433, or use the
  kb_update_story / kb_create_story MCP tools in a fresh Claude Code session:

  INSERT INTO stories (
    story_id, feature, title, story_dir, story_file, story_type,
    points, priority, state, touches_backend, touches_frontend,
    touches_database, touches_infra
  ) VALUES (
    'KFMB-6010',
    'kb-first-migration',
    'Script Modernization — Remove Filesystem Fallbacks and Consolidate KB-First Helpers',
    'plans/future/platform/kb-first-migration/backlog/KFMB-6010',
    'plans/future/platform/kb-first-migration/backlog/KFMB-6010/KFMB-6010.md',
    'tech_debt',
    3,
    'medium',
    'backlog',
    false,
    false,
    false,
    false
  )
  ON CONFLICT (story_id) DO UPDATE SET
    title = EXCLUDED.title,
    story_dir = EXCLUDED.story_dir,
    story_file = EXCLUDED.story_file,
    story_type = EXCLUDED.story_type,
    points = EXCLUDED.points,
    priority = EXCLUDED.priority,
    state = EXCLUDED.state,
    touches_backend = EXCLUDED.touches_backend,
    touches_frontend = EXCLUDED.touches_frontend,
    touches_database = EXCLUDED.touches_database,
    touches_infra = EXCLUDED.touches_infra;

story_data:
  story_id: KFMB-6010
  feature: kb-first-migration
  title: "Script Modernization — Remove Filesystem Fallbacks and Consolidate KB-First Helpers"
  story_dir: plans/future/platform/kb-first-migration/backlog/KFMB-6010
  story_file: plans/future/platform/kb-first-migration/backlog/KFMB-6010/KFMB-6010.md
  story_type: tech_debt
  points: 3
  priority: medium
  state: backlog
  touches_backend: false
  touches_frontend: false
  touches_database: false
  touches_infra: false
