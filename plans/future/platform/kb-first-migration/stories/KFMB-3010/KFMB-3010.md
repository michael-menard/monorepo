---
id: KFMB-3010
title: "Eliminate stories.index.md — Agent Updates"
status: ready-to-work
priority: medium
phase: 3
experiment_variant: "control"
depends_on:
  - KFMB-2010
  - KFMB-2020
  - KFMB-1020
infrastructure:
  - MCP Tools
  - Agents
sizing_warning: true
story_points: 5
risk_notes: "Large blast radius (~32 agents). Risk of missed references."

predictions:
  split_risk: 0.9
  review_cycles: 3
  token_estimate: 200000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-26T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"

pm_artifacts:
  test_plan:
    strategy: manual+grep-audit
    scope:
      endpoints: []
      ui_touched: false
      data_touched: false
      agents_touched: 32
      infra_touched: false
    happy_path_tests:
      - id: HP-1
        title: "pm-story-followup-leader uses kb_list_stories for ID allocation (AC-1)"
        setup: "Open .claude/agents/pm-story-followup-leader.agent.md"
        action: "Review Phase 3/3.5 collision detection section; verify reference to kb_list_stories({feature, limit: 100}) and MAX(story_id_numeric)+10 calculation"
        expected: "No grep match for 'stories.index.md' in this file; kb_list_stories call present"
        evidence: "grep 'stories.index.md' .claude/agents/pm-story-followup-leader.agent.md returns no output"
      - id: HP-2
        title: "pm-story-split-leader uses kb_list_stories for ID allocation (AC-2)"
        setup: "Open .claude/agents/pm-story-split-leader.agent.md"
        action: "Review ID-allocation section; verify kb_list_stories pattern matches AC-1 pattern"
        expected: "No grep match for 'stories.index.md'; kb_list_stories call present"
        evidence: "grep 'stories.index.md' .claude/agents/pm-story-split-leader.agent.md returns no output"
      - id: HP-3
        title: "elab-epic-updates-leader writes stories to KB (AC-3)"
        setup: "Open .claude/agents/elab-epic-updates-leader.agent.md"
        action: "Verify all story write operations use kb_create_story or kb_update_story_status"
        expected: "No grep match for 'stories.index.md'; kb_create_story and/or kb_update_story_status present"
        evidence: "grep 'stories.index.md' .claude/agents/elab-epic-updates-leader.agent.md returns no output"
      - id: HP-4
        title: "audit-promote-leader deduplicates via KB (AC-4)"
        setup: "Open .claude/agents/audit-promote-leader.agent.md"
        action: "Verify dedup logic uses kb_list_stories or kb_search instead of reading plans/future/*/stories.index.md glob"
        expected: "No grep match for 'stories.index.md'; kb_list_stories or kb_search present in dedup section"
        evidence: "grep 'stories.index.md' .claude/agents/audit-promote-leader.agent.md returns no output"
      - id: HP-5
        title: "pm-story-generation-leader claims story via kb_update_story_status (AC-5)"
        setup: "Open .claude/agents/pm-story-generation-leader.agent.md"
        action: "Verify Phase 0.6 uses kb_update_story_status and Phase 5 verifies via kb_get_story"
        expected: "No grep match for 'stories.index.md'; kb_update_story_status and kb_get_story present"
        evidence: "grep 'stories.index.md' .claude/agents/pm-story-generation-leader.agent.md returns no output"
      - id: HP-13
        title: "Final grep audit — zero stories.index.md references (AC-13)"
        setup: "Run from monorepo root after all changes applied"
        action: "grep -r 'stories\\.index\\.md' .claude/agents/"
        expected: "Zero matches returned"
        evidence: "Command exits with no stdout output"
    error_cases:
      - id: EC-1
        title: "kb_list_stories call missing feature filter"
        expected: "Every kb_list_stories call in ID-allocation agents includes feature filter"
        evidence: "Code review of all modified agents' kb_list_stories call signatures"
      - id: EC-2
        title: "Partial migration — agent still has both old and new reference"
        expected: "Any agent found must be intentionally in-progress or excluded from scope"
        evidence: "All grep results are accounted for in the migration tracker"
      - id: EC-3
        title: "Agent passes index_path to worker that no longer accepts it"
        expected: "No mismatch between spawner parameter lists and worker input expectations"
        evidence: "Diff review of parent agents that pass context to modified workers"
    risks:
      - "Blast radius: ~32 agent files increase probability of a missed reference"
      - "KB tool parameter names may differ from assumed values — manual schema validation required"
      - "Agents spawned by other agents: parent prompt templates may still pass index_path to updated workers"
      - "No automated test harness for agent .md files — all verification is manual review + grep"
      - "KFMB-2010 and KFMB-2020 must complete before AC-6 and AC-7 can be tested"

  dev_feasibility:
    feasible: true
    confidence: high
    rationale: >
      The work is entirely documentation editing of .md files in .claude/agents/. No TypeScript
      compilation, no DB migrations, no infrastructure changes. The primary challenge is blast
      radius (~32 files), not technical complexity. The KB MCP tools required are already
      available. The main risk is missed references — mitigated by final grep audit (AC-13)
      and incremental subtask grouping.
    complexity: medium
    change_surface:
      areas:
        - ".claude/agents/ (32 .md files)"
      endpoints: []
      deploy_touchpoints:
        - "No deployment required — agent .md files are read at runtime by Claude"
    risks:
      - finding: "~32 files with similar but not identical patterns — one missed reference invalidates AC-13"
        mitigation: "Process agents in named groups (Subtasks 1-10); run grep audit after each group"
      - finding: "Parent agents pass index_path to workers — removing parameter from workers without updating parents breaks the contract"
        mitigation: "For each worker updated in Subtasks 3/6, trace all spawning parents and update their prompt templates"
      - finding: "KFMB-2010 and KFMB-2020 are hard dependencies for AC-6 and AC-7"
        mitigation: "Do Subtask 4 (bootstrap agents) last, only after KFMB-2010 and KFMB-2020 are merged"
    evidence_expectations:
      - "grep -r 'stories\\.index\\.md' .claude/agents/ returns zero results (final audit)"
      - "Each modified agent file has a visible kb_list_stories, kb_get_story, or kb_update_story_status call"
      - "All completion signal strings unchanged from pre-modification versions"
---

# KFMB-3010: Eliminate stories.index.md — Agent Updates

## Context

The current agent ecosystem (~32 `.claude/agents/*.agent.md` files) reads and writes `stories.index.md` as the authoritative source of story state and ID allocation. This is a filesystem artifact that duplicates information now stored in the KB `stories` table. Phase 2 stories (KFMB-2010, KFMB-2020) establish KB-native bootstrap patterns. This story eliminates all remaining `stories.index.md` reads and writes across agents, replacing them with `kb_list_stories`, `kb_get_story`, `kb_update_story_status`, and related KB MCP tools.

The work touches agents across three categories:
1. **ID allocation agents** (`pm-story-followup-leader`, `pm-story-split-leader`): Replace grep-based highest-ID scans with `kb_list_stories` queries sorted by story_id.
2. **State-mutation agents** (`elab-epic-updates-leader`, `audit-promote-leader`, `pm-story-generation-leader`): Replace direct file writes to `stories.index.md` with `kb_update_story_status` / `kb_create_story` calls.
3. **Read-only consumers** (seed agent, elab workers, fanout agents, gap analytics): Replace index file reads with targeted `kb_get_story` or `kb_list_stories` calls; remove `index_path` input parameters where KB lookup is sufficient.

**Reality baseline (2026-02-13):** ~32 agent files reference `stories.index.md`. KB MCP tools (`mcp__knowledge-base__*`) are available. Dependencies KFMB-2010 and KFMB-2020 must complete first to provide canonical KB-native bootstrap patterns.

## Goal

No agent in `.claude/agents/` reads or writes `stories.index.md`. All story state and ID allocation goes through KB MCP tool calls.

## Non-Goals

- Do NOT delete or modify the `stories.index.md` file itself — that is out of scope (addressed in KFMB-3020/3030 cleanup).
- Do NOT update commands (`.claude/skills/`) — that is KFMB-3020.
- Do NOT update shell/Python scripts — that is KFMB-3030.
- Do NOT modify `pm-story-generation-leader.agent.md.bak` or other backup files.
- Do NOT change agent orchestration logic, story content structure, or AC formats.
- Do NOT change the `_shared/work-order-claim.md` file — it references stories.index.md for historical context only.
- Do NOT add KB tools to agents that don't currently reference stories.index.md.
- Do NOT change the psql connection string or KB infrastructure — use existing MCP tools only.

## Scope

### Surfaces

- `.claude/agents/` — ~32 agent `.md` files (documentation edits only)
- No TypeScript source code changes
- No database migrations
- No infrastructure changes

### Agents in Scope (32 files)

**ID-Allocation Agents (2):**
- `pm-story-followup-leader.agent.md`
- `pm-story-split-leader.agent.md`

**State-Mutation Agents (4):**
- `elab-epic-updates-leader.agent.md`
- `audit-promote-leader.agent.md`
- `pm-story-generation-leader.agent.md`
- `pm.agent.md`

**Bootstrap Agents (2 — requires KFMB-2010/2020 first):**
- `pm-bootstrap-setup-leader.agent.md`
- `pm-bootstrap-generation-leader.agent.md`

**Setup/Validation Agents (3):**
- `elab-epic-setup-leader.agent.md`
- `pm-harness-setup-leader.agent.md`
- `reality-intake-collector.agent.md`

**Read-Only Worker Consumers (8):**
- `pm-story-seed-agent.agent.md`
- `pm-draft-test-plan.agent.md`
- `pm-uiux-recommendations.agent.md`
- `pm-dev-feasibility-review.agent.md`
- `elab-analyst.agent.md`
- `story-fanout-pm.agent.md`
- `story-fanout-qa.agent.md`
- `story-fanout-ux.agent.md`

**Audit/Analytics Agents (3):**
- `audit-devils-advocate.agent.md`
- `gap-analytics-agent.agent.md`
- (audit-promote-leader already in State-Mutation group)

**Elab Epic Review Agents (6):**
- `elab-epic-engineering.agent.md`
- `elab-epic-platform.agent.md`
- `elab-epic-product.agent.md`
- `elab-epic-qa.agent.md`
- `elab-epic-security.agent.md`
- `elab-epic-ux.agent.md`

**Remaining Agents (3):**
- `story-attack-agent.agent.md`
- `pm-story-adhoc-leader.agent.md`
- `pm-story-bug-leader.agent.md`

## Acceptance Criteria

- [ ] AC-1: `pm-story-followup-leader` determines the next available story ID by querying `kb_list_stories({feature, limit: 100})` and computing `MAX(story_id_numeric) + 10`, with no reference to `stories.index.md`.
- [ ] AC-2: `pm-story-split-leader` determines the next available story ID via the same KB query pattern as AC-1, with no reference to `stories.index.md`.
- [ ] AC-3: `elab-epic-updates-leader` writes new stories and splits to the KB via `kb_create_story` (or `kb_update_story_status` for existing), removing its direct `stories.index.md` write step while still writing its filesystem `UPDATES-LOG` KB artifact.
- [ ] AC-4: `audit-promote-leader` deduplicates against existing stories by querying `kb_list_stories` (or `kb_search`) instead of reading `plans/future/*/stories.index.md` files.
- [ ] AC-5: `pm-story-generation-leader` claims a story (Phase 0.6) via `kb_update_story_status({story_id, state: "in_progress"})` and verifies status via `kb_get_story`, with no `/index-update` skill call for stories.index.md.
- [ ] AC-6: `pm-bootstrap-setup-leader` collision detection uses `kb_list_stories({feature: prefix})` to check for existing stories instead of checking `stories.index.md` existence on disk. (Dependent on KFMB-2020 pattern.)
- [ ] AC-7: `pm-bootstrap-generation-leader` does not write `stories.index.md` — all story creation goes through `kb_create_story`. (Dependent on KFMB-2010 pattern.)
- [ ] AC-8: Read-only consumer agents (`pm-story-seed-agent`, `pm-draft-test-plan`, `pm-uiux-recommendations`, `pm-dev-feasibility-review`, `elab-analyst`, `story-fanout-*`, `gap-analytics-agent`) retrieve story data from `kb_get_story({story_id})` rather than parsing `stories.index.md` entries. The `index_path` input parameter is replaced with `story_id` (already available).
- [ ] AC-9: `elab-epic-setup-leader` and `pm-harness-setup-leader` validate bootstrap completion by querying `kb_list_stories` for stories with the given prefix rather than checking `stories.index.md` existence.
- [ ] AC-10: `reality-intake-collector` scans for completed stories via `kb_list_stories({state: "completed"})` instead of globbing `plans/future/*/stories.index.md`.
- [ ] AC-11: `audit-devils-advocate` deduplication check queries `kb_search` or `kb_list_stories` instead of reading `plans/future/*/stories.index.md`.
- [ ] AC-12: All updated agents preserve their existing completion signals (`PM COMPLETE`, `SETUP COMPLETE`, `GENERATION COMPLETE`, etc.) — only internal mechanics change.
- [ ] AC-13: No agent in `.claude/agents/` contains a reference to `stories.index.md` as a read or write target after this story completes. (Verification via `grep -r "stories\.index\.md" .claude/agents/` returning zero results.)
- [ ] AC-14: KFMB-1020 (`kb_create_story` MCP tool) is complete and merged before any work begins on ST-2 (AC-3) or ST-4 (AC-7). The `kb_create_story` parameter schema (story_id, feature, title, state, etc.) must be validated against the KFMB-1020 deliverable before writing prose for `elab-epic-updates-leader` or `pm-bootstrap-generation-leader`. If KFMB-1020 is not yet complete, ST-2 (AC-3) and ST-4 (AC-7) are gated and must not proceed.
- [ ] AC-15: `pm-bootstrap-setup-leader.agent.md` filesystem fallback path (introduced by KFMB-2020 AC-4: falls back to checking `stories.index.md` existence on disk if `kb_list_stories` fails) is removed within ST-4's scope. The fallback is replaced with a hard `SETUP BLOCKED` signal with a diagnostic message if `kb_list_stories` fails, rather than silently falling back to a filesystem check. This is required for AC-13 to pass — without this removal, `pm-bootstrap-setup-leader.agent.md` will still contain a `stories.index.md` reference after all other agents are migrated.
- [ ] AC-16: A pre-flight parameter validation step (mirroring KFMB-2020 ST-1) is completed before any agent edits begin. The implementer runs `kb_list_stories` against the dev KB with known KFMB story IDs to confirm the correct filter parameter name (`feature`, `epic`, or `prefix`). The confirmed parameter name is documented and applied consistently across all agent edits in ST-1 (AC-1/2), ST-2 (AC-4), and ST-5 (AC-9). No subtask that writes `kb_list_stories` call patterns may begin until this validation is complete.

## Reuse Plan

- `mcp__knowledge-base__kb_list_stories` — replace grep-based ID scans and dedup glob patterns
- `mcp__knowledge-base__kb_get_story` — replace single-story lookups from index entries
- `mcp__knowledge-base__kb_update_story_status` — replace `/index-update` skill calls and direct index writes
- `mcp__knowledge-base__kb_get_next_story` — replace "find next eligible story" scans
- `mcp__knowledge-base__kb_search` — replace keyword scans over index content
- Collision detection pattern from KFMB-2020 (once complete): `kb_list_stories` filtered by feature prefix
- Story creation pattern from KFMB-2010 (once complete): `kb_create_story` replacing filesystem writes
- Post-KFMB-2010/2020 versions of `pm-bootstrap-generation-leader.agent.md` and `pm-bootstrap-setup-leader.agent.md` serve as canonical reference implementations

## Architecture Notes

- All changes are to `.md` documentation files — no compiled code, no runtime artifacts
- `stories.index.md` file itself is NOT deleted in this story; its content becomes stale after this change completes
- The KB `stories` table is the single source of truth for story state after this migration
- `index_path` parameter is removed from workers that previously needed it for index file reads; workers now receive `story_id` directly and call `kb_get_story` themselves
- Parent orchestrators that passed `index_path` to workers must be updated in the same pass to remove that parameter
- When `pm.agent.md` passes context to generation leaders, replace `Index file: {FEATURE_DIR}/stories.index.md` with `Feature: {FEATURE_SLUG}` so workers can construct their own KB queries

## Infrastructure Notes

- KB PostgreSQL: `postgresql://kbuser:TestPassword123!@localhost:5433/knowledgebase`
- KB MCP tool namespace: `mcp__knowledge-base__*`
- All MCP tool calls are made directly from agent markdown instructions — no new infrastructure required
- Protected: `apps/api/knowledge-base/` — KB schema and pgvector setup are NOT modified

## Test Plan

All verification is manual review and grep audit. No unit tests required — agent .md files are not executable code.

### Primary Verification (AC-13)

```bash
grep -r "stories\.index\.md" .claude/agents/
```

This command MUST return zero results after all subtasks complete.

### Per-Agent Verification

For each of the 32 agents in scope, after modification:
```bash
grep "stories.index.md" .claude/agents/<agent-file>.agent.md
# Must return no output
```

### Completion Signal Regression

For each updated agent, compare completion signal strings before and after:
- `PM COMPLETE`, `SETUP COMPLETE`, `GENERATION COMPLETE` strings must be unchanged

### KB Tool Signature Validation

Before writing any replacements, validate KB MCP tool parameter names against the actual tool schema:
- `kb_list_stories` — confirm `feature` or `prefix` filter parameter name
- `kb_get_story` — confirm `story_id` parameter name
- `kb_update_story_status` — confirm `story_id` and `state` parameter names and valid state enum values

### Parent-Worker Contract Check

For each worker updated to remove `index_path`:
1. Identify all agents that spawn this worker
2. Verify those parent agents no longer pass `index_path` to the worker prompt

## Reality Baseline

- **Baseline date**: 2026-02-13
- **KB availability**: KB PostgreSQL running on port 5433; MCP tools registered
- **Agent count**: ~130 agent files in `.claude/agents/`; ~32 reference `stories.index.md`
- **Dependencies**: KFMB-2010 (KB-Native Bootstrap Generation Leader) and KFMB-2020 (KB-Native Bootstrap Setup Leader) must complete before Subtask 4 (bootstrap agents) can be worked
- **KFMB-2030**: In Elaboration — establishes bootstrap KB-native patterns; KFMB-3010 builds on these patterns

## Canonical References

| Pattern | File | Why |
|---------|------|-----|
| Agent with KB tool calls (current best example) | `.claude/agents/elab-epic-setup-leader.agent.md` | Already uses `kb_write_artifact` / `kb_read_artifact`; shows the pattern of replacing filesystem artifacts with KB calls |
| Agent with KB tool calls (reads) | `.claude/agents/elab-epic-updates-leader.agent.md` | Reads context from KB (`kb_read_artifact`) but still writes to `stories.index.md` — exemplifies the before/after pattern for this story |
| Bootstrap generation with dual output | `.claude/agents/pm-bootstrap-generation-leader.agent.md` | Already writes to both filesystem AND KB `stories` table — shows the transitional dual-write pattern |
| Agent collision detection (current) | `.claude/agents/pm-story-followup-leader.agent.md` | Phase 3/3.5 shows the full current collision-detection pattern to be replaced with `kb_list_stories` |

## Subtasks

### ST-1: ID-Allocation Agents — pm-story-followup-leader and pm-story-split-leader
- **Goal**: Replace grep-based highest-ID scan with `kb_list_stories({feature, limit: 100})` + `MAX(id)+10` pattern
- **Files to modify**:
  - `.claude/agents/pm-story-followup-leader.agent.md`
  - `.claude/agents/pm-story-split-leader.agent.md`
- **ACs covered**: AC-1, AC-2
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/pm-story-followup-leader.agent.md .claude/agents/pm-story-split-leader.agent.md | wc -l` = 0
- **Estimated tokens**: 6,000

### ST-2: State-Mutation Agents — elab-epic-updates-leader and audit-promote-leader
- **Goal**: Replace direct stories.index.md writes with `kb_create_story`/`kb_update_story_status` and `kb_list_stories` dedup
- **Files to modify**:
  - `.claude/agents/elab-epic-updates-leader.agent.md`
  - `.claude/agents/audit-promote-leader.agent.md`
- **ACs covered**: AC-3, AC-4
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/elab-epic-updates-leader.agent.md .claude/agents/audit-promote-leader.agent.md | wc -l` = 0
- **Estimated tokens**: 7,000

### ST-3: Generation-Leader Index Claim — pm-story-generation-leader
- **Goal**: Replace Phase 0.6 `/index-update` skill call with `kb_update_story_status` and Phase 5 with `kb_get_story`
- **Files to modify**:
  - `.claude/agents/pm-story-generation-leader.agent.md`
- **ACs covered**: AC-5
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/pm-story-generation-leader.agent.md | wc -l` = 0
- **Estimated tokens**: 5,000

### ST-4: Bootstrap Agents — pm-bootstrap-setup-leader and pm-bootstrap-generation-leader
- **Goal**: Replace collision detection with `kb_list_stories` and story creation with `kb_create_story`
- **Files to modify**:
  - `.claude/agents/pm-bootstrap-setup-leader.agent.md`
  - `.claude/agents/pm-bootstrap-generation-leader.agent.md`
- **ACs covered**: AC-6, AC-7
- **Depends on**: ST-1 (pattern reference); REQUIRES KFMB-2010 and KFMB-2020 complete
- **Verification**: `grep 'stories.index.md' .claude/agents/pm-bootstrap-setup-leader.agent.md .claude/agents/pm-bootstrap-generation-leader.agent.md | wc -l` = 0
- **Estimated tokens**: 8,000

### ST-5: Setup/Validation Agents — elab-epic-setup-leader, pm-harness-setup-leader, reality-intake-collector
- **Goal**: Replace stories.index.md existence checks with `kb_list_stories` and completed-story scan with `kb_list_stories({state: 'completed'})`
- **Files to modify**:
  - `.claude/agents/elab-epic-setup-leader.agent.md`
  - `.claude/agents/pm-harness-setup-leader.agent.md`
  - `.claude/agents/reality-intake-collector.agent.md`
- **ACs covered**: AC-9, AC-10
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/elab-epic-setup-leader.agent.md .claude/agents/pm-harness-setup-leader.agent.md .claude/agents/reality-intake-collector.agent.md | wc -l` = 0
- **Estimated tokens**: 6,000

### ST-6: Read-Only Worker Consumers — pm-story-seed-agent and worker PM agents
- **Goal**: Replace index file reads with `kb_get_story({story_id})`; remove `index_path` parameter from inputs
- **Files to modify**:
  - `.claude/agents/pm-story-seed-agent.agent.md`
  - `.claude/agents/pm-draft-test-plan.agent.md`
  - `.claude/agents/pm-uiux-recommendations.agent.md`
  - `.claude/agents/pm-dev-feasibility-review.agent.md`
  - `.claude/agents/elab-analyst.agent.md`
- **ACs covered**: AC-8
- **Depends on**: ST-3 (parent orchestrator must also be updated)
- **Verification**: `grep 'stories.index.md' .claude/agents/pm-story-seed-agent.agent.md .claude/agents/pm-draft-test-plan.agent.md .claude/agents/pm-uiux-recommendations.agent.md .claude/agents/pm-dev-feasibility-review.agent.md .claude/agents/elab-analyst.agent.md | wc -l` = 0
- **Estimated tokens**: 8,000

### ST-7: Fanout, Audit, and Analytics Agents
- **Goal**: Replace index reads with `kb_get_story`/`kb_list_stories`/`kb_search` in story-fanout-*, audit-devils-advocate, gap-analytics-agent
- **Files to modify**:
  - `.claude/agents/story-fanout-pm.agent.md`
  - `.claude/agents/story-fanout-qa.agent.md`
  - `.claude/agents/story-fanout-ux.agent.md`
  - `.claude/agents/audit-devils-advocate.agent.md`
  - `.claude/agents/gap-analytics-agent.agent.md`
- **ACs covered**: AC-8, AC-11
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/story-fanout-pm.agent.md .claude/agents/story-fanout-qa.agent.md .claude/agents/story-fanout-ux.agent.md .claude/agents/audit-devils-advocate.agent.md .claude/agents/gap-analytics-agent.agent.md | wc -l` = 0
- **Estimated tokens**: 7,000

### ST-8: Elab Epic Review Agents — elab-epic-engineering/platform/product/qa/security/ux
- **Goal**: Replace stories.index.md references in all 6 elab-epic-* review agents
- **Files to modify**:
  - `.claude/agents/elab-epic-engineering.agent.md`
  - `.claude/agents/elab-epic-platform.agent.md`
  - `.claude/agents/elab-epic-product.agent.md`
  - `.claude/agents/elab-epic-qa.agent.md`
  - `.claude/agents/elab-epic-security.agent.md`
  - `.claude/agents/elab-epic-ux.agent.md`
- **ACs covered**: AC-13
- **Depends on**: none
- **Verification**: `grep 'stories.index.md' .claude/agents/elab-epic-*.agent.md | wc -l` = 0
- **Estimated tokens**: 6,000

### ST-9: Remaining Agents and Parent Orchestrators — pm-story-adhoc-leader, pm-story-bug-leader, story-attack-agent, pm.agent.md
- **Goal**: Update remaining agents including `pm.agent.md` which passes `index_path` to leaders; replace with `feature` slug
- **Files to modify**:
  - `.claude/agents/pm-story-adhoc-leader.agent.md`
  - `.claude/agents/pm-story-bug-leader.agent.md`
  - `.claude/agents/story-attack-agent.agent.md`
  - `.claude/agents/pm.agent.md`
- **ACs covered**: AC-12, AC-13
- **Depends on**: ST-3
- **Verification**: `grep 'stories.index.md' .claude/agents/pm-story-adhoc-leader.agent.md .claude/agents/pm-story-bug-leader.agent.md .claude/agents/story-attack-agent.agent.md .claude/agents/pm.agent.md | wc -l` = 0
- **Estimated tokens**: 7,000

### ST-10: Final Grep Audit — verify zero stories.index.md references across all agents
- **Goal**: Run comprehensive grep audit confirming AC-13
- **Files to read**: `.claude/agents/` (all `*.agent.md` files)
- **Files to modify**: none
- **ACs covered**: AC-13
- **Depends on**: ST-9
- **Verification**: `grep -r 'stories.index.md' .claude/agents/` returns zero matches
- **Estimated tokens**: 2,000

---

## QA Notes

```yaml
qa_notes:
  verdict: CONDITIONAL_PASS
  decided_at: "2026-02-26T00:00:00Z"
  mode: autonomous
  acs_added: 3
  gaps_resolved: 3
  opportunities_logged: 3
  notes:
    - "AC-14 added: KFMB-1020 dependency gate for AC-3/AC-7 subtasks"
    - "AC-15 added: pm-bootstrap-setup-leader filesystem fallback removal in ST-4"
    - "AC-16 added: Pre-flight kb_list_stories filter parameter validation"
    - "Split recommendation logged to KB (HIGH priority) — PM review required"
    - "Story sizing FAIL noted (13 ACs, 30 files, external blockers) but proceeds as single story pending PM split decision"
```
