---
id: KFMB-5010
title: "Migrate _implementation/ Writer Agents to kb_write_artifact"
status: ready-to-work
priority: medium
experiment_variant: "control"
epic: kb-first-migration
phase: 4
story_type: tech_debt
points: 3
sizing_warning: true
dependencies:
  - KFMB-1030
  - KFMB-2040
blocks:
  - KFMB-5020
created: "2026-02-26"
baseline_date: "2026-02-13"
surfaces:
  - agents
touches_backend: false
touches_frontend: false
touches_database: false
touches_infra: false
frontend_impacted: false
pm_artifacts:
  test_plan:
    strategy: manual
    scope:
      endpoints: []
      ui_touched: false
      data_touched: false
    happy_path_tests:
      - id: HP-1
        title: "End-to-end pipeline run — checkpoint written to KB after setup phase"
        setup: "KFMB-1030 and KFMB-2040 are complete. KB running on port 5433. A test story (e.g., TEST-9999) is in backlog state."
        action: "Run /dev-implement-story TEST-9999 through the setup phase using dev-setup-leader."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'checkpoint', phase: 'setup' }) returns the checkpoint artifact. No CHECKPOINT.yaml file is created under _implementation/."
        evidence: "kb_read_artifact response logged; ls _implementation/ shows no YAML files."
      - id: HP-2
        title: "End-to-end pipeline run — scope written to KB after setup phase"
        setup: "Same as HP-1."
        action: "After dev-setup-leader completes its scope write call."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'scope' }) returns the scope artifact."
        evidence: "kb_read_artifact response logged showing populated scope content."
      - id: HP-3
        title: "End-to-end pipeline run — plan written to KB after planning phase"
        setup: "dev-setup-leader has completed. KB contains checkpoint and scope for TEST-9999."
        action: "Run /dev-implement-story through the planning phase using dev-plan-leader."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'plan' }) returns the plan artifact."
        evidence: "kb_read_artifact response logged showing populated plan content."
      - id: HP-4
        title: "End-to-end pipeline run — evidence written to KB after execute phase"
        setup: "Planning phase complete. dev-execute-leader runs implementation phase."
        action: "Run /dev-implement-story through the execute phase."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'evidence' }) returns the evidence artifact."
        evidence: "kb_read_artifact response logged."
      - id: HP-5
        title: "End-to-end pipeline run — review written to KB after code review phase"
        setup: "Execute phase complete. review-aggregate-leader runs."
        action: "Trigger code review phase."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'review' }) returns the review artifact."
        evidence: "kb_read_artifact response logged."
      - id: HP-6
        title: "End-to-end pipeline run — verification written to KB after QA phase"
        setup: "Code review complete. qa-verify-verification-leader or qa-verify-completion-leader runs."
        action: "Trigger QA verification phase."
        expected: "kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'verification' }) returns the verification artifact."
        evidence: "kb_read_artifact response logged showing PASS or FAIL verdict."
      - id: HP-7
        title: "Frontmatter kb_tools updated — no artifact_write in any of the 7 agents"
        setup: "KFMB-5010 implementation complete. Agent files updated."
        action: "Run: grep -rn 'artifact_write' across all 7 target agent files."
        expected: "No matches returned. All 7 files are free of artifact_write references."
        evidence: "grep output shows zero matches."
    error_cases:
      - id: EC-1
        title: "KB write failure — pipeline phase continues gracefully"
        setup: "KB unavailable. dev-setup-leader attempts kb_write_artifact for checkpoint."
        action: "Run dev-setup-leader against a test story while KB is unavailable."
        expected: "Agent logs a warning about KB write failure. Pipeline phase continues to next step without halting."
        evidence: "Agent output includes warning log line. Pipeline proceeds to next phase."
      - id: EC-2
        title: "file_path parameter absent — no _implementation/ YAML files created"
        setup: "KFMB-5010 implementation complete."
        action: "Run a complete /dev-implement-story end-to-end. After completion, check _implementation/ directory."
        expected: "_implementation/ contains no .yaml artifact files. Only BACKEND-LOG.md and FRONTEND-LOG.md may be present."
        evidence: "ls _implementation/ shows no .yaml files."
      - id: EC-3
        title: "dev-implement-backend-coder and dev-implement-frontend-coder unchanged"
        setup: "KFMB-5010 implementation complete."
        action: "Read both coder agent files. Check their kb_tools frontmatter."
        expected: "These two files are unchanged. Their artifact_write frontmatter entry for log file context is still present."
        evidence: "File diff shows no changes to these two agents."
    edge_cases:
      - id: ED-1
        title: "fix_summary artifact written by dev-setup-leader during fix flow"
        setup: "dev-setup-leader is invoked in its fix/retry flow."
        action: "Trigger the fix path in dev-setup-leader that writes fix_summary."
        expected: "kb_read_artifact({ artifact_type: 'fix_summary' }) returns the artifact."
        evidence: "kb_read_artifact response logged."
      - id: ED-2
        title: "qa-verify-completion-leader FAIL branch writes verification"
        setup: "QA verification reaches a FAIL outcome."
        action: "Trigger the FAIL branch in qa-verify-completion-leader."
        expected: "kb_read_artifact for verification returns a verification artifact with FAIL verdict."
        evidence: "kb_read_artifact response logged with verdict: FAIL."
    manual_cases:
      - id: MC-1
        title: "Spot-check: dev-setup-leader frontmatter kb_tools"
        steps:
          - "Open .claude/agents/dev-setup-leader.agent.md"
          - "Read the YAML frontmatter kb_tools list"
        expected: "kb_tools contains kb_write_artifact. kb_tools does NOT contain artifact_write."
      - id: MC-2
        title: "Spot-check: dev-plan-leader call sites — no file_path"
        steps:
          - "Open .claude/agents/dev-plan-leader.agent.md"
          - "Search for all kb_write_artifact call blocks"
          - "Verify no file_path parameter appears"
        expected: "All call blocks use story_id, artifact_type, phase, iteration, content only."
      - id: MC-3
        title: "Spot-check: graceful failure handling in each agent"
        steps:
          - "Open each of the 7 agent files"
          - "Find the KB write failure handling section"
        expected: "All 7 agents have graceful failure language for kb_write_artifact."
    fixture_definitions:
      - name: test_story_TEST-9999
        type: story
        description: "Minimal test story in backlog state for end-to-end pipeline integration verification."
    risks:
      - "Integration tests require KFMB-1030 and KFMB-2040 to be complete."
      - "End-to-end pipeline run requires real KB on port 5433 per ADR-005."
      - "Graceful failure test (EC-1) requires manually stopping the KB server."
  dev_feasibility:
    feasible: true
    confidence: high
    rationale: "All 7 target agents are markdown files under .claude/agents/. The migration pattern is well-defined: replace artifact_write calls with kb_write_artifact calls, remove file_path parameters, and update kb_tools frontmatter. No TypeScript compilation, no DB schema changes, no package.json changes. The knowledge-context-loader.agent.md already serves as a validated reference implementation."
    complexity: medium
    change_surface:
      areas:
        - ".claude/agents/ (7 agent instruction files)"
      endpoints: []
      deploy_touchpoints:
        - "No deployment required — agent instruction files are read directly from filesystem by Claude sessions"
        - "Implementation gated on KFMB-1030 and KFMB-2040 reaching completed status"
    risks:
      - finding: "Partial migration leaves inconsistent pipeline state — if fewer than all 7 agents are updated, some phases write to KB while others write to filesystem, breaking KFMB-5020 reader migration."
        mitigation: "Treat all 7 agent file updates as a single atomic batch. If scope must be split, define clear sub-story boundaries and implement sequentially, not in parallel."
      - finding: "artifact_write and kb_write_artifact have different call signatures — artifact_write accepts file_path; kb_write_artifact does not. Including file_path in migrated calls will cause tool errors."
        mitigation: "Dev agent must explicitly drop file_path when migrating each call site. The _shared/kb-integration.md pattern (lines 293-390) provides the canonical call template."
      - finding: "dev-implement-backend-coder and dev-implement-frontend-coder are out-of-scope (KBAR-0170 exemption) but have artifact_write in frontmatter — accidental inclusion would modify files that should not change."
        mitigation: "Explicitly document exclusion in SCOPE.yaml. Dev agent must confirm these two agents are unchanged as part of AC-10 verification."
    missing_requirements: []
    evidence_expectations:
      - "grep -rn 'artifact_write' across the 7 target agent files returns zero matches after implementation"
      - "grep -rn 'kb_write_artifact' across the 7 target agent files returns at least one match per file"
      - "dev-implement-backend-coder.agent.md and dev-implement-frontend-coder.agent.md are byte-for-byte identical to their pre-implementation state"
      - "All 7 agent files have kb_tools frontmatter listing kb_write_artifact and not artifact_write"
predictions:
  split_risk: 0.9
  review_cycles: 2
  token_estimate: 150000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-26T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
---

# KFMB-5010: Migrate _implementation/ Writer Agents to kb_write_artifact

## Context

The implementation pipeline — from dev setup through execution, code review, and QA verification — currently persists its workflow artifacts (`CHECKPOINT.yaml`, `SCOPE.yaml`, `PLAN.yaml`, `EVIDENCE.yaml`, `REVIEW.yaml`, `QA-VERIFY.yaml`) to the filesystem under `_implementation/` directories using a transitional dual-write tool called `artifact_write`. This tool writes the file to disk as primary and attempts a secondary KB write as best-effort.

The KB-first migration plan (KFMB) designates `_implementation/` filesystem artifacts as deprecated. The `_shared/kb-integration.md` policy document already declares direct `kb_write_artifact` calls as the mandatory standard, but the 7 leader agents that use `artifact_write` have not yet been updated to comply.

This story is in Phase 4 of the KFMB plan (Artifact Migration). It depends on:
- **KFMB-1030** (PM Artifact Types and Detail Tables): Ensures the KB infrastructure and CRUD layer are stable. All 7 artifact types needed by this story (`checkpoint`, `scope`, `plan`, `evidence`, `review`, `verification`, `fix_summary`) already exist — no new DB work is needed, but this dependency ensures migration stability.
- **KFMB-2040** (KB-Native Story Generation Pipeline): Establishes the KB-native pattern and validates the `kb_write_artifact` call approach for the full story generation workflow. KFMB-5010 extends the same approach to the implementation pipeline.

A `sizing_warning` is in effect: 7 files with 13 total write-call replacements. A partial migration — updating only some of the 7 agents — leaves an inconsistent pipeline where some phases write to KB and others write to filesystem. KFMB-5020 reader migration cannot proceed safely until all 7 writers are confirmed KB-only.

## Goal

Migrate all 7 implementation pipeline leader agents from `artifact_write` (dual-write transitional tool) to `kb_write_artifact` (KB-only), removing the filesystem write path entirely. After this story, the full implementation pipeline will persist all workflow artifacts exclusively through KB MCP tools, enabling KFMB-5020 to safely migrate reader agents.

## Non-Goals

- This story does NOT migrate reader agents away from `_implementation/` filesystem reads — that is KFMB-5020.
- This story does NOT remove `_implementation/` directories from existing stories on disk — those are pre-existing artifacts unaffected by agent instruction changes.
- This story does NOT modify `dev-implement-backend-coder` or `dev-implement-frontend-coder` — their log-writing pattern is exempt (KBAR-0170).
- This story does NOT modify `knowledge-context-loader` — it already uses `kb_write_artifact` natively.
- This story does NOT add new artifact types to the KB — all types used already exist.
- This story does NOT update `workflow-retro`, `story-status`, or `checkpoint` commands that read `_implementation/` files — those are part of KFMB-5020/5030.
- This story does NOT delete the `artifact_write` tool implementation — that cleanup is deferred to KFMB-6020.
- This story does NOT touch `_pm/` artifact writers — that is KFMB-5040.

## Scope

**Agent files to modify (7 total):**

| Agent | File | Calls to Replace |
|-------|------|-----------------|
| `dev-setup-leader` | `.claude/agents/dev-setup-leader.agent.md` | 3 (`checkpoint`-setup, `scope`, `fix_summary`) |
| `dev-plan-leader` | `.claude/agents/dev-plan-leader.agent.md` | 2 (`checkpoint`-planning, `plan`) |
| `dev-execute-leader` | `.claude/agents/dev-execute-leader.agent.md` | 2 (`evidence`, `checkpoint`-implementation) |
| `dev-fix-fix-leader` | `.claude/agents/dev-fix-fix-leader.agent.md` | 2 (`evidence`, `review`) |
| `review-aggregate-leader` | `.claude/agents/review-aggregate-leader.agent.md` | 1 (`review`) |
| `qa-verify-verification-leader` | `.claude/agents/qa-verify-verification-leader.agent.md` | 1 (`verification`) |
| `qa-verify-completion-leader` | `.claude/agents/qa-verify-completion-leader.agent.md` | 2 (`verification` PASS + FAIL) |

**Total:** 13 `artifact_write` call site replacements across 7 files.

**Explicitly excluded:**
- `.claude/agents/dev-implement-backend-coder.agent.md` (KBAR-0170 exemption — log files only)
- `.claude/agents/dev-implement-frontend-coder.agent.md` (KBAR-0170 exemption — log files only)
- `.claude/agents/knowledge-context-loader.agent.md` (already KB-native)

**No TypeScript, no DB changes, no HTTP endpoints.**

## Acceptance Criteria

- [ ] **AC-1**: `dev-setup-leader` calls `kb_write_artifact` directly for `checkpoint`, `scope`, and `fix_summary` artifacts. No `artifact_write` calls remain. No `_implementation/` file paths remain in artifact write paths.
- [ ] **AC-2**: `dev-plan-leader` calls `kb_write_artifact` directly for `checkpoint` and `plan` artifacts. No `artifact_write` calls remain.
- [ ] **AC-3**: `dev-execute-leader` calls `kb_write_artifact` directly for `evidence` and `checkpoint` artifacts. No `artifact_write` calls remain.
- [ ] **AC-4**: `dev-fix-fix-leader` calls `kb_write_artifact` directly for `evidence` and `review` artifacts. No `artifact_write` calls remain.
- [ ] **AC-5**: `review-aggregate-leader` calls `kb_write_artifact` directly for `review` artifacts. No `artifact_write` calls remain.
- [ ] **AC-6**: `qa-verify-verification-leader` calls `kb_write_artifact` directly for `verification` artifacts. No `artifact_write` calls remain.
- [ ] **AC-7**: `qa-verify-completion-leader` calls `kb_write_artifact` directly for `verification` artifacts in both PASS and FAIL branches. No `artifact_write` calls remain.
- [ ] **AC-8**: All 7 agents have their `kb_tools` frontmatter updated: `artifact_write` is removed; `kb_write_artifact` is added (if not already present).
- [ ] **AC-9**: Each agent retains graceful failure handling for `kb_write_artifact` failures — a KB write failure logs a warning and allows the pipeline phase to continue; it does not block or halt execution.
- [ ] **AC-10**: `dev-implement-backend-coder` and `dev-implement-frontend-coder` are confirmed unchanged — their `artifact_write` frontmatter references relate only to log files (KBAR-0170 exemption) and are out of scope.
- [ ] **AC-11**: No agent in the set references `_implementation/` filesystem paths in artifact write outputs (these may still appear in read fallback paths until KFMB-5020 removes them).
- [ ] **AC-12**: The `artifact_write` entry is removed from the `kb_tools` frontmatter of all 7 migrated agents.
- [ ] **AC-13**: Any Non-Negotiables or prose instructions in the 7 migrated agents that reference `artifact_write` as the required write method are updated or removed to reference `kb_write_artifact` instead. _(Added by autonomous elaboration — GAP-2)_

## Reuse Plan

- **`kb_write_artifact`**: Existing MCP tool — no new tool registration needed.
- **`knowledge-context-loader.agent.md`**: Reference implementation of a fully migrated writer agent. Study this file before modifying any of the 7 target agents.
- **`_shared/kb-integration.md` (lines 293-390)**: Authoritative Artifact Type Reference table mapping deprecated file names to `artifact_type` strings and phases. Use as canonical call template.
- **KFMB-2040 migration pattern**: Sister story migrating the PM pipeline — use its agent update approach as structural model.
- **No TypeScript packages modified**: This is a docs-only story.

## Architecture Notes

### Migration Pattern Per Call Site

```
BEFORE (artifact_write dual-write):
artifact_write({
  story_id: "{STORY_ID}",
  artifact_type: "checkpoint",
  phase: "setup",
  iteration: 0,
  content: { ... },
  file_path: "{FEATURE_DIR}/in-progress/{STORY_ID}/_implementation/CHECKPOINT.yaml",
})

AFTER (kb_write_artifact direct):
kb_write_artifact({
  story_id: "{STORY_ID}",
  artifact_type: "checkpoint",
  phase: "setup",
  iteration: 0,
  content: { ... },
  // file_path is REMOVED — not a valid parameter for kb_write_artifact
})
```

### Frontmatter Update Pattern Per Agent

```
BEFORE:
kb_tools:
  - artifact_write
  - kb_read_artifact

AFTER:
kb_tools:
  - kb_write_artifact
  - kb_read_artifact
```

### Graceful Failure Pattern

Each agent must retain graceful failure handling updated for the new call:

```
If kb_write_artifact fails:
  - Log a warning: "KB write failed for [artifact_type] — continuing pipeline"
  - Do NOT halt or block pipeline execution
  - Continue to next step
```

### Atomicity Requirement

All 7 agents must be updated in a single story implementation. A partial migration leaves an inconsistent pipeline state where some phases write to KB and others write to filesystem. If scope must be split during elaboration, define sequential sub-stories (e.g., setup/plan group first, then execute/review/QA group), not parallel.

### KBAR-0170 Exemption (Log Files)

`dev-implement-backend-coder` and `dev-implement-frontend-coder` write `BACKEND-LOG.md` and `FRONTEND-LOG.md` using direct `Write` tool calls, not artifact YAML files. Their `artifact_write` frontmatter entry exists only for context about log handling. These two agents are out of scope and MUST NOT be modified.

## Infrastructure Notes

No infrastructure changes. Agent instruction files are read directly from the filesystem by Claude sessions — no deployment required.

## Test Plan

See `_pm/test-plan.yaml` for the full test plan.

**Strategy**: Manual verification (docs-only story — no runnable unit tests apply to agent instruction files per WKFL-010).

**Key verification steps:**
1. Grep-based static analysis: `grep -rn 'artifact_write'` across all 7 target files must return zero matches post-implementation.
2. Frontmatter spot-check: All 7 agents list `kb_write_artifact` in `kb_tools`; none list `artifact_write`.
3. Integration E2E (post-prerequisites): Run `/dev-implement-story` against a test story end-to-end; confirm each artifact type appears in KB via `kb_read_artifact`; confirm no `_implementation/` YAML files are created.
4. Exclusion confirmation: `dev-implement-backend-coder` and `dev-implement-frontend-coder` are byte-for-byte unchanged.

**ADR-005**: Integration verification uses real KB database (port 5433), not mocked MCP responses.

**ADR-006 skip**: `frontend_impacted: false` — no Playwright E2E tests required.

## Reality Baseline

- **Baseline date**: 2026-02-13
- **KB `storyArtifacts` jump table**: Exists in production. All 7 artifact types required by this story (`checkpoint`, `scope`, `plan`, `evidence`, `review`, `verification`, `fix_summary`) have entries in `ARTIFACT_TYPES` and corresponding detail tables.
- **`artifact_write` tool**: Still present and functional — this story replaces calls in agent files but does NOT delete the tool implementation (deferred to KFMB-6020).
- **`kb_write_artifact` tool**: Available in `apps/api/knowledge-base/src/mcp-server/tool-schemas.ts` (line 2323). Parameters: `story_id`, `artifact_type`, `phase`, `iteration`, `content`. No `file_path` parameter.
- **`_shared/kb-integration.md`**: Policy document declaring `_implementation/` files deprecated and `kb_write_artifact` / `kb_read_artifact` as mandatory standard.
- **`knowledge-context-loader.agent.md`**: Already uses `kb_write_artifact` natively — the reference "done state" for all 7 agents.
- **KFMB-1030 and KFMB-2040**: Required prerequisites. Not yet complete as of baseline date. Implementation must not begin until both reach `completed` status.

## Canonical References

| Pattern | File | Why |
|---------|------|-----|
| Fully migrated writer agent | `.claude/agents/knowledge-context-loader.agent.md` | Uses `kb_write_artifact` natively with no `artifact_write` fallback; shows the target state for all 7 agents |
| `kb_write_artifact` call schema | `apps/api/knowledge-base/src/mcp-server/tool-schemas.ts` (line 2323) | Authoritative input schema: `story_id`, `artifact_type`, `phase`, `iteration`, `content` — required before updating any agent |
| KB integration policy | `.claude/agents/_shared/kb-integration.md` (lines 293-390) | Defines the Artifact Type Reference table mapping deprecated file names to `artifact_type` strings, phases, and writing agents |
| Orchestrator checkpoint schema | `packages/backend/orchestrator/src/artifacts/checkpoint.ts` | Defines `content` payload structure for `checkpoint` artifact type — one of the most-written types in this story |

## Subtasks

### ST-1: Read and audit all 7 target agent files to establish current state
**Goal**: Understand current `artifact_write` call sites, `file_path` parameters, and `kb_tools` frontmatter in each agent. Establish baseline for migration.

**Files to read**:
- `.claude/agents/dev-setup-leader.agent.md`
- `.claude/agents/dev-plan-leader.agent.md`
- `.claude/agents/dev-execute-leader.agent.md`
- `.claude/agents/dev-fix-fix-leader.agent.md`
- `.claude/agents/review-aggregate-leader.agent.md`
- `.claude/agents/qa-verify-verification-leader.agent.md`
- `.claude/agents/qa-verify-completion-leader.agent.md`
- `.claude/agents/knowledge-context-loader.agent.md`
- `.claude/agents/_shared/kb-integration.md`

**Files to modify**: None
**ACs covered**: (audit only)
**Depends on**: none
**Verification**: No file changes. Confirm call counts match seed: dev-setup-leader (3), dev-plan-leader (2), dev-execute-leader (2), dev-fix-fix-leader (2), review-aggregate-leader (1), qa-verify-verification-leader (1), qa-verify-completion-leader (2).
**Estimated tokens**: 6,000

---

### ST-2: Migrate dev-setup-leader — 3 artifact_write calls to kb_write_artifact
**Goal**: Replace all 3 `artifact_write` calls (`checkpoint`-setup, `scope`, `fix_summary`) with `kb_write_artifact` calls. Remove `file_path` parameters. Update `kb_tools` frontmatter.

**Files to read**:
- `.claude/agents/dev-setup-leader.agent.md`
- `apps/api/knowledge-base/src/mcp-server/tool-schemas.ts`

**Files to modify**:
- `.claude/agents/dev-setup-leader.agent.md`

**ACs covered**: AC-1, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/dev-setup-leader.agent.md` returns no matches. `grep -n 'kb_write_artifact'` returns 3+ matches.
**Estimated tokens**: 5,000

---

### ST-3: Migrate dev-plan-leader — 2 artifact_write calls to kb_write_artifact
**Goal**: Replace 2 `artifact_write` calls (`checkpoint`-planning, `plan`) with `kb_write_artifact` calls. Remove `file_path` parameters. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/dev-plan-leader.agent.md`

**ACs covered**: AC-2, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/dev-plan-leader.agent.md` returns no matches.
**Estimated tokens**: 4,000

---

### ST-4: Migrate dev-execute-leader — 2 artifact_write calls to kb_write_artifact
**Goal**: Replace 2 `artifact_write` calls (`evidence`, `checkpoint`-implementation) with `kb_write_artifact` calls. Remove `file_path` parameters. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/dev-execute-leader.agent.md`

**ACs covered**: AC-3, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/dev-execute-leader.agent.md` returns no matches.
**Estimated tokens**: 4,000

---

### ST-5: Migrate dev-fix-fix-leader — 2 artifact_write calls to kb_write_artifact
**Goal**: Replace 2 `artifact_write` calls (`evidence`, `review`) with `kb_write_artifact` calls. Remove `file_path` parameters. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/dev-fix-fix-leader.agent.md`

**ACs covered**: AC-4, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/dev-fix-fix-leader.agent.md` returns no matches.
**Estimated tokens**: 4,000

---

### ST-6: Migrate review-aggregate-leader — 1 artifact_write call to kb_write_artifact
**Goal**: Replace 1 `artifact_write` call (`review`) with `kb_write_artifact` call. Remove `file_path` parameter. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/review-aggregate-leader.agent.md`

**ACs covered**: AC-5, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/review-aggregate-leader.agent.md` returns no matches.
**Estimated tokens**: 3,500

---

### ST-7: Migrate qa-verify-verification-leader — 1 artifact_write call to kb_write_artifact
**Goal**: Replace 1 `artifact_write` call (`verification`) with `kb_write_artifact` call. Remove `file_path` parameter. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/qa-verify-verification-leader.agent.md`

**ACs covered**: AC-6, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/qa-verify-verification-leader.agent.md` returns no matches.
**Estimated tokens**: 3,500

---

### ST-8: Migrate qa-verify-completion-leader — 2 artifact_write calls to kb_write_artifact (PASS + FAIL branches)
**Goal**: Replace 2 `artifact_write` calls (`verification`-PASS, `verification`-FAIL) with `kb_write_artifact` calls. Remove `file_path` parameters. Update `kb_tools` frontmatter.

**Files to modify**:
- `.claude/agents/qa-verify-completion-leader.agent.md`

**ACs covered**: AC-7, AC-8, AC-9, AC-11, AC-12
**Depends on**: ST-1
**Verification**: `grep -n 'artifact_write' .claude/agents/qa-verify-completion-leader.agent.md` returns no matches. Both PASS and FAIL branch call sites are replaced.
**Estimated tokens**: 4,000

---

### ST-9: Final verification — confirm exclusions and cross-agent sweep
**Goal**: Verify `dev-implement-backend-coder` and `dev-implement-frontend-coder` are unchanged (AC-10). Run cross-agent grep to confirm zero `artifact_write` references remain across all 7 target files (AC-11, AC-12). Confirm no `_implementation/` write paths remain.

**Files to read**:
- `.claude/agents/dev-implement-backend-coder.agent.md`
- `.claude/agents/dev-implement-frontend-coder.agent.md`

**Files to modify**: None
**ACs covered**: AC-10, AC-11, AC-12
**Depends on**: ST-8
**Verification**: `grep -rn 'artifact_write'` across all 7 target agent files returns zero. `git diff --name-only` shows no changes to the two coder agents.
**Estimated tokens**: 3,000
