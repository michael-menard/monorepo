---
id: KFMB-5040
title: "Migrate _pm/ Writer Agents to kb_write_artifact"
status: ready-to-work
priority: medium
phase: 4
experiment_variant: "control"
points: 1
feature: kb-first-migration
epic: kb-first-migration
story_type: tech_debt
depends_on:
  - KFMB-1030
  - KFMB-2040
surfaces:
  - agents
touches_backend: false
touches_frontend: false
touches_database: false
touches_infra: false
sizing_warning: false
created: "2026-02-26T00:00:00Z"
pm_artifacts:
  test_plan:
    strategy: integration+manual
    scope:
      endpoints: []
      ui_touched: false
      data_touched: true
    happy_path_tests:
      - id: HP-1
        title: "pm-story-seed-agent writes story_seed artifact to KB after filesystem write"
        setup: "KFMB-1030 and KFMB-2040 complete; KB running on port 5433; test story TEST-9999 seeded"
        action: "Run /pm-story seed TEST-9999 against a test epic directory"
        expected: "STORY-SEED.md exists in TEST-9999/_pm/ AND kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'story_seed' }) returns the artifact"
        evidence: "File present in _pm/; kb_read_artifact returns non-null with correct story_id and artifact_type"
      - id: HP-2
        title: "pm-draft-test-plan writes test_plan artifact to KB after filesystem write"
        setup: "KFMB-1030 and KFMB-2040 complete; KB running on port 5433"
        action: "Run /pm-story generate which spawns pm-draft-test-plan for TEST-9999"
        expected: "test-plan.yaml exists in TEST-9999/_pm/ AND kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'test_plan' }) returns the artifact"
        evidence: "File present in _pm/; kb_read_artifact returns non-null with strategy field"
      - id: HP-3
        title: "pm-dev-feasibility-review writes dev_feasibility artifact to KB after filesystem write"
        setup: "KFMB-1030 and KFMB-2040 complete; KB running on port 5433"
        action: "Run /pm-story generate which spawns pm-dev-feasibility-review for TEST-9999"
        expected: "dev-feasibility.yaml exists in TEST-9999/_pm/ AND kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'dev_feasibility' }) returns the artifact"
        evidence: "File present in _pm/; kb_read_artifact returns non-null with feasible and confidence fields"
      - id: HP-4
        title: "pm-uiux-recommendations writes uiux_notes artifact to KB after filesystem write"
        setup: "KFMB-1030 and KFMB-2040 complete; KB running on port 5433; test story has UI touches"
        action: "Run /pm-story generate which spawns pm-uiux-recommendations for a UI-touching test story"
        expected: "uiux-notes.yaml exists in _pm/ AND kb_read_artifact returns artifact with skipped: false"
        evidence: "File present in _pm/; kb_read_artifact returns non-null with skipped: false"
    error_cases:
      - id: EC-1
        title: "KB unavailable during pm-draft-test-plan execution — filesystem write still succeeds"
        setup: "KB port 5433 unavailable; test story TEST-9999"
        action: "Run pm-draft-test-plan with KB unreachable"
        expected: "test-plan.yaml still written to _pm/; agent logs warning; agent does NOT halt"
        evidence: "test-plan.yaml present; WARNING log present; pm-story-generation-leader completes normally"
      - id: EC-2
        title: "KB unavailable during pm-story-seed-agent — filesystem write still succeeds"
        setup: "KB port 5433 unavailable; test story TEST-9999"
        action: "Run pm-story-seed-agent with KB unreachable"
        expected: "STORY-SEED.md still written to _pm/; agent logs warning and continues"
        evidence: "STORY-SEED.md present; WARNING log present"
      - id: EC-3
        title: "pm-uiux-recommendations skipped branch — both filesystem and KB writes handled"
        setup: "KFMB-1030 complete; test story with no UI touches"
        action: "Run pm-uiux-recommendations for a story with no UI touches"
        expected: "uiux-notes.yaml with skipped: true written to _pm/; KB artifact written with content.skipped: true"
        evidence: "uiux-notes.yaml present with skipped: true; KB artifact verifiable via kb_read_artifact"
    edge_cases:
      - id: ED-1
        title: "Full pipeline dual-write regression — all 4 _pm/ files present after migration"
        setup: "KFMB-5040 implemented; pm-story-generation-leader unchanged"
        action: "Run full /pm-story generate for TEST-9999"
        expected: "All 4 _pm/ files present; story.yaml pm_artifacts embedded correctly by leader"
        evidence: "All 4 files present; story.yaml pm_artifacts section has test_plan, dev_feasibility"
      - id: ED-2
        title: "All 4 agent frontmatter have kb_write_artifact in kb_tools"
        setup: "KFMB-5040 implemented"
        action: "Inspect frontmatter of all 4 agent files"
        expected: "kb_write_artifact present in kb_tools list for all 4; no duplicates"
        evidence: "YAML frontmatter inspection"
      - id: ED-3
        title: "pm-story-generation-leader unchanged — still reads _pm/ filesystem"
        setup: "KFMB-5040 implemented"
        action: "Inspect pm-story-generation-leader.agent.md"
        expected: "No kb_read_artifact calls added; _pm/ read logic unchanged"
        evidence: "git diff shows no modifications to leader agent"
    risks:
      - "Integration tests require KFMB-1030 and KFMB-2040 complete — PM artifact types don't exist yet"
      - "KB availability on port 5433 required; ADR-005 prohibits mocked MCP responses"
      - "skipped: true branch KB write behavior must be decided before implementation"
  dev_feasibility:
    feasible: true
    confidence: high
    rationale: "Docs-only story. 4 agent markdown files modified via dual-write (retain Write + append kb_write_artifact). Migration pattern established by KFMB-5010. Gated on KFMB-1030 and KFMB-2040."
    complexity: low
    change_surface:
      areas:
        - ".claude/agents/ (4 agent markdown files)"
      endpoints: []
      deploy_touchpoints: []
    risks:
      - finding: "pm-uiux-recommendations skipped: true branch — KB write behavior undefined"
        mitigation: "Write { skipped: true } artifact to KB for consistency; document in story"
      - finding: "Hard dependency: KFMB-1030 must complete before implementation"
        mitigation: "Gate implementation; do not begin until KFMB-1030 is confirmed completed"
      - finding: "Content shapes for each artifact type must match KFMB-1030 detail table schemas"
        mitigation: "Read KFMB-1030 test plan HP-1 through HP-4 for expected field shapes"
predictions:
  split_risk: 0.3
  review_cycles: 1
  token_estimate: 80000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-26T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
qa_notes:
  verdict: CONDITIONAL_PASS
  decided_at: "2026-02-26T00:00:00Z"
  mode: autonomous
  acs_added: 2
  gaps_resolved: 2
  opportunities_logged: 4
---

# KFMB-5040: Migrate _pm/ Writer Agents to kb_write_artifact

## Context

The PM story generation pipeline — covering story seed, test plan, dev feasibility, and UI/UX recommendations — currently persists its PM artifact outputs exclusively to the filesystem under `_pm/` directories via direct Write tool calls. The `pm-story-generation-leader` then reads these files to embed them as `pm_artifacts` in `story.yaml`.

The KB-first migration plan (KFMB) designates `_pm/` filesystem artifacts as deprecated. All PM workflow artifact persistence must also be written to the Knowledge Base via `kb_write_artifact` calls. KFMB-1030 adds the four required PM artifact types (`test_plan`, `dev_feasibility`, `uiux_notes`, `story_seed`) and their detail tables. KFMB-2040 validates the KB-native pipeline pattern end-to-end. This story migrates the 4 PM worker agents to call `kb_write_artifact` alongside their existing filesystem writes.

This story implements a **dual-write model**: PM workers write to both `_pm/` filesystem AND to KB via `kb_write_artifact`. The `pm-story-generation-leader` continues reading from `_pm/` until KFMB-5050 updates it to read from KB instead. The dual-write model maintains backward compatibility while establishing KB as the authoritative store.

The 4 agents to migrate:
1. `pm-story-seed-agent` — writes `story_seed` artifact
2. `pm-draft-test-plan` — writes `test_plan` artifact
3. `pm-dev-feasibility-review` — writes `dev_feasibility` artifact
4. `pm-uiux-recommendations` — writes `uiux_notes` artifact (including `skipped: true` branch)

## Goal

All PM pipeline outputs are written as typed story artifacts in the KB alongside filesystem writes, enabling KFMB-5050 to safely migrate the leader's reader logic away from `_pm/` filesystem reads.

## Non-Goals

- This story does NOT remove `_pm/` filesystem Write calls from any PM worker agent — dual-write is required for backward compatibility until KFMB-5050.
- This story does NOT update `pm-story-generation-leader` to read from KB — that is KFMB-5050.
- This story does NOT add new artifact types to the KB — those are added by KFMB-1030.
- This story does NOT delete the `_pm/` directory structure or stage directories.
- This story does NOT migrate `_pm/` reader logic in any agent — that is KFMB-5050.
- This story does NOT touch `_implementation/` artifact writers — that is KFMB-5010.
- This story does NOT update `pm-story-risk-predictor` — that agent produces inline YAML embedded directly in story.yaml by the leader, not a separate `_pm/` file.
- This story does NOT update the Artifact Type Reference table in `_shared/kb-integration.md` — that update should occur as part of KFMB-1030 or KFMB-2040 once the PM artifact types are confirmed live.

## Scope

**Agent files modified (4 total):**
- `.claude/agents/pm-story-seed-agent.agent.md`
- `.claude/agents/pm-draft-test-plan.agent.md`
- `.claude/agents/pm-dev-feasibility-review.agent.md`
- `.claude/agents/pm-uiux-recommendations.agent.md`

**Agent files confirmed unchanged:**
- `.claude/agents/pm-story-generation-leader.agent.md`

**No TypeScript code changes. No build or test pipeline involvement.**

## Acceptance Criteria

- [ ] **AC-1**: `pm-story-seed-agent` calls `kb_write_artifact` with `artifact_type: "story_seed"` after writing `STORY-SEED.md` to `_pm/`. The existing Write tool call to `_pm/STORY-SEED.md` is retained.
- [ ] **AC-2**: `pm-draft-test-plan` calls `kb_write_artifact` with `artifact_type: "test_plan"` after writing `test-plan.yaml` to `_pm/`. The existing Write tool call to `_pm/test-plan.yaml` is retained.
- [ ] **AC-3**: `pm-dev-feasibility-review` calls `kb_write_artifact` with `artifact_type: "dev_feasibility"` after writing `dev-feasibility.yaml` to `_pm/`. The existing Write tool call to `_pm/dev-feasibility.yaml` is retained.
- [ ] **AC-4**: `pm-uiux-recommendations` calls `kb_write_artifact` with `artifact_type: "uiux_notes"` after writing `uiux-notes.yaml` to `_pm/`, including the `skipped: true` branch. The existing Write tool call to `_pm/uiux-notes.yaml` is retained in both branches.
- [ ] **AC-5**: All 4 agents have `kb_write_artifact` added to their `kb_tools` frontmatter (if not already present).
- [ ] **AC-6**: Each agent retains graceful failure handling for `kb_write_artifact` failures — a KB write failure logs a warning and allows the PM pipeline phase to continue; it does not block or halt execution.
- [ ] **AC-7**: The `pm-story-generation-leader` is confirmed unchanged — it still reads `_pm/` filesystem outputs and embeds them as `pm_artifacts` in `story.yaml`.
- [ ] **AC-8**: `kb_read_artifact({ story_id, artifact_type: "story_seed" })` returns the artifact written by `pm-story-seed-agent` for a test story after agent execution.
- [ ] **AC-9**: `kb_read_artifact({ story_id, artifact_type: "test_plan" })` returns the artifact written by `pm-draft-test-plan` for a test story after agent execution.
- [ ] **AC-10**: `kb_read_artifact({ story_id, artifact_type: "dev_feasibility" })` returns the artifact written by `pm-dev-feasibility-review` for a test story after agent execution.
- [ ] **AC-11**: `kb_read_artifact({ story_id, artifact_type: "uiux_notes" })` returns the artifact written by `pm-uiux-recommendations` for a test story after agent execution.
- [ ] **AC-12**: `_pm/` filesystem outputs are still created after migration — the dual-write model preserves backward compatibility with `pm-story-generation-leader`.
- [ ] **AC-13**: Integration verification subtask ST-7 is present covering AC-8 through AC-11 with explicit prerequisite gates for KFMB-1030 and KFMB-2040 completion. ST-7 must be executed using the real KB on port 5433 (ADR-005) and must not be skipped before marking the story complete.
- [ ] **AC-14**: ST-1 explicitly includes reading the KFMB-1030 story file (HP-1 through HP-4) to confirm exact content shapes for all 4 artifact types (`story_seed`, `test_plan`, `dev_feasibility`, `uiux_notes`) before any `kb_write_artifact` content blocks are written. Field names must match the KFMB-1030 detail table schemas.

## Reuse Plan

- **Tools**: `kb_write_artifact` (existing MCP tool — new PM artifact types added by KFMB-1030), `kb_read_artifact` (existing, used for integration verification). No new tool registration needed.
- **Patterns**: `_shared/kb-integration.md` write pattern (lines 299-310) as the authoritative call template; `knowledge-context-loader.agent.md` as the reference implementation of a fully KB-native writer agent; KFMB-5010 agent update approach as the structural model for the dual-write migration.
- **Packages**: No TypeScript packages modified — docs-only story targeting 4 `.claude/agents/` files.

## Architecture Notes

### Dual-Write Model

Each PM worker agent must apply this pattern:

```
CURRENT (Write tool only):
Write({ file_path: "{OUTPUT_DIR}/_pm/test-plan.yaml", content: yamlContent })

AFTER (dual-write: Write + kb_write_artifact):
Write({ file_path: "{OUTPUT_DIR}/_pm/test-plan.yaml", content: yamlContent })
kb_write_artifact({
  story_id: "{STORY_ID}",
  artifact_type: "test_plan",
  phase: "analysis",
  iteration: 0,
  content: { strategy, happy_path_tests, error_cases, edge_cases, ... },
})
// If kb_write_artifact fails: log warning "KB write failed for test_plan — continuing PM pipeline" and continue
```

### Frontmatter Update Pattern

```yaml
# ADD to existing frontmatter (agents currently have no kb_tools key):
kb_tools:
  - kb_write_artifact
```

### skipped: true Branch Resolution

For `pm-uiux-recommendations`, when the story has no UI touches, the agent writes `{ skipped: true }` to the `_pm/uiux-notes.yaml` file and stops. After migration, the KB write for this branch must also be handled. The recommended approach (option a): write a minimal `{ skipped: true, reason: "..." }` artifact to KB so that `kb_read_artifact` callers receive a definitive answer rather than a "not found" error. This provides a consistent artifact contract for KFMB-5050 downstream.

### Content Shapes

Content shapes for each `kb_write_artifact` call must be confirmed against KFMB-1030's detail table schemas before implementation:
- `story_seed`: expects `conflicts_found`, `blocking_conflicts`, `baseline_loaded`, and JSONB `data` (full seed content)
- `test_plan`: expects `strategy` and test arrays
- `dev_feasibility`: expects `feasible`, `confidence`, `complexity`
- `uiux_notes`: expects `has_ui_changes`, `component_count` (or `skipped: true`)

Consult KFMB-1030 test plan HP-1 through HP-4 and the `_shared/kb-integration.md` Artifact Type Reference table (once updated by KFMB-1030).

### Lessons Applied

- **[KBAR-0190]** Frontmatter `kb_tools` must be updated atomically with the call-site change in each agent file.
- **[KBAR-0190]** Minimal tool footprint: none of the 4 agents currently have `kb_tools` frontmatter, so simply add `kb_tools: [kb_write_artifact]` without auditing for pre-existing entries.
- **[WKFL-010]** Non-code agent story: PROOF-based QA via direct file spot-checking — no unit tests; verify agent files directly.
- **[KFMB-5010 pattern]** All 4 agents should be treated as an atomic batch for KFMB-5050 downstream readiness, even though partial migration is not catastrophically broken due to the dual-write model.

## Infrastructure Notes

No infrastructure changes. KB writes use the existing `kb_write_artifact` MCP tool against PostgreSQL/Aurora (port 5433 in development). ADR-005 applies: integration verification must use the real KB database, not mocked MCP responses.

## Test Plan

See `_pm/test-plan.yaml` for full test plan. Summary:

- **Strategy**: integration + manual (no unit tests — docs-only agent file changes)
- **Integration verification** (post-prerequisites): Run full `/pm-story generate` for TEST-9999 after KFMB-1030 and KFMB-2040 complete; confirm all 4 KB artifacts readable via `kb_read_artifact`
- **Dual-write regression**: Confirm all 4 `_pm/` files still written after migration
- **Graceful failure**: Simulate KB unavailability; confirm `_pm/` files still written and pipeline continues
- **Skip branch**: Confirm `uiux-notes.yaml` with `skipped: true` still written; confirm KB artifact written with `content.skipped: true`
- **ADR-005**: Real KB database on port 5433 required for integration tests
- **ADR-006**: No Playwright tests required (no UI changes)

## Reality Baseline

- Baseline: `plans/baselines/BASELINE-REALITY-2026-02-13.md`
- No KFMB stories are in-progress per baseline snapshot
- `kb_write_artifact` tool exists in `apps/api/knowledge-base/src/mcp-server/tool-schemas.ts` (line 2323)
- `kb_read_artifact` tool exists at line 2408 of same file
- PM artifact types (`test_plan`, `dev_feasibility`, `uiux_notes`, `story_seed`) do NOT yet exist — added by KFMB-1030
- All 4 target agents confirmed to lack `kb_tools` frontmatter key
- All 4 target agents currently write only to `_pm/` filesystem via Write tool

## Canonical References

| Pattern | File | Why |
|---------|------|-----|
| Fully migrated writer agent | `.claude/agents/knowledge-context-loader.agent.md` | Uses `kb_write_artifact` natively; shows target output section pattern for all 4 PM agents |
| `kb_write_artifact` call schema | `apps/api/knowledge-base/src/mcp-server/tool-schemas.ts` (line 2323) | Authoritative input schema: `story_id`, `artifact_type`, `phase`, `iteration`, `content` |
| KB integration policy + Artifact Type Reference | `.claude/agents/_shared/kb-integration.md` (lines 293-390) | Canonical write/read patterns; PM artifact types will appear here after KFMB-1030 |
| Sister story implementation notes | `plans/future/platform/kb-first-migration/elaboration/KFMB-5010/KFMB-5010.md` | Direct structural model: migration pattern, frontmatter update, graceful failure, atomicity |

## Subtasks

### ST-1: Read all 4 target agent files + reference materials

**Goal**: Confirm exact write locations in each agent's Output section; confirm no pre-existing `kb_write_artifact` calls; confirm content shapes from KFMB-1030 HP-1 through HP-4.

**Files to read**:
- `.claude/agents/pm-story-seed-agent.agent.md`
- `.claude/agents/pm-draft-test-plan.agent.md`
- `.claude/agents/pm-dev-feasibility-review.agent.md`
- `.claude/agents/pm-uiux-recommendations.agent.md`
- `.claude/agents/knowledge-context-loader.agent.md`
- `.claude/agents/_shared/kb-integration.md`
- `plans/future/platform/kb-first-migration/elaboration/KFMB-1030/KFMB-1030.md` (HP-1 through HP-4 — confirm exact field shapes for all 4 artifact types before writing any kb_write_artifact content blocks)

**ACs**: AC-5, AC-14
**Depends on**: none
**Verification**: Read all 6 files; note current frontmatter and output section structure for each agent
**Estimated tokens**: 6,000

---

### ST-2: Migrate pm-story-seed-agent

**Goal**: Append `kb_write_artifact` call with `artifact_type: "story_seed"` after Write call in Output section; add `kb_tools: [kb_write_artifact]` to frontmatter; add graceful failure handling.

**Files to modify**:
- `.claude/agents/pm-story-seed-agent.agent.md`

**ACs**: AC-1, AC-5, AC-6
**Depends on**: ST-1
**Verification**: `grep kb_write_artifact .claude/agents/pm-story-seed-agent.agent.md` returns call site and frontmatter entry; Write call for `_pm/STORY-SEED.md` still present
**Estimated tokens**: 4,000

---

### ST-3: Migrate pm-draft-test-plan

**Goal**: Append `kb_write_artifact` call with `artifact_type: "test_plan"` after Write call; add `kb_tools: [kb_write_artifact]` to frontmatter; add graceful failure handling.

**Files to modify**:
- `.claude/agents/pm-draft-test-plan.agent.md`

**ACs**: AC-2, AC-5, AC-6
**Depends on**: ST-1
**Verification**: `grep kb_write_artifact .claude/agents/pm-draft-test-plan.agent.md` returns call site and frontmatter entry; Write call for `_pm/test-plan.yaml` still present
**Estimated tokens**: 3,500

---

### ST-4: Migrate pm-dev-feasibility-review

**Goal**: Append `kb_write_artifact` call with `artifact_type: "dev_feasibility"` after Write call; add `kb_tools: [kb_write_artifact]` to frontmatter; add graceful failure handling.

**Files to modify**:
- `.claude/agents/pm-dev-feasibility-review.agent.md`

**ACs**: AC-3, AC-5, AC-6
**Depends on**: ST-1
**Verification**: `grep kb_write_artifact .claude/agents/pm-dev-feasibility-review.agent.md` returns call site and frontmatter entry; Write call for `_pm/dev-feasibility.yaml` still present
**Estimated tokens**: 3,500

---

### ST-5: Migrate pm-uiux-recommendations

**Goal**: Append `kb_write_artifact` call with `artifact_type: "uiux_notes"` in BOTH the `skipped: true` branch AND the normal output branch. Add `kb_tools: [kb_write_artifact]` to frontmatter. Add graceful failure handling. For the skip branch, write `{ skipped: true, reason: "..." }` to KB.

**Files to modify**:
- `.claude/agents/pm-uiux-recommendations.agent.md`

**ACs**: AC-4, AC-5, AC-6
**Depends on**: ST-1
**Verification**: `grep kb_write_artifact .claude/agents/pm-uiux-recommendations.agent.md` returns 2 call sites (one per branch); Write call for `_pm/uiux-notes.yaml` still present in both branches
**Estimated tokens**: 4,000

---

### ST-6: Verify leader unchanged; spot-check all 4 agents

**Goal**: Confirm `pm-story-generation-leader` has no modifications. Confirm all 4 migrated agents have correct `kb_tools` frontmatter, retained Write calls, `kb_write_artifact` calls, and graceful failure handling.

**Files to read**:
- `.claude/agents/pm-story-generation-leader.agent.md`
- All 4 migrated agent files

**ACs**: AC-7, AC-12
**Depends on**: ST-5
**Verification**: `git diff` shows 0 changes to `pm-story-generation-leader`; all 4 agents pass spot-check criteria
**Estimated tokens**: 5,000

---

### ST-7: Integration verification — kb_read_artifact for all 4 artifact types

**Goal**: Confirm that `kb_read_artifact` returns non-null artifacts for each of the 4 PM artifact types written during implementation. This subtask exercises the live KB and validates the end-to-end dual-write pipeline.

**Prerequisite gate**: KFMB-1030 and KFMB-2040 must be complete before this subtask can execute. PM artifact types (`story_seed`, `test_plan`, `dev_feasibility`, `uiux_notes`) do not exist in the KB until KFMB-1030 ships. Integration verification requires the real KB on port 5433 (ADR-005 — mocked MCP responses prohibited).

**Steps**:
1. Run full `/pm-story generate` for TEST-9999 against a test epic directory
2. Call `kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'story_seed' })` — verify non-null response with correct `story_id` and `artifact_type`
3. Call `kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'test_plan' })` — verify non-null response with `strategy` field present
4. Call `kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'dev_feasibility' })` — verify non-null response with `feasible` and `confidence` fields
5. Call `kb_read_artifact({ story_id: 'TEST-9999', artifact_type: 'uiux_notes' })` — verify non-null response with `skipped` field (true or false depending on test story)

**Files to read**: none (live KB verification via MCP calls)

**ACs**: AC-8, AC-9, AC-10, AC-11
**Depends on**: ST-2, ST-3, ST-4, ST-5 (all 4 agent migrations complete); KFMB-1030 and KFMB-2040 (prerequisite gate)
**Verification**: All 4 `kb_read_artifact` calls return non-null artifacts matching the written content; no "not found" errors
**Estimated tokens**: 4,000
