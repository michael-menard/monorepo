---
# EVIDENCE.yaml - WINT-0200 Implementation Evidence
# Story: Create User Flows Schema with State/Capability Enums
# Purpose: Source of truth for implementation completion and quality gates

story_id: WINT-0200
phase: execution
iteration: 0
completed: "2026-02-16T21:27:47Z"

# === IMPLEMENTATION ARTIFACTS ===
implementation:
  files_created:
    - path: "packages/backend/orchestrator/src/artifacts/__types__/user-flows.ts"
      purpose: "Zod schema for user flows with states/capabilities enums"
      lines: 152
      exports:
        - UserFlowStateEnum (z.enum with 5 values)
        - UserFlowState (inferred type)
        - UserFlowCapabilityEnum (z.enum with 7 values)
        - UserFlowCapability (inferred type)
        - UserFlowStepSchema (z.object)
        - UserFlowStep (inferred type)
        - UserFlowSchema (z.object with max 7 steps)
        - UserFlow (inferred type)
        - UserFlowsSchema (z.object with max 5 flows)
        - UserFlows (inferred type)
      coverage: 100%

    - path: "packages/backend/orchestrator/src/artifacts/__tests__/user-flows.test.ts"
      purpose: "Comprehensive validation tests for user flows schema"
      lines: 535
      test_suites: 6
      total_tests: 29
      coverage_areas:
        - State enum validation (all 5 states)
        - Capability enum validation (all 7 capabilities)
        - Step schema validation (happy path, errors, edge cases)
        - Flow schema validation (max constraints, boundaries)
        - UserFlows schema validation (empty arrays, max flows)
        - Round-trip JSON validation
        - Example flow validation (all 5 states, 4+ capabilities)
        - JSON Schema structure validation

    - path: "packages/backend/orchestrator/src/artifacts/README.md"
      purpose: "Integration documentation for schema usage"
      lines: 253
      sections:
        - Overview and file locations
        - Required states documentation
        - Standard capabilities with CRUD mapping
        - Hard constraints (5 flows, 7 steps)
        - Agent validation usage examples
        - PO cohesion check integration (WINT-0210, WINT-4010, WINT-4070)
        - Extensibility guidance (adding states/capabilities)
        - Schema versioning strategy
        - Related stories links

    - path: "packages/backend/orchestrator/src/schemas/user-flows.schema.json"
      purpose: "JSON Schema Draft 2020-12 for file validation"
      lines: 107
      validation:
        - $schema: https://json-schema.org/draft/2020-12/schema
        - States enum (5 values)
        - Capabilities enum (7 values)
        - Max 5 flows constraint
        - Max 7 steps per flow constraint
        - Semver schema_version pattern
      notes: "Location is temporary pending WINT-0180 AC-2 decision"

    - path: "packages/backend/orchestrator/src/artifacts/__tests__/fixtures/example-user-flow.json"
      purpose: "Example user flow demonstrating all required elements"
      lines: 108
      demonstrates:
        - All 5 required states (loading, empty, validation_error, server_error, permission_denied)
        - 4 common capabilities (create, view, edit, delete)
        - Additional capabilities (upload, replace, download)
        - 4 flows (within max 5 constraint)
        - All flows have ≤7 steps
        - Proper JSON structure with descriptions
        - Schema version 1.0.0

  files_modified: []

# === UNIT TESTS ===
unit_tests:
  status: pass
  mode: local
  command: "cd packages/backend/orchestrator && pnpm test user-flows.test.ts"
  results:
    total: 29
    passed: 29
    failed: 0
    skipped: 0
  coverage:
    lines: 100
    branches: 100
    functions: 100
    statements: 100
  output: |
    ✓ src/artifacts/__tests__/user-flows.test.ts (29 tests) 7ms

    Test Files  1 passed (1)
         Tests  29 passed (29)
      Start at  21:27:47
      Duration  213ms (transform 34ms, setup 0ms, collect 39ms, tests 7ms, environment 0ms, prepare 37ms)

# === BUILD ===
build:
  status: pass
  command: "pnpm tsc --noEmit"
  output: |
    No type errors in user-flows files

# === E2E TESTS ===
e2e_tests:
  status: exempt
  reason: "Schema definition story - no UI/API endpoints or user-facing functionality"
  mode: n/a

# === ACCEPTANCE CRITERIA ===
acceptance_criteria:
  - id: AC-1
    title: "Create JSON Schema Definition"
    status: pass
    evidence: |
      JSON Schema file created at packages/backend/orchestrator/src/schemas/user-flows.schema.json
      - Valid JSON Schema Draft 2020-12 format
      - $schema, $id, title, description metadata present
      - States enum with 5 values (loading, empty, validation_error, server_error, permission_denied)
      - Capabilities enum with 7 values (create, view, edit, delete, upload, replace, download)
      - Max 5 flows constraint defined (maxItems: 5)
      - Max 7 steps per flow constraint defined (maxItems: 7)
      - schema_version field with semver pattern
      - Example flow validates successfully
      - Location is temporary pending WINT-0180 AC-2 decision

  - id: AC-2
    title: "Create Zod Schema for Runtime Validation"
    status: pass
    evidence: |
      Zod schema created at packages/backend/orchestrator/src/artifacts/__types__/user-flows.ts
      - UserFlowStateEnum: z.enum with 5 values
      - UserFlowCapabilityEnum: z.enum with 7 values
      - UserFlowsSchema: flows array with .max(5) constraint
      - UserFlowSchema: steps array with .max(7) constraint
      - All types exported via z.infer<>:
        * UserFlowState
        * UserFlowCapability
        * UserFlowStep
        * UserFlow
        * UserFlows
      - Valid flow parses successfully (29 passing tests)
      - Invalid flows throw ZodError with clear messages
      - Round-trip validation succeeds (JSON → Zod → JSON)

  - id: AC-3
    title: "Document States Enum"
    status: pass
    evidence: |
      Each state documented with TSDoc comments in user-flows.ts:
      - loading: Initial data fetch in progress (spinner, skeleton state)
      - empty: No data exists yet (empty state, call-to-action)
      - validation_error: User input failed validation (form errors, inline feedback)
      - server_error: Backend returned 5xx error (error boundary, retry option)
      - permission_denied: User lacks required permissions (403 state, upgrade prompt)

      Extensibility documentation in README.md:
      - Step-by-step guide for adding new states
      - Schema version increment process (minor version for new values)
      - Example showing before/after enum expansion
      - Coordination with WINT-0210/WINT-4xxx for cohesion rule updates

  - id: AC-4
    title: "Document Capabilities Enum"
    status: pass
    evidence: |
      Each capability documented with CRUD mapping in TSDoc comments:
      - create: Create new entity (C in CRUD)
      - view: Read/view entity (R in CRUD)
      - edit: Update existing entity (U in CRUD)
      - delete: Remove entity (D in CRUD)
      - upload: Upload file/media (initial upload)
      - replace: Replace file/media (update existing file)
      - download: Download file/media

      Extensibility documentation in README.md:
      - Step-by-step guide for adding new capabilities
      - CRUD mapping process for new capabilities
      - Schema version increment process
      - Coordination with WINT-4050 cohesion rules

  - id: AC-5
    title: "Create Validation Tests"
    status: pass
    evidence: |
      Test file created at packages/backend/orchestrator/src/artifacts/__tests__/user-flows.test.ts
      - Happy path tests:
        * Valid flow with all 5 states (passes)
        * Valid flow with all 7 capabilities (passes)
        * Flow with max 7 steps (boundary test, passes)
        * Feature with max 5 flows (boundary test, passes)
      - Error case tests:
        * Flow with >7 steps (fails with clear error: "cannot exceed 7 steps")
        * Feature with >5 flows (fails with clear error: "cannot exceed 5 flows")
        * Invalid state enum value (fails with enum error)
        * Invalid capability enum value (fails with enum error)
      - Edge case tests:
        * Empty flows array (passes, documented as early planning state)
        * Single flow with single step (minimum valid, passes)
        * Flow using all 7 capabilities (passes)
        * Flow using all 5 states (passes)
      - Round-trip validation test:
        * JSON → Zod → JSON consistency verified (passes)

      Test results: 29/29 passed
      Test coverage: 100% (lines, branches, functions, statements)

  - id: AC-6
    title: "Create Example User Flow"
    status: pass
    evidence: |
      Example file created at packages/backend/orchestrator/src/artifacts/__tests__/fixtures/example-user-flow.json
      - Demonstrates all 5 required states:
        * loading (3 instances)
        * empty (4 instances)
        * validation_error (1 instance)
        * server_error (1 instance)
        * permission_denied (1 instance)
      - Demonstrates 6 capabilities:
        * create (3 instances)
        * view (6 instances)
        * edit (3 instances)
        * delete (2 instances)
        * upload (1 instance)
        * replace (1 instance)
        * download (1 instance)
      - 4 flows (within max 5 constraint)
      - All flows have ≤7 steps (max is 4 steps)
      - Validates successfully against Zod schema (tests pass)
      - Validates successfully against JSON Schema (structure verified in tests)
      - Real-world scenario: LEGO Set Management feature

  - id: AC-7
    title: "Document Schema Integration"
    status: pass
    evidence: |
      README created at packages/backend/orchestrator/src/artifacts/README.md with:
      - Schema file locations documented:
        * Zod schema location (artifacts/__types__/user-flows.ts)
        * JSON Schema location (schemas/user-flows.schema.json - temp pending WINT-0180)
        * Example flow location (artifacts/__tests__/fixtures/example-user-flow.json)
      - Agent validation usage examples:
        * Import statement for Zod schema
        * .parse() usage with error handling
        * Type-safe access pattern
      - PO cohesion check integration explained:
        * WINT-0210 (Role Pack Templates) - validation during elaboration
        * WINT-4010 (Cohesion Sidecar) - automated validation service
        * WINT-4070 (cohesion-prosecutor Agent) - PO enforcement
        * Code examples for each integration point
      - WINT-0180 link and storage pattern notes
      - Related stories linked (WINT-0210, WINT-4010, WINT-4070)

# === QUALITY GATES ===
quality_gates:
  linting: pass
  linting_command: "pnpm eslint --no-ignore packages/backend/orchestrator/src/artifacts/__types__/user-flows.ts packages/backend/orchestrator/src/artifacts/__tests__/user-flows.test.ts"
  linting_output: "No errors (0 errors, 0 warnings)"

  type_checking: pass
  type_checking_command: "pnpm tsc --noEmit"
  type_checking_output: "No type errors in user-flows files"

  unit_tests: pass
  unit_tests_command: "pnpm test user-flows.test.ts"
  unit_tests_output: "29/29 tests passed"

  test_coverage: 100%
  test_coverage_detail: |
    user-flows.ts: 100% statements, 100% branches, 100% functions, 100% lines

  e2e_gate: exempt
  e2e_gate_reason: "Schema definition story - no user-facing functionality"

# === NOTES ===
implementation_notes:
  - JSON Schema location is temporary (packages/backend/orchestrator/src/schemas/user-flows.schema.json)
  - Will be moved once WINT-0180 AC-2 determines storage strategy
  - README.md documents this dependency
  - Zod schema is storage-agnostic and works with any WINT-0180 decision
  - Achieved 100% test coverage (deterministic validation logic)
  - All 7 acceptance criteria verified and passing
  - Example flow demonstrates real-world LEGO Set Management feature
  - Extensibility documentation enables future enum additions

next_steps:
  - Await WINT-0180 AC-2 decision on storage strategy
  - Move JSON Schema to final location once determined
  - Update README.md with final JSON Schema path
  - WINT-0210 (Role Pack Templates) can now consume schema
  - WINT-4010 (Cohesion Sidecar) can use schema for validation
  - WINT-4070 (cohesion-prosecutor Agent) can enforce completeness

risks_realized: []

blockers: []

# === SUMMARY ===
summary: |
  WINT-0200 execution phase completed successfully.

  All 7 acceptance criteria verified and passing:
  - AC-1: JSON Schema created with all required enums and constraints ✓
  - AC-2: Zod schema created with runtime validation ✓
  - AC-3: States enum documented with TSDoc and extensibility guide ✓
  - AC-4: Capabilities enum documented with CRUD mapping ✓
  - AC-5: Comprehensive test suite with 100% coverage ✓
  - AC-6: Example flow demonstrating all states and capabilities ✓
  - AC-7: Integration documentation with agent usage examples ✓

  Quality gates:
  - Linting: PASS (0 errors, 0 warnings)
  - Type checking: PASS (no type errors)
  - Unit tests: PASS (29/29 tests)
  - Test coverage: 100%
  - E2E tests: EXEMPT (schema definition, no UI)

  Files created: 5 (1,155 total lines)
  - Zod schema (152 lines, 100% coverage)
  - Test suite (535 lines, 29 tests)
  - README documentation (253 lines)
  - JSON Schema (107 lines)
  - Example flow (108 lines)

  Ready for Phase 3: Proof Generation
