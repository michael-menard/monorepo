schema: 1
story_id: INFR-0120
timestamp: "2026-02-15T20:30:00Z"
generated_by: dev-plan-leader
domain: infra-persistence
scope: "Create PostgreSQL schemas for 3 review/QA artifact types (evidence, review, qa-verify) with Drizzle ORM and Zod validation"

lessons_learned:
  - id: INFR-0110-L1
    title: "UUID primary keys with defaultRandom() pattern"
    summary: "Use uuid('id').primaryKey().defaultRandom() for consistency across all artifact tables"
    source: INFR-0110
    category: database-design
    applies_to: INFR-0120
    reason: "evidence_artifacts, review_artifacts, qa_verify_artifacts tables need UUID PKs"
    pattern_to_follow: "Apply uuid('id').primaryKey().defaultRandom() to all 3 review/QA artifact tables"

  - id: INFR-0110-L2
    title: "artifacts pgSchema and artifact_type_enum already exist"
    summary: "INFR-0110 created artifacts pgSchema and artifact_type_enum with all 7 types upfront (including evidence, review, qa-verify)"
    source: INFR-0110
    category: schema-reuse
    applies_to: INFR-0120
    reason: "INFR-0120 tables belong in existing artifacts pgSchema namespace"
    pattern_to_follow: "No pgSchema or enum creation needed - migration only creates 3 tables"

  - id: INFR-0110-L3
    title: "JSONB denormalization for array fields"
    summary: "Store small arrays (<20 items) as JSONB for co-location, not normalized tables"
    source: INFR-0110
    category: database-design
    applies_to: INFR-0120
    reason: "evidence_artifacts has ac_evidence, touched_files, commands_run, e2e_tests arrays; review_artifacts has findings, worker_results; qa_verify_artifacts has ac_verifications, test_results, qa_issues"
    pattern_to_follow: "Use jsonb().$type<T>() for typed JSONB columns per drizzle-zod pattern"

  - id: INFR-0110-L4
    title: "Foreign keys with ON DELETE CASCADE"
    summary: "Story deletion should automatically clean up artifacts"
    source: INFR-0110
    category: data-integrity
    applies_to: INFR-0120
    reason: "All 3 tables reference wint.stories.id foreign key"
    pattern_to_follow: "Add references(() => stories.id, { onDelete: 'cascade' }) to story_id columns"

  - id: INFR-0110-L5
    title: "drizzle-zod auto-generated schemas"
    summary: "Use createInsertSchema and createSelectSchema for runtime validation"
    source: INFR-0110
    category: type-safety
    applies_to: INFR-0120
    reason: "AC-5 requires Zod insert/select schemas for 3 review/QA artifact tables"
    pattern_to_follow: "Generate insert/select schemas immediately after table definitions"

  - id: INFR-0110-L6
    title: "Drizzle relations for lazy loading"
    summary: "Define relations: story → artifacts (one-to-many, lazy), artifact → story (many-to-one, eager)"
    source: INFR-0110
    category: orm-patterns
    applies_to: INFR-0120
    reason: "AC-6 requires Drizzle relations for 3 review/QA artifact tables"
    pattern_to_follow: "Use relations(stories, ({ many }) => ({ evidenceArtifacts: many(evidenceArtifactsTable) })) pattern"

  - id: INFR-0040-L1
    title: "Timestamp fields with timezone"
    summary: "Use timestamp('created_at', { withTimezone: true }).notNull().defaultNow()"
    source: INFR-0040
    category: database-design
    applies_to: INFR-0120
    reason: "All 3 tables require created_at and updated_at timestamp fields"
    pattern_to_follow: "Apply to created_at and updated_at columns in all 3 tables"

  - id: WINT-0010-L1
    title: "Snake_case for Postgres, camelCase for TypeScript"
    summary: "Drizzle ORM handles transformation automatically via column aliases"
    source: WINT-0010
    category: naming-conventions
    applies_to: INFR-0120
    reason: "Schema alignment between Postgres and TypeScript (documented in story Architecture Notes)"
    pattern_to_follow: "Use snake_case in table definitions, auto-mapped to camelCase in TypeScript"

architecture_decisions:
  - id: ADR-INFR-120-001
    title: "Reuse artifacts pgSchema and artifact_type_enum from INFR-0110"
    status: active
    decision: "Place 3 review/QA artifact tables in existing artifacts pgSchema, use existing enum values"
    rationale: "INFR-0110 created artifacts pgSchema and artifact_type_enum with all 7 types upfront for this purpose"
    constraints:
      - "No pgSchema creation in INFR-0120 migration"
      - "No enum creation in INFR-0120 migration"
      - "Migration only creates 3 tables with FKs to existing wint.stories"
    applies_to: INFR-0120

  - id: ADR-INFR-120-002
    title: "JSONB denormalization for evidence/review/qa-verify fields"
    status: active
    decision: "Store ac_evidence, findings, test_results as JSONB arrays"
    rationale: "Co-location pattern for small arrays (<20 items), matches INFR-0110 pattern"
    alternatives:
      - option: "Normalize to separate evidence_items, review_findings, qa_issues tables"
        rejected_reason: "Over-engineering for current query patterns - no need to query individual evidence items"
    applies_to: INFR-0120

  - id: ADR-INFR-120-003
    title: "No composite indexes in INFR-0120"
    status: active
    decision: "Reuse INFR-0110's idx_story_artifact composite index for evidence/review/qa-verify queries"
    rationale: "INFR-0110 created idx_story_artifact (story_id, artifact_type) covering all 7 artifact types"
    notes: "INFR-0120 migration does NOT add new indexes - existing composite index is sufficient"
    applies_to: INFR-0120

  - id: ADR-INFR-120-004
    title: "Update SCHEMA-REFERENCE.md instead of creating new doc"
    status: active
    decision: "AC-8 requires updating existing _implementation/SCHEMA-REFERENCE.md from INFR-0110"
    rationale: "Centralized schema documentation for all artifact tables (both INFR-0110 and INFR-0120)"
    applies_to: INFR-0120

migration_notes:
  - title: "Simpler migration than INFR-0110"
    note: "INFR-0120 migration is ~150 lines (3 tables only), INFR-0110 was ~250 lines (pgSchema + enum + 4 tables + indexes)"
    action: "Migration creates tables only - no pgSchema, no enum, no indexes"

  - title: "Migration dependencies verified"
    note: "INFR-0110 in UAT stage (artifacts pgSchema and artifact_type_enum exist), WINT-0010 in UAT stage (wint.stories table exists)"
    action: "Dependencies satisfied - safe to proceed"

  - title: "Rollback safety"
    note: "Rollback should drop 3 review/QA artifact tables cleanly without affecting INFR-0110 artifacts (story, plan, scope, knowledge_context tables)"
    action: "Test rollback immediately after apply to verify clean removal"

dependencies:
  blocking_resolved:
    - story_id: INFR-0110
      reason: "Requires artifacts pgSchema and artifact_type_enum"
      status: "UAT stage - completed"
    - story_id: WINT-0010
      reason: "Requires wint.stories table for foreign key constraints"
      status: "UAT stage - completed"

  related:
    - story_id: INFR-0020
      reason: "Artifact Writer/Reader service will use these schemas for bi-directional sync"
      note: "INFR-0020 blocked until INFR-0120 completes"

patterns_to_follow:
  - pattern: "Reuse existing pgSchema namespace"
    example: "artifactsSchema = pgSchema('artifacts')"
    source: "packages/backend/database-schema/src/schema/artifacts.ts"
    notes: "Already defined by INFR-0110"

  - pattern: "UUID primary keys with defaultRandom()"
    example: "id: uuid('id').primaryKey().defaultRandom()"
    source: "packages/backend/database-schema/src/schema/wint.ts:142"

  - pattern: "Timestamp fields with timezone"
    example: "timestamp('created_at', { withTimezone: true }).notNull().defaultNow()"
    source: "packages/backend/database-schema/src/schema/wint.ts:170"

  - pattern: "JSONB fields with type safety"
    example: "jsonb('metadata').$type<{ surfaces?: {...} }>()"
    source: "packages/backend/database-schema/src/schema/wint.ts:161"

  - pattern: "Foreign keys with ON DELETE CASCADE"
    example: "references(() => stories.id, { onDelete: 'cascade' })"
    source: "packages/backend/database-schema/src/schema/wint.ts:192"

  - pattern: "Auto-generated Zod schemas"
    example: "createInsertSchema(evidenceArtifactsTable)"
    source: "packages/backend/database-schema/src/schema/wint.ts:1636"

  - pattern: "Drizzle relations for lazy loading"
    example: "relations(stories, ({ many }) => ({ evidenceArtifacts: many(evidenceArtifactsTable) }))"
    source: "packages/backend/database-schema/src/schema/wint.ts:1454"

risks:
  - id: RISK-001
    description: "Schema alignment drift between Zod artifacts (packages/backend/orchestrator) and Postgres schema"
    severity: medium
    mitigation: "AC-7 unit tests validate JSONB field structure matches Zod schemas for evidence/review/qa-verify"

  - id: RISK-002
    description: "JSONB type safety not enforced at database level"
    severity: low
    mitigation: "Use drizzle-zod createInsertSchema for runtime validation before insert"

  - id: RISK-003
    description: "SCHEMA-REFERENCE.md location ambiguity"
    severity: low
    mitigation: "AC-8 clarifies update to _implementation/SCHEMA-REFERENCE.md (created by INFR-0110)"

constraints:
  - "Do not create new pgSchema - use existing artifacts schema from INFR-0110"
  - "Do not modify artifact_type_enum - use existing 'evidence', 'review', 'qa-verify' values"
  - "Do not create new indexes - reuse INFR-0110's idx_story_artifact"
  - "Use snake_case for Postgres columns, camelCase for TypeScript (Drizzle handles transformation)"
  - "All schemas must use Zod with z.infer<>, no TypeScript interfaces (CLAUDE.md requirement)"
  - "Foreign keys must reference wint.stories.id (UUID field, not story_id text field)"

token_optimization_patterns:
  - pattern: "Targeted file reads for schema patterns"
    description: "Read artifacts.ts from INFR-0110 for patterns, not full wint.ts schema"
    estimated_savings: "50% reduction in file read tokens (artifacts.ts ~200 lines vs wint.ts ~1600 lines)"
    applies_to: INFR-0120

  - pattern: "Reuse INFR-0110 test patterns"
    description: "Copy test structure from INFR-0110's review-qa-artifacts-schema.test.ts (if exists) or wint tests"
    estimated_savings: "Avoid reading multiple test files - focused on artifact validation patterns"
    applies_to: INFR-0120

metadata:
  sources_queried:
    - infr_0110_knowledge_context: 1
    - adr_log: 1
    - lessons_learned: 8
    - architecture_decisions: 4
  confidence: high
  notes: "Strong alignment with INFR-0110 patterns. All dependencies resolved (INFR-0110 and WINT-0010 in UAT). Migration is simpler than INFR-0110 (tables only, no pgSchema/enum)."
