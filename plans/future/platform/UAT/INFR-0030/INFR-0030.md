---
id: INFR-0030
title: MinIO/S3 Docker Setup + Client Adapter
status: uat
priority: P3
epic: INFR
wave: 2
created_at: "2026-02-14T20:45:00Z"
updated_at: "2026-02-15T04:35:00Z"
experiment_variant: control
depends_on:
  - INFR-0010
blocks: []
points: 3
touches_backend: true
touches_frontend: false
touches_database: false
touches_infra: true
story_type: infrastructure
qa_verdict: PASS
qa_timestamp: "2026-02-15T04:35:00Z"
---

# INFR-0030: MinIO/S3 Docker Setup + Client Adapter

## Context

The INFR epic establishes a three-tier persistence architecture:
1. **Postgres** - structured metadata, state, events
2. **MinIO/S3** - large artifact blobs (>500KB)
3. **pgvector** - embeddings for knowledge base

Currently, all orchestrator artifacts (YAML files, evidence, plans) are stored as files in the `plans/` directory tree. This works for small artifacts but doesn't scale for large files or distributed workflows.

**INFR-0010** (Postgres Artifact Schemas) defines artifact metadata tables. **INFR-0020** (Artifact Writer/Reader Service) will implement the inline vs blob storage split. **INFR-0030** provides the infrastructure and client for blob storage.

## Problem

- No local S3-compatible blob storage for development (production has AWS S3)
- Existing `@repo/s3-client` is AWS-only (no MinIO support)
- No bucket initialization or environment-aware client configuration
- Large artifacts (>500KB) can't be offloaded from Postgres without blob storage
- Developers need consistent local/production experience for artifact storage

## Proposed Solution

### 1. Docker Infrastructure

Add MinIO to Docker Compose stack at `infra/compose.lego-app.yaml`:
- S3 API on port 9000, web console on port 9001
- Persistent volume for data
- Health checks and auto-restart
- Default bucket creation on startup (`workflow-artifacts`)

### 2. S3 Client Adapter Enhancement

Enhance `@repo/s3-client` with MinIO compatibility:
- Environment detection (local vs production)
- `forcePathStyle: true` for MinIO
- Endpoint override for local development
- Bucket initialization helper
- Preserve existing AWS Lambda optimization (connection reuse)

### 3. Unified Storage Interface

Create S3-compatible adapter that works with both MinIO (local) and S3 (production):
- `uploadArtifact(key, content, metadata)` - upload blob
- `downloadArtifact(key)` - retrieve blob
- `deleteArtifact(key)` - remove blob
- `listArtifacts(prefix)` - list artifacts with prefix

## Acceptance Criteria

### Docker Infrastructure

- [ ] **AC-1:** MinIO service added to `infra/compose.lego-app.yaml` with health check
  - Service name: `minio`
  - Container name: `monorepo-minio`
  - Health check: `mc ready local` with retries
  - Restart policy: `unless-stopped`

- [ ] **AC-2:** MinIO data persisted in named volume `minio_data`
  - Volume definition in docker-compose.yml
  - Data survives container restarts
  - Mounted at `/data` in container

- [ ] **AC-3:** MinIO S3 API accessible on `localhost:9000` after `docker compose up`
  - Port 9000 exposed and accessible
  - S3 API responds to requests
  - Connection succeeds with proper credentials

- [ ] **AC-4:** MinIO web console accessible on `localhost:9001` (optional convenience)
  - Port 9001 exposed for web UI
  - Console login works with root credentials

- [ ] **AC-5:** Default bucket `workflow-artifacts` created automatically on startup
  - Bucket exists after MinIO initialization
  - Created via entrypoint script or init container
  - Idempotent (doesn't fail if bucket exists)

### S3 Client Adapter

- [ ] **AC-6:** S3Client enhanced with environment detection (local vs production)
  - Reads `NODE_ENV` environment variable
  - Reads `S3_ENDPOINT` for custom endpoint
  - Config schema validated with Zod
  - Clear error messages for misconfiguration

- [ ] **AC-7:** When `NODE_ENV=development` and `S3_ENDPOINT` set, client uses MinIO endpoint with `forcePathStyle: true`
  - S3Client configured with custom endpoint
  - `forcePathStyle: true` set for MinIO compatibility
  - Credentials from `S3_ACCESS_KEY_ID` and `S3_SECRET_ACCESS_KEY`

- [ ] **AC-8:** When production, client uses standard AWS S3 (existing behavior preserved)
  - No custom endpoint when `S3_ENDPOINT` not set
  - Default AWS credential provider chain
  - `forcePathStyle` not set (defaults to virtual-hosted style)
  - Existing AWS Lambda optimizations preserved

- [ ] **AC-9:** `initializeBucket(bucketName)` helper creates bucket if it doesn't exist
  - Function exported from `@repo/s3-client`
  - Idempotent (succeeds if bucket already exists)
  - Throws clear error if creation fails
  - Works with both MinIO and S3

- [ ] **AC-10:** Existing S3 functions (`uploadToS3`, `deleteFromS3`, `uploadToS3Multipart`) work with both MinIO and S3
  - All existing functions preserved
  - Work correctly with MinIO endpoint
  - Work correctly with AWS S3
  - No breaking changes to function signatures

### Integration

- [ ] **AC-11:** README updated with MinIO setup instructions (`docker compose up` starts MinIO)
  - Section: "Local Development Setup"
  - Command: `docker compose -f infra/compose.lego-app.yaml up -d minio`
  - Verification steps included
  - Troubleshooting section for common issues

- [ ] **AC-12:** Environment variable documentation added (`.env.example` shows S3/MinIO config)
  - Variables: `NODE_ENV`, `S3_ENDPOINT`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `AWS_REGION`
  - Default values for local development
  - Comments explaining each variable

- [ ] **AC-13:** S3 client adapter tested against local MinIO (manual smoke test: upload, download, delete)
  - Test script: `packages/backend/s3-client/scripts/test-minio.ts`
  - Performs: upload, download, delete operations
  - Outputs: success/failure for each operation
  - Runnable via: `pnpm tsx scripts/test-minio.ts`

## Non-Goals

- Artifact Writer/Reader Service implementation (INFR-0020)
- Postgres artifact schema implementation (INFR-0010)
- Automatic artifact migration from filesystem to MinIO
- S3 bucket lifecycle policies or retention rules
- MinIO multi-tenancy or access control beyond root credentials
- Production S3 bucket creation (handled by IaC/CloudFormation)
- Grafana/Prometheus metrics for MinIO
- S3 presigned URLs or public access configuration
- Cross-region replication or backup strategies
- Integration with orchestrator artifact writers (comes in INFR-0020)

## Reuse Plan

### Components to Reuse

- **Existing S3 client structure** in `packages/backend/s3-client/`
  - Singleton S3Client pattern with connection reuse
  - Existing upload/download/delete functions
  - Error handling patterns

- **Docker Compose pattern** from `infra/compose.lego-app.yaml`
  - Service naming convention: `monorepo-{service}`
  - Health check patterns (CMD, interval, retries)
  - Named volume patterns for data persistence
  - Restart policy: `unless-stopped`

- **Environment-driven configuration** following existing patterns
  - Zod schema validation for client configuration
  - Environment variable loading with defaults
  - Error messages for misconfiguration

### Packages to Use

- `@aws-sdk/client-s3` (already in `@repo/s3-client`)
- MinIO official Docker image (`minio/minio:latest`)
- MinIO Client (`mc`) for health checks and initialization

## Change Surface

### Files to Modify

1. **`infra/compose.lego-app.yaml`**
   - Add MinIO service definition
   - Configure ports, volumes, health checks
   - Estimated: 40-50 lines

2. **`packages/backend/s3-client/src/s3-client.ts`**
   - Add configuration schema (Zod)
   - Add environment detection logic
   - Add `initializeBucket()` helper
   - Update S3Client initialization
   - Estimated: 80-100 lines

3. **`packages/backend/s3-client/src/config.ts`** (new file)
   - S3/MinIO configuration loading
   - Environment variable parsing
   - Zod schema definitions
   - Estimated: 40-50 lines

### Configuration Files

- **`.env.example`** - Add S3/MinIO environment variables
- **`README.md`** (root or infra/) - MinIO setup instructions
- **`packages/backend/s3-client/README.md`** - Client configuration documentation

### New Infrastructure

- Docker named volume: `minio_data`
- MinIO service in Docker Compose stack
- MinIO initialization script (bucket creation)

## Test Plan Summary

### Manual Testing

1. **MinIO Startup:**
   - Start MinIO: `docker compose -f infra/compose.lego-app.yaml up -d minio`
   - Verify health: `docker ps | grep minio` shows healthy status
   - Check bucket: `docker exec monorepo-minio mc ls local/workflow-artifacts`

2. **S3 Client Operations:**
   - Upload file to MinIO
   - Download file from MinIO
   - Delete file from MinIO
   - Verify operations succeed

3. **Persistence:**
   - Upload file
   - Restart MinIO: `docker compose restart minio`
   - Verify file still exists

### Integration Testing

- Test file: `packages/backend/s3-client/__tests__/s3-client-minio.test.ts`
- Requires Docker running with MinIO
- Tests: environment detection, upload/download/delete, multipart upload, error handling

### Edge Cases

- MinIO not running (graceful error)
- Bucket already exists (idempotent)
- Large file upload (multipart)
- Empty file upload
- Special characters in keys

See: `_pm/TEST-PLAN.md` for complete test cases.

## Risk Assessment

### Split Risk: 0.2 (Low)

- **Reasoning:** Infrastructure stories typically have clear technical scope
- **13 ACs** reflect detailed testing requirements, not scope creep
- Well-defined boundaries: Docker service + S3 client enhancement

### Review Cycles: 2 (Expected)

- **Cycle 1:** Initial implementation review
- **Cycle 2:** Edge case handling and documentation polish

### Token Estimate: 120,000

- Conservative estimate for infrastructure work
- No historical INFR data available for comparison
- Adjusted from default (150k) based on clear scope

### Confidence: Low

- **Reason:** No similar stories in KB (INFR epic is new)
- WKFL-006 patterns unavailable
- Predictions based on heuristics only

See: `_pm/RISK-PREDICTIONS.yaml` for calculation details.

## Implementation Guidance

### Phase 1: Docker Setup (1-2 hours)

1. Add MinIO service to `infra/compose.lego-app.yaml`
2. Configure health check and named volume
3. Create bucket initialization script
4. Test: `docker compose up -d minio && docker ps`

### Phase 2: S3 Client Enhancement (2-3 hours)

1. Create configuration schema with Zod
2. Add environment detection logic
3. Update S3Client initialization
4. Implement `initializeBucket()` helper
5. Test against local MinIO

### Phase 3: Integration & Documentation (1 hour)

1. Update `.env.example` with variables
2. Add MinIO section to README
3. Manual smoke test
4. Create test script

**Total Estimate:** 8-10 hours (1-1.5 days)

## Evidence Requirements

### Proof of Completion

1. **MinIO Running:**
   ```bash
   docker ps | grep minio
   # Output: STATUS = healthy
   ```

2. **Bucket Exists:**
   ```bash
   docker exec monorepo-minio mc ls local/
   # Output: workflow-artifacts/
   ```

3. **Upload/Download Works:**
   ```typescript
   // Test script output:
   ✓ Uploaded test-artifact.txt
   ✓ Downloaded test-artifact.txt
   ✓ Content matches
   ```

4. **Environment Detection:**
   ```typescript
   // Log output shows:
   S3 Client initialized:
   - Mode: local (MinIO)
   - Endpoint: http://localhost:9000
   - ForcePathStyle: true
   ```

See: `_pm/DEV-FEASIBILITY.md` for complete evidence expectations.

## Dependencies

**Depends On:**
- **INFR-0010** (Postgres Artifact Schemas) - Metadata tables for artifact storage

**Blocks:**
- **INFR-0020** (Artifact Writer/Reader Service) - Needs MinIO infrastructure

**External Dependencies:**
- Docker installed and running
- MinIO Docker image pullable from Docker Hub
- AWS SDK v3 already in package.json

## Architecture Alignment

### Three-Tier Persistence

This story implements **Tier 2: Blob Storage** for the INFR persistence architecture:
- **Tier 1:** Postgres (metadata) - INFR-0010
- **Tier 2:** MinIO/S3 (blobs) - **INFR-0030** ← this story
- **Tier 3:** pgvector (embeddings) - existing

### Inline-First Strategy

MinIO/S3 supports the inline-first artifact storage strategy:
- Small artifacts (<500KB): inline in Postgres
- Large artifacts (>500KB): offload to MinIO/S3
- Metadata in Postgres, content location in S3

Implementation of storage decisions happens in INFR-0020.

## Related Documentation

- Test Plan: `_pm/TEST-PLAN.md`
- Dev Feasibility: `_pm/DEV-FEASIBILITY.md`
- Future Risks: `_pm/FUTURE-RISKS.md`
- Risk Predictions: `_pm/RISK-PREDICTIONS.yaml`

## Questions & Decisions

### Bucket Creation Strategy

**Decision:** Explicit initialization via `initializeBucket()` helper
- Helper function must be called explicitly (not automatic)
- Idempotent (succeeds if bucket exists)
- Clear error messages on failure
- Used in test scripts and setup documentation

**Rationale:** Clearer contract, less magic, explicit initialization

### MinIO Credentials

**Decision:** Use default credentials for local development
- Default: `minioadmin:minioadmin`
- Override via environment variables if needed
- README includes security warning: Never use defaults in production

### Health Check Timeout

**Decision:** Conservative timeouts for reliability
- Interval: 10s
- Timeout: 5s
- Retries: 3
- Start period: 30s (allows bucket initialization)

---

## Predictions

```yaml
split_risk: 0.2
review_cycles: 2
token_estimate: 120000
confidence: low
similar_stories: []
generated_at: "2026-02-14T20:45:00Z"
model: haiku
wkfl_version: "007-v1"
```

---

**Generated:** 2026-02-14T20:45:00Z
**Story Type:** Infrastructure
**Epic:** INFR (Infrastructure)
**Wave:** 2 - LangGraph Adapters & Schema
**Experiment Variant:** control

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-15_

### MVP Gaps Resolved

| # | Finding | Resolution | AC Added |
|---|---------|------------|----------|
| — | All critical gaps addressed | N/A | 0 |

### Non-Blocking Items (Logged to KB)

| # | Finding | Category | KB Entry |
|---|---------|----------|----------|
| 1 | No S3 presigned URLs for direct uploads | enhancement | deferred |
| 2 | No bucket versioning configuration | operational | deferred |
| 3 | No S3 lifecycle policies | cost-optimization | deferred |
| 4 | No MinIO access control beyond root | security | deferred |
| 5 | No cross-region replication setup | disaster-recovery | deferred |
| 6 | No automated bucket backup strategy | operational | deferred |
| 7 | Edge case: very large files (>5GB) | edge-case | deferred |
| 8 | Edge case: special characters in keys | edge-case | deferred |
| 9 | Edge case: MinIO not running | ux-enhancement | deferred |
| 10 | MinIO metrics to Prometheus | observability | deferred |
| 11 | MinIO web console SSO | ux-enhancement | deferred |
| 12 | CDN integration for public assets | performance | deferred |
| 13 | S3 Transfer Acceleration | performance | deferred |
| 14 | Artifact catalog indexing | integration | deferred |
| 15 | S3 event notifications | integration | deferred |
| 16 | MinIO replication to S3 | testing | deferred |
| 17 | CLI tool for MinIO operations | developer-ux | deferred |
| 18 | Integration tests for S3 operations | testing | deferred |
| 19 | Architecture decision record | documentation | deferred |

### Summary

- ACs added: 0
- KB entries created: 19 (deferred)
- Mode: autonomous
- Verdict: PASS
- Status: Ready for implementation
