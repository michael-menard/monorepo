---
story_id: WINT-0070
created_at: "2026-02-14T00:00:00Z"
reason: "KB connection unavailable during story generation"
retry_count: 0
---

# Deferred KB Write: WINT-0070

## SQL to Execute

```sql
INSERT INTO stories (
  story_id,
  feature,
  title,
  story_dir,
  story_file,
  story_type,
  points,
  priority,
  state,
  touches_backend,
  touches_frontend,
  touches_database,
  touches_infra
) VALUES (
  'WINT-0070',
  'platform',
  'Create Workflow Tracking Tables',
  'plans/future/platform/backlog/WINT-0070',
  'plans/future/platform/backlog/WINT-0070/WINT-0070.md',
  'validation',
  1,
  'critical',
  'backlog',
  true,
  false,
  true,
  true
)
ON CONFLICT (story_id) DO UPDATE SET
  title = EXCLUDED.title,
  story_dir = EXCLUDED.story_dir,
  story_file = EXCLUDED.story_file,
  story_type = EXCLUDED.story_type,
  points = EXCLUDED.points,
  priority = EXCLUDED.priority,
  state = EXCLUDED.state,
  touches_backend = EXCLUDED.touches_backend,
  touches_frontend = EXCLUDED.touches_frontend,
  touches_database = EXCLUDED.touches_database,
  touches_infra = EXCLUDED.touches_infra,
  updated_at = CURRENT_TIMESTAMP;
```

## Retry Instructions

Run this SQL when KB connection is available:
```bash
psql $KNOWLEDGE_BASE_URL -f /tmp/wint-0070-kb-insert.sql
```

Or via MCP tools when available:
```javascript
await kb.execute(sql)
```

## Story Metadata

- **Story ID**: WINT-0070
- **Feature**: platform
- **Type**: validation
- **Points**: 1
- **Priority**: critical
- **State**: backlog
- **Surfaces**: database, backend, infrastructure
