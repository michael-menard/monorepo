schema: 1
story_id: WINT-0150
timestamp: 2026-02-14T21:30:00Z

knowledge_context:
  loaded: true

  lessons_learned:
    count: 0
    relevant_to_scope: []
    blockers_to_avoid: []
    patterns_to_follow:
      - "Skills are documentation artifacts, not executable code"
      - "Use existing Skills (/review, /qa-gate) as structure templates"
      - "Extract content from existing command and agent files"
      - "YAML frontmatter format: name, description, mcp_tools_available"
      - "Section structure: Description → Usage → Parameters → Execution → Examples → Integration → Troubleshooting"
    patterns_to_avoid:
      - "Do not create unit tests for documentation artifacts"
      - "Do not modify existing command or agent files"
      - "Do not implement new features - this is documentation only"

  architecture_decisions:
    active_count: 0
    relevant_adrs: []
    constraints: {}
    notes:
      - "No ADRs apply to this story - documentation task with no code/infra changes"

  token_optimization:
    high_cost_operations: []
    recommended_patterns:
      - "Read existing Skill files for templates"
      - "Use targeted Read operations to extract specific sections from command/agent files"
      - "Single Write operation for SKILL.md creation"
    notes:
      - "This is a low-token task - primarily documentation assembly"

  story_context:
    domain: "documentation"
    scope: "Create standardized Skill definition file for doc-sync functionality"
    complexity: "XS"
    risk_level: "low"

  reuse_sources:
    structure_templates:
      - path: ".claude/skills/review/SKILL.md"
        purpose: "Multi-phase skill with sub-agents pattern"
      - path: ".claude/skills/qa-gate/SKILL.md"
        purpose: "Simpler skill pattern example"
    content_sources:
      - path: ".claude/commands/doc-sync.md"
        purpose: "Extract usage patterns, flags, examples"
      - path: ".claude/agents/doc-sync.agent.md"
        purpose: "Extract execution phases (lines 40-382)"
        note: "Contains 7 phase definitions with detailed workflows"

  validation_strategy:
    type: "manual_verification"
    checks:
      - "Verify all flags from command spec are documented"
      - "Verify all execution phases from agent are documented"
      - "Verify all examples from command spec are included"
      - "Verify troubleshooting section covers agent error scenarios"
      - "Cross-reference with existing Skills for format consistency"

  warnings: []
