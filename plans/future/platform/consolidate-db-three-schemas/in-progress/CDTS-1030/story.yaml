id: CDTS-1030
title: Update Drizzle schema.ts
status: in-progress
priority: medium
phase: 1
feature: "Update all table definitions in schema.ts to match CDTS-1020 DDL exactly. Add planDetails, storyDetails, planDependencies, storyKnowledgeLinks tables. Add soft-delete columns to entity tables. All FK .references() calls with onDelete RESTRICT. All tables remain in public schema (no pgSchema needed for public)."
goal: "Drizzle schema.ts compiles correctly and matches the database state after CDTS-1020 migration."
depends_on:
  - CDTS-1020
endpoints: []
infrastructure: []
acceptance_criteria:
  - "Add planDetails table definition (1:1 with plans) matching CDTS-1020 DDL"
  - "Add storyDetails table definition (1:1 with stories) matching CDTS-1020 DDL"
  - "Add planDependencies table definition matching CDTS-1020 DDL"
  - "Add storyKnowledgeLinks table definition with link_type enum and confidence score"
  - "Remove migrated columns from plans and stories table definitions (moved to detail tables)"
  - "Add deleted_at and deleted_by columns to plans, stories, tasks, knowledge_entries"
  - "All new FK .references() calls use { onDelete: 'restrict' }"
  - "analyticsSchema tables from CDTS-1010 already defined — verify they compile alongside new tables"
  - "pnpm check-types passes with zero errors"
risk_notes: "Drizzle raw SQL (sql tag) may be required for PostgreSQL enum ::text casts. Self-referencing FKs need forward-ref syntax. Cross-schema FK references (analytics -> public) require explicit table object references."
resolves_blockers:
  - "ENG-003: Atomic deployment (Drizzle must match target schema)"
sizing_warning: false
created: "2026-03-07T22:16:29Z"
