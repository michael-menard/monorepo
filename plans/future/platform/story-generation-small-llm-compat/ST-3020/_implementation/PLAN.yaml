schema: 2
story_id: "ST-3020"
timestamp: "2026-03-11T00:00:00Z"
subtask_source: story

steps:
  - id: 1
    subtask_id: "ST-1"
    description: "Spot-check dev-plan-leader.agent.md for subtask-mapping logic at lines 103-136 and document findings in EVIDENCE.yaml"
    files: []
    files_to_read:
      - .claude/agents/dev-plan-leader.agent.md
    dependencies: []
    slice: packages
    verification: "echo 'Verification: agent file contains subtask-mapping logic per line numbers documented in EVIDENCE.yaml'"
    acs_covered: ["AC-1"]

  - id: 2
    subtask_id: "ST-2"
    description: "Add PlanStepSchemaV2 and PlanSchemaV2 to packages/backend/orchestrator/src/artifacts/plan.ts with new fields: subtask_id, files_to_read, verification, acs_covered, subtask_source"
    files:
      - packages/backend/orchestrator/src/artifacts/plan.ts
    files_to_read:
      - .claude/agents/dev-plan-leader.agent.md
    dependencies: []
    slice: packages
    verification: "pnpm check-types --filter @repo/orchestrator"
    acs_covered: ["AC-2"]

files_to_change:
  - path: packages/backend/orchestrator/src/artifacts/plan.ts
    action: modify
    reason: "Add PlanStepSchemaV2 (with subtask_id, files_to_read, verification, acs_covered) and PlanSchemaV2 (schema: 2 literal + subtask_source) to support schema: 2 PLAN.yaml from dev-plan-leader"

commands_to_run:
  - command: "pnpm check-types --filter @repo/orchestrator"
    when: "after all code changes"
    required: true
  - command: "pnpm test --filter @repo/orchestrator"
    when: "after all code changes"
    required: true
  - command: "pnpm build --filter @repo/orchestrator"
    when: "after all code changes"
    required: true

acceptance_criteria_map:
  - ac_id: "AC-1"
    planned_evidence: "File inspection: dev-plan-leader.agent.md lines 103-136 contain subtask-mapping logic; documented in EVIDENCE.yaml with exact line citations"
    evidence_type: file
  - ac_id: "AC-2"
    planned_evidence: "Code change: plan.ts exports PlanStepSchemaV2 and PlanSchemaV2 with all required fields; TypeScript compilation passes"
    evidence_type: command

architectural_decisions: []

complexity: simple

notes:
  - "ST-3020 risk_notes confirm dev-execute-leader.agent.md v3.2.0 already has schema: 2 execution path at lines 55-121 (Step 2: Determine Execution Mode + Step 4: subtask iteration)"
  - "dev-plan-leader.agent.md v1.3.0 already has subtask-mapping logic at lines 103-136 (Step 3: Read Story ACs, Subtasks... and Step 4: Generate PLAN.yaml schema: 2 path)"
  - "Step 1 is read-only verification — no code written; findings go directly into EVIDENCE.yaml"
  - "Step 2 adds schema: 2 Zod types to plan.ts to match the PLAN.yaml structure already produced by dev-plan-leader"
  - "No story file created under ST-3020 (story lives in story.yaml not ST-3020.md); story ACs inferred from feature/goal fields"
