knowledge_context:
  loaded: true
  timestamp: "2026-02-15T02:15:00Z"

  lessons_learned:
    count: 0
    relevant_to_scope: []
    blockers_to_avoid: []
    patterns_to_follow:
      - "Use Drizzle ORM table() builder with wintSchema namespace (see WINT-0010)"
      - "Define enums in public schema using pgEnum() for cross-namespace reusability"
      - "Use UUID PKs with defaultRandom() for all tables"
      - "Include timestamps (createdAt, updatedAt) with timezone: true"
      - "Define indexes inline in table callback using composite and single-column patterns"
      - "Define relations in separate *Relations objects outside table definitions"
      - "Use cascade delete for all FKs to stories.id"
      - "Auto-generate Zod schemas using drizzle-zod createInsertSchema/createSelectSchema"
    patterns_to_avoid:
      - "Manual Zod schema writing (use drizzle-zod generators)"
      - "Creating new pgSchema namespaces (extend existing wintSchema)"
      - "Modifying existing WINT-0010 tables (protected)"

  architecture_decisions:
    active_count: 0
    relevant_adrs: []
    constraints: {}

  notes:
    - "Story is pure backend database schema work - no frontend, no API endpoints"
    - "ADR-001 through ADR-006 do not apply (no API paths, no auth, no testing strategy beyond unit tests)"
    - "WINT-0010 provides the foundation - extend existing wintSchema with 5 new tables"
    - "KBAR-0010 provides artifact tracking reference implementation patterns"
    - "Drizzle ORM v0.44.3 active with PostgreSQL Aurora"

  token_optimization:
    high_cost_operations: []
    recommended_patterns:
      - "Read wint.ts in sections (table definitions, then relations) to avoid large file reads"
      - "Use targeted file reads for test patterns"
      - "Batch related table definitions together"

  warnings: []
