feasible: true
confidence: high
rationale: "All production code for the 4 artifact MCP tool handlers is complete (delivered by KBAR-0110/KBAR-0120). This story is test authorship only. The structural template (story-tools-integration.test.ts) exists and has the identical pattern required. The fixture factories (createMockArtifact, createMockArtifactListItem) are already defined in artifact-tools.test.ts and just need to be copied. The only novel element is the authorization test for kb_delete_artifact (PM-only), which is well-documented in the seed."
complexity: low

change_surface:
  areas:
    - "apps/api/knowledge-base/src/mcp-server/__tests__/ (new file only)"
  endpoints: []
  deploy_touchpoints: []

risks:
  - finding: "KBAR-0130 and KBAR-0140 must be merged before implementation to avoid mcp-integration.test.ts tool count drift"
    mitigation: "Implementation start gate: confirm both KBAR-0130 and KBAR-0140 are merged to main. Run pnpm test --filter @repo/knowledge-base first to establish baseline pass."

missing_requirements: []

evidence_expectations:
  - "pnpm test --filter @repo/knowledge-base passes with all new test cases"
  - "pnpm check-types --filter @repo/knowledge-base passes with no TypeScript errors"
  - "Test output shows all 3 authorization FORBIDDEN cases for kb_delete_artifact (dev, qa, all roles)"
  - "Test output shows all 4 DB error sanitization cases pass"

subtasks:
  - id: ST-1
    title: "Verify dependency landing state and create artifact-tools-integration.test.ts scaffold"
    goal: "Confirm KBAR-0130 and KBAR-0140 are merged; verify artifact-tools.test.ts and story-tools-integration.test.ts exist; create the new test file with vi.hoisted + vi.mock setup copied from those files"
    files_to_read:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools-integration.test.ts"
      - "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts"
      - "apps/api/knowledge-base/src/mcp-server/__tests__/test-helpers.ts"
    files_to_modify:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools-integration.test.ts (create)"
    acs: ["AC-10"]
    depends_on: "none"
    verification: "pnpm check-types --filter @repo/knowledge-base"
    estimated_tokens: 6000

  - id: ST-2
    title: "Implement happy-path tests for kb_write_artifact, kb_read_artifact (hit + miss), kb_list_artifacts"
    goal: "Write tests for AC-1, AC-2, AC-3 that call handleToolCall with valid inputs and assert correct routing, isError: undefined, and parseable JSON response shapes"
    files_to_read:
      - "apps/api/knowledge-base/src/crud-operations/artifact-operations.ts"
      - "apps/api/knowledge-base/src/mcp-server/tool-handlers.ts"
    files_to_modify:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools-integration.test.ts"
    acs: ["AC-1", "AC-2", "AC-3"]
    depends_on: "ST-1"
    verification: "pnpm test --filter @repo/knowledge-base -- --reporter=verbose 2>&1 | grep 'PASS\\|FAIL'"
    estimated_tokens: 8000

  - id: ST-3
    title: "Implement kb_delete_artifact happy-path and validation/DB error tests for all 4 tools"
    goal: "Write AC-4 (delete success/not-found), AC-5 (validation errors for all 4 tools), AC-6 (DB error sanitization for all 4 tools) test cases"
    files_to_read:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/test-helpers.ts"
    files_to_modify:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools-integration.test.ts"
    acs: ["AC-4", "AC-5", "AC-6"]
    depends_on: "ST-2"
    verification: "pnpm test --filter @repo/knowledge-base -- --reporter=verbose"
    estimated_tokens: 8000

  - id: ST-4
    title: "Implement authorization tests for kb_delete_artifact FORBIDDEN paths and open-access verification"
    goal: "Write AC-7 (FORBIDDEN for dev/qa/all), AC-8 (open-access for write/read/list), AC-9 (business logic not called when FORBIDDEN) authorization tests"
    files_to_read:
      - "apps/api/knowledge-base/src/mcp-server/access-control.ts"
      - "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools-integration.test.ts"
    files_to_modify:
      - "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools-integration.test.ts"
    acs: ["AC-7", "AC-8", "AC-9"]
    depends_on: "ST-3"
    verification: "pnpm test --filter @repo/knowledge-base && pnpm check-types --filter @repo/knowledge-base"
    estimated_tokens: 7000
