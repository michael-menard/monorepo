# KB Write Queue for KBAR-0050
# Generated by elab-autonomous-decider
# To be processed by kb-writer agent or orchestration system

story_id: KBAR-0050
generated_at: "2026-02-17T00:00:00Z"

# 12 non-blocking findings queued for KB writes
# (Gap #1 from ANALYSIS.md — integration test subtask — was promoted to AC-11)
# (Gap #2 from ANALYSIS.md — dry-run approach ambiguity — was promoted to AC-12)
kb_write_requests:
  # Non-blocking gaps (5 items from FUTURE-OPPORTUNITIES.md)
  - request_id: "kbar-0050-gap-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] Relative --story-dir Path Resolution Test Coverage Gap**
        - **Finding**: --story-dir accepts relative paths but syncStoryToDatabase requires an absolute filePath. No test specifically asserts that relative input (e.g., plans/future/...) is correctly resolved to an absolute path before calling the sync function
        - **Impact**: Low - AC-12 clarifies the pattern; gap is in test coverage only
        - **Effort**: Low - add one test case to ST-7 asserting path.resolve(process.cwd(), storyDir, storyId + '.md') is passed as filePath when a relative --story-dir is provided
        - **Recommendation**: Add this test case explicitly to sync-story.test.ts (ST-7) to prevent a latent boundary type error
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "test-coverage"
        - "path-resolution"
        - "kbar-sync"

  - request_id: "kbar-0050-gap-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] --from-db Flag Missing from AC List**
        - **Finding**: --from-db flag (syncStoryFromDatabase) is mentioned in the Goal section and Reuse Plan but is absent from the AC list. It has no AC, no subtask, and no test scenario
        - **Impact**: Low - does not block MVP delivery; implementing agent may add it as bonus behavior or skip it entirely
        - **Effort**: Low - add a minimal AC covering --from-db behavior in a follow-up story
        - **Recommendation**: Either add a minimal AC covering --from-db behavior in KBAR-0050-B or a follow-up story, or add an explicit note in the story's Non-Goals section to prevent scope creep during implementation
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "scope-clarity"
        - "from-db"
        - "kbar-sync"

  - request_id: "kbar-0050-gap-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] --help Flag Output Not Unit-Tested**
        - **Finding**: --help flag is specified in AC-7 ("prints usage without error") but no subtask tests the --help output format or validates that all flags are listed. The reference script (populate-story-status.ts) does not implement --help either
        - **Impact**: Low - AC-7 requires --help to work; test coverage is the gap
        - **Effort**: Low - add one test asserting that --help exits 0 and prints flag names; consider using Node's built-in node:util parseArgs which handles --help automatically
        - **Recommendation**: Implement --help using parseArgs from node:util (built-in, no new dependency) and add a unit test asserting exit 0 and presence of key flag names in output
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "test-coverage"
        - "help-flag"
        - "kbar-sync"

  - request_id: "kbar-0050-gap-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] Single-Artifact Targeting Path Untested in Unit Tests**
        - **Finding**: --artifact-file + --artifact-type single-artifact targeting (AC-2) has no unit test scenario in AC-10 or TEST-PLAN.md. The test plan covers batch artifacts (--artifacts) but not the single-artifact path that calls syncArtifactToDatabase directly
        - **Impact**: Low - single-artifact path is a distinct code branch with its own error surface
        - **Effort**: Low - add one test case to ST-7: invoke sync:story --artifact-file _implementation/PLAN.yaml --artifact-type plan and assert syncArtifactToDatabase called with correct input
        - **Recommendation**: Add this test case to sync-story.test.ts (ST-7) to ensure the single-artifact path is exercised in coverage measurement
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "test-coverage"
        - "single-artifact"
        - "kbar-sync"

  - request_id: "kbar-0050-gap-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] --force Implementation Detail Implicit in Architecture Notes Only**
        - **Finding**: SyncStoryToDatabaseInput does not accept a force parameter. The CLI must implement force bypass by skipping its pre-sync checksum check. This is implied by Architecture Notes but not explicit in any subtask. If an implementing agent reads only subtasks, they may miss this implementation detail
        - **Impact**: Low - resolved by AC-12 addition which explicitly states the --force pattern
        - **Effort**: Low - AC-12 now covers this; no additional action needed
        - **Recommendation**: AC-12 is the canonical reference. Note to future elaborators: always cross-check function signatures in __types__/index.ts before writing Architecture Notes that assume parameter support
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "elab-lesson"
        - "force-flag"
        - "kbar-sync"

  # Enhancement opportunities (7 items from FUTURE-OPPORTUNITIES.md)
  - request_id: "kbar-0050-enh-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] Machine-Readable JSON Output Mode (--json flag)**
        - **Finding**: The story specifies human-readable stdout output only. For CI pipeline integration, a structured JSON output mode would allow downstream tools to parse sync results without brittle text parsing
        - **Impact**: Medium - CI adoption of sync commands depends on machine-readable output
        - **Effort**: Low - add a --json flag that switches output from human-readable to { storyId, status, checksum, syncEventId, error } JSON object per operation
        - **Recommendation**: Add --json flag to both commands in a follow-up story (KBAR-0050-followup or KBAR-0060+); output one JSON object per line (NDJSON format) for streaming compatibility
      additional_tags:
        - "non-blocking"
        - "integration"
        - "future-work"
        - "ci-compatibility"
        - "json-output"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] Estimated Time in Dry-Run Output — No Implementation Guidance**
        - **Finding**: AC-6 requires "estimated time based on average sync rate (if available)" in dry-run output. No implementation guidance exists for computing or storing the average sync rate. Could result in the field being silently omitted or hardcoded to a placeholder
        - **Impact**: Low - estimated time is informational; omitting it does not break dry-run correctness
        - **Effort**: Medium - requires tracking sync duration in kbar.syncEvents and computing a rolling average from recent events
        - **Recommendation**: In the initial implementation, emit "estimated time: unavailable (no historical data)" as the fallback. Track sync duration in kbar.syncEvents as a follow-up. Defer computation to KBAR-0060+ or an analytics story
      additional_tags:
        - "non-blocking"
        - "future-work"
        - "estimated-time"
        - "dry-run"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] sync:story --from-db Reverse Sync Direction**
        - **Finding**: syncStoryFromDatabase is listed in the Reuse Plan but excluded from the ACs. A reverse sync command would be useful for restoring filesystem state from the database (DB-to-filesystem direction)
        - **Impact**: Medium - useful for disaster recovery and state reconciliation scenarios
        - **Effort**: Low - thin CLI wrapper around existing syncStoryFromDatabase function; same parse-validate-delegate-print pattern
        - **Recommendation**: Add --from-db as a future AC in KBAR-0050-B or a follow-up story targeting the DB-to-filesystem sync path; distinguish output format from filesystem-to-DB sync to avoid confusion
      additional_tags:
        - "non-blocking"
        - "integration"
        - "future-work"
        - "from-db"
        - "reverse-sync"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] Columnar Output Formatting for Large Batch Results**
        - **Finding**: Current output spec ("KBAR-0050: synced (checksum: abc123, syncEventId: ev-001)") is functional but not visually scannable for large batches. A columnar format with aligned columns for ID, status, checksum would improve readability
        - **Impact**: Low - developer ergonomics improvement for sync:epic output with 50+ stories
        - **Effort**: Low - use Node's built-in string padding (padEnd) or a lightweight column formatter
        - **Recommendation**: Not MVP-blocking; add as a UX enhancement in a polish story. Ensure NO_COLOR and non-TTY modes are respected before adding any formatting
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "enhancement"
        - "output-formatting"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] sync:epic --artifacts Mode for Per-Story Artifact Sync**
        - **Finding**: sync:epic does not specify artifact sync in addition to story sync. A common workflow would be sync:epic --artifacts (sync all stories AND all artifacts in one pass). AC-5 adds artifact-type batch sync (batchSyncByType) but this is type-scoped, not per-story
        - **Impact**: Medium - common operator workflow requires multiple command invocations without this flag
        - **Effort**: Medium - extend sync:epic to call batchSyncArtifactsForStory for each story discovered after syncStoryToDatabase; aggregate artifact results alongside story results in the summary
        - **Recommendation**: Add sync:epic --artifacts mode as a natural extension of AC-4; intentionally excluded from KBAR-0050 to keep scope bounded. Add in KBAR-0055 or KBAR-0060
      additional_tags:
        - "non-blocking"
        - "future-work"
        - "enhancement"
        - "epic-sync"
        - "artifact-sync"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-6"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] --since <date> Incremental Filter for Large Story Bases**
        - **Finding**: Default incremental behavior relies on checksum comparison. For 100+ stories, a date-based filter (--since 2026-01-01) would allow skipping checksum computation for stories with filesystem mtime older than the last sync
        - **Impact**: Low - checksum comparison is already efficient for typical story counts; becomes relevant at 1000+ stories
        - **Effort**: High - requires mtime tracking in kbar.stories or kbar.artifacts; adds complexity to the batch query path
        - **Recommendation**: Defer to a performance-focused story. Track as a potential optimization when kbar.stories row count exceeds 500
      additional_tags:
        - "non-blocking"
        - "performance"
        - "optimization"
        - "incremental-filter"
        - "kbar-sync"

  - request_id: "kbar-0050-enh-7"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "KBAR-0050"
      category: "future-opportunities"
      content: |
        **[KBAR-0050] ANSI Color Output for CLI Status Differentiation**
        - **Finding**: Synced vs skipped vs failed are plain text. Adding green/yellow/red ANSI coloring (behind NO_COLOR env var check and process.stdout.isTTY guard for CI) would improve developer ergonomics when scanning large sync:epic output
        - **Impact**: Low - UX improvement; CI pipelines already use NO_COLOR by convention
        - **Effort**: Low - check process.env.NO_COLOR and process.stdout.isTTY before wrapping status strings with ANSI escape codes; no new dependency needed
        - **Recommendation**: Add as a polish enhancement. Implement only after core functionality is complete and tested; ensure TTY detection prevents ANSI codes in piped/CI output
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "enhancement"
        - "ansi-color"
        - "cli"
        - "kbar-sync"

summary:
  total_requests: 12
  gaps: 5
  enhancements: 7
  note: >
    Gap #1 (integration test subtask missing) and Gap #2 (dry-run approach ambiguity) from
    ANALYSIS.md MVP-Critical Gaps section were promoted to AC-11 and AC-12 in KBAR-0050.md.
    ST-9 subtask was added to cover integration test implementation.
    All 12 KB write requests here are genuinely non-blocking findings from FUTURE-OPPORTUNITIES.md
    and do not impact MVP delivery of KBAR-0050.

notes: |
  Categories:
  - Edge cases / test coverage gaps: 5 (relative path resolution, --from-db scope, --help test, single-artifact test, --force implicit pattern)
  - Feature completeness / integrations: 4 (--json output, --from-db reverse sync, sync:epic --artifacts, --since date filter)
  - UX polish: 3 (columnar output, estimated time in dry-run, ANSI color)
