schema: 1
story_id: "KBAR-0060"
version: 1
timestamp: "2026-02-20T23:00:00Z"

acceptance_criteria:
  - ac_id: "AC-1"
    ac_text: "syncStoryToDatabase persists kbar.stories + kbar.artifacts rows for all 5 story phases, returns syncStatus=skipped on identical second sync, and returns syncStatus=completed with updated checksum on content modification"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts"
        description: "5 tests pass: AC-1 TC-1.1 (new story completed), AC-1 TC-1.2 (idempotency skipped), AC-1 TC-1.3 (checksum update completed), AC-1 TC-1.4 (plan phase), AC-1 TC-1.5 (done phase)"

  - ac_id: "AC-2"
    ac_text: "syncArtifactToDatabase accepts all 9 NonStoryArtifactType values, returns skipped on idempotent re-sync, batchSyncArtifactsForStory discovers and syncs multiple artifacts with fail-soft"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts"
        description: "12 tests pass: AC-2 TC-2.1-2.9 (all 9 artifact types), AC-2 TC-2.10 (idempotency), AC-2 TC-2.11 (batch discovery), AC-2 TC-2.12 (fail-soft missing file)"

  - ac_id: "AC-3"
    ac_text: "detectConflict returns hasConflict=true with conflictType=checksum_mismatch when DB checksum differs from filesystem; returns hasConflict=false when checksums match"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts"
        description: "3 tests pass: AC-3 TC-3.1 (story conflict checksum_mismatch), AC-3 TC-3.2 (artifact conflict), AC-3 TC-3.3 (no-conflict matching checksums)"

  - ac_id: "AC-4"
    ac_text: "sync:epic discovers 5+ story directories, filters by --epic prefix, exits 0 on empty dir, exits 0 on no-match filter, and continues processing remaining stories when one fails (fail-soft)"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/scripts/__tests__/integration/kbar0060.integration.test.ts"
        description: "5 tests pass: AC-4 TC-4.1 (5+ batch dryRunEpic), AC-4 TC-4.2 (KBAR prefix filter skips WINT-*), AC-4 TC-4.3 (empty dir exits 0), AC-4 TC-4.4 (no-match filter exits 0), AC-4 TC-4.5 (corrupt YAML fail-soft)"

  - ac_id: "AC-5"
    ac_text: "batchSyncByType reads kbar.sync_checkpoints row on startup and resumes from last_processed_path; updates checkpoint row after successful batch"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/scripts/__tests__/integration/kbar0060.integration.test.ts"
        description: "3 tests pass: AC-5 TC-5.1 (pre-seeded checkpoint row), AC-5 TC-5.2 (batchSyncByType resumes from seeded position), AC-5 TC-5.3 (checkpoint row updated_at after batch)"

  - ac_id: "AC-6"
    ac_text: "dryRunStory leaves kbar.stories row count unchanged; dryRunEpic issues exactly 1 DB query for N stories (N+1 prevention via single batch SELECT)"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/scripts/__tests__/integration/kbar0060.integration.test.ts"
        description: "3 tests pass: AC-6 TC-6.1 (dryRunStory zero-mutation row count), AC-6 TC-6.2 (dryRunEpic 4 stories 1 DB query), AC-6 TC-6.3 (dryRunEpic batch query counter verification)"

  - ac_id: "AC-7"
    ac_text: "syncStoryToDatabase rejects symlinks, path traversal, corrupt YAML, and missing files without leaving partial DB writes; handles unicode content correctly"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts"
        description: "5 tests pass: AC-7 TC-7.1 (symlink rejection before DB), AC-7 TC-7.2 (path traversal rejection no partial writes), AC-7 TC-7.3 (corrupt YAML records failed sync_event), AC-7 TC-7.4 (missing file no partial writes), AC-7 TC-7.5 (unicode content raw-byte checksum)"

  - ac_id: "AC-8"
    ac_text: "All integration tests use describe.skipIf(SKIP_TESTCONTAINERS), shared helper provides full KBAR schema setup, beforeAll timeout 90000ms, afterAll cleans up containers and temp dirs, vitest.integration.config.ts includes src/**/*.integration.test.ts"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts"
        description: "Shared helper with SKIP_TESTCONTAINERS guard, startKbarTestContainer, applyKbarSchema (6 enums + 7 tables), createTempDir (under cwd/plans/), stopKbarTestContainer, afterAll cleanup"
      - type: test
        path: "packages/backend/kbar-sync/vitest.integration.config.ts"
        description: "Updated include to ['scripts/**/*.integration.test.ts', 'src/**/*.integration.test.ts'], pool:forks singleFork:true, testTimeout:90000"
      - type: command
        command: "SKIP_TESTCONTAINERS=true pnpm --filter @repo/kbar-sync test:integration"
        result: "SUCCESS — 39 tests skipped (3 files)"

touched_files:
  - path: "packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts"
    action: created
    lines: 264
    description: "Shared testcontainers helper: SKIP_TESTCONTAINERS guard, startKbarTestContainer, applyKbarSchema (6 enums + 7 tables), createTempDir (under cwd/plans/), stopKbarTestContainer, removeTempDir"

  - path: "packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts"
    action: created
    lines: 530
    description: "Integration tests for AC-1 (story sync, 5 phases), AC-2 (artifact sync, 9 types), AC-3 (conflict detection), AC-7 (edge cases: symlink, path traversal, corrupt YAML, missing file, unicode)"

  - path: "packages/backend/kbar-sync/scripts/__tests__/integration/kbar0060.integration.test.ts"
    action: created
    lines: 389
    description: "Integration tests for AC-4 (epic batch sync), AC-5 (checkpoint resumption), AC-6 (dry-run zero-mutation + N+1 prevention)"

  - path: "packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts"
    action: modified
    lines: 302
    description: "Fixed pre-existing KBAR-0050 issues: CREATE TYPE IF NOT EXISTS replaced with DO $$ BEGIN ... EXCEPTION pattern, added POSTGRES_USERNAME env var, testDir moved to cwd/plans/ for validateFilePath compatibility"

  - path: "packages/backend/kbar-sync/vitest.integration.config.ts"
    action: modified
    lines: 27
    description: "Updated include to cover src/**/*.integration.test.ts, added pool:forks singleFork:true for testcontainers Ryuk reaper compatibility"

commands_run:
  - command: "pnpm --filter @repo/kbar-sync check-types"
    result: SUCCESS
    output: "No type errors"
    timestamp: "2026-02-20T22:50:00Z"

  - command: "pnpm --filter @repo/kbar-sync build"
    result: SUCCESS
    output: "Build complete"
    timestamp: "2026-02-20T22:50:00Z"

  - command: "pnpm --filter @repo/kbar-sync test:integration"
    result: SUCCESS
    output: "Test Files 3 passed (3), Tests 39 passed (39)"
    timestamp: "2026-02-20T22:57:00Z"

  - command: "SKIP_TESTCONTAINERS=true pnpm --filter @repo/kbar-sync test:integration"
    result: SUCCESS
    output: "Test Files 3 skipped (3), Tests 39 skipped (39)"
    timestamp: "2026-02-20T22:58:00Z"

endpoints_exercised: []

test_summary:
  integration: { pass: 39, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "Backend-only story (story_type: feature, no frontend). Per ADR-006: for backend-only stories, testcontainers integration tests with real PostgreSQL fulfill the E2E requirement."
  tests_written: false
  results:
    total: 0
    passed: 0
    failed: 0
    skipped: 0

coverage: {}

notable_decisions:
  - "Separated KBAR-0060 tests into a dedicated kbar0060.integration.test.ts file instead of appending to sync-cli.integration.test.ts (KBAR-0050). This avoids @repo/db pg.Pool singleton invalidation when multiple containers are used in the same Vitest worker."
  - "Used pool:forks singleFork:true in vitest.integration.config.ts to ensure testcontainer Ryuk reaper runs sequentially across test files, preventing 'Expected Reaper to map exposed port 8080' errors."
  - "testcontainers.ts createTempDir creates directories under cwd/plans/ (not tmpdir()) to pass validateFilePath security check in syncStoryToDatabase."
  - "Fixed pre-existing KBAR-0050 sync-cli.integration.test.ts issues: CREATE TYPE IF NOT EXISTS syntax (PG 16.3+ only) replaced with DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN null; END $$ pattern, added POSTGRES_USERNAME env var alongside POSTGRES_USER."

known_deviations:
  - "PLAN.yaml ST-4 specified extending sync-cli.integration.test.ts with KBAR-0060 tests. Instead, a separate kbar0060.integration.test.ts was created. This deviation is intentional and superior: prevents @repo/db pool singleton conflicts between containers in the same file, follows the principle of test isolation."

token_summary:
  execute: { in: 150000, out: 80000 }
