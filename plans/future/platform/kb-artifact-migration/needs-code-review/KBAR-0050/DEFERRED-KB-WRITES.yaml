---
# Deferred KB Writes — KBAR-0050
# Generated: 2026-02-17
# Reason: KB unavailable (psql not accessible at story generation time)
# Retry: Run this SQL against the knowledge base PostgreSQL instance
---

writes:
  - table: stories
    operation: upsert
    attempted_at: "2026-02-17T00:00:00Z"
    reason_deferred: "psql not available in PM generation environment"
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'KBAR-0050',
        'kb-artifact-migration',
        'CLI Sync Commands',
        'plans/future/platform/kb-artifact-migration/backlog/KBAR-0050',
        'plans/future/platform/kb-artifact-migration/backlog/KBAR-0050/KBAR-0050.md',
        'feature',
        3,
        'medium',
        'backlog',
        true,
        false,
        false,
        false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;
