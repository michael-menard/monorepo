---
id: KBAR-0060
title: "Sync Integration Tests"
status: ready-for-qa
priority: P2
story_type: feature
points: 5
phase: 2
epic: kb-artifact-migration
experiment_variant: "control"
depends_on:
  - KBAR-0050
blocks:
  - KBAR-0070
frontend_impacted: false
touches_backend: true
touches_frontend: false
touches_database: true
touches_infra: false
created: "2026-02-20"
---

# KBAR-0060: Sync Integration Tests

## Context

The `packages/backend/kbar-sync/` package now has a complete set of sync functions (KBAR-0030, KBAR-0040) and CLI commands (KBAR-0050). Unit tests for each story validated individual function behavior in isolation. However, no integration test suite validates the entire sync pipeline end-to-end with:

- Real production-like story data (multi-state, multi-dependency KBAR stories)
- Edge cases: missing files, corrupt YAML, empty directories, cross-epic prefix filtering
- Batch operations at scale with checkpoint resumption
- The full CLI command flow (argument parsing → Zod validation → sync function delegation → output)

Without integration tests against real PostgreSQL and real-world story fixtures:
- Checksum idempotency across story states is unverified with authentic data
- `sync:epic --epic KBAR` filter correctness is only unit-tested with synthetic data
- Batch checkpoint resumption has not been exercised with real `kbar.sync_checkpoints` rows
- Edge cases unique to real KBAR story YAML structures (multi-dependency frontmatter, large artifact counts) may cause failures not caught by unit tests
- ADR-005 compliance for this phase of KBAR development is incomplete without a full integration pass

**Implementation gate:** KBAR-0050 must be merged to `main` before KBAR-0060 implementation begins. The CLI scripts (`sync-story.ts`, `sync-epic.ts`) and their exported helpers (`dryRunStory`, `dryRunEpic`) must be stable before integration tests import them.

## Goal

Create a comprehensive integration test suite for `packages/backend/kbar-sync/` that:

1. Uses testcontainers (`@testcontainers/postgresql`) against real PostgreSQL to test all story sync scenarios
2. Tests with real KBAR story YAML fixtures covering all story states, artifact types, and dependency structures
3. Validates edge cases: empty directories, corrupt/missing YAML, symlink paths, cross-epic prefix filtering, large batches
4. Exercises the CLI commands (`sync:story`, `sync:epic`) via their exported module functions (`dryRunStory`, `dryRunEpic`)
5. Verifies batch performance characteristics: single-query dry-run, checkpoint resumption from pre-seeded state, fail-soft behavior

The test suite runs via `pnpm --filter @repo/kbar-sync test:integration` using the already-configured `vitest.integration.config.ts`.

## Non-Goals

- **Adding new sync functions** — KBAR-0060 tests existing functions only; no new business logic
- **Modifying `@repo/kbar-sync` source files** — tests are additive; sync function implementations are not changed
- **Modifying CLI scripts** (`sync-story.ts`, `sync-epic.ts`) — CLI behavior tested as-is
- **Performance benchmarking** — sync rate measurement and throughput benchmarks are deferred to a future performance story
- **UI or API testing** — no HTTP endpoints, no frontend components
- **Full epic scan of production data** — tests use synthetic temp-directory fixtures, not live `plans/` directory content
- **Automated CI integration** — KBAR-0060 delivers the test suite; CI pipeline integration is a separate concern
- **MCP tool integration** — testing the MCP tool layer (KBAR-0070+) is out of scope

## Scope

### Package Under Test

- `packages/backend/kbar-sync/` (`@repo/kbar-sync`)

### New Files

| File | Purpose |
|------|---------|
| `packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts` | Shared testcontainers helper: schema setup, container lifecycle, `skipIf` guard |
| `packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts` | Integration tests for AC-1, AC-2, AC-3, AC-7, AC-8 |

### Extended Files

| File | Change |
|------|--------|
| `packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts` | Add AC-4, AC-5, AC-6 test cases (extends existing KBAR-0050 AC-11 tests) |

### Functions Under Test

| Function | Source | ACs |
|----------|--------|-----|
| `syncStoryToDatabase` | `@repo/kbar-sync` (KBAR-0030) | AC-1 |
| `syncStoryFromDatabase` | `@repo/kbar-sync` (KBAR-0030) | AC-1 |
| `detectSyncConflicts` | `@repo/kbar-sync` (KBAR-0030) | AC-3 |
| `syncArtifactToDatabase` | `@repo/kbar-sync` (KBAR-0040) | AC-2 |
| `detectArtifactConflicts` | `@repo/kbar-sync` (KBAR-0040) | AC-3 |
| `batchSyncArtifactsForStory` | `@repo/kbar-sync` (KBAR-0040) | AC-2 |
| `batchSyncByType` | `@repo/kbar-sync` (KBAR-0040) | AC-4, AC-5 |
| `dryRunStory` | `scripts/sync-story.ts` (KBAR-0050) | AC-6 |
| `dryRunEpic` | `scripts/sync-epic.ts` (KBAR-0050) | AC-4, AC-6 |

## Acceptance Criteria

- [ ] **AC-1**: Integration tests for `syncStoryToDatabase` with real story state fixtures
  - Test with story fixtures covering all states: `backlog`, `in-progress`, `ready-to-work`, `uat`, `completed`
  - Verify idempotency: second sync of unchanged story returns `skipped`
  - Verify checksum update: modified story content produces `completed` sync with updated checksum in `kbar.stories`
  - Fixture files must be valid YAML matching `StoryFrontmatterSchema` (validated before writing)
  - Uses testcontainers (real PostgreSQL) — zero mocks

- [ ] **AC-2**: Integration tests for artifact sync functions with real artifact type coverage
  - Test `syncArtifactToDatabase` for all `NonStoryArtifactType` values: `elaboration`, `plan`, `scope`, `evidence`, `review`, `test_plan`, `decisions`, `checkpoint`, `knowledge_context`
  - Verify idempotency per artifact type: second sync returns `skipped` when file unchanged
  - Verify that `batchSyncArtifactsForStory` discovers and syncs all artifacts for a story directory with multiple artifact types present
  - Test handles missing artifact files gracefully (fail-soft: other artifacts sync successfully when one is missing)

- [ ] **AC-3**: Integration tests for conflict detection with story and artifact data
  - Test `detectSyncConflicts`: sync story → modify file → detect conflict returns `hasConflict: true` with `conflictType: 'checksum_mismatch'`
  - Test `detectArtifactConflicts`: sync artifact → modify file → detect conflict returns `hasConflict: true`
  - Test no-conflict scenario: sync then detect without modification returns `hasConflict: false`
  - Verify conflict is recorded in `kbar.sync_conflicts` table with correct checksums

- [ ] **AC-4**: Integration tests for `sync:epic` batch with real KBAR story fixtures
  - Test batch sync of 5+ real KBAR story directories using `batchSyncByType` or `sync:epic` CLI function
  - Test `--epic KBAR` prefix filter: batch only processes stories matching `KBAR-*` prefix, skips `WINT-*` directories in same base
  - Test empty base directory: exits 0 with "no stories found" message, zero DB mutations
  - Test `--epic` filter with no matches: exits 0 with informational message
  - Test fail-soft: one story with corrupt YAML causes that story to fail; remaining stories sync successfully; final exit code reflects failure count

- [ ] **AC-5**: Integration tests for checkpoint resumption with real PostgreSQL
  - Pre-seed `kbar.sync_checkpoints` row with `last_processed_path` pointing to mid-batch position
  - Call `batchSyncByType` with matching `checkpointName` — verify it resumes from seeded position (stories before checkpoint are skipped, stories after are processed)
  - Verify checkpoint row is updated (`last_processed_path`, `total_processed`) after batch completes
  - Test interruption scenario: verify checkpoint state is persisted before batch completes

- [ ] **AC-6**: Dry-run zero-mutation verification with batch queries
  - Verify `dryRunStory` (from `sync-story.ts`) makes zero writes to `kbar.stories` or `kbar.artifacts`
  - Verify `dryRunEpic` (from `sync-epic.ts`) issues exactly ONE batch DB query for N stories (N+1 prevention per KBAR-0040 lesson)
  - Verify row counts in `kbar.stories` and `kbar.artifacts` are identical before and after dry-run execution
  - Test with 3+ story directories in a single `dryRunEpic` call

- [ ] **AC-7**: Edge case integration tests
  - **Symlink path**: Pass symlink path as `storyDir` — verify security rejection before any DB interaction
  - **Path traversal**: Pass `../../etc/passwd` style path — verify rejection with exit 1
  - **Corrupt YAML**: Story directory contains invalid YAML frontmatter — sync fails gracefully with error in `kbar.sync_events` (status: `failed`), does not throw uncaught exception
  - **Missing story file**: Directory exists but `STORY-ID.md` is absent — sync returns appropriate error, no partial DB writes
  - **Unicode in story content**: Story with unicode titles/descriptions syncs correctly; checksum computed from raw bytes

- [ ] **AC-8**: Test infrastructure and configuration
  - All integration tests use `describe.skipIf(process.env.SKIP_TESTCONTAINERS === 'true')` guard
  - Container setup creates complete KBAR schema: all enums, all tables with `IF NOT EXISTS` guards, `sync_checkpoints` table
  - `beforeAll` timeout: 90 seconds (container startup)
  - `afterAll` cleans up container and temp directories
  - Tests run via `pnpm --filter @repo/kbar-sync test:integration` (configured in `vitest.integration.config.ts`)
  - No `as any` casts in test code; all fixture data validated against `StoryFrontmatterSchema`

## Reuse Plan

| Item | Location | How to Reuse |
|------|----------|--------------|
| Full testcontainers schema setup | `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` | Extract `beforeAll` schema SQL to shared `helpers/testcontainers.ts` — all enums + 7 tables with `IF NOT EXISTS` guards |
| KBAR-0050 AC-11 CLI tests | `packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts` | Extend this file with AC-4, AC-5, AC-6 — do NOT replace existing tests |
| Story sync patterns | `packages/backend/kbar-sync/src/__tests__/integration.integration.test.ts` | Happy-path story sync, idempotency, and conflict test patterns |
| `@repo/kbar-sync` exports | `packages/backend/kbar-sync/src/index.ts` | All sync functions and Zod schemas (`StoryFrontmatterSchema`, `NonStoryArtifactTypeSchema`) |
| `vitest.integration.config.ts` | `packages/backend/kbar-sync/vitest.integration.config.ts` | Already includes `scripts/**/*.integration.test.ts` — no new config required |

## Architecture Notes

### ADR Compliance

- **ADR-005** (Testing Strategy): All integration tests use real PostgreSQL via testcontainers. Zero mocks permitted in any test file covered by this story.
- **ADR-006** (E2E Tests): `frontend_impacted: false` — Playwright E2E is not required. Testcontainers integration tests fulfill the E2E requirement for this backend-only story.

### Test Isolation Pattern

Each integration test must be independent:
- No shared mutable state between `it` blocks
- Data seeded in `beforeEach` or within the test body itself
- Temp directories use `os.tmpdir()` + unique `Date.now()` suffix

### Schema Setup Canonical Reference

Use `artifact-sync.integration.test.ts` schema (lines 66-176) as the canonical source — it is the most complete, including `sync_checkpoints`, `artifact_versions`, and `artifact_content_cache` tables. The `integration.integration.test.ts` schema from KBAR-0030 is incomplete (missing these tables) and must NOT be used as the reference.

### CLI Import Pattern

```typescript
// CORRECT: direct module import (in-process, not subprocess)
import { dryRunStory } from '../../../scripts/sync-story.js'
import { dryRunEpic } from '../../../scripts/sync-epic.js'

// WRONG: subprocess invocation
import { execSync } from 'node:child_process'
execSync('pnpm sync:story ...')  // fragile, slow, env var issues
```

### Fixture Validation Pattern

```typescript
import { StoryFrontmatterSchema } from '@repo/kbar-sync'

// REQUIRED: validate before writing to disk
const frontmatter = StoryFrontmatterSchema.parse({
  id: 'KBAR-TEST-001',
  title: 'Test Story Backlog',
  status: 'backlog',
  priority: 'P2',
  story_type: 'feature',
})
// Write validated frontmatter to temp file
```

### Query Count Instrumentation (AC-6)

```typescript
// Pattern for N+1 query count assertion
let queryCount = 0
client.on('query', () => { queryCount++ })

const before = queryCount
await dryRunEpic({ baseDir: testDir, epic: 'KBAR' })
const queryCountForBatch = queryCount - before

expect(queryCountForBatch).toBe(1)  // exactly 1 batch query for N stories
```

## Infrastructure Notes

No new infrastructure required:
- `@testcontainers/postgresql` already in `kbar-sync` devDependencies
- `pg` already in dependencies
- `vitest.integration.config.ts` already configured
- No new NPM packages needed
- No new Drizzle schemas (tests use raw `pg.Client` for schema setup + `@repo/kbar-sync` functions for sync operations)

## Test Plan

See `_pm/TEST-PLAN.md` for the complete test plan.

### Summary

| AC | Test Cases | Strategy |
|----|-----------|---------|
| AC-1 | TC-1.1 through TC-1.5 | All 5 story states + idempotency + checksum update |
| AC-2 | TC-2.1 through TC-2.4 | All 9 artifact types + batch discovery + fail-soft |
| AC-3 | TC-3.1 through TC-3.4 | Story + artifact conflict detection, no-conflict scenarios |
| AC-4 | TC-4.1 through TC-4.5 | 5+ stories, prefix filter, empty dir, fail-soft |
| AC-5 | TC-5.1 through TC-5.3 | Pre-seeded checkpoint, resume, row update |
| AC-6 | TC-6.1 through TC-6.3 | Zero-mutation by row count, 1 batch query by count |
| AC-7 | TC-7.1 through TC-7.5 | Symlink, path traversal, corrupt YAML, missing file, unicode |
| AC-8 | Checklist | skipIf guard, schema IF NOT EXISTS, 90s timeout, afterAll cleanup |

**Run command:** `pnpm --filter @repo/kbar-sync test:integration`

## Reality Baseline

- Baseline file: Not available (baseline_path: null)
- Context reconstructed from: codebase scanning, KBAR-0050 story file, existing integration test files, ADR-LOG.md
- KBAR-0050 status: `ready-to-work` (not yet implemented — KBAR-0060 implementation must wait for KBAR-0050 merge)
- Existing integration test files referenced as canonical patterns exist at:
  - `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` (KBAR-0040)
  - `packages/backend/kbar-sync/src/__tests__/integration.integration.test.ts` (KBAR-0030)
  - `packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts` (KBAR-0050 AC-11)

## Canonical References

Files that demonstrate the patterns this story should follow:

| Pattern | File | Why |
|---------|------|-----|
| Integration test with full testcontainers setup and KBAR schema | `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` | Most complete testcontainers harness: all enums, all tables including `sync_checkpoints`, `beforeAll`/`afterAll` lifecycle, `skipIf` guard, 90s timeout — KBAR-0060 copies and extends this pattern |
| CLI-layer integration tests with exported test helpers | `packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts` | Already tests `dryRunStory` and `dryRunEpic` via imported module — KBAR-0060 extends these with real epic story fixtures and edge cases |
| Batch sync with checkpoint resumption | `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` (AC-5 test) | Pattern for pre-seeding `kbar.sync_checkpoints` and verifying resumption via `batchSyncByType` |
| Testcontainers skip guard for CI | `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` | `shouldSkip` pattern using `process.env.SKIP_TESTCONTAINERS` or Docker availability check |

## Subtasks

### ST-1: Shared Test Harness Helper
**File:** `packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts`
**Scope:** Extract container startup, KBAR schema creation SQL (all 7 tables + 6 enums with `IF NOT EXISTS`), `skipIf` guard function, temp dir utilities from `artifact-sync.integration.test.ts`
**Canonical reference:** `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` (lines 38-187)
**ACs:** AC-8
**Estimate:** ~1.5 hours

### ST-2: Story Sync Integration Tests
**File:** `packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts` (new file)
**Scope:** AC-1 (all 5 story states, idempotency, checksum update) + AC-3 story-level conflict detection + AC-7 edge cases (symlink, path traversal, corrupt YAML, missing file, unicode)
**Canonical reference:** `packages/backend/kbar-sync/src/__tests__/integration.integration.test.ts` (story sync patterns); shared helper from ST-1
**ACs:** AC-1, AC-3 (story portion), AC-7
**Estimate:** ~2 hours

### ST-3: Artifact Sync Integration Tests
**File:** `packages/backend/kbar-sync/src/__tests__/sync-integration.integration.test.ts` (extend with `describe` block)
**Scope:** AC-2 (all 9 `NonStoryArtifactType` values, idempotency, batch discovery via `batchSyncArtifactsForStory`, fail-soft for missing file) + AC-3 artifact-level conflict detection
**Canonical reference:** `packages/backend/kbar-sync/src/__tests__/artifact-sync.integration.test.ts` (AC-1, AC-4 patterns); `NonStoryArtifactTypeSchema` from `@repo/kbar-sync`
**ACs:** AC-2, AC-3 (artifact portion)
**Estimate:** ~1.5 hours

### ST-4: Epic Batch + Checkpoint + Dry-Run Integration Tests
**File:** `packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts` (extend existing)
**Scope:** AC-4 (5+ story batch, `--epic KBAR` filter, empty dir, fail-soft) + AC-5 (pre-seeded checkpoint, resume from mid-batch, checkpoint row update) + AC-6 (zero-mutation by row count, exactly 1 batch query by count for `dryRunEpic`)
**Canonical reference:** Existing KBAR-0050 AC-11 tests in same file; direct module import pattern for `dryRunStory`/`dryRunEpic`
**ACs:** AC-4, AC-5, AC-6
**Estimate:** ~2.5 hours

### ST-5: Integration Verification Pass
**Scope:** Run `pnpm --filter @repo/kbar-sync test:integration` — verify all new tests pass, zero regressions in existing integration tests, `SKIP_TESTCONTAINERS=true` skips all tests, no `as any` casts, no barrel file imports, `afterAll` cleans up all containers and temp dirs
**ACs:** AC-8 (infrastructure verification)
**Estimate:** ~30 minutes

---

## AC-Subtask Coverage Matrix

| AC | ST-1 | ST-2 | ST-3 | ST-4 | ST-5 |
|----|------|------|------|------|------|
| AC-1 | | X | | | |
| AC-2 | | | X | | |
| AC-3 | | X | X | | |
| AC-4 | | | | X | |
| AC-5 | | | | X | |
| AC-6 | | | | X | |
| AC-7 | | X | | | |
| AC-8 | X | | | | X |

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-20_

### MVP Gaps Resolved

No MVP gaps identified that require AC additions. All 9 audit checks passed.

### Non-Blocking Items (Logged to KB)

| # | Finding | Category | KB Entry |
|---|---------|----------|----------|
| 1 | AC-5 TC-5.3 interruption simulation underdefined | design-ambiguity | Pre-seed partial checkpoint state mitigation (non-blocking) |
| 2 | Index scope uses "wish epic stories" language | artifact-precision | stories.index.md KBAR-006 entry imprecise vs. synthetic fixtures scope |
| 3 | AC-5 TC-5.3 live interruption simulation not fully specified | testing-future | Defer fault-injection interruption testing to dedicated future story |
| 4 | Symlink test (AC-7 TC-7.1) may skip in CI environments without fs.symlinkSync | edge-case | Wrap in try/catch; detect CI and skip with descriptive message |
| 5 | No cross-package integration — only @repo/kbar-sync tested | scope-boundary | Future story: full vertical integration from API route handler to kbar.stories |
| 6 | Performance benchmarking deferred | future-enhancement | Future perf story: time batchSyncByType over 100+ stories, establish baseline |
| 7 | CI pipeline integration out of scope | devops-future | Future story: add SKIP_TESTCONTAINERS toggle to CI workflow |
| 8 | No monorepo testcontainers shared helper | architecture-opportunity | Future: packages/backend/test-helpers/ for PostgreSQL integration tests |
| 9 | computeChecksum import path may silently change after KBAR-0050 merge | regression-guard | Add smoke-test assertion in ST-5: computeChecksum returns non-empty string |
| 10 | Unicode edge case (AC-7 TC-7.5) only tests story frontmatter | test-coverage-future | Future: unicode testing for artifact file content + Node.js encoding modes |

### Summary

- ACs added: 0
- KB entries created: 10
- Mode: autonomous
- Verdict: PASS
- All 8 acceptance criteria ready for implementation
- All 5 subtasks have clear file assignments and canonical references
- Test infrastructure verified: testcontainers pattern, fixture validation with Zod, CLI module import patterns documented
- No story modifications required beyond elaboration completion
