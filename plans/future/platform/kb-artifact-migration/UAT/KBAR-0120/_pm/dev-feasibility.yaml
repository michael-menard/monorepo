feasible: true
confidence: high
rationale: |
  All 4 artifact tool handlers (handleKbReadArtifact, handleKbWriteArtifact, handleKbListArtifacts,
  handleKbDeleteArtifact) are fully implemented and registered in the MCP dispatch table. The
  canonical test pattern exists in story-tools.test.ts (KBAR-0090 output) and is directly
  reusable. No new files need to be modified — only a single new test file created.
  The vi.hoisted + vi.mock pattern is well-established in this codebase. No DB schema changes,
  no new tool registrations, no integration test modifications required.
complexity: low
change_surface:
  areas:
    - apps/api/knowledge-base/src/mcp-server/__tests__/
  endpoints: []
  deploy_touchpoints: []

risks:
  - finding: "null-return serialization: handleKbReadArtifact returns the literal string 'null' as text content (not JSON null) when artifact not found. Tests must assert text === 'null' exactly, not null === null."
    mitigation: "Explicit EC-1 test case in test plan covers this. Dev must read handler code at lines 3397-3407 before writing this test."

  - finding: "Mock isolation: artifact operations must be mocked via vi.mock('../../crud-operations/artifact-operations.js') not the broader '../../crud-operations/index.js' used by mcp-integration.test.ts."
    mitigation: "Seed explicitly calls this out. Canonical reference is story-tools.test.ts which uses the specific module path pattern."

missing_requirements: []

evidence_expectations:
  - "pnpm test apps/api/knowledge-base passes with new artifact-tools.test.ts included"
  - "pnpm check-types --filter @repo/api-kb passes with no type errors"
  - "Tool count assertion in mcp-integration.test.ts remains 53 (unchanged)"
  - "All 4 handler describe blocks have: happy path, not-found/null path, invalid input, DB rejection subtests"

subtasks:
  - id: ST-1
    title: "Scaffold artifact-tools.test.ts with vi.hoisted mocks and vi.mock block"
    goal: "Create the test file skeleton: file header, vi.hoisted mock fns for all 4 operations, vi.mock('../../crud-operations/artifact-operations.js'), logger mock, handler imports, mockDeps, createMockArtifact fixture factory"
    files_to_read:
      - apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts
      - apps/api/knowledge-base/src/mcp-server/__tests__/test-helpers.ts
      - apps/api/knowledge-base/src/crud-operations/artifact-operations.ts
    files_to_modify:
      - apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts
    acs: ["AC-1", "AC-6"]
    depends_on: none
    verification: "pnpm check-types --filter @repo/api-kb"
    estimated_tokens: 6000

  - id: ST-2
    title: "Write tests for handleKbReadArtifact (AC-2)"
    goal: "Implement describe('handleKbReadArtifact') with 4 test cases: artifact found (HP-1), artifact not found returns string 'null' (EC-1), invalid input (EC-2), DB rejection (EC-3). Focus on the null-string serialization edge case."
    files_to_read:
      - apps/api/knowledge-base/src/mcp-server/tool-handlers.ts
    files_to_modify:
      - apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts
    acs: ["AC-2"]
    depends_on: ST-1
    verification: "pnpm test apps/api/knowledge-base -- --reporter=verbose artifact-tools"
    estimated_tokens: 5000

  - id: ST-3
    title: "Write tests for handleKbWriteArtifact (AC-3)"
    goal: "Implement describe('handleKbWriteArtifact') with 3 test cases: successful write (HP-2), invalid input/missing fields (EC-4), DB rejection (EC-5). Include iteration: 2 edge case (ED-2)."
    files_to_read:
      - apps/api/knowledge-base/src/mcp-server/tool-handlers.ts
    files_to_modify:
      - apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts
    acs: ["AC-3"]
    depends_on: ST-1
    verification: "pnpm test apps/api/knowledge-base -- --reporter=verbose artifact-tools"
    estimated_tokens: 4000

  - id: ST-4
    title: "Write tests for handleKbListArtifacts and handleKbDeleteArtifact (AC-4, AC-5)"
    goal: "Implement describe('handleKbListArtifacts') with 4 tests (HP-3, HP-5, EC-6, EC-7) and describe('handleKbDeleteArtifact') with 4 tests (HP-4, EC-8, EC-9, EC-10). Cover empty list, include_content, deleted:false, invalid UUID."
    files_to_read:
      - apps/api/knowledge-base/src/mcp-server/tool-handlers.ts
    files_to_modify:
      - apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts
    acs: ["AC-4", "AC-5"]
    depends_on: ST-1
    verification: "pnpm test apps/api/knowledge-base -- --reporter=verbose artifact-tools"
    estimated_tokens: 5000

  - id: ST-5
    title: "Run full test suite and verify type checks and coverage gate"
    goal: "Run pnpm test apps/api/knowledge-base (all tests), pnpm check-types, verify mcp-integration.test.ts still asserts 53 tools. Fix any type errors. Confirm coverage gate (45% global) is met or exceeded."
    files_to_read:
      - apps/api/knowledge-base/src/mcp-server/__tests__/mcp-integration.test.ts
    files_to_modify: []
    acs: ["AC-7", "AC-8"]
    depends_on: ST-4
    verification: "pnpm test:all && pnpm check-types:all"
    estimated_tokens: 3000
