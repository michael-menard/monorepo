schema: 1
story_id: "KBAR-0080"
timestamp: "2026-02-25T04:30:00Z"

failure_source: "needs-work"
previous_iteration: 0
current_iteration: 1
phase: "fix"

qa_verdict: "FAIL"
qa_failed_at: "2026-02-24T21:20:00Z"

critical_issues:
  - ac_id: "AC-2"
    severity: "FAIL"
    description: >
      handleKbListStories filter tests incomplete. Story AC-2 explicitly requires
      'Each filter is independently tested' for 9 filter types: feature, epic, state,
      states[] (plural takes precedence over singular), phase, blocked, priority, limit,
      offset. Only feature+limit and state+limit are covered. Missing 7 scenarios:
      epic filter, states[] filter, states[] precedence over state, phase filter,
      blocked filter, priority filter, offset pagination.
    action_required: >
      Add independent unit tests for each missing filter: epic, states[], states[]
      precedence over singular state, phase, blocked, priority, offset. Minimum 7
      additional test cases needed in story-tools.test.ts handleKbListStories describe block.
    test_file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    auto_fixable: false

secondary_issues:
  - ac_id: "AC-8"
    severity: "NOTE"
    description: >
      Terminal-state guard returns {story: existing[0], updated: false, message} rather than
      {story: null, updated: false, message}. Story Architecture Notes specify soft-return
      pattern but do not explicitly specify the story field value in the error case.
      Not a blocking failure — implementation is consistent and functional.
    action_required: >
      PM clarification only: confirm whether story field in terminal-state error response
      should be null or the existing story object. No code change required for QA pass.
    code_file: "apps/api/knowledge-base/src/crud-operations/story-crud-operations.ts"
    auto_fixable: false

issues_to_fix:
  - id: 1
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for epic filter"
    severity: "high"
    auto_fixable: false
  - id: 2
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for states[] filter"
    severity: "high"
    auto_fixable: false
  - id: 3
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add test for states[] precedence over singular state parameter"
    severity: "high"
    auto_fixable: false
  - id: 4
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for phase filter"
    severity: "high"
    auto_fixable: false
  - id: 5
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for blocked filter"
    severity: "high"
    auto_fixable: false
  - id: 6
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for priority filter"
    severity: "high"
    auto_fixable: false
  - id: 7
    file: "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
    issue: "Add independent unit test for offset pagination"
    severity: "high"
    auto_fixable: false
  - id: 8
    file: "apps/api/knowledge-base/src/crud-operations/story-crud-operations.ts"
    issue: "Verify terminal-state guard returns correct story object in error response (AC-8 NOTE, PM clarification needed)"
    severity: "medium"
    auto_fixable: false

focus_files:
  - "apps/api/knowledge-base/src/mcp-server/__tests__/story-tools.test.ts"
  - "apps/api/knowledge-base/src/crud-operations/story-crud-operations.ts"

lessons_from_qa:
  - lesson: >
      Evidence artifacts stored only in the filesystem UAT/_implementation/EVIDENCE.yaml
      may not be migrated to KB. Always fall back to reading EVIDENCE.yaml from the story's
      UAT directory when kb_read_artifact returns null for the evidence type.
    category: "pattern"
    tags: ["qa-verify", "evidence", "kb-migration"]

  - lesson: >
      Unit tests for MCP handlers that mock the DB CRUD layer only verify handler
      pass-through and response parsing — they do not independently exercise each filter
      parameter against actual query logic. AC requirements for 'each filter independently
      tested' must distinguish between DB-layer tests (integration) and handler-layer tests
      (unit with mocks). Handler-layer unit tests should still exercise each filter
      parameter through the handler's argument parsing to confirm Zod schemas accept them.
    category: "pattern"
    tags: ["testing", "unit-tests", "filters", "mcp-handlers", "ac-precision"]

  - lesson: >
      Tool count in mcp-integration.test.ts drifts with each new story that adds tools.
      Story KBAR-0080 spec claimed 54 but actual verified count at implementation time was 53.
      The context artifact correctly warned about this. Story specs should not hardcode tool
      counts — reference getToolDefinitions().length dynamically or re-verify at setup time.
    category: "blocker"
    tags: ["tool-count", "mcp-integration", "spec-drift", "KBAR"]

constraints:
  - "Use Zod schemas for all types - per CLAUDE.md"
  - "No barrel files - per CLAUDE.md"
  - "Use @repo/logger, not console - per CLAUDE.md"
  - "Minimum 45% test coverage required"
  - "Named exports preferred"
  - "Functional components only in TypeScript"
  - "Pre-existing terminal-state guard in story-crud-operations.ts lines 292-307 (do not modify core logic, only verify behavior)"

next_steps:
  - "Review QA-VERIFY.yaml for full context (7846 bytes)"
  - "Read story KBAR-0080.md for complete requirements"
  - "Examine current test file: story-tools.test.ts"
  - "Examine DB CRUD layer: story-crud-operations.ts (focus on terminal-state guard at lines 292-307)"
  - "Add 7+ new unit tests for missing filter scenarios"
  - "Verify test coverage remains >= 45%"
  - "Run pnpm test --filter @repo/knowledge-base to verify"
  - "Create PR with fixes"

estimated_effort:
  scope: "test expansion + verification"
  complexity: "low-medium"
  estimated_time_hours: 2-3
  test_focus: "unit tests for filter combinations in handleKbListStories"
