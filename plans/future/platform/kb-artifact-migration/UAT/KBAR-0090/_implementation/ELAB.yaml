schema: 1
story_id: "KBAR-0090"
timestamp: "2026-02-23T00:00:00Z"

audit:
  - id: scope_alignment
    status: WARN
    note: |
      stories.index.md KBAR-009 entry still reads "Implement MCP tool to find stories ready to work
      (unblocked, dependencies satisfied)" — the original feature intent. The story itself documents
      the recharacterization clearly (tool is already implemented; scope shifted to test authorship),
      but the index entry was not updated. This is a documentation hygiene gap in the index, not a
      story defect. Recommend updating stories.index.md KBAR-009 section to reflect the actual scope
      of test coverage + integration test hygiene.
  - id: internal_consistency
    status: PASS
    note: |
      Goal, Non-goals, Scope, ACs, and Subtasks are internally consistent. Goal says "produce unit
      tests"; Non-goals explicitly prohibit all code changes; ACs 1-9 all cover test coverage or
      integration test hygiene; AC-10 covers the final verification gate. No contradictions found.
  - id: reuse_first
    status: PASS
    note: |
      Story explicitly identifies reuse: vi.hoisted + vi.mock pattern from tool-handlers.test.ts;
      handleKbGetStory handler shape as canonical pattern; existing story-tools.test.ts if landed
      by KBAR-0080. Only touches apps/api/knowledge-base/src/mcp-server/__tests__/. No per-story
      one-off utilities. No new shared packages introduced.
  - id: ports_adapters
    status: PASS
    note: |
      No API endpoints, no production code changes. Test-only story. Ports & Adapters compliance
      is N/A — no adapters created or modified. Handler under test (handleKbGetNextStory) already
      follows authorize → validate → call DB → log → return pattern per story Architecture Notes.
  - id: testability
    status: PASS
    note: |
      Concrete, executable test commands provided: `pnpm test --filter @repo/knowledge-base` and
      `pnpm check-types --filter @repo/knowledge-base`. Each AC maps to a specific test scenario
      with setup, action, expected, and evidence. vi.mock pattern with .js ESM extension documented.
      Both happy-path and error-path scenarios covered.
  - id: decision_completeness
    status: PASS
    note: |
      The only conditional decision is KBAR-0080 landing state (create vs extend story-tools.test.ts;
      fix mcp-integration.test.ts if not already fixed). This is explicitly handled by ST-1 as a
      read-only verification step before any writes. Tool count drift risk acknowledged with
      re-verification instruction. No blocking TBDs or unresolved design decisions.
  - id: risk_disclosure
    status: PASS
    note: |
      Risks documented in test_plan.risks and dev_feasibility.risks: (1) tool count may drift from
      53 — mitigated by ST-1 re-verification; (2) story-tools.test.ts may exist if KBAR-0080
      landed — mitigated by ST-1 check; (3) mock return shape must match TypeScript return type —
      mitigated by referencing story-crud-operations.ts lines 369-496; (4) ESM mock path must use
      .js extension. Verified codebase state: toolDefinitions has 53 entries (confirmed),
      mcp-integration.test.ts asserts 52 and is missing kb_update_story (confirmed),
      story-tools.test.ts does not exist (confirmed).
  - id: story_sizing
    status: PASS
    note: |
      AC count = 10 (triggers the >8 indicator), but only 1 of 8 sizing indicators applies.
      Story touches exactly 1 package (apps/api/knowledge-base), 0 endpoints, 0 frontend work,
      2 files maximum (story-tools.test.ts + mcp-integration.test.ts). Test authorship stories are
      inherently verbose in AC count (each scenario = one AC) but are low-complexity execution.
      Splitting would create unnecessary coordination overhead for a 1-point tech-debt story.
      PASS with note: AC count of 10 is expected for comprehensive test coverage stories.
  - id: subtask_decomposition
    status: PASS
    note: |
      Four subtasks with clear boundaries: ST-1 (read-only codebase verification, no files), ST-2
      (unit tests for handleKbGetNextStory, 1 file, ACs 1-8), ST-3 (mcp-integration.test.ts hygiene,
      1 file, AC-9), ST-4 (final verification gate, no files, AC-10). ST-2 and ST-3 are parallel
      (both depend on ST-1 only). No subtask exceeds 1 file modified. DAG is valid (no cycles).
      Each subtask has a verification command. Canonical References section present with 4 entries.
      All 10 ACs are covered by subtasks.

gaps: []

opportunities:
  - id: opp-1
    category: ux_polish
    finding: |
      The mcp-integration.test.ts describe string for the tool count test is extremely long
      ("should return all 52 tool definitions (CRUD + search + typed entry + work state + sync +
      fallback + inheritance + archive + task + triage + promotion + stale + deferred + admin +
      audit + artifacts + story + tokens + analytics + worktree)"). Consider parameterizing the
      tool category list or using a shorter description — readability degrades as more categories
      are added.
    effort: low
    decision: log_to_kb
    kb_entry_id: "230da957-833e-4adc-8ee7-d1531ad22c56"
  - id: opp-2
    category: edge_cases
    finding: |
      The story tests handleKbGetNextStory at the handler layer only. The underlying DB function
      kb_get_next_story (story-crud-operations.ts lines 369-496) has no unit or integration tests.
      This is intentionally out of scope for KBAR-0090 (see Non-Goals: "Do NOT add integration
      tests against a real PostgreSQL instance"), but represents a future testing gap. A subsequent
      story could add DB-layer unit tests for the priority ordering and dependency resolution logic.
    effort: medium
    decision: log_to_kb
    kb_entry_id: "442bfbff-f7e4-4704-b969-59f311c040da"
  - id: opp-3
    category: observability
    finding: |
      The story does not assert on logger.info / logger.error call behavior. The handler logs
      correlation_id, epic, found, story_id, candidates_count, query_time_ms on success and
      logs on error. Adding spy assertions on logger calls would improve observability coverage,
      though the tool-handlers.test.ts pattern does not currently assert on logger calls either.
    effort: low
    decision: log_to_kb
    kb_entry_id: "ca28b2ba-e713-40d3-8c0c-ae62c579a061"

split_recommendation: null

preliminary_verdict: PASS
verdict: PASS
decided_at: "2026-02-23T00:00:00Z"
summary:
  gaps_found: 0
  gaps_resolved: 0
  opportunities_found: 3
  opportunities_logged: 3
  acs_added: 0
  scope_alignment_warn: |
    stories.index.md KBAR-009 entry still reads original feature intent (not a story defect).
    Auto-noted: recommend updating the index entry to reflect test-authorship scope at
    implementation start. Does not block ready-to-work.
