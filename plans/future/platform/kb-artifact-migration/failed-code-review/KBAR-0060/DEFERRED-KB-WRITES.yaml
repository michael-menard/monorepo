# Deferred KB Writes for KBAR-0060
# KB was unavailable during story generation — retry these writes when KB is available.
generated: "2026-02-20"
story_id: KBAR-0060

writes:
  - table: stories
    operation: upsert
    data:
      story_id: KBAR-0060
      feature: kb-artifact-migration
      title: "Sync Integration Tests"
      story_dir: plans/future/platform/kb-artifact-migration/backlog/KBAR-0060
      story_file: plans/future/platform/kb-artifact-migration/backlog/KBAR-0060/KBAR-0060.md
      story_type: feature
      points: 5
      priority: medium
      state: backlog
      touches_backend: true
      touches_frontend: false
      touches_database: true
      touches_infra: false
    on_conflict: story_id DO UPDATE SET
      title = EXCLUDED.title,
      story_dir = EXCLUDED.story_dir,
      story_file = EXCLUDED.story_file,
      points = EXCLUDED.points,
      priority = EXCLUDED.priority,
      state = EXCLUDED.state
