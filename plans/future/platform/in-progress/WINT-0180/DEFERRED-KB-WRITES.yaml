---
# Deferred KB Writes for WINT-0180
# To be processed when KB MCP becomes available
generated_at: "2026-02-14T00:00:00Z"
story_id: WINT-0180
reason: "KB MCP not available during story generation"
---

stories_table_insert:
  story_id: WINT-0180
  feature: wint
  title: "Define Examples + Negative Examples Framework"
  story_dir: plans/future/platform/backlog/WINT-0180
  story_file: plans/future/platform/backlog/WINT-0180/WINT-0180.md
  story_type: feature
  points: 3
  priority: high
  state: backlog
  touches_backend: true
  touches_frontend: false
  touches_database: true
  touches_infra: false
  epic: wint
  blocks: [WINT-0190, WINT-0200, WINT-0210]
  created_at: "2026-02-14T00:00:00Z"

retry_instructions: |
  When KB MCP becomes available, execute:

  INSERT INTO stories (
    story_id, feature, title, story_dir, story_file, story_type,
    points, priority, state, touches_backend, touches_frontend,
    touches_database, touches_infra, epic, created_at
  ) VALUES (
    'WINT-0180',
    'wint',
    'Define Examples + Negative Examples Framework',
    'plans/future/platform/backlog/WINT-0180',
    'plans/future/platform/backlog/WINT-0180/WINT-0180.md',
    'feature',
    3,
    'high',
    'backlog',
    true,
    false,
    true,
    false,
    'wint',
    '2026-02-14T00:00:00Z'
  )
  ON CONFLICT (story_id) DO UPDATE SET
    title = EXCLUDED.title,
    story_dir = EXCLUDED.story_dir,
    story_file = EXCLUDED.story_file,
    state = EXCLUDED.state,
    updated_at = CURRENT_TIMESTAMP;
