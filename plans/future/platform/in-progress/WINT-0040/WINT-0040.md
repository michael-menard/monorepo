---
id: WINT-0040
title: "Create Telemetry Tables"
status: ready-to-work
priority: P3
epic: WINT
created_at: "2026-02-14T19:00:00Z"
updated_at: "2026-02-14T20:30:00Z"
elab_completed_at: "2026-02-14T20:30:00Z"
elab_verdict: CONDITIONAL PASS
depends_on: [WINT-0010]
blocks: [WINT-0120, WINT-3100]
wave: 2
experiment_variant: control
predictions:
  split_risk: 0.2
  review_cycles: 2
  token_estimate: 120000
  confidence: medium
  similar_stories:
    - story_id: WINT-0010
      similarity_score: 0.92
      actual_cycles: null
      actual_tokens: null
      split_occurred: false
    - story_id: INFR-0040
      similarity_score: 0.78
      actual_cycles: 2
      actual_tokens: 135000
      split_occurred: false
  generated_at: "2026-02-14T19:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
---

# WINT-0040: Create Telemetry Tables

## Context

WINT-0010 (UAT) established the WINT schema foundation with 6 schema groups in the `wint.*` PostgreSQL namespace. Section 3 (Telemetry Schema) currently contains 4 tables:
- `agentInvocations` (tracks agent invocations)
- `agentDecisions` (records key decisions)
- `agentOutcomes` (tracks invocation outcomes)
- `stateTransitions` (audit trail for state changes)

These tables provide basic observability for agent execution, but lack advanced telemetry capabilities needed for ML pipeline training (WINT-0050), quality analysis, and workflow optimization.

INFR-0040 (ready-for-qa) created a separate `telemetry.workflow_events` table for orchestrator-level events, establishing a pattern for workflow observability at the infrastructure layer.

## Problem

The current WINT telemetry tables are sufficient for basic agent tracking but insufficient for:

1. **Token/Cost Tracking**: No granular LLM usage metrics (input/output/cached tokens, API costs)
2. **Performance Metrics**: Limited timing and resource consumption data
3. **Quality Analysis**: No structured quality scores, test coverage, or review feedback aggregation
4. **ML Training Data**: Missing metadata needed for quality prediction models (WINT-0050)
5. **Decision Context**: Limited context capture for decision outcomes and learning

## Goal

Extend Section 3 (Telemetry Schema) in `wint.ts` with additional columns to capture:
- Token usage metrics (input, output, cached tokens per invocation)
- Cost tracking (API costs, model selection metadata)
- Enhanced performance metrics (CPU time, memory usage, duration breakdowns)
- Quality scores and test coverage (for outcome analysis)
- Rich decision context (alternatives considered, confidence scores, correctness tracking)
- Review feedback and quality ratings

This story focuses on **schema definition only** - actual ingestion adapters and MCP tools will be implemented in WINT-0120 (Telemetry MCP Tools) and WINT-3020 (Invocation Logging).

The schema design aligns with INFR-0040's patterns (indexed append-only logs, JSONB for flexible metadata) while remaining in the `wint.*` namespace for agent-level telemetry.

## Non-Goals

- **Telemetry ingestion adapters**: Deferred to WINT-0120 (Telemetry MCP Tools)
- **Telemetry collection logic**: Deferred to WINT-3020 (Invocation Logging)
- **Analytics queries or dashboards**: Deferred to TELE-0030 (Dashboards-as-Code)
- **Event export to Prometheus**: Covered by TELE-0020 (Prometheus Metrics)
- **ML model training**: Covered by WINT-0050 (ML Pipeline)
- **Real-time telemetry streaming**: Not in scope for Wave 2
- **Cross-namespace telemetry joins**: Keep `wint.*` and `telemetry.*` separate
- **New telemetry tables**: Extend existing 4 tables only (avoid schema sprawl)

## Scope

### Packages Touched

- `packages/backend/database-schema` - Extend wint.ts telemetry tables
- `packages/backend/db` - Auto-generated Zod schemas update

### Files Modified

- `packages/backend/database-schema/src/schema/wint.ts` - Extend Section 3 (lines 600-749)
- `packages/backend/database-schema/src/schema/index.ts` - Export updated Zod schemas
- `packages/backend/db/src/generated-schemas.ts` - Auto-regenerated by drizzle-zod
- `packages/backend/db/src/index.ts` - Verify new schema exports

### New Files Created

- `packages/backend/database-schema/src/migrations/app/00XX_*.sql` - Migration file
- `packages/backend/database-schema/src/schema/__tests__/wint-telemetry.test.ts` - Unit tests

## Acceptance Criteria

### AC-1: Extend agentInvocations Table with Token Tracking

- [ ] Add `cachedTokens` column (INTEGER, DEFAULT 0, NOT NULL)
  - Tracks prompt caching metrics
  - Default 0 for backward compatibility
- [ ] Add `totalTokens` column (INTEGER, DEFAULT 0, NOT NULL)
  - Computed or application-calculated: inputTokens + outputTokens + cachedTokens
  - **PM Decision Required**: DB GENERATED column vs app-level computation
- [ ] Add `estimatedCost` column (NUMERIC(10,4), DEFAULT 0.0, NOT NULL)
  - Tracks API cost estimates based on token counts
  - 4 decimal precision for sub-cent accuracy
- [ ] Add `modelName` column (TEXT, NULL)
  - Tracks which LLM was used (e.g., 'claude-sonnet-4.5', 'gpt-4')
  - Nullable (may be unknown for legacy data)
- [ ] Update Zod schemas via drizzle-zod
- [ ] Verify backward compatibility: existing inserts work without new columns

### AC-2: Extend agentDecisions Table with Outcome Tracking

- [ ] Add `evaluatedAt` column (TIMESTAMP WITH TIME ZONE, NULL)
  - When decision correctness was assessed (nullable, evaluation happens later)
- [ ] Add `evaluatedBy` column (TEXT, NULL)
  - Agent or user who evaluated correctness
- [ ] Add `correctnessScore` column (INTEGER, NULL, CHECK 0-100 range)
  - Replace binary wasCorrect with 0-100 score
  - Nullable (not always evaluated)
  - Add CHECK constraint: `correctness_score >= 0 AND correctness_score <= 100`
- [ ] Add `alternativesConsidered` column (INTEGER, DEFAULT 0, NOT NULL)
  - Count of alternatives evaluated during decision
- [ ] Update Zod schemas via drizzle-zod
- [ ] Verify timestamp uses `timestamp('col', { withTimezone: true })` pattern

### AC-3: Extend agentOutcomes Table with Detailed Quality Metrics

- [ ] Add `lintErrors` column (INTEGER, DEFAULT 0, NOT NULL)
  - Count of linting issues
- [ ] Add `typeErrors` column (INTEGER, DEFAULT 0, NOT NULL)
  - Count of TypeScript errors
- [ ] Add `securityIssues` column (JSONB, DEFAULT '[]'::jsonb, NOT NULL)
  - Array of security findings
  - Structure: `[{ severity: string, description: string, location?: string }]`
  - Use `jsonb().$type<SecurityIssue[]>()` pattern
- [ ] Add `performanceMetrics` column (JSONB, DEFAULT '{}'::jsonb, NOT NULL)
  - Timing breakdowns and resource usage
  - Structure: `{ duration_ms: number, memory_mb: number, api_calls: number }`
- [ ] Add `artifactsMetadata` column (JSONB, DEFAULT '{}'::jsonb, NOT NULL)
  - Detailed artifact tracking
  - Structure: `{ files_modified: number, lines_added: number, lines_removed: number }`
- [ ] Update Zod schemas via drizzle-zod
- [ ] Document expected JSONB structures in JSDoc comments

### AC-4: Extend stateTransitions Table with Audit Enhancements

- [ ] Add `previousMetadata` column (JSONB, NULL)
  - Snapshot of state before transition
  - Nullable (may not have previous state)
- [ ] Add `newMetadata` column (JSONB, NULL)
  - Snapshot of state after transition
  - Nullable (may not have new state)
- [ ] Add `validationErrors` column (JSONB, DEFAULT '[]'::jsonb, NOT NULL)
  - Array of validation issues during transition
  - Structure: `[{ field: string, message: string }]`
- [ ] Add `rollbackAllowed` column (BOOLEAN, DEFAULT true, NOT NULL)
  - Flag for safety checks (can this transition be rolled back?)
- [ ] Update Zod schemas via drizzle-zod

### AC-5: Create Composite Indexes for Common Telemetry Queries

Follow WINT-0010 AC-008 index ordering pattern (high cardinality first):

- [ ] Create `idx_agent_invocations_agent_name_started_at` index
  - `CREATE INDEX idx_agent_invocations_agent_name_started_at ON wint.agent_invocations(agent_name, started_at)`
  - Supports timeline views filtered by agent
- [ ] Create `idx_agent_decisions_decision_type_evaluated_at` index
  - `CREATE INDEX idx_agent_decisions_decision_type_evaluated_at ON wint.agent_decisions(decision_type, evaluated_at)`
  - Supports quality analysis queries
- [ ] Create `idx_agent_outcomes_outcome_type_created_at` index
  - `CREATE INDEX idx_agent_outcomes_outcome_type_created_at ON wint.agent_outcomes(outcome_type, created_at)`
  - Supports outcome trend analysis
- [ ] Create `idx_state_transitions_entity_type_transitioned_at` index
  - `CREATE INDEX idx_state_transitions_entity_type_transitioned_at ON wint.state_transitions(entity_type, transitioned_at)`
  - Supports audit trail queries
- [ ] Document index rationale in migration file comments

### AC-6: Generate Drizzle Migration File for Schema Changes

- [ ] Run `pnpm --filter @repo/database-schema migrate:generate` to create migration
- [ ] Verify migration includes:
  - All new columns with correct types and defaults
  - All 4 composite indexes
  - CHECK constraint for correctnessScore (0-100 range)
- [ ] Test migration is idempotent (safe to run multiple times with IF NOT EXISTS checks)
- [ ] Test migration rollback (down migration):
  - `pnpm --filter @repo/database-schema migrate:down`
  - Verify schema reverts to WINT-0010 state
  - Verify no orphaned constraints or data
  - Reapply migration to ensure clean rollback path

### AC-7: Auto-Generate Zod Schemas for Updated Telemetry Tables

- [ ] Ensure `createInsertSchema()` includes new columns for all 4 tables
  - Verify optional columns marked correctly (nullable vs default)
  - Verify JSONB columns have proper type inference
- [ ] Ensure `createSelectSchema()` includes new columns for all 4 tables
- [ ] Verify type inference with `z.infer<>` works for all new fields
- [ ] Export updated schemas from `packages/backend/database-schema/src/schema/index.ts`
- [ ] Verify `@repo/db` re-exports updated schemas

### AC-8: Write Unit Tests for New Telemetry Columns

Create `packages/backend/database-schema/src/schema/__tests__/wint-telemetry.test.ts`:

- [ ] Test insert operations with new columns (null handling, defaults)
  - Test backward compatibility: inserts without new columns succeed
  - Test new columns: inserts with new columns succeed
  - Test constraint violations: correctnessScore > 100 throws error
- [ ] Test index performance on composite indexes
  - EXPLAIN ANALYZE queries show Index Scan (not Sequential Scan)
  - Query execution time < 50ms on 1000+ row dataset
- [ ] Test Zod schema validation for new JSONB fields
  - Valid JSONB structures pass validation
  - Invalid JSONB structures throw Zod errors (not DB errors)
  - Test each JSONB structure: securityIssues, performanceMetrics, artifactsMetadata, validationErrors
- [ ] Achieve 80%+ coverage for telemetry section (per WINT-0010 standard)

### AC-9: Update Schema Documentation in wint.ts

- [ ] Add JSDoc comments for all new columns explaining purpose
  - Example: `/** Tracks prompt caching token count for cost optimization */`
- [ ] Document expected JSONB structure for each column:
  - `securityIssues`: Array of `{ severity: 'low' | 'medium' | 'high' | 'critical', description: string, location?: string }`
  - `performanceMetrics`: Object with `{ duration_ms: number, memory_mb: number, api_calls: number }`
  - `artifactsMetadata`: Object with `{ files_modified: number, lines_added: number, lines_removed: number }`
  - `validationErrors`: Array of `{ field: string, message: string }`
  - `previousMetadata` / `newMetadata`: Free-form state snapshots
- [ ] Reference related stories in comments:
  - WINT-0120 (MCP Tools) for ingestion
  - WINT-3020 (Invocation Logging) for collection logic
  - WINT-0050 (ML Pipeline) for training data usage

### AC-10: Verify No Breaking Changes to Existing Telemetry Queries

- [ ] All new columns are nullable OR have defaults (backward compatible)
- [ ] Existing indexes remain functional (no dropped indexes)
- [ ] Drizzle ORM relations still work for joins
  - Test one-to-many relations (invocation → decisions, invocation → outcomes)
  - Test many-to-one relations (decision → invocation)
- [ ] @repo/db exports include updated schemas
- [ ] Run existing WINT-0010 tests against new schema (if `wint-schema.test.ts` exists)
  - All tests must pass without modification
  - Proves backward compatibility

## Reuse Plan

| Component | Existing Location | Reuse Strategy |
|-----------|-------------------|----------------|
| `wintSchema` namespace | `wint.ts:41` | Use existing pgSchema, no new namespace needed |
| Telemetry table definitions | `wint.ts:600-749` | Extend existing tables in Section 3 |
| Composite index pattern | WINT-0010 AC-008 | Follow high→low cardinality ordering |
| Zod schema generation | `wint.ts:1453+` | Use createInsertSchema/createSelectSchema |
| JSONB typing | `wint.ts:612, 658, 694, 738` | Use jsonb().$type<T>() with Zod schemas |
| Timestamp pattern | All WINT tables | Use timestamp('col', { withTimezone: true }) |
| Migration tooling | Drizzle Kit | Use existing migrate:generate and migrate:apply |
| Test patterns | `unified-wint.test.ts` or `wishlist-schema.test.ts` | Copy structure for new test file |

## Architecture Notes

### Schema Namespace Isolation

- All WINT telemetry tables live in `wint.*` PostgreSQL schema (not `telemetry.*`)
- INFR-0040's `telemetry.workflow_events` is separate (workflow-level orchestrator events)
- Separation enables independent evolution and prevents circular dependencies

### Zod-First Type Strategy

Per CLAUDE.md requirements:
- NEVER use TypeScript interfaces
- ALWAYS use Zod schemas with `z.infer<>`
- drizzle-zod auto-generates schemas from Drizzle table definitions
- Export both Drizzle tables AND Zod schemas from index.ts

### Composite Index Strategy (WINT-0010 AC-008)

**Ordering Rule**: High cardinality → low cardinality
- `(agentName, startedAt)` not `(startedAt, agentName)`
- Rationale: agentName has ~20-50 distinct values, startedAt is unique

**Query Pattern Support**:
```sql
-- Optimized by index
WHERE agent_name = 'pm-leader' AND started_at > NOW() - INTERVAL '1 day'

-- Also optimized (partial index scan)
WHERE agent_name = 'pm-leader'

-- NOT optimized (cannot use index)
WHERE started_at > NOW() - INTERVAL '1 day'
```

### JSONB Column Design

**Type Safety**: Use `jsonb().$type<T>()` with Zod schemas:
```typescript
export const securityIssueSchema = z.object({
  severity: z.enum(['low', 'medium', 'high', 'critical']),
  description: z.string(),
  location: z.string().optional()
})

export const agentOutcomes = wintSchema.table('agent_outcomes', {
  // ...
  securityIssues: jsonb('security_issues')
    .$type<z.infer<typeof securityIssueSchema>[]>()
    .notNull()
    .default(sql`'[]'::jsonb`)
})
```

**Validation Strategy**:
- Primary: Zod schema validation at ORM layer (drizzle-zod)
- Fallback: Database allows any valid JSON (no CHECK constraints for JSONB structure)
- Future: Consider PostgreSQL 14+ JSONB schema validation for critical fields

## Infrastructure Notes

### Database Migration Execution

1. **Development**:
   ```bash
   pnpm --filter @repo/database-schema migrate:generate
   pnpm --filter @repo/database-schema migrate:apply
   ```

2. **Production** (via CI/CD):
   - Apply migration to Aurora PostgreSQL during deployment
   - Rollback available: `migrate:down` if issues detected
   - Monitor migration duration (expect <5 seconds for column additions)

### Backward Compatibility Guarantee

- All new columns are nullable OR have defaults
- Existing code works without modifications
- No service restarts required (schema-only changes)

### Index Write Overhead

**Concern**: 4 new composite indexes increase write latency

**Mitigation**:
- Measure baseline insert performance before migration
- Monitor write latency after migration
- If degradation >20%, consider partial indexes (AC-5 future enhancement)
- Document trade-off: faster queries vs slower inserts

## Test Plan

See `_pm/TEST-PLAN.md` for complete test specification.

### Summary

- **Happy Path**: 5 tests covering each AC (token tracking, outcome tracking, quality metrics, audit enhancements, index performance)
- **Error Cases**: 3 tests (invalid ranges, malformed JSONB, NULL handling)
- **Edge Cases**: 4 tests (zero tokens, large JSONB arrays, migration rollback, concurrent inserts)
- **Coverage Target**: 80%+ for telemetry section

### Critical Checkpoints

1. Migration idempotency (apply/rollback/reapply)
2. Backward compatibility (existing queries work)
3. Index performance (EXPLAIN ANALYZE shows index usage)
4. Zod validation (catches invalid JSONB before DB)

## Dev Feasibility

See `_pm/DEV-FEASIBILITY.md` for complete analysis.

### Summary

- **Feasible for MVP**: Yes
- **Confidence**: High
- **Estimated Effort**: ~8 hours (1 development day)

### MVP-Critical Risks

1. **Drizzle ORM Nullable Column Handling**: Must verify `.optional()` / `.nullable()` chaining for new columns
2. **Composite Index Cardinality Ordering**: Wrong order defeats performance gains
3. **JSONB Schema Validation Gap**: Zod validation only, no DB-level enforcement

### Missing Requirements (PM Must Clarify)

1. **NULL vs DEFAULT Strategy**: Specify for each column (see DEV-FEASIBILITY.md)
2. **Index Naming Convention**: Confirm `idx_{table}_{col1}_{col2}` pattern
3. **totalTokens Computation**: DB GENERATED column vs application-level?

## Reality Baseline

**Generated from**: `plans/baselines/BASELINE-REALITY-2026-02-13.md`

### Dependencies Satisfied

- ✅ WINT-0010 (UAT) - Core WINT schemas exist
- ✅ Drizzle ORM v0.44.3 - Schema definition patterns
- ✅ drizzle-zod - Zod schema auto-generation
- ✅ @repo/db - Database client

### Active Work (Non-Conflicting)

- WINT-0020 (ready-to-work) - Story Management Tables (Section 1)
- WINT-0070 (ready-for-qa) - Workflow Tracking Tables (Section 6)
- INFR-0040 (ready-for-qa) - Telemetry workflow_events (separate namespace)

### Constraints to Respect

1. **Schema Isolation**: All WINT tables in `wint.*` namespace
2. **Zod-First Types**: Never use TypeScript interfaces
3. **Pattern Reuse**: Follow WINT-0010 patterns exactly
4. **No Breaking Changes**: Protected features: @repo/db API, existing WINT schemas
5. **Index Strategy**: High cardinality first (WINT-0010 AC-008)

### Patterns to Follow

- Use `wintSchema.table()` for all tables
- Use `pgEnum()` for constrained value sets
- Use `timestamp('col', { withTimezone: true })` for timestamps
- Use `jsonb().$type<T>()` for typed JSON columns
- Create composite indexes with descriptive names
- Export Drizzle tables AND Zod schemas

### Patterns to Avoid

- ❌ TypeScript interfaces (Zod-first requirement)
- ❌ WINT tables in `telemetry.*` namespace
- ❌ One-off index naming schemes
- ❌ Skip relations() definitions
- ❌ Mix cardinality in indexes (low → high)

## Related Stories

**Depends On**:
- WINT-0010 (UAT) - Core Database Schemas (6 schemas)

**Blocks**:
- WINT-0120 - Create Telemetry MCP Tools (needs schema)
- WINT-3100 - Create State Transition Event Log (extends stateTransitions table)

**Related**:
- INFR-0040 (ready-for-qa) - Workflow Events Table (separate namespace, complementary)
- WINT-3020 - Implement Invocation Logging (will use these tables)
- WINT-0050 - Create ML Pipeline Tables (consumes telemetry data)
- TELE-0030 - Dashboards-as-Code (queries telemetry data)

## Story Predictions

Low split risk (0.2) due to:
- Additive schema changes only (no breaking changes)
- Clear patterns from WINT-0010 to follow
- Well-scoped to 4 table extensions
- No cross-domain dependencies

2 review cycles estimated due to:
- Database schema work requires careful review
- Multiple JSONB columns with validation requirements
- Index performance verification needed

Token estimate (120,000) based on INFR-0040 similarity (telemetry schema work).
Medium confidence due to limited similar story data (only 2 comparable stories).

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-14_

### MVP Gaps Resolved

| # | Finding | Resolution | Implementation Notes |
|---|---------|------------|----------------------|
| 1 | totalTokens computation strategy unspecified | Add to Implementation Notes | Developer can choose implementation strategy during development. Recommendation documented in DEV-FEASIBILITY.md (prefer DB GENERATED for data integrity). |
| 2 | NULL vs DEFAULT strategy inconsistent | Add to Implementation Notes | DEV-FEASIBILITY.md provides clear recommendations. Developer will follow documented patterns. Not blocking implementation. |
| 3 | Index naming convention not explicitly specified | Add to Implementation Notes | AC-5 examples provide clear pattern (idx_{table}_{col1}_{col2}). Developer will follow demonstrated pattern. |
| 4 | JSONB validation enforcement gap | Add to Implementation Notes | Intentional design decision per Architecture Notes. Zod-first validation is project standard. DB-level JSONB schema validation deferred to future enhancement. |

### Non-Blocking Items (Logged to KB)

All 31 future opportunities have been logged to Knowledge Base for tracking:

- **Performance** (7 items): JSONB schema validation, partial indexes, GIN indexes, materialized views, index selectivity monitoring, TOAST compression, connection pooling under load
- **Observability** (4 items): Automated index monitoring, audit trail for schema changes, migration health checks, schema drift detection, query performance baselines
- **Integration** (5 items): Real-time streaming support, OpenTelemetry export, Prometheus metrics, ML pipeline training data, external BI tools
- **Security** (2 items): PII scanning in JSONB fields, row-level security policies
- **Data Quality** (5 items): Telemetry retention policy, anomaly detection, consistency checks, referential integrity monitoring
- **Enhancement** (4 items): Embedding columns for semantic search, helper functions for JSONB queries, cost prediction model training data, edge case documentation
- **UX Polish** (2 items): Inline SQL comments, helper functions for queries

### Summary

- **Specification clarifications added as Implementation Notes**: 4
- **Non-blocking items logged to KB**: 31
- **Mode**: Autonomous (decisions from DECISIONS.yaml)
- **Ready for Implementation**: YES
