schema: 1
story_id: "WKFL-008"
timestamp: "2026-02-22T00:00:00Z"
lessons: []
adrs: []

constraints:
  - "This story touches only .claude/ agent/config/command files — no TypeScript"
  - "dev-documentation-leader.agent.md is the integration point for AC-7 traffic routing"
  - "OUTCOME.yaml format established by WKFL-001 must be extended, not replaced"
  - "Experiment variant field: experiment_variant"
  - ".claude/config/experiments.yaml already exists with schema stub and paused example experiments"
  - ".claude/agents/experiment-analyzer.agent.md already exists with full Welch t-test logic"
  - ".claude/commands/experiment-report.md already exists and routes to experiment-analyzer"
  - "dev-documentation-leader.agent.md already contains OUTCOME.yaml experiment_variant field (Step 5)"
  - "dev-documentation-leader.agent.md already documents Experiment Variant Propagation section (WKFL-008 annotation)"
  - "Traffic routing hook in dev-documentation-leader.agent.md is partially in place (OUTCOME.yaml reads experiment_variant from story.yaml) but story creation variant ASSIGNMENT logic is not yet present — that belongs in a story creation agent"
  - "Story creation flow uses pm-story-generation-leader.agent.md and/or dev-documentation-leader.agent.md"
  - "AC-7 requires the routing hook at story creation time, not documentation time — the experiment_variant must be WRITTEN to story.yaml during creation, not just READ during documentation"

patterns: []

pre_existing_artifacts:
  - path: ".claude/config/experiments.yaml"
    status: "exists — schema stub with commented examples, experiments: [] (no active experiments)"
    gap: "Schema is documented in comments only; no formal schema file at .claude/schemas/experiment-schema.md yet"
  - path: ".claude/agents/experiment-analyzer.agent.md"
    status: "exists — complete implementation with Welch t-test, min-sample guard, decision tree"
    gap: "None identified; AC-4, AC-5, AC-6 coverage appears complete"
  - path: ".claude/commands/experiment-report.md"
    status: "exists — routes /experiment-report to experiment-analyzer agent"
    gap: "None identified; AC-5 command coverage appears complete"
  - path: ".claude/agents/dev-documentation-leader.agent.md"
    status: "exists — Step 5 reads experiment_variant from story.yaml and writes to OUTCOME.yaml"
    gap: "This covers AC-3 (metrics per variant in OUTCOME.yaml) and the READ side of AC-7. The WRITE side of AC-7 (assigning experiment_variant to story.yaml at creation time) needs investigation of pm-story-generation-leader.agent.md"

notes: "No KB available — using story-level context only. Pre-existing artifacts cover most ACs. Primary remaining work is: (1) verify/complete traffic routing assignment in story creation agent per AC-7, (2) document OUTCOME.yaml variant field extension formally per AC-3, (3) verify the schema file reference in experiments.yaml (.claude/schemas/experiment-schema.md) exists, and (4) create verification fixtures for AC-6 end-to-end test."
