schema: 1
story_id: "WKFL-008"
timestamp: "2026-02-22T23:15:00Z"
failure_source: failed-qa
iteration: 2

issues_to_fix:
  - id: 1
    file: ".claude/agents/experiment-analyzer.agent.md"
    line: 214
    issue: "Step 8 missing explicit Insufficient Data Path section with separate YAML template. When Step 4 sample guard triggers (insufficient data), agent needs clear template omitting statistical fields."
    severity: critical
    auto_fixable: true
    resolution: "Added 'Insufficient Data Path' section before 'Normal Path' with minimal YAML template (action/rationale/confidence only, no primary_metric/secondary_metrics fields)"

  - id: 2
    file: ".claude/agents/experiment-analyzer.agent.md"
    line: 214
    issue: "Step 8 single unified template doesn't distinguish sufficient vs insufficient data output shapes. Creates ambiguity for agent execution when Step 4 early redirect occurs."
    severity: critical
    auto_fixable: true
    resolution: "Restructured Step 8 into two explicit paths: 'Insufficient Data Path' (Step 4 → Step 8 early redirect) and 'Normal Path' (Step 4 passes, full statistical analysis). Each path has distinct YAML template."

  - id: 3
    file: ".claude/agents/pm-story-generation-leader.agent.md"
    line: 87
    issue: "Missing cross-reference comment in Phase 0.5a pinning integration contract. EVIDENCE.yaml claims comment was added but is absent."
    severity: medium
    auto_fixable: true
    resolution: "Added INTEGRATION NOTE explaining that Phase 0.5a writes experiment_variant to story.yaml, which is read by dev-documentation-leader Step 5 and used by experiment-analyzer.agent.md."

  - id: 4
    file: ".claude/agents/dev-documentation-leader.agent.md"
    line: 132
    issue: "Missing cross-reference comment in Step 5 pinning integration contract. EVIDENCE.yaml claims comment was added but is absent."
    severity: medium
    auto_fixable: true
    resolution: "Added INTEGRATION NOTE explaining that Step 5 reads experiment_variant from story.yaml (written by pm-story-generation-leader Phase 0.5a) and propagates it to OUTCOME.yaml for experiment-analyzer.agent.md classification."

focus_files:
  - ".claude/agents/experiment-analyzer.agent.md"
  - ".claude/agents/pm-story-generation-leader.agent.md"
  - ".claude/agents/dev-documentation-leader.agent.md"

summary: "Added 'Insufficient Data Path' and 'Normal Path' conditional sections to experiment-analyzer Step 8 with explicit YAML templates distinguishing output structure when sample guard triggers vs when sufficient data available. Added cross-reference comments in pm-story-generation-leader Phase 0.5a and dev-documentation-leader Step 5 documenting the integration contract for experiment_variant propagation."

remediation_category: agent-instructions
verification_approach: |
  QA will re-verify:
  1. experiment-analyzer Step 8 now has two explicit sub-sections with proper conditional logic
  2. Insufficient Data Path template does NOT include primary_metric/secondary_metrics
  3. Normal Path template includes full statistical fields
  4. Cross-reference comments added to both pm-story-generation-leader and dev-documentation-leader
  5. AC-6 compliance: distinct output templates for each path with clear field omission rules
