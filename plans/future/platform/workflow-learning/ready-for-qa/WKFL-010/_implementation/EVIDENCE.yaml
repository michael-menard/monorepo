schema: 1
story_id: "WKFL-010"
version: 1
timestamp: "2026-02-22T10:51:00Z"

acceptance_criteria:
  - ac_id: "AC-1"
    ac_text: "Aggregate inputs from calibration, patterns, experiments"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Data Sources section specifies kb_search queries for calibration
          (tags: ['calibration']), feedback (tags: ['feedback']), experiments
          (tags: ['experiment', 'result']); YAML path pattern
          '.claude/patterns/PATTERNS-*.yaml' for patterns source;
          '.claude/config/HEURISTIC-PROPOSALS.yaml' for heuristics;
          '.claude/experiments/EXPERIMENT-*.yaml' for experiments.
          All 5 sources covered: calibration (WKFL-002), patterns (WKFL-006),
          heuristics (WKFL-003), feedback (WKFL-004), experiments (WKFL-008).

  - ac_id: "AC-2"
    ac_text: "Generate proposals with effort/impact ratings"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Phase 8 Output Generation shows each proposal entry with
          **Impact:** {level}, **Effort:** {level}, and **ROI Score:** {N} (formula)
          fields. Example P-001: Impact: High, Effort: Low, ROI Score: 9 (3 * 3).
          Example P-002: Impact: Medium, Effort: Low, ROI Score: 6 (2 * 3).
          Tracking Summary table also included.

  - ac_id: "AC-3"
    ac_text: "Prioritize by impact/effort ratio"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Phase 6 Prioritization uses 1-9 ROI scale: High >= 7, Medium >= 5
          and < 7, Low < 5. Sort within groups is descending by ROI score.
          Output sections are "High Priority (ROI >= 7)", "Medium Priority
          (ROI >= 5 and < 7)", "Low Priority (ROI < 5)".

  - ac_id: "AC-4"
    ac_text: "Track: proposed, accepted, rejected, implemented"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Phase 9 KB Persistence section shows kb_add_entry calls with tags
          ['proposal', 'status:proposed', source:{source}, priority:{level}].
          KB tag taxonomy section explicitly documents status:proposed,
          status:accepted, status:rejected, status:implemented — all queryable
          by status tag. source:{calibration|patterns|heuristics|feedback|experiments}
          and priority:{high|medium|low} also specified.

  - ac_id: "AC-5"
    ac_text: "Learn from acceptance patterns"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Phase 7 Meta-Learning section documents PROPOSAL-ACCEPTANCE-BASELINE.yaml
          creation logic: after 3 completed non-dry-run invocations (tracked via
          kb_search on tags ['improvement-proposer', 'run-log']), creates
          .claude/proposals/PROPOSAL-ACCEPTANCE-BASELINE.yaml with fields:
          total_proposed, accepted, rejected, acceptance_rate (decimal 0-1),
          by_source breakdown, by_effort breakdown. Run 4+ reads baseline to
          contextualize divergence.

  - ac_id: "AC-6"
    ac_text: "Agent file Data Sources section specifies exact KB tag queries and YAML file path patterns for all 5 sources"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Each Data Source entry now has explicit Query (KB tag) and YAML path
          pattern fields. calibration: kb_search({tags:['calibration']}),
          patterns: .claude/patterns/PATTERNS-*.yaml,
          heuristics: .claude/config/HEURISTIC-PROPOSALS.yaml,
          feedback: kb_search({tags:['feedback']}),
          experiments: kb_search({tags:['experiment','result']}) +
          .claude/experiments/EXPERIMENT-*.yaml.

  - ac_id: "AC-7"
    ac_text: "ROI formula: impact_value * effort_inverse (range 1-9)"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Phase 4 ROI Calculation specifies: impact_value (high=3, medium=2,
          low=1), effort_inverse (low=3, medium=2, high=1), formula:
          roi_score = impact_value * effort_inverse, range 1-9.
          Mission statement and frontmatter roi_formula field also reflect
          the corrected formula. Previous formula (impact/effort)*(10/9)
          fully replaced.

  - ac_id: "AC-8"
    ac_text: "Cold-start skip-with-log per source, continue, missing sources noted, all-sources-missing handled"
    status: PASS
    evidence_items:
      - type: file
        path: ".claude/agents/improvement-proposer.agent.md"
        description: >
          Each of 5 Data Sources has Cold-start behavior: specifying exact
          SKIP log message and "continue with remaining sources".
          Phase 2 loading logs per-source skip messages. Error Handling table
          shows "1-4 sources skipped: Log skip message per source; continue;
          include Missing Data Sources section". "All 5 sources skipped" edge
          case produces No Proposals Available section with full Sources Skipped
          list and retry instructions.

  - ac_id: "AC-9"
    ac_text: "story.yaml dependencies field contains WKFL-002, WKFL-003, WKFL-004, WKFL-006, WKFL-008"
    status: PASS
    evidence_items:
      - type: file
        path: "plans/future/platform/workflow-learning/in-progress/WKFL-010/story.yaml"
        description: >
          story.yaml dependencies field contains: WKFL-006, WKFL-002, WKFL-003,
          WKFL-004, WKFL-008. All 5 required dependencies present. No change
          required (verified in planning as noted in PLAN.yaml).

touched_files:
  - path: ".claude/agents/improvement-proposer.agent.md"
    action: modified
    lines: 534
    description: >
      Updated ROI formula to impact_value * effort_inverse (range 1-9);
      added explicit YAML path pattern and KB tag query per all 5 data sources;
      added cold-start skip-with-log behavior per source with all-sources-missing
      edge case; updated output format with Impact/Effort/ROI Score fields;
      updated priority groups to use 1-9 ROI scale (High>=7, Medium>=5, Low<5);
      documented PROPOSAL-ACCEPTANCE-BASELINE.yaml creation after 3rd run;
      expanded experiments source from deferred to active with full contract.

commands_run:
  - command: "pnpm --filter @repo/orchestrator test"
    result: SUCCESS
    output: "131 test files passed, 3311 tests passed (18 skipped)"
    timestamp: "2026-02-22T10:51:52Z"

endpoints_exercised: []

test_summary:
  unit: { pass: 3311, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "Deliverable is a markdown agent configuration file. No UI surface, no HTTP API endpoints, no TypeScript code — no Playwright-testable surface area exists. Story touches only .claude/agents/improvement-proposer.agent.md."
  config: null
  project: null
  mode: null
  tests_written: false
  results:
    total: 0
    passed: 0
    failed: 0
    skipped: 0
  failed_tests: []
  config_issues: []
  videos: []
  screenshots: []

coverage: {}

notable_decisions:
  - "ROI formula corrected from (impact/effort)*(10/9) to impact_value*effort_inverse per AC-7 contract — integer range 1-9"
  - "Experiments source (WKFL-008) upgraded from deferred/Phase 3 to active source with full cold-start contract"
  - "Feedback source (WKFL-004) added as distinct source 4 to complete all 5 dependency integrations"
  - "PROPOSAL-ACCEPTANCE-BASELINE.yaml triggered at exactly 3 non-dry-run completions (not 50+ proposals)"

known_deviations:
  - "E2E tests exempt: agent markdown file has no testable surface area. autonomy_level=conservative; deviation logged rather than blocked because no Playwright-executable surface exists."

token_summary:
  execute: { in: 0, out: 0 }
