deferred_at: "2026-03-07"
reason: "psql not available in current shell environment"
writes:
  - table: stories
    operation: upsert
    data:
      story_id: "WINT-4020"
      feature: "wint"
      title: "Create Rules Registry Sidecar"
      story_dir: "plans/future/platform/wint/backlog/WINT-4020"
      story_file: "plans/future/platform/wint/backlog/WINT-4020/WINT-4020.md"
      story_type: "feature"
      points: 8
      priority: "high"
      state: "backlog"
      touches_backend: true
      touches_frontend: false
      touches_database: true
      touches_infra: false
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'WINT-4020', 'wint', 'Create Rules Registry Sidecar',
        'plans/future/platform/wint/backlog/WINT-4020',
        'plans/future/platform/wint/backlog/WINT-4020/WINT-4020.md',
        'feature', 8, 'high', 'backlog', true, false, true, false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;

# TTL stamp — added by import-deferred-writes
processed_at: "2026-03-08T23:55:28.324Z"
imported: true
