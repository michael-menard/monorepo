schema: 1
story_id: WINT-9020
generated_at: "2026-02-20T00:00:00Z"
domain: backend

lessons: []

adrs: []

notes:
  - "No prior stories with direct lessons for native LangGraph node porting found in KB."
  - "The 7-phase contract is specified in .claude/skills/doc-sync/SKILL.md (LangGraph Porting Notes section)."
  - "The createToolNode factory pattern is established across all existing nodes (delta-detect, synthesize, etc.)."
  - "The existing nodes/workflow/doc-sync.ts uses subprocess delegation — this story implements the native port."
  - "The nodes/sync/ directory was pre-created in the elaboration setup phase with skeleton files."
  - "AC-1 path decision: nodes/sync/doc-sync.ts (auto-resolved in QA Discovery Notes)."
  - "database_status field: z.enum(['success', 'timeout', 'connection_failed', 'unavailable']).optional() added to DocSyncResultSchema (auto-resolved in QA Discovery Notes)."
