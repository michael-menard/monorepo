---
id: WINT-2080
title: "Create context-warmer Agent (Haiku-powered cache-warm skill executor)"
status: ready-to-work
priority: medium
experiment_variant: "control"
epic: wint
phase: 2
story_type: feature
points: 1
depends_on:
  - WINT-2070
surfaces:
  - agent
touches_backend: false
touches_frontend: false
touches_database: false
touches_infra: false
created: "2026-03-03"
updated: "2026-03-03T00:00:00Z"
seed_path: "plans/future/platform/wint/backlog/WINT-2080/_pm/STORY-SEED.md"
pm_artifacts:
  test_plan:
    strategy: manual+behavioral
    scope:
      endpoints: []
      ui_touched: false
      data_touched: false
    happy_path_tests:
      - id: HP-1
        title: "Agent file exists with valid WINT frontmatter"
        setup: "Story delivered: .claude/agents/context-warmer.agent.md is present"
        action: "Read .claude/agents/context-warmer.agent.md and inspect frontmatter block"
        expected: "Frontmatter contains type: worker, model: haiku, tools: [...], version, created, updated fields"
        evidence: "cat .claude/agents/context-warmer.agent.md | head -20"
      - id: HP-2
        title: "Agent mission is defined and concise"
        setup: "context-warmer.agent.md exists"
        action: "Read the ## Mission section"
        expected: "Mission is ≤5 sentences, describes invoking cache-warm skill and returning warm-result"
        evidence: "Manual inspection of ## Mission section"
      - id: HP-3
        title: "Agent defines phased execution model with required phases"
        setup: "context-warmer.agent.md exists"
        action: "Read the ## Execution Phases section"
        expected: "At least 4 phases present: validate inputs, invoke cache-warm skill, handle per-cache failures, emit warm-result summary"
        evidence: "Manual inspection of phase headings in agent file"
      - id: HP-4
        title: "Partial failure path documented with CACHE-WARM COMPLETE WITH WARNINGS signal"
        setup: "context-warmer.agent.md exists"
        action: "Read failure handling phase and completion signals section"
        expected: "Agent increments failed counter, continues with remaining caches, ends with CACHE-WARM COMPLETE WITH WARNINGS: {N} warnings"
        evidence: "Grep for 'WITH WARNINGS' in agent file"
      - id: HP-5
        title: "All three completion signals are present and reachable"
        setup: "context-warmer.agent.md exists"
        action: "Read the ## Completion Signals section"
        expected: "CACHE-WARM COMPLETE, CACHE-WARM COMPLETE WITH WARNINGS, CACHE-WARM BLOCKED — all three listed"
        evidence: "grep 'CACHE-WARM' .claude/agents/context-warmer.agent.md — matches 3 signal variants"
      - id: HP-6
        title: "LangGraph Porting Notes section is present with required tables"
        setup: "context-warmer.agent.md exists"
        action: "Read ## LangGraph Porting Notes section"
        expected: "Section contains input state fields table, output contract table, tool requirements"
        evidence: "Manual inspection of ## LangGraph Porting Notes section"
      - id: HP-7
        title: "Non-Goals section lists all four required exclusions"
        setup: "context-warmer.agent.md exists"
        action: "Read ## Non-Goals section"
        expected: "Section explicitly excludes direct populate-*.ts invocation, direct wint.context_packs writes, cache invalidation, scheduling/cron"
        evidence: "Manual inspection of ## Non-Goals section"
    error_cases:
      - id: EC-1
        title: "cache-warm skill unavailable — agent emits BLOCKED, does not attempt direct scripts"
        setup: "context-warmer.agent.md exists"
        action: "Read graceful degradation section of the agent file"
        expected: "Agent emits CACHE-WARM BLOCKED: cache-warm skill unavailable — NO fallback to populate scripts"
        evidence: "Grep for 'BLOCKED' and confirm no mention of populate-*.ts in fallback path"
      - id: EC-2
        title: "All caches fail to warm — agent still reports structured result"
        setup: "context-warmer.agent.md exists"
        action: "Read embedded fallback constraint section"
        expected: "Total failure still produces { attempted: N, succeeded: 0, failed: N } warm-result"
        evidence: "Manual inspection of embedded constraints block"
      - id: EC-3
        title: "One of N caches fails — agent continues and counts failures"
        setup: "context-warmer.agent.md exists"
        action: "Read partial failure handling phase"
        expected: "Agent logs individual failure, increments failed counter, proceeds with remaining caches"
        evidence: "Manual inspection of failure phase logic"
    edge_cases:
      - id: ED-1
        title: "Agent file does not import from barrel files"
        setup: "context-warmer.agent.md exists"
        action: "Read the agent file for any import patterns"
        expected: "No barrel file imports present"
        evidence: "Grep for barrel-style imports — should return empty"
      - id: ED-2
        title: "Warm-result schema is PopulateResultSchema-compatible"
        setup: "context-warmer.agent.md exists"
        action: "Read embedded constraints / output contract sections"
        expected: "warm-result includes attempted, succeeded, failed counts matching PopulateResultSchema shape"
        evidence: "Manual inspection: confirm { attempted, succeeded, failed } fields documented"
      - id: ED-3
        title: "Agent frontmatter uses haiku model"
        setup: "context-warmer.agent.md exists"
        action: "Read frontmatter model field"
        expected: "model: haiku"
        evidence: "grep 'model:' .claude/agents/context-warmer.agent.md"
    manual_cases:
      - id: MC-1
        title: "Full agent file behavioral review (AC checklist)"
        steps:
          - "Read .claude/agents/context-warmer.agent.md"
          - "Verify AC-1: frontmatter has type: worker, model: haiku, tools list, version, created, updated"
          - "Verify AC-2: ## Mission is ≤5 sentences"
          - "Verify AC-3: At least 4 execution phases present"
          - "Verify AC-4: Partial failure path documented with COMPLETE WITH WARNINGS signal"
          - "Verify AC-5: Skill unavailability → CACHE-WARM BLOCKED (no fallback to populate scripts)"
          - "Verify AC-6: Exactly three completion signals defined"
          - "Verify AC-7: ## LangGraph Porting Notes section present"
          - "Verify AC-8: Embedded fallback constraint documenting { attempted, succeeded, failed } schema"
          - "Verify AC-9: ## Non-Goals excludes direct script invocation, DB writes, cache invalidation, scheduling"
        expected: "All 9 ACs verified by file inspection"
    risks:
      - "WINT-2070 skill API undefined — placeholder contract cannot be verified until WINT-2070 is elaborated"
      - "Behavioral testing of agent logic is deferred — no TypeScript helper to unit test in this story"
      - "Integration test requiring real skill invocation is blocked until WINT-2070 is delivered"
  dev_feasibility:
    feasible: true
    confidence: high
    rationale: "Single .agent.md file (~150-200 lines). No TypeScript source code, no schema changes, no new packages. Pattern well-established via scope-defender.agent.md and kb-writer.agent.md. Only risk is WINT-2070 skill API undefined, handled by placeholder contract section."
    complexity: low
    change_surface:
      areas:
        - ".claude/agents/ (new file: context-warmer.agent.md)"
      endpoints: []
      deploy_touchpoints: []
    risks:
      - finding: "WINT-2070 (cache-warm skill) API surface is not yet defined"
        mitigation: "Add clearly marked TODO in skill invocation section. Placeholder contract serves as spec for WINT-2070."
    missing_requirements: []
    evidence_expectations:
      - "cat .claude/agents/context-warmer.agent.md — file present and all 9 ACs satisfied"
      - "grep 'CACHE-WARM' .claude/agents/context-warmer.agent.md — 3 signal variants visible"
      - "grep 'model: haiku' .claude/agents/context-warmer.agent.md — confirms haiku model"
      - "grep 'LangGraph Porting Notes' .claude/agents/context-warmer.agent.md — section present for WINT-9090"
---

# WINT-2080: Create context-warmer Agent (Haiku-powered cache-warm skill executor)

## Context

Phase 2 of the WINT platform targets 80% token reduction by pre-populating four cache layers before agent workflows start: project context (WINT-2030), agent missions (WINT-2040), domain knowledge (WINT-2050), and library patterns (WINT-2060). WINT-2070 creates the `cache-warm` skill that orchestrates invocation of all populate scripts.

Without an agent to trigger this skill, cache warming remains a manual CLI operation. Caches may be stale or empty when high-volume workflow operations begin, negating the token reduction benefits of Phase 2.

The baseline (2026-02-13) predates the Phase 2 cache populate stories — WINT-2030/2040/2050/2060 are active/near-complete and not yet reflected in the baseline. WINT-2070 has status `created` and is the direct dependency for this story.

## Goal

Create `.claude/agents/context-warmer.agent.md` — a lightweight, haiku-powered worker agent whose sole responsibility is to invoke the `cache-warm` skill (WINT-2070), handle partial failures gracefully, and return a structured warm-result summary to its caller.

The agent is designed to be:
- Invokable by any leader agent or CLI trigger
- Resilient to individual cache failures (partial success is not a failure)
- Portable to a LangGraph node in WINT-9090

## Non-Goals

- Do not implement the cache-warm skill itself — that is WINT-2070
- Do not invoke `populate-project-context.ts`, `populate-domain-kb.ts`, or other populate scripts directly — delegate only through the skill
- Do not add cache scheduling or cron-based invocation — manual/triggered-on-demand is MVP
- Do not implement cache invalidation logic — out of scope for this agent
- Do not create any TypeScript source files — this story delivers only an `.agent.md` file
- Do not create tests for the agent file itself — behavioral testing of agent logic is deferred (future opportunity per WINT-1150 pattern)
- Do not modify existing populate scripts or context cache MCP tools
- Do not modify the `wint.context_packs` table schema (protected: WINT-0010)

## Scope

**Files to create:**
- `.claude/agents/context-warmer.agent.md` (~150-200 lines)

**Files to read (for pattern reference):**
- `.claude/agents/scope-defender.agent.md` — gold-standard haiku worker template
- `.claude/agents/kb-writer.agent.md` — fallback behavior and structured result contract pattern

**No endpoints, no packages, no database changes.**

## Acceptance Criteria

- [ ] **AC-1**: A new agent file `.claude/agents/context-warmer.agent.md` exists with valid WINT frontmatter: `type: worker`, `model: haiku`, explicit `tools` list, `version`, `created`, `updated`.

- [ ] **AC-2**: The agent defines a clear mission: invoke the `cache-warm` skill and return a structured warm-result to the caller. Mission fits in ≤ 5 sentences.

- [ ] **AC-3**: The agent defines a phased execution model with at least: (1) validate inputs / check skill availability, (2) invoke cache-warm skill with configured options, (3) handle per-cache failures, (4) emit structured warm-result summary.

- [ ] **AC-4**: The agent handles partial failures gracefully — if 1 of N caches fails to warm, the agent logs the failure, increments a `failed` counter, and continues with remaining caches. A partial success is `CACHE-WARM COMPLETE WITH WARNINGS`, not a BLOCKED result.

- [ ] **AC-5**: The agent defines explicit graceful degradation: if the cache-warm skill (WINT-2070) is not available, the agent emits `CACHE-WARM BLOCKED: cache-warm skill unavailable` and does NOT attempt direct script invocation.

- [ ] **AC-6**: The agent emits exactly one of three completion signals as its final output line:
  - `CACHE-WARM COMPLETE` — all caches warmed successfully
  - `CACHE-WARM COMPLETE WITH WARNINGS: {N} warnings` — partial success
  - `CACHE-WARM BLOCKED: {reason}` — unrecoverable failure

- [ ] **AC-7**: The agent includes a `## LangGraph Porting Notes` section documenting the input/output contract for WINT-9090 (port to `nodes/context/context-warmer.ts`). Minimum: input state fields table, output contract table, tool requirements.

- [ ] **AC-8**: The agent defines an embedded fallback constraint: if any single cache fails, the warm-result must still include `{ attempted: N, succeeded: M, failed: K }` counts conforming to the same `PopulateResultSchema` shape used by populate scripts.

- [ ] **AC-9**: The agent file includes a `## Non-Goals` section explicitly excluding: (a) directly invoking populate-*.ts scripts, (b) writing to `wint.context_packs` directly, (c) cache invalidation (deferred to WINT-2070 or later), (d) scheduling or recurring warm jobs.

## Reuse Plan

| Component | Source | How Used |
|-----------|--------|----------|
| Haiku worker frontmatter | `.claude/agents/scope-defender.agent.md` | Copy frontmatter structure: `type: worker`, `model: haiku`, `tools`, `version`, `created`, `updated` |
| Phase-based execution model | `.claude/agents/scope-defender.agent.md` | Copy 4-phase structure with blocking checks at phase boundaries |
| Graceful degradation pattern | `.claude/agents/scope-defender.agent.md` | Missing optional inputs counted as warnings, proceed with available inputs |
| Completion signals pattern | `.claude/agents/kb-writer.agent.md` | Three-signal pattern: COMPLETE / COMPLETE WITH WARNINGS / BLOCKED |
| Structured result contract | populate-project-context.ts pattern | `{ attempted, succeeded, failed }` warm-result schema |
| LangGraph porting notes section | `.claude/agents/scope-defender.agent.md` | Input/output contract tables for WINT-9090 consumption |
| Embedded constraints block | `.claude/agents/scope-defender.agent.md` | Hard limits documented in agent file when role-pack unavailable |

**No package-level code is created.** Downstream packages `@repo/mcp-tools` (contextCachePut) and `@repo/logger` are used by the skill, not the agent directly.

## Architecture Notes

The context-warmer agent sits at the invocation boundary between workflow orchestration and the cache-warm skill. Its architecture follows the haiku worker model:

1. **Thin executor**: The agent does not know the internals of any populate script. It only knows how to invoke the `cache-warm` skill and interpret its result.

2. **Partial failure semantics**: The agent explicitly documents that `attempted > succeeded` is a valid success state. This prevents callers from treating partial warm as a hard failure.

3. **Skill API placeholder**: Because WINT-2070 is not yet elaborated, the agent file must include a `# TODO: Update when WINT-2070 skill API is finalized` marker in the skill invocation section. The placeholder API shape should document expected inputs (cache types, options) and expected output (warm-result with attempted/succeeded/failed counts).

4. **LangGraph portability**: WINT-9090 will port this agent to `nodes/context/context-warmer.ts`. The `## LangGraph Porting Notes` section is required now to avoid integration friction later (learned from WINT-9090 pattern).

5. **No direct DB access**: The agent must not call `contextCachePut()` or `contextCacheGet()` directly — all cache writes go through the cache-warm skill abstraction.

## Test Plan

**Strategy**: Manual behavioral verification — no TypeScript code is produced.

**Primary verification**: Read `.claude/agents/context-warmer.agent.md` and confirm all 9 ACs are satisfied by file inspection.

**Key checks**:
- `grep 'CACHE-WARM' .claude/agents/context-warmer.agent.md` — must show all three signal variants
- `grep 'model: haiku' .claude/agents/context-warmer.agent.md` — confirms haiku model
- `grep 'LangGraph Porting Notes' .claude/agents/context-warmer.agent.md` — section present for WINT-9090

**Deferred** (blocked on WINT-2070):
- Integration test: trigger agent with real cache-warm skill against dev DB, verify `{ attempted, succeeded, failed }` warm-result
- Behavioral unit test of agent logic as TypeScript pure functions (per WINT-1150 lesson — future opportunity)

## Reality Baseline

| Feature | Status | Notes |
|---------|--------|-------|
| `wint.context_packs` table | Active (WINT-0010, protected) | Cache write target — DO NOT modify schema |
| `contextCachePut()` / `contextCacheGet()` MCP tools | UAT-complete (WINT-0100) | Used by populate scripts, not directly by this agent |
| `populate-project-context.ts` | needs-code-review (WINT-2030) | Sibling populate script — NOT invoked directly |
| `populate-domain-kb.ts` | needs-code-review (WINT-2050) | Sibling populate script — NOT invoked directly |
| cache-warm skill (WINT-2070) | created | DIRECT DEPENDENCY — API not yet defined |
| `.claude/agents/*.agent.md` pattern | Active (115+ agents) | Structural template for new agent |

## Canonical References

Files that demonstrate the patterns this story should follow:

| Pattern | File | Why |
|---------|------|-----|
| Haiku worker agent structure | `.claude/agents/scope-defender.agent.md` | Complete haiku worker with phases, graceful degradation, embedded constraints, completion signals, LangGraph porting notes — gold-standard template |
| Lightweight worker with fallback | `.claude/agents/kb-writer.agent.md` | Structured result contract, KB unavailable fallback pattern, clean completion signals for worker that can fail gracefully |
| Skill invocation pattern | `.claude/skills/doc-sync/SKILL.md` | How skills are documented and invoked by agents; the cache-warm skill (WINT-2070) will follow this pattern |
| Agent .md frontmatter standard | `.claude/agents/scope-defender.agent.md` | Frontmatter with `type: worker`, `model: haiku`, `tools` list — required fields for WINT compliance |

## Subtasks

### ST-1: Write frontmatter, mission, and non-goals sections
**Goal**: Establish the agent file with valid WINT frontmatter and bounded mission statement.
**ACs covered**: AC-1, AC-2, AC-9
**Files to read**: `.claude/agents/scope-defender.agent.md`, `.claude/agents/kb-writer.agent.md`
**Files to modify**: `.claude/agents/context-warmer.agent.md` (create)
**Depends on**: none
**Verification**: `cat .claude/agents/context-warmer.agent.md | head -30` — confirm frontmatter and mission
**Estimated tokens**: 4000

### ST-2: Write execution phases with placeholder cache-warm skill API contract
**Goal**: Define phased execution model with explicit TODO for WINT-2070 skill API.
**ACs covered**: AC-3, AC-4, AC-5
**Files to read**: `.claude/agents/scope-defender.agent.md`
**Files to modify**: `.claude/agents/context-warmer.agent.md`
**Depends on**: ST-1
**Verification**: `grep -c 'Phase' .claude/agents/context-warmer.agent.md` — should show ≥4 phases
**Estimated tokens**: 5000

### ST-3: Write graceful degradation, embedded constraints, and completion signals
**Goal**: Ensure all failure paths are documented and exactly three completion signals are defined.
**ACs covered**: AC-4, AC-5, AC-6, AC-8
**Files to read**: `.claude/agents/scope-defender.agent.md`, `.claude/agents/kb-writer.agent.md`
**Files to modify**: `.claude/agents/context-warmer.agent.md`
**Depends on**: ST-2
**Verification**: `grep 'CACHE-WARM' .claude/agents/context-warmer.agent.md` — shows 3 signal variants
**Estimated tokens**: 4000

### ST-4: Write LangGraph Porting Notes section
**Goal**: Document input/output contract for WINT-9090 node port at `nodes/context/context-warmer.ts`.
**ACs covered**: AC-7
**Files to read**: `.claude/agents/scope-defender.agent.md`
**Files to modify**: `.claude/agents/context-warmer.agent.md`
**Depends on**: ST-3
**Verification**: `grep 'LangGraph Porting Notes' .claude/agents/context-warmer.agent.md` — section present
**Estimated tokens**: 3000

---

## QA Notes

```yaml
qa_notes:
  verdict: PASS
  decided_at: "2026-03-03T12:01:00Z"
  mode: autonomous
  acs_added: 0
  gaps_resolved: 0
  opportunities_logged: 2
  notes: "Clean story with strong reuse plan. Missing Examples/Edge Cases sections noted as non-blocking (covered by test plan). No MVP gaps."
```
