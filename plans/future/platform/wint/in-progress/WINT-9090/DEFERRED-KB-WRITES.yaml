deferred_at: "2026-02-24T12:00:00Z"
reason: "KB MCP server not directly accessible during PM story generation phase"
retry_on: "next dev session with KB MCP available"
writes:
  - type: story_insert
    table: stories
    data:
      story_id: WINT-9090
      feature: wint
      title: "Create Context Cache LangGraph Nodes — Port context-warmer and session-manager to nodes/context/"
      story_dir: "plans/future/platform/wint/backlog/WINT-9090"
      story_file: "plans/future/platform/wint/backlog/WINT-9090/WINT-9090.md"
      story_type: feature
      points: 5
      priority: medium
      state: backlog
      touches_backend: true
      touches_frontend: false
      touches_database: true
      touches_infra: false
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'WINT-9090', 'wint',
        'Create Context Cache LangGraph Nodes — Port context-warmer and session-manager to nodes/context/',
        'plans/future/platform/wint/backlog/WINT-9090',
        'plans/future/platform/wint/backlog/WINT-9090/WINT-9090.md',
        'feature', 5, 'medium', 'backlog',
        true, false, true, false
      ) ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title, story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file, story_type = EXCLUDED.story_type,
        points = EXCLUDED.points, priority = EXCLUDED.priority,
        state = EXCLUDED.state, touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;
