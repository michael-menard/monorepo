---
id: WINT-2090
title: "Implement Session Context Management — session-create and session-inherit Skills"
status: in-progress
priority: P2
phase: 2
points: 1
story_type: feature
epic: wint
experiment_variant: "control"
touches_backend: false
touches_frontend: false
touches_database: false
touches_infra: false
touches_skills: true
depends_on:
  - WINT-0110
blocks:
  - WINT-2100
  - WINT-2110
created: "2026-02-20"
predictions:
  split_risk: 0.2
  review_cycles: 1
  token_estimate: 50000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-20T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
---

# WINT-2090: Implement Session Context Management — session-create and session-inherit Skills

## Context

The WINT platform is in Phase 2 (Context Cache & Sidecars). Phase 2's goal is to achieve an 80% token reduction by introducing shared sidecar services, context caching, and session-based state sharing between leader agents and their spawned workers.

**The current gap**: The `wint.contextSessions` table and its 5 MCP tools (from WINT-0110) are fully implemented on disk. However, there are no Claude Code skills that guide leader agents on how to open sessions or guide worker agents on how to inherit and report into them. Without these skills, the session infrastructure sits idle — the `wint.contextSessions` table accumulates no records and token accounting per workflow is impossible.

**What this story delivers**: Two Claude Code skills — procedural markdown documents that agents follow as step-by-step instructions:

1. **`session-create`** — Called by a leader agent at workflow start. Invokes `sessionCreate` MCP tool to open a record in `wint.contextSessions`. Emits a machine-parseable `SESSION CREATED` output block that the leader captures and injects into worker system prompts.

2. **`session-inherit`** — Called by a worker agent upon receiving a session ID. Invokes `sessionQuery` to verify the session is active. Documents how workers report incremental token usage via `sessionUpdate`. Explicitly forbids workers from calling `sessionComplete` (only the leader that opened the session may close it).

Both skills handle graceful degradation: if the database is unavailable, they emit a warning and allow the workflow to proceed without session tracking — session tracking is a telemetry concern, not a core workflow gate.

**Baseline**: Baseline date 2026-02-13. The baseline captures application-level reality (Drizzle schemas, pgvector KB, Docker Compose) but does not reflect WINT-specific session infrastructure. Session tools from WINT-0110 confirmed present on disk via codebase scan.

---

## Goal

Enable stateful workflow coordination between leader agents and spawned workers by providing standard skills for opening, inheriting, and closing sessions — activating the `wint.contextSessions` infrastructure from WINT-0110 for use by the first group of high-volume agents.

---

## Non-Goals

- **Do not implement a session cleanup scheduler** — deferred to WINT-2100 (session-manager Agent)
- **Do not modify `wint.contextSessions` schema** — the DB table is fixed from WINT-0010/WINT-0110
- **Do not create TypeScript code** — skills are markdown procedural documents only
- **Do not update existing leader agents** to use these skills — that is the scope of WINT-2110
- **Do not implement the context-pack sidecar** (WINT-2020) or role-pack sidecar (WINT-2010)
- **Do not touch the context-cache MCP tools** (WINT-0100) — this story is session-only
- **Do not inject session IDs into `.agent.md` frontmatter or `CHECKPOINT.yaml`** — pass via system prompt injection or initial context only

---

## Scope

**New files created by this story**:
- `.claude/skills/session-create/SKILL.md` — New Claude Code skill
- `.claude/skills/session-inherit/SKILL.md` — New Claude Code skill

**Read-only references** (no modifications to these files):
- `packages/backend/mcp-tools/src/session-management/session-create.ts`
- `packages/backend/mcp-tools/src/session-management/session-query.ts`
- `packages/backend/mcp-tools/src/session-management/session-update.ts`
- `packages/backend/mcp-tools/src/session-management/session-complete.ts`
- `packages/backend/mcp-tools/src/session-management/session-cleanup.ts`
- `packages/backend/mcp-tools/src/session-management/__types__/index.ts`
- `.claude/skills/wt-new/SKILL.md` (canonical skill structure reference)
- `.claude/skills/doc-sync/SKILL.md` (canonical complex skill reference)

**No TypeScript packages touched. No migrations. No endpoints.**

---

## Acceptance Criteria

- [ ] **AC-1**: `session-create` skill file created at `.claude/skills/session-create/SKILL.md` with valid YAML frontmatter (`name`, `description` fields)
- [ ] **AC-2**: `session-create` skill invokes `sessionCreate` MCP tool with `agentName`, `storyId`, and `phase` as inputs; emits structured output block:
  ```
  SESSION CREATED
    session_id: {uuid}
    agent: {agentName}
    story: {storyId}
  ```
- [ ] **AC-3**: `session-create` skill handles null return from `sessionCreate` (DB error) by emitting `SESSION UNAVAILABLE — continuing without session tracking` and proceeding without failing the workflow
- [ ] **AC-4**: `session-inherit` skill file created at `.claude/skills/session-inherit/SKILL.md` with valid YAML frontmatter (`name`, `description` fields)
- [ ] **AC-5**: `session-inherit` skill accepts a `session_id` argument, invokes `sessionQuery` with `activeOnly: true` to confirm the session is active, and emits a confirmation or a `SESSION NOT FOUND` warning
- [ ] **AC-6**: `session-inherit` skill documents how a worker calls `sessionUpdate` (incremental mode) to report token usage — with an explicit example showing `mode: 'incremental'` and the fields (`inputTokens`, `outputTokens`, `cachedTokens`)
- [ ] **AC-7**: Both skills include a "Session Lifecycle" section documenting the full contract: leader opens (session-create) → workers inherit (session-inherit) → leader closes (`sessionComplete`) → periodic cleanup (`sessionCleanup`, future WINT-2100, `retentionDays: 90`)
- [ ] **AC-8**: Both skills include a "Graceful Degradation" section: if the DB is unavailable or session creation returns null, the workflow continues without session tracking (warning emitted, not an error)
- [ ] **AC-9**: `session-create` skill output format is parseable by a calling leader orchestrator — structured block, not prose. Must be capturable by pattern: `/SESSION CREATED\n\s+session_id:\s+([0-9a-f-]{36})/`
- [ ] **AC-10**: `session-inherit` skill explicitly states — in a prominent, visually distinct section — that workers must **NOT** call `sessionComplete`. Only the leader that opened the session via `session-create` may complete it. (Prevents double-completion Business Logic Errors from WINT-0110)
- [ ] **AC-11**: `session-inherit` skill documents the correct `sessionQuery` invocation pattern — `sessionQuery({ activeOnly: true, limit: 50 })` (optionally with `storyId` if known), followed by client-side filtering of the returned array to find the matching `sessionId`. Skill must NOT show `sessionQuery({ sessionId: ... })` as `sessionId` is not a valid filter field in `SessionQueryInputSchema`.
  _Added by autonomous elaboration_

---

## Reuse Plan

- **Tools**: Invoke `sessionCreate`, `sessionQuery`, `sessionUpdate`, `sessionComplete`, `sessionCleanup` from `@repo/mcp-tools/session-management/` — do not reimplement
- **Skill structure patterns**:
  - `/wt-new/SKILL.md` — for single-purpose skill structure (frontmatter, Usage, What It Does, Workflow, Output)
  - `/doc-sync/SKILL.md` — for multi-phase execution with graceful degradation, integration patterns
- **Tool API reference**: `packages/backend/mcp-tools/src/session-management/__types__/index.ts` — Zod schemas define exact input parameters
- **Packages**: No new packages. `@repo/mcp-tools` and `@repo/database-schema` are already in place.

---

## Architecture Notes

**Session ownership model**:
- One leader opens → one `session-create` call → one UUID
- N workers inherit → N `session-inherit` calls → N `sessionUpdate` calls (incremental)
- One leader closes → one `sessionComplete` call
- Workers never call `sessionComplete`

**Token update safety**:
- `sessionUpdate` with `mode: 'incremental'` uses SQL arithmetic (`+= value`)
- This is concurrency-safe: multiple workers can call in parallel without race conditions
- `mode: 'absolute'` is explicitly forbidden in skill documentation for worker token reporting

**Graceful degradation contract**:
- `sessionCreate` returns `null` on DB error (already enforced by WINT-0110 implementation)
- Skills must surface this as a **warning**, not an exception
- Downstream workflow steps must not be gated on session tracking

**Skill invocation model**:
- Skills are markdown procedural documents
- Agents follow skill steps manually during their execution — there is no runtime skill executor
- Session IDs are passed between leader and workers via system prompt injection or initial context, not via file writes

---

## Infrastructure Notes

No infrastructure changes required. This story relies entirely on the existing:
- `wint.contextSessions` table (WINT-0010 / WINT-0030)
- Session management MCP tools (WINT-0110)
- Claude Code skill directory (`.claude/skills/`)

---

## Prerequisite: WINT-0110 Promotion

**Warning (non-blocking for story generation)**: WINT-0110 is listed as `pending` in the stories index, but the implementation is confirmed on disk at `packages/backend/mcp-tools/src/session-management/` with 5 tools, test suites, and documentation.

**Dev action required before implementation**:
1. Run `pnpm --filter @repo/mcp-tools test` to confirm all session-management tests pass
2. If all tests pass, promote WINT-0110 to `uat` or `completed` in `stories.index.md`
3. Only then proceed with WINT-2090 skill authoring

If tests fail, WINT-0110 must be fixed before WINT-2090 can proceed. Block and raise.

---

## Test Plan

See full test plan: `_pm/TEST-PLAN.md`

### Test Summary

| Test | Type | AC(s) | Tool Required |
|------|------|-------|--------------|
| HT-1: session-create creates session and emits structured output | Integration | AC-1, AC-2, AC-9 | Live DB |
| HT-2: session-inherit verifies active session | Integration | AC-4, AC-5 | Live DB |
| HT-3: Worker reports token usage via sessionUpdate incremental | Integration | AC-6 | Live DB |
| HT-4: Leader completes session via sessionComplete | Integration | — | Live DB |
| HT-5: Skill frontmatter fields valid | Docs check | AC-1, AC-4 | File read |
| EC-1: sessionCreate returns null — graceful degradation | Degradation | AC-3, AC-8 | DB stopped |
| EC-2: session-inherit with unknown/inactive session_id | Degradation | AC-5, AC-8 | Live DB |
| EC-3: Worker attempts sessionComplete (prevented by docs) | Docs check | AC-10 | File read |
| EG-4: Output block format is machine-parseable | Format | AC-9 | File read |
| EG-5: Session Lifecycle section completeness | Docs check | AC-7 | File read |

**UAT requirement (ADR-005)**: All integration tests must use live `postgres-knowledgebase` with the real `wint.contextSessions` table. No mocks.

---

## Reality Baseline

- **Baseline date**: 2026-02-13
- **Session tools on disk**: Confirmed — `packages/backend/mcp-tools/src/session-management/` has 5 tools, test suites, and `SESSION-MANAGEMENT-TOOLS.md`
- **Skill directory**: `.claude/skills/` exists with `wt-new/`, `doc-sync/`, etc.
- **`wint.contextSessions` table**: Exists (WINT-0010 + WINT-0030)
- **WINT-0110 status gap**: Tool code is present and appears functional; formal status promotion is a prerequisite for implementation

---

## Canonical References

Files that demonstrate the patterns this story must follow:

| Pattern | File | Why |
|---------|------|-----|
| Skill structure (simple) | `.claude/skills/wt-new/SKILL.md` | Clean frontmatter, Usage block, step-by-step workflow, structured output format (`WORKTREE CREATED` block) |
| Skill structure (complex multi-phase) | `.claude/skills/doc-sync/SKILL.md` | Phase-based execution sections, graceful degradation, integration patterns |
| Session MCP tool (creation) | `packages/backend/mcp-tools/src/session-management/session-create.ts` | Zod validation at entry, resilient null return on DB error, `@repo/logger` warn pattern |
| Session MCP tool types | `packages/backend/mcp-tools/src/session-management/__types__/index.ts` | Zod-first input schemas, optional UUID generation, incremental vs absolute mode |

---

## Subtasks

From Dev Feasibility review (`_pm/DEV-FEASIBILITY.md`):

### ST-1: Prerequisite — Verify WINT-0110 Tool Stability

- **Goal**: Confirm that session management MCP tools pass their test suite and their API is stable enough to document in skill files.
- **Files to read**: `packages/backend/mcp-tools/src/session-management/__types__/index.ts`, `packages/backend/mcp-tools/src/session-management/session-create.ts`
- **Files to create/modify**: None (verification step)
- **ACs covered**: Prerequisite for all ACs
- **Depends on**: none
- **Verification**: `pnpm --filter @repo/mcp-tools test 2>&1 | tail -20` — all tests pass

### ST-2: Write session-create/SKILL.md

- **Goal**: Create the `session-create` skill file with valid frontmatter, usage section, execution steps invoking `sessionCreate`, and the structured `SESSION CREATED` output block.
- **Files to read**: `.claude/skills/wt-new/SKILL.md`, `packages/backend/mcp-tools/src/session-management/__types__/index.ts`
- **Files to create/modify**: `.claude/skills/session-create/SKILL.md` (create new)
- **ACs covered**: AC-1, AC-2, AC-9
- **Depends on**: ST-1
- **Verification**: `head -6 .claude/skills/session-create/SKILL.md | grep -E "^name:|^description:"` — both fields present

### ST-3: Add Null-Return Handling to session-create/SKILL.md

- **Goal**: Add the `SESSION UNAVAILABLE` fallback section that documents how to handle a null return from `sessionCreate`.
- **Files to read**: `.claude/skills/session-create/SKILL.md` (from ST-2)
- **Files to create/modify**: `.claude/skills/session-create/SKILL.md` (modify)
- **ACs covered**: AC-3
- **Depends on**: ST-2
- **Verification**: `grep "SESSION UNAVAILABLE" .claude/skills/session-create/SKILL.md` — text present

### ST-4: Write session-inherit/SKILL.md

- **Goal**: Create the `session-inherit` skill file with valid frontmatter, usage section, execution steps for `sessionQuery`, confirmation/warning output, and incremental `sessionUpdate` example.
- **Files to read**: `.claude/skills/doc-sync/SKILL.md`, `packages/backend/mcp-tools/src/session-management/__types__/index.ts`
- **Files to create/modify**: `.claude/skills/session-inherit/SKILL.md` (create new)
- **ACs covered**: AC-4, AC-5, AC-6
- **Depends on**: ST-1
- **Verification**: `grep -c "mode: 'incremental'" .claude/skills/session-inherit/SKILL.md` — at least 1 match

### ST-5: Add sessionComplete Restriction to session-inherit/SKILL.md

- **Goal**: Add the prominent `sessionComplete` restriction section to `session-inherit/SKILL.md`.
- **Files to read**: `.claude/skills/session-inherit/SKILL.md` (from ST-4)
- **Files to create/modify**: `.claude/skills/session-inherit/SKILL.md` (modify)
- **ACs covered**: AC-10
- **Depends on**: ST-4
- **Verification**: `grep "sessionComplete" .claude/skills/session-inherit/SKILL.md` — restriction text present

### ST-6: Add Session Lifecycle and Graceful Degradation Sections to Both Skills

- **Goal**: Add "Session Lifecycle" (4-step contract) and "Graceful Degradation" sections to both skill files.
- **Files to read**: `.claude/skills/session-create/SKILL.md`, `.claude/skills/session-inherit/SKILL.md`
- **Files to create/modify**: `.claude/skills/session-create/SKILL.md` (modify), `.claude/skills/session-inherit/SKILL.md` (modify)
- **ACs covered**: AC-7, AC-8
- **Depends on**: ST-3, ST-5
- **Verification**: `grep -l "Session Lifecycle" .claude/skills/session-create/SKILL.md .claude/skills/session-inherit/SKILL.md | wc -l` — outputs `2`

---

## AC–Subtask Coverage

| AC | Subtask(s) |
|----|------------|
| AC-1 (session-create frontmatter) | ST-2 |
| AC-2 (sessionCreate invocation + structured output) | ST-2 |
| AC-3 (null return handling) | ST-3 |
| AC-4 (session-inherit frontmatter) | ST-4 |
| AC-5 (sessionQuery verification) | ST-4 |
| AC-6 (incremental sessionUpdate example) | ST-4 |
| AC-7 (Session Lifecycle section) | ST-6 |
| AC-8 (Graceful Degradation section) | ST-6 |
| AC-9 (parseable output format) | ST-2 |
| AC-10 (sessionComplete restriction) | ST-5 |
| AC-11 (sessionQuery client-side filter pattern — autonomous elaboration) | ST-4 |

All 11 ACs are covered. No gaps.

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-20_

### MVP Gaps Resolved

| # | Finding | Resolution | AC Added |
|---|---------|------------|----------|
| 1 | sessionQuery lacks direct sessionId filter; worker cannot confirm session active without query strategy | Skill must document client-side filter pattern: call sessionQuery({ activeOnly: true, limit: 50 }) then filter returned array for matching UUID | AC-11 |

### Non-Blocking Items (Logged to KB)

| # | Finding | Category | Status |
|---|---------|----------|--------|
| 1 | sessionQuery pagination boundary — workers may miss session if beyond default limit 50 | edge-case | Logged for future skill revision |
| 2 | Missing UUID format validation in session-inherit | edge-case | Logged for future skill revision |
| 3 | sessionComplete double-completion risk — documentation-only enforcement | edge-case | Logged for WINT-2100+ runtime guard |
| 4 | UX Polish: SESSION CREATED capture pattern lacks orchestrator integration example | enhancement | Logged for post-MVP enhancement |
| 5 | Observability: No structured SESSION UNAVAILABLE log format | enhancement | Logged for WINT-3020 |
| 6 | Performance: session-inherit per-worker DB reads in high-parallelism workflows | enhancement | Logged for WINT-2100 optimization |
| 7 | Integration: No LangGraph porting notes for WINT-9090 parity | enhancement | Logged for WINT-9090 elaboration |
| 8 | Discoverability: Skills not listed in SKILLS.md until post-implementation doc-sync | enhancement | Logged for post-implementation |

### Summary

- ACs added: 1 (AC-11 for sessionQuery pattern documentation)
- KB entries created: 8 (non-blocking items for future work)
- Mode: autonomous
- Verdict: PASS — story ready for skill authoring implementation
