schema: 1
story_id: WINT-0240
timestamp: '2026-02-20T00:00:00Z'
generated_by: dev-plan-leader (inline, no worker spawned — infra/docs story)

domain: infra
scope_summary: "Shell script fleet setup, health check, and VRAM documentation for Ollama model fleet"

lessons:
  - id: L-001
    source: "WINT-0220-STRATEGY.yaml baseline"
    lesson: "Tier 2 Ollama primary models: deepseek-coder-v2:16b, codellama:13b, qwen2.5-coder:14b. Tier 3: qwen2.5-coder:7b, llama3.2:3b."
    applies_to: ["ST-1", "ST-2"]

  - id: L-002
    source: "packages/backend/orchestrator/setup-ollama.sh"
    lesson: "Existing script pattern: set -e, command -v ollama check, curl /api/tags check, confirmation prompt, pull loop, ollama list verification. All new scripts must follow this structure."
    applies_to: ["ST-2"]

  - id: L-003
    source: "packages/backend/orchestrator/src/providers/ollama.ts (OllamaConfigSchema)"
    lesson: "Env vars: OLLAMA_BASE_URL (default: http://127.0.0.1:11434), OLLAMA_TEMPERATURE (default: 0), OLLAMA_TIMEOUT_MS (default: 60000). No OLLAMA_ENABLE_FALLBACK in OllamaConfigSchema — this var is referenced in AC-9 but not in the schema."
    applies_to: ["ST-2", "ST-3", "ST-4"]

  - id: L-004
    source: "model-assignments.yaml"
    lesson: "model-assignments.yaml uses deepseek-coder-v2:33b for dev-implement-backend-coder and dev-implement-frontend-coder. WINT-0220-STRATEGY.yaml Tier 2 primary uses deepseek-coder-v2:16b. Both must be reconciled — recommend including both with :16b as default full-fleet and :33b as optional heavy variant."
    applies_to: ["ST-1", "ST-2"]

  - id: L-005
    source: "WINT-0220-STRATEGY.yaml integration.ollama_requirements"
    lesson: "Minimum required models for integration: Tier 2 = deepseek-coder-v2:16b + codellama:13b; Tier 3 = qwen2.5-coder:7b + llama3.2:3b. The --lite flag must at minimum provide Tier 3 models."
    applies_to: ["ST-2"]

  - id: L-006
    source: "model-assignments.yaml (llama3.2:8b)"
    lesson: "model-assignments.yaml references llama3.2:8b (not llama3.2:3b from WINT-0220-STRATEGY.yaml). Another discrepancy to document. Both 3b and 8b variants are valid Ollama registry entries."
    applies_to: ["ST-1"]

adrs: []

warnings:
  - "OLLAMA_ENABLE_FALLBACK is cited in AC-9 but does NOT appear in OllamaConfigSchema. Include it in --help output and docs with note 'runtime env var, not in OllamaConfigSchema' or omit if not implemented."
  - "model-assignments.yaml uses llama3.2:8b; WINT-0220-STRATEGY.yaml Tier 3 uses llama3.2:3b — document discrepancy."
  - "qwen2.5-coder:14b appears in WINT-0220-STRATEGY.yaml Tier 2 but NOT in model-assignments.yaml — include in full fleet with comment."
