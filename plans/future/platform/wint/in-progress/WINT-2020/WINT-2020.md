---
id: WINT-2020
title: "Create Context Pack Sidecar"
status: in-progress
updated_at: "2026-03-03T17:15:00Z"
priority: P1
phase: 2
points: 5
story_type: feature
experiment_variant: "control"
depends_on:
  - WINT-2010
epic: wint
surfaces:
  - backend
touches_backend: true
touches_frontend: false
touches_database: true
touches_infra: false
created_at: "2026-03-02"
seed_generated: "2026-03-02"
pm_artifacts:
  test_plan:
    strategy: unit+integration
    scope:
      endpoints:
        - "POST /context-pack"
      ui_touched: false
      data_touched: true
    happy_path_tests:
      - id: HP-1
        title: "Cache miss: assemble context pack from KB and store in cache"
        setup: "Ensure no contextPacks row exists for packKey 'WINT-2020:dev_execute:dev'. Start postgres-knowledgebase on port 5433."
        action: "POST /context-pack with { story_id: 'WINT-2020', node_type: 'dev_execute', role: 'dev' }"
        expected: "200 response with { story_brief, kb_facts[], kb_rules[], kb_links[], repo_snippets[] }. New contextPacks row upserted."
        evidence: "Assert response body matches ContextPackResponseSchema. Query contextPacks table to confirm row inserted."
      - id: HP-2
        title: "Cache hit: return cached content without re-querying KB"
        setup: "Insert contextPacks row with valid non-expired content for packKey."
        action: "POST /context-pack for same packKey"
        expected: "200, cached content returned, response time < 100ms"
        evidence: "Assert response matches cache row content. Assert elapsed < 100ms."
      - id: HP-3
        title: "MCP tool context_pack_get returns same shape as HTTP endpoint"
        action: "Call context_pack_get with { story_id, node_type, role }"
        expected: "Returns ContextPackResponseSchema-valid object"
        evidence: "Zod parse passes on response."
      - id: HP-4
        title: "Custom TTL stored in cache"
        action: "POST /context-pack with ttl: 7200"
        expected: "contextPacks row has ttl=7200"
        evidence: "Query DB to verify."
      - id: HP-5
        title: "Empty array returned for sections with no KB results"
        action: "POST /context-pack for story with no KB entries"
        expected: "All array fields are [] not null"
        evidence: "Strict null check on each array field."
    error_cases:
      - id: EC-1
        title: "Invalid role enum rejected with 400"
        action: "POST /context-pack with role: 'invalid'"
        expected: "400 with Zod validation error"
      - id: EC-2
        title: "Missing required fields rejected with 400"
        action: "POST /context-pack with empty body"
        expected: "400 listing missing fields"
      - id: EC-3
        title: "Cache write failure does not block response"
        setup: "Simulate DB error on contextPacks upsert"
        expected: "200 with fresh context, warning logged"
      - id: EC-4
        title: "Expired cache entry triggers fresh assembly"
        setup: "Insert row with expiresAt in the past"
        expected: "Cache miss path followed, fresh row written"
    edge_cases:
      - id: ED-1
        title: "Token budget: trim repo_snippets first"
        expected: "estimateTokens(response) <= 2000"
      - id: ED-2
        title: "Token budget: trim kb_links after repo_snippets exhausted"
        expected: "kb_links trimmed in second pass"
      - id: ED-3
        title: "Concurrent cache-miss: ON CONFLICT prevents duplicates"
        expected: "Single row in contextPacks for packKey after concurrent calls"
      - id: ED-4
        title: "Cache hit < 100ms"
        expected: "Timing assertion passes"
      - id: ED-5
        title: "Cache miss < 2000ms"
        expected: "Timing assertion passes"
    fixture_definitions:
      - name: "validContextPacksRow"
        type: "database row"
        description: "Non-expired contextPacks row for cache-hit tests"
      - name: "mockKbSearchResult"
        type: "object"
        description: "Mock KB result with known token counts for unit-level budget testing"
      - name: "oversizedContextPack"
        type: "object"
        description: "Context pack with estimated tokens > 2000 for trimming tests"
    risks:
      - "Integration tests require postgres-knowledgebase on port 5433"
      - "Token estimation via char-count/4 may need tolerance in assertions"
      - "Concurrent race condition test may be flaky without proper synchronization"
  dev_feasibility:
    feasible: true
    confidence: medium
    complexity: high
    rationale: "Technically sound with all dependencies in place. Medium confidence due to unknown WINT-2010 package layout decisions."
    change_surface:
      areas:
        - "packages/backend/sidecars/context-pack/ (new)"
        - "packages/backend/mcp-tools/ (register context_pack_get)"
        - "apps/api/knowledge-base/ (MCP tool registration)"
      endpoints:
        - "POST /context-pack (new)"
      deploy_touchpoints:
        - "New sidecar package added to pnpm workspace"
        - "context_pack_get registered in kb MCP server"
        - "No DB migration (reuses contextPacks)"
    risks:
      - finding: "Dependency gate: WINT-2010 must reach UAT first"
        mitigation: "Block dev pickup until WINT-2010 status = uat|completed"
      - finding: "kb_search requires db + embeddingClient injected at startup"
        mitigation: "Mirror WINT-2010 initialization pattern"
      - finding: "Concurrent cache-miss race condition on INSERT"
        mitigation: "Use contextCachePut's existing ON CONFLICT DO UPDATE upsert"
      - finding: "Token estimation via char-count/4 is approximate"
        mitigation: "Use tolerance margin in tests. Define estimateTokens as pure utility."
      - finding: "packType 'story' must exist in contextPackTypeEnum"
        mitigation: "Confirmed in context-cache/__types__/index.ts — direct reuse."
---

# WINT-2020: Create Context Pack Sidecar

## Context

Phase 2 of the WINT initiative targets 80% token reduction by eliminating repeated context re-injection across agent invocations. Phase 1 established a database-first story lifecycle with MCP tools for story/session/worktree management. The context cache infrastructure (WINT-0030 + WINT-0100) provides a `contextPacks` table and `contextCacheGet`/`contextCachePut` tools. WINT-2010 establishes the sidecar package pattern at `packages/backend/sidecars/role-pack/` — WINT-2020 follows that same pattern for context pack assembly.

**Problem**: Agents currently receive large system prompts with project context on every invocation. Each spawn re-reads CLAUDE.md, re-queries the KB, and re-assembles the same facts repeatedly. This wastes tokens and prevents achieving Phase 2's efficiency goals.

**Dependency**: This story cannot begin implementation until WINT-2010 (role pack sidecar) reaches UAT or completed status, as WINT-2010 establishes the HTTP server infrastructure pattern this sidecar must follow.

---

## Goal

Create a context pack sidecar service that accepts a POST request identifying an agent node (story_id, node_type, role), assembles a node-scoped context bundle from the KB, caches it in the existing `contextPacks` table, and returns a token-budget-enforced JSON response. An MCP tool wrapper makes the sidecar callable from within Claude Code agent sessions.

---

## Non-Goals

- Implementing the HTTP server infrastructure itself — follow WINT-2010's established pattern
- Populating the context cache with project context (WINT-2030), agent missions (WINT-2040), domain KB (WINT-2050), or library patterns (WINT-2060) — those are downstream stories
- Modifying existing `contextPacks` schema — reuse the existing `story` packType
- Authentication/authorization on the sidecar endpoint — deferred to later phase
- Modifying any protected schemas in `packages/backend/database-schema/`
- Modifying the `@repo/db` client API surface
- Frontend UI for context pack inspection
- Real-time streaming of context pack assembly

---

## Scope

### New Package
- `packages/backend/sidecars/context-pack/` — new sidecar package following WINT-2010 structure

### New Endpoints
- `POST /context-pack` — assemble and return node-scoped context pack

### Modified Packages
- `packages/backend/mcp-tools/` — add `context_pack_get` MCP tool
- `apps/api/knowledge-base/` — register `context_pack_get` in KB MCP server

### No DB Migrations Required
- Reuses existing `contextPacks` table with `packType: 'story'`

---

## Acceptance Criteria

- [ ] **AC-1**: POST /context-pack accepts a request body with at minimum: `story_id` (string), `node_type` (string identifying the agent phase, e.g., `pm_seed`, `dev_execute`), `role` (enum: pm|dev|qa|po), and returns a JSON response containing: `story_brief`, `kb_facts` (array), `kb_rules` (array), `kb_links` (array), `repo_snippets` (array)
- [ ] **AC-2**: All request/response types are defined as Zod schemas in `__types__/index.ts` — no TypeScript interfaces
- [ ] **AC-3**: Assembled context packs are cached in the existing `contextPacks` table using `packType: 'story'` and `packKey: '{story_id}:{node_type}:{role}'` — cache hit returns cached content without re-querying KB
- [ ] **AC-4**: Cache miss path queries the KB (hybrid search) using the story_id + node_type as the query, with a configurable result limit (default: 10 items per category)
- [ ] **AC-5**: Token budget enforcement: assembled context must not exceed a configurable `max_tokens` value (default: 2000 tokens); if assembly would exceed budget, lower-priority sections (repo_snippets first, then kb_links) are trimmed
- [ ] **AC-6**: An MCP tool `context_pack_get` is exported and registered in the knowledge-base MCP server, accepting the same parameters as the HTTP endpoint and returning the same response shape
- [ ] **AC-7**: Cache TTL for assembled context packs defaults to 1 hour (3600 seconds) — configurable per-request via optional `ttl` parameter
- [ ] **AC-8**: If KB query returns no results for a section, that section returns an empty array (not null/undefined) — response shape is always consistent
- [ ] **AC-9**: Integration tests run against real postgres-knowledgebase (port 5433) — no DB mocking (ADR-005)
- [ ] **AC-10**: Unit tests cover: Zod schema validation, token budget enforcement logic, cache key generation, and section trimming logic
- [ ] **AC-11**: If the cache write fails (DB error), the sidecar still returns the freshly-assembled context — cache write failure is a warning, not a blocking error
- [ ] **AC-12**: Response time for a cache hit is under 100ms; response time for a cache miss (KB query + cache write) is under 2000ms (measurable via integration test timing assertions)

---

## Reuse Plan

| Component | Location | How Used |
|-----------|----------|----------|
| `contextCacheGet` | `packages/backend/mcp-tools/src/context-cache/context-cache-get.ts` | Cache-first retrieval: check contextPacks before assembling |
| `contextCachePut` | `packages/backend/mcp-tools/src/context-cache/context-cache-put.ts` | Upsert assembled pack to contextPacks (ON CONFLICT DO UPDATE) |
| `kb_search` | `apps/api/knowledge-base/src/search/kb-search.ts` | Hybrid semantic+keyword search for kb_facts, kb_rules, kb_links queries |
| `storyGetStatus` | `packages/backend/mcp-tools/src/story-management/story-get-status.ts` | Retrieve story metadata for story_brief assembly |
| `session-query` | `packages/backend/mcp-tools/src/session-management/session-query.ts` | Retrieve session data for node-scoped context |
| `__types__` pattern | `packages/backend/mcp-tools/src/context-cache/__types__/index.ts` | Template for Zod schema organization |
| `@repo/db` | `packages/backend/db/` | Database client for contextPacks upsert |
| `@repo/logger` | `packages/core/logger/` | Required for all logging |
| `@repo/database-schema` | `packages/backend/database-schema/` | contextPacks table reference |
| `zod` | — | All type definitions, validation at entry point |

---

## Architecture Notes

### Cache-First Assembly Pattern

```
Request → Validate (Zod) → Generate packKey → contextCacheGet
  → Cache HIT: return cached content (<100ms)
  → Cache MISS: query KB → apply token budget → contextCachePut (non-blocking) → return assembled
```

### Cache Key Convention

`packKey = '{story_id}:{node_type}:{role}'`

Defined as a constant in `__types__/index.ts` to prevent drift across files.

### Token Budget Enforcement

Token estimation: `estimateTokens(text) = Math.ceil(text.length / 4)` — character count approximation.

Trimming priority (lowest to highest importance):
1. Trim `repo_snippets` items from end until within budget
2. Trim `kb_links` items from end until within budget
3. `kb_rules`, `kb_facts`, `story_brief` are never trimmed

`max_tokens` and `DEFAULT_TTL` are constants in `__types__/index.ts`.

### Concurrent Cache-Miss Safety

`contextCachePut` uses `ON CONFLICT DO UPDATE` — two simultaneous requests for the same packKey will not produce duplicate rows or errors. This is inherited from WINT-0100.

### Error Resilience

All functions follow the null-returning pattern from `contextCacheGet`: catch all errors, log with `@repo/logger`, return null or degrade gracefully. Cache write failure (AC-11) specifically must not surface as a 500 — log warning, return assembled content.

---

## Infrastructure Notes

- **New package**: `packages/backend/sidecars/context-pack/` must be added to `pnpm-workspace.yaml`
- **Package structure**: Follow WINT-2010's `packages/backend/sidecars/role-pack/` layout exactly
- **HTTP server**: Reuse WINT-2010's HTTP server initialization and startup pattern
- **KB dependencies**: `kb_search` requires `db` and `embeddingClient` injected at startup — mirror WINT-2010's initialization pattern
- **No DB migration**: Reuses existing `contextPacks` table

---

## HTTP Contract Plan

### POST /context-pack

**Request:**
```json
{
  "story_id": "WINT-2020",
  "node_type": "dev_execute",
  "role": "dev",
  "ttl": 3600
}
```

**Response (200):**
```json
{
  "story_brief": "string (may be empty string if no story data)",
  "kb_facts": ["..."],
  "kb_rules": ["..."],
  "kb_links": ["..."],
  "repo_snippets": ["..."]
}
```

**Error (400) — Zod validation failure:**
```json
{
  "error": "Validation failed",
  "details": [{ "path": ["role"], "message": "Invalid enum value. Expected 'pm' | 'dev' | 'qa' | 'po'" }]
}
```

---

## Test Plan

**Strategy**: unit + integration (no E2E — backend-only story, ADR-006 N/A)

**Integration test requirements**:
- Connect to real postgres-knowledgebase on port 5433 (ADR-005 — no DB mocking)
- Test fixtures: pre-inserted contextPacks row for cache-hit tests

**Key test scenarios**:
1. Cache hit path — returns cached content with timing < 100ms
2. Cache miss path — queries KB, writes to cache, returns result with timing < 2000ms
3. Token budget enforcement — repo_snippets trimmed first, then kb_links
4. Concurrent cache-miss race condition — ON CONFLICT DO UPDATE prevents duplicate rows
5. Cache write failure resilience — 200 returned even on DB write error
6. Empty arrays for sections with no KB results — never null
7. Zod validation rejection — 400 on invalid role, 400 on missing fields

**Fixture definitions**:
- `validContextPacksRow`: Non-expired DB row for cache-hit tests
- `mockKbSearchResult`: Mock KB result with known token counts for unit-level budget testing
- `oversizedContextPack`: Pack with estimated tokens > 2000 for trimming tests

---

## Reality Baseline

- **Baseline date**: 2026-02-13
- **Gaps**: Baseline predates Phase 1 UAT completions. Phase 2 sidecar infrastructure does not yet exist.
- **WINT-2010 status**: created (must reach UAT before WINT-2020 dev begins)
- **WINT-1120 status**: needs-code-review (WINT-2010's blocker)
- **contextPacks table**: Exists with `story` packType available
- **contextCacheGet/Put**: Implemented and available in `@repo/mcp-tools`
- **kb_search**: Available in `apps/api/knowledge-base/src/search/kb-search.ts`

---

## Canonical References

Files that demonstrate the patterns this story should follow:

| Pattern | File | Why |
|---------|------|-----|
| MCP tool with Zod validation + DB operation | `packages/backend/mcp-tools/src/context-cache/context-cache-get.ts` | Clean Zod validation, resilient error handling returning null on failure, @repo/db usage |
| Zod input/output schema definitions | `packages/backend/mcp-tools/src/context-cache/__types__/index.ts` | Zod-first types with enum validation, inferred types, no interfaces |
| Cache upsert pattern | `packages/backend/mcp-tools/src/context-cache/context-cache-put.ts` | ON CONFLICT DO UPDATE pattern for idempotent cache writes |
| Hybrid semantic search | `apps/api/knowledge-base/src/search/kb-search.ts` | Semantic+keyword RRF search with fallback mode; direct integration target |

---

## Subtasks

### ST-1: Create context-pack sidecar package scaffold

**Goal**: Initialize `packages/backend/sidecars/context-pack/` following WINT-2010's role-pack structure. Create package.json, tsconfig.json, src/ directory.

**ACs covered**: AC-1

**Files to read**:
- `packages/backend/sidecars/role-pack/package.json`
- `packages/backend/sidecars/role-pack/tsconfig.json`

**Files to create/modify**:
- `packages/backend/sidecars/context-pack/package.json` (new)
- `packages/backend/sidecars/context-pack/tsconfig.json` (new)
- `pnpm-workspace.yaml` (add new package)

**Depends on**: none

**Verification**: `pnpm install && pnpm check-types --filter @repo/context-pack-sidecar`

**Estimated tokens**: 6,000

---

### ST-2: Define Zod schemas in __types__/index.ts

**Goal**: Create all request/response Zod schemas: `ContextPackRequestSchema` (story_id, node_type, role enum pm|dev|qa|po, optional ttl), `ContextPackResponseSchema` (story_brief, kb_facts[], kb_rules[], kb_links[], repo_snippets[]), `CACHE_KEY_FORMAT` constant, `DEFAULT_TTL` (3600) and `DEFAULT_MAX_TOKENS` (2000) constants, `estimateTokens(text: string): number` utility.

**ACs covered**: AC-2, AC-5, AC-7

**Files to read**:
- `packages/backend/mcp-tools/src/context-cache/__types__/index.ts`

**Files to create**:
- `packages/backend/sidecars/context-pack/src/__types__/index.ts` (new)

**Depends on**: ST-1

**Verification**: `pnpm check-types --filter @repo/context-pack-sidecar`

**Estimated tokens**: 8,000

---

### ST-3: Implement cache-key generation and cache-first assembly logic

**Goal**: Implement `assembleContextPack(input)`: generate packKey as `{story_id}:{node_type}:{role}`, call `contextCacheGet`, on hit return cached content, on miss call KB search (`kb_search`), apply token budget trimming (repo_snippets first, then kb_links), call `contextCachePut` (non-blocking on failure — log warning, continue), return assembled pack with empty arrays for missing sections.

**ACs covered**: AC-3, AC-4, AC-5, AC-7, AC-8, AC-11

**Files to read**:
- `packages/backend/mcp-tools/src/context-cache/context-cache-get.ts`
- `packages/backend/mcp-tools/src/context-cache/context-cache-put.ts`
- `apps/api/knowledge-base/src/search/kb-search.ts`

**Files to create**:
- `packages/backend/sidecars/context-pack/src/assemble-context-pack.ts` (new)

**Depends on**: ST-2

**Verification**: `pnpm test --filter @repo/context-pack-sidecar` (unit tests)

**Estimated tokens**: 20,000

---

### ST-4: Implement HTTP server route handler

**Goal**: Add POST /context-pack route handler following WINT-2010's HTTP server pattern. Validate request with `ContextPackRequestSchema` (fail fast with 400 on validation failure), call `assembleContextPack`, return JSON response.

**ACs covered**: AC-1, AC-2

**Files to read**:
- `packages/backend/sidecars/role-pack/src/server.ts`
- `packages/backend/sidecars/role-pack/src/index.ts`

**Files to create**:
- `packages/backend/sidecars/context-pack/src/routes/context-pack.ts` (new)
- `packages/backend/sidecars/context-pack/src/server.ts` (new)

**Depends on**: ST-3

**Verification**: `curl -s -X POST localhost:{port}/context-pack -d '{"story_id":"test","node_type":"dev_execute","role":"dev"}' | jq .`

**Estimated tokens**: 10,000

---

### ST-5: Implement context_pack_get MCP tool wrapper

**Goal**: Create `context_pack_get` MCP tool in `packages/backend/mcp-tools/src/context-pack/` that accepts `story_id`, `node_type`, `role`, optional `ttl` and calls `assembleContextPack` directly (imported from the sidecar package). Register in the knowledge-base MCP server.

**Architecture Decision (resolved during elaboration)**: The MCP tool imports `assembleContextPack` directly from `packages/backend/sidecars/context-pack/`. This is a uni-directional dependency (mcp-tools → sidecars/context-pack) which is acceptable — the sidecar package is a library with an HTTP entry point, not the other way around. The alternative (HTTP call) was rejected to avoid requiring the sidecar to be running during MCP tool invocation.

**ACs covered**: AC-6

**Files to read**:
- `packages/backend/mcp-tools/src/context-cache/context-cache-get.ts`
- `apps/api/knowledge-base/src/` (MCP server registration pattern)

**Files to create/modify**:
- `packages/backend/mcp-tools/src/context-pack/context-pack-get.ts` (new)
- `apps/api/knowledge-base/src/` (register tool)

**Depends on**: ST-3

**Verification**: `pnpm check-types --filter @repo/mcp-tools && grep -r context_pack_get apps/api/knowledge-base/src/`

**Estimated tokens**: 10,000

---

### ST-6: Write unit tests

**Goal**: Unit tests covering: Zod schema validation (valid/invalid inputs for all fields), token budget enforcement (trim repo_snippets first, then kb_links, using `mockKbSearchResult` fixture), cache key generation (`'{story_id}:{node_type}:{role}'` format verified), section trimming with `oversizedContextPack` fixture, empty array (not null) returned when sections absent.

**ACs covered**: AC-2, AC-5, AC-8, AC-10

**Files to read**:
- `packages/backend/mcp-tools/src/context-cache/__tests__/` (test pattern reference)

**Files to create**:
- `packages/backend/sidecars/context-pack/src/__tests__/context-pack.unit.test.ts` (new)

**Depends on**: ST-3

**Verification**: `pnpm test --filter @repo/context-pack-sidecar` (unit suite only)

**Estimated tokens**: 15,000

---

### ST-7: Write integration tests against real postgres-knowledgebase

**Goal**: Integration tests (port 5433, no mocking per ADR-005): (1) cache hit path with timing assertion < 100ms, (2) cache miss path with timing assertion < 2000ms, (3) concurrent cache-miss race condition — assert single contextPacks row after two simultaneous requests, (4) cache write failure resilience — assert 200 response even when DB write errors, (5) expired cache entry triggers fresh assembly, (6) MCP tool `context_pack_get` returns ContextPackResponseSchema-valid response (HP-3 coverage).

**ACs covered**: AC-3, AC-6, AC-9, AC-11, AC-12

**Files to read**:
- `packages/backend/mcp-tools/src/context-cache/__tests__/` (integration test patterns)

**Files to create**:
- `packages/backend/sidecars/context-pack/src/__tests__/context-pack.integration.test.ts` (new)

**Depends on**: ST-4

**Verification**: `pnpm test --filter @repo/context-pack-sidecar` (integration suite, requires postgres-knowledgebase on port 5433)

**Estimated tokens**: 18,000

---

## Risk Predictions

```yaml
split_risk: medium
split_risk_rationale: >
  12 ACs with 7 subtasks. High-risk category due to new sidecar package + HTTP endpoint +
  MCP tool + KB integration + token budget logic + test suite. History on similar code
  stories (WKFL-004/007/010) shows 4-8x token overrun. Story is well-scoped but complex.
  Split risk is medium — story could be split at ST-5 (MCP tool) if token budget runs out.
review_cycles_predicted: 2
token_estimate_floor: 200000
token_multiplier: "4-6x"
risk_flags:
  - "Dependency gate: WINT-2010 must reach UAT first"
  - "kb_search initialization pattern unknown until WINT-2010 is done"
  - "Token estimation via char-count/4 may need calibration"
  - "Integration tests require live postgres-knowledgebase on port 5433"
```
