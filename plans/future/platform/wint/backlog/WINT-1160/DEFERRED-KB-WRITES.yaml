---
generated: "2026-02-18"
reason: "postgres-knowledgebase MCP tool not available in pm-story-generation session"
retry_after: "next session with MCP tools available"
---

worktree_registration:
  branch: story/WINT-1160
  path: tree/story/WINT-1160
  pr_number: 364
  pr_url: https://github.com/michael-menard/monorepo/pull/364
  note: "worktree_register MCP call deferred — MCP not available in this session. Worktree exists on disk and remote. Register when MCP available."

deferred_writes:
  - type: stories_upsert
    table: stories
    operation: INSERT ON CONFLICT (story_id) DO UPDATE SET
    payload:
      story_id: WINT-1160
      feature: wint
      title: "Add Parallel Work Conflict Prevention"
      story_dir: "plans/future/platform/wint/backlog/WINT-1160"
      story_file: "plans/future/platform/wint/backlog/WINT-1160/WINT-1160.md"
      story_type: feature
      points: 3
      priority: medium
      state: backlog
      touches_backend: false
      touches_frontend: false
      touches_database: false
      touches_infra: false
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'WINT-1160', 'wint', 'Add Parallel Work Conflict Prevention',
        'plans/future/platform/wint/backlog/WINT-1160',
        'plans/future/platform/wint/backlog/WINT-1160/WINT-1160.md',
        'feature', 3, 'medium', 'backlog',
        false, false, false, false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;
