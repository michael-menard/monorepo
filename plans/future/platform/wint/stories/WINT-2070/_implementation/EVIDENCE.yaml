schema: 1
story_id: WINT-2070
version: 1
timestamp: '2026-03-04T04:00:00Z'

acceptance_criteria:
  - ac_id: AC-1
    ac_text: Valid skill file with frontmatter at .claude/skills/cache-warm/SKILL.md
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          File created with valid YAML frontmatter block containing name, description fields.
          242 lines total.

  - ac_id: AC-2
    ac_text: Document DATABASE_URL (5432) and KB_DATABASE_URL (5433)
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 1 documents DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lego_dev
          and KB_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/lego_kb.
          Environment Reference table lists both vars with ports, databases, and required-by info.

  - ac_id: AC-3
    ac_text: Invoke populate-project-context.ts via pnpm tsx
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 2 documents exact command:
          pnpm tsx packages/backend/mcp-tools/src/scripts/populate-project-context.ts

  - ac_id: AC-4
    ac_text: Invoke populate-domain-kb.ts via pnpm tsx
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 3 documents exact command:
          pnpm tsx packages/backend/mcp-tools/src/scripts/populate-domain-kb.ts

  - ac_id: AC-5
    ac_text: Invoke populate-library-cache.ts via pnpm tsx
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 4 documents exact command:
          pnpm tsx packages/backend/mcp-tools/src/scripts/populate-library-cache.ts

  - ac_id: AC-6
    ac_text: Invoke agent-mission-cache-populator.ts (non-standard path at database-schema/src/seed/)
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 5 explicitly calls out the non-standard path with a NOTE block:
          pnpm tsx packages/backend/database-schema/src/seed/agent-mission-cache-populator.ts
          Script reference table also highlights this distinction.

  - ac_id: AC-7
    ac_text: Individual failures are non-fatal
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Each phase (2-5) specifies "On failure: Log the error. Mark as FAIL. Continue to Phase N+1."
          Failure Handling section states: "Individual script failures are non-fatal."

  - ac_id: AC-8
    ac_text: Final summary block N/4 with pass/fail/skip
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 6 defines a structured summary block with "Result: N/4 scripts succeeded"
          and per-script status lines with PASS | FAIL: reason | SKIP: reason values.

  - ac_id: AC-9
    ac_text: --skip={script} option documented
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Parameters section documents --skip={script} with four valid values:
          project-context, domain-kb, library-cache, agent-missions.
          Phases 3 and 5 also explicitly check for the skip flag before running.

  - ac_id: AC-10
    ac_text: LLM-executable instructions (not just shell commands)
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Each phase includes natural-language instructions explaining what the script does,
          what success/failure signals look like, and what to do on each outcome.
          The file follows the lint-fix SKILL.md pattern with prose-driven execution phases.

  - ac_id: AC-11
    ac_text: Precondition check for DATABASE_URL; warn if KB_DATABASE_URL absent
    status: PASS
    evidence_items:
      - type: file
        path: .claude/skills/cache-warm/SKILL.md
        description: >
          Phase 1 checks DATABASE_URL first — emits CACHE-WARM BLOCKED if missing.
          KB_DATABASE_URL check follows with explicit warning text and SKIP behavior
          for populate-domain-kb.ts. Neither check is conflated.

touched_files:
  - path: .claude/skills/cache-warm/SKILL.md
    action: created
    lines: 242
    description: Skill definition file orchestrating 4 cache populate scripts

commands_run: []

endpoints_exercised: []

notable_decisions:
  - >
    Followed lint-fix SKILL.md format with numbered phases and structured summary block.
  - >
    KB_DATABASE_URL absence is a soft skip (not a hard block) because domain-kb script
    would fail anyway without a KB connection — auto-skipping is safer than letting it fail.
  - >
    The agent-mission-cache-populator.ts non-standard path is called out with a NOTE block
    in Phase 5 and a distinct column in the Script Reference table to prevent agents from
    guessing the wrong path.

known_deviations: []

test_summary:
  unit: { pass: 0, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: markdown-only deliverable — manual test strategy per story
  tests_written: false

coverage: {}

token_summary: {}
