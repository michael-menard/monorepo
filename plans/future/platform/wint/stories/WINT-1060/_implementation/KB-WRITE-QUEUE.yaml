# KB Write Queue for WINT-1060
# Generated by elab-autonomous-decider
# To be processed by kb-writer agent or orchestration system

story_id: WINT-1060
generated_at: "2026-02-17T00:00:00Z"

# 10 non-blocking findings queued for KB writes
# (Gap 2 from FUTURE-OPPORTUNITIES.md was promoted to AC-10 — not queued here)
kb_write_requests:
  # Non-blocking gaps (3 items: gaps 1, 3, 4 — gap 2 became AC-10)
  - request_id: "wint-1060-gap-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] done Stage Ambiguity in story-move Valid Stages Table**
        - **Finding**: `done` is present in SWIM_LANE_TO_STATE but absent from the story-move command's Valid Stages table, creating a latent documentation gap
        - **Impact**: Low - no behavioral gap exists; `done` is correctly excluded as a TO_STAGE target
        - **Effort**: Low - one sentence note in story-move.md Valid Stages table or Step 2.5 inline mapping table
        - **Recommendation**: When updating story-move.md, add an explicit note that `done` is in SWIM_LANE_TO_STATE but intentionally excluded as a TO_STAGE argument for /story-move to prevent future implementers from treating it as a valid target
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "documentation-clarity"
        - "story-move"
        - "swim-lane-to-state"

  - request_id: "wint-1060-gap-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] MOVE SKIPPED Path Leaves DB Out of Sync**
        - **Finding**: EDGE-4 (MOVE SKIPPED: Already in {TO_STAGE}) does not attempt a DB reconciliation write — if the story is already in the target directory but the DB record shows a different state, MOVE SKIPPED silently leaves the DB out of sync
        - **Impact**: Low - shim's directory fallback compensates in Phase 1
        - **Effort**: Medium - even on SKIPPED, attempt shimUpdateStoryStatus to reconcile DB with filesystem position
        - **Recommendation**: Defer to Phase 7 (WINT-7030) when directory move is removed; at that point a DB reconciliation write on SKIPPED becomes the primary resolution path
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "db-sync"
        - "story-move"
        - "wint-7030"

  - request_id: "wint-1060-gap-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] Skipped-Stage Warning Log Missing Stage Name**
        - **Finding**: When db_updated: skipped due to unmapped TO_STAGE (AC-5), the warning log does not include the name of the stage that was skipped, reducing diagnostic value
        - **Impact**: Low - stage can be inferred from context but is not explicit in log output
        - **Effort**: Low - include stage name in the skipped warning log message
        - **Recommendation**: Update Step 2.5 warning to log: "DB write skipped: TO_STAGE '{stageName}' is not in SWIM_LANE_TO_STATE" for improved operator diagnostics
      additional_tags:
        - "non-blocking"
        - "observability"
        - "logging"
        - "story-move"

  # Enhancement opportunities (6 items)
  - request_id: "wint-1060-enh-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] ShimDiagnostics Source Field in story-move Return YAML**
        - **Finding**: WINT-1012 adds an opt-in ShimDiagnostics field (source: db | directory | not_found) to all four shim functions; story-move return YAML could expose db_source from the locate step to give operators visibility into whether the story was found via DB or directory fallback
        - **Impact**: Low - operators can infer locate source from context
        - **Effort**: Low - add db_source field to return YAML alongside db_updated
        - **Recommendation**: Defer to Phase 7 cleanup (WINT-7030); pre-wire as a comment in the return YAML block so it has a clear insertion point when WINT-1012 ShimDiagnostics is activated
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "observability"
        - "story-move"
        - "wint-1012"
        - "shim-diagnostics"

  - request_id: "wint-1060-enh-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] --dry-run Flag for story-move Pre-Flight Verification**
        - **Finding**: A --dry-run flag that echoes what DB state would be written and what directory would be moved, without executing either, would enable debugging and pre-flight verification without side effects
        - **Impact**: Low - useful for debugging but not required for correctness
        - **Effort**: Medium - requires flag parsing, no-op execution path, dry-run output format
        - **Recommendation**: Defer to WINT-7030 (Phase 7 cleanup) when the command undergoes a major revision; --dry-run is most valuable after directory move is removed and DB becomes the sole state store
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "developer-experience"
        - "story-move"
        - "wint-7030"

  - request_id: "wint-1060-enh-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] Bulk Move Operation (/story-move-batch)**
        - **Finding**: /story-move operates on one story per invocation; a batch variant accepting a list of story IDs and a target stage would reduce agent invocations for common operations like "move all stories in backlog to ready-to-work"
        - **Impact**: Medium - reduces agent turn count for bulk operations
        - **Effort**: High - requires new command file, loop execution, partial-success handling, and per-story db_updated reporting
        - **Recommendation**: Defer to Phase 6 batch mode; design as a new /story-move-batch command rather than a flag on /story-move to preserve the existing command contract
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "batch-operations"
        - "story-move"
        - "phase-6"

  - request_id: "wint-1060-enh-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] Runtime DB Enum Validation Guard for newState**
        - **Finding**: /story-move currently trusts SWIM_LANE_TO_STATE as authoritative; a future enhancement could validate that the resolved newState is a member of the live storyStateEnum before writing, adding a runtime guard against schema drift
        - **Impact**: Low - SWIM_LANE_TO_STATE is maintained in sync with the schema; drift is unlikely in Phase 1
        - **Effort**: Low - add a guard check against the DB enum values list before calling shimUpdateStoryStatus
        - **Recommendation**: Defer to Phase 7 (WINT-7030); this guard becomes more valuable when the command is decoupled from the shim and operates closer to the raw DB layer
      additional_tags:
        - "non-blocking"
        - "future-proofing"
        - "db-validation"
        - "story-move"
        - "wint-7030"

  - request_id: "wint-1060-enh-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] stories.index.md Progress Summary Sync from story-move**
        - **Finding**: After a successful DB write, stories.index.md Progress Summary is updated by /story-update (when --update-status is used) but not by /story-move alone; a future story could update the index as part of the move command to keep markdown index in sync without requiring a separate --update-status invocation
        - **Impact**: Low - WINT-1070 (Deprecate stories.index.md as Source of Truth) already partially defers this concern
        - **Effort**: Low - add optional index update step after successful DB write
        - **Recommendation**: Defer to WINT-1070 resolution; if stories.index.md is deprecated as source of truth, this enhancement becomes unnecessary
      additional_tags:
        - "non-blocking"
        - "future-proofing"
        - "stories-index"
        - "story-move"
        - "wint-1070"

  - request_id: "wint-1060-enh-6"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-1060"
      category: "future-opportunities"
      content: |
        **[WINT-1060] Telemetry Hook Pre-Wire for WINT-3070**
        - **Finding**: WINT-3070 plans to add telemetry-log calls to story-move; the db_updated field introduced by WINT-1060 provides a natural telemetry signal (DB-write success/failure/skipped) that should be logged as a state transition event per WINT-3100's telemetry.state_transitions table schema (from_state, to_state, reason, actor)
        - **Impact**: Medium - pre-wiring a comment insertion point now reduces WINT-3070 implementation effort
        - **Effort**: Low - add a comment in Step 2.5 after the DB write call: "# TELEMETRY HOOK: log state_transition event here (WINT-3070)"
        - **Recommendation**: During ST-2 implementation, add the comment hook so WINT-3070 has a clear, documented insertion point; zero behavioral impact in Phase 1
      additional_tags:
        - "non-blocking"
        - "observability"
        - "telemetry"
        - "story-move"
        - "wint-3070"
        - "wint-3100"

summary:
  total_requests: 10
  gaps: 3
  enhancements: 6
  note: "Gap 2 (locate-then-write independence) was promoted to AC-10 in the story file — not queued here"

notes: |
  All KB write requests are for genuinely non-blocking findings from elaboration phase.
  These represent future improvement opportunities that do not impact MVP delivery of WINT-1060.

  MVP-critical gap (EC-2 path independence) was resolved by adding AC-10 to WINT-1060.md.

  Categories:
  - Edge cases: 3 (done stage ambiguity, MOVE SKIPPED DB sync, skipped-stage log)
  - UX polish / developer experience: 3 (ShimDiagnostics in return YAML, --dry-run flag, bulk move)
  - Future-proofing: 2 (DB enum validation, stories.index.md sync deferral to WINT-1070)
  - Observability / telemetry: 2 (skipped-stage log already in edge-case above; telemetry hook pre-wire)
