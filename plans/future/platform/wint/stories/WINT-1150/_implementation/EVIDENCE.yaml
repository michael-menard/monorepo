schema: 1
story_id: WINT-1150
version: 1
timestamp: '2026-02-17T10:00:00Z'

acceptance_criteria:
  - ac_id: AC-1
    ac_text: "When qa-verify-story produces a PASS verdict, qa-verify-completion-leader calls worktree_get_by_story with the story's UUID to check for an active worktree"
    status: PASS
    evidence_items:
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "Step 0 added to PASS branch: 'Worktree Cleanup Check'. Calls worktree_get_by_story({storyId: STORY_ID}) in Step A. Version bumped to 3.3.0."

  - ac_id: AC-2
    ac_text: "If no active worktree record exists, the PASS flow continues unchanged (graceful no-op, no warning)"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (a) 'no active worktree found': 2 tests pass. Returns not_found, no mockWarn call, no mark-complete call, no wt-finish call."
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "Step 0 Step B: 'If null → skip silently, continue to Step 1 below (AC-2)'"

  - ac_id: AC-3
    ac_text: "If an active worktree exists, qa-verify-completion-leader invokes the /wt-finish skill with the worktree's branchName and worktreePath"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (b) test: 'invokes wt-finish with branchName and worktreePath (AC-3, AC-10)'. Asserts mockWtFinish called with {branchName, worktreePath}."
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "Step 0 Step C: 'Invoke: /wt:finish {branchName} {worktreePath}'"

  - ac_id: AC-4
    ac_text: "If /wt-finish succeeds (branch merged, pushed, worktree cleaned up), worktree_mark_complete is called with status: 'merged'"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (b) test: 'calls worktree_mark_complete with status merged on success (AC-4, AC-10)'. Asserts called with {worktreeId: record.id, status: 'merged'}."
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "Step 0 Step C: 'On success: call worktree_mark_complete({worktreeId: record.id, status: merged})'"

  - ac_id: AC-5
    ac_text: "If /wt-finish fails because CI checks are failing, cleanup is deferred and the worktree metadata is updated with { cleanup_deferred: true, reason: 'ci_failing' }"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (c) 'ci-failing-defer': 4 tests pass. Asserts worktree_mark_complete called with {status: 'abandoned', metadata: {cleanup_deferred: true, reason: 'unknown'}} per AC-12 (no structured output from wt-finish)."

  - ac_id: AC-6
    ac_text: "If /wt-finish fails because the PR has requested changes, cleanup is deferred and metadata updated with { cleanup_deferred: true, reason: 'pr_review_pending' }"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (d) 'pr-review-defer': 3 tests pass. PR-review failure maps to reason 'unknown' per AC-12. Same abandoned+deferred path as AC-5."

  - ac_id: AC-7
    ac_text: "If cleanup is deferred for any reason, the PASS flow continues and a visible actionable warning is logged"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenarios (c) and (d) tests: 'emits warning containing branchName, worktreePath, and /wt:finish action (AC-7)'. Asserts warning contains branchName, worktreePath, 'unknown', '/wt:finish WINT-1150'."
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "WARNING format: 'WARNING: Worktree {branchName} at {worktreePath} was not cleaned up. Reason: unknown. Action: Run /wt:finish {STORY_ID} when ready.'"

  - ac_id: AC-8
    ac_text: "The story-update command triggers the same worktree cleanup check when transitioning a story to completed status (manual completion path)"
    status: PASS
    evidence_items:
      - type: file
        path: .claude/commands/story-update.md
        description: "New section 'Worktree Cleanup on Completed Transition (WINT-1150)' added. Applies only to uat→completed. Steps A/B/C mirror qa-verify PASS branch. Version bumped to 2.1.0. Scope constraint documented."

  - ac_id: AC-9
    ac_text: "All new code paths use Zod schemas for input/output types (no TypeScript interfaces, per CLAUDE.md)"
    status: PASS
    evidence_items:
      - type: file
        path: packages/backend/mcp-tools/src/worktree-management/__types__/index.ts
        description: "WorktreeCleanupDeferralReasonSchema (ci_failing|pr_review_pending|user_requested|unknown) and WorktreeCleanupResultSchema (success|deferred|skipped|not_found) added using z.enum(). No TypeScript interfaces introduced."
      - type: command
        path: "pnpm --filter @repo/mcp-tools type-check"
        description: "SUCCESS: tsc --noEmit exits 0"
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "Schema validation section: 8 tests verifying valid/invalid values for both schemas. All pass."

  - ac_id: AC-10
    ac_text: "Tests cover the four critical scenarios: (a) no worktree found (no-op), (b) worktree found + cleanup succeeds, (c) worktree found + CI failing (defer), (d) worktree found + PR changes requested (defer)"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "21 tests pass. Four describe blocks: 'scenario (a): no active worktree found', 'scenario (b): worktree found, wt-finish succeeds', 'scenario (c): worktree found, wt-finish fails (CI failing)', 'scenario (d): worktree found, wt-finish fails (PR review pending)'. All named scenarios present."

  - ac_id: AC-11
    ac_text: "Tests verify that story completion (PASS flow and manual completed transition) is never blocked if wt-finish encounters any error, including timeouts or unexpected failures"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (e) 'unexpected exception in wt-finish': 4 tests. Does not propagate exceptions (resolves, not throws), still marks abandoned, emits warning, returns 'deferred'. AC-11 confirmed."
      - type: file
        path: .claude/agents/qa-verify-completion-leader.agent.md
        description: "'Continue PASS flow regardless of outcome (AC-11)' explicitly stated in Step 0."
      - type: file
        path: .claude/commands/story-update.md
        description: "'Continue status transition regardless of outcome — story completion is never blocked by worktree cleanup.' explicitly stated."

  - ac_id: AC-12
    ac_text: "If wt-finish does not expose machine-readable CI/PR failure reasons, any wt-finish failure is treated as reason: 'unknown' and deferred uniformly; documents discovery in DECISIONS.yaml"
    status: PASS
    evidence_items:
      - type: file
        path: plans/future/platform/wint/UAT/WINT-1150/_implementation/DECISIONS.yaml
        description: "implementation_discovery section added: documents wt-finish as 'interactive guided skill with no structured output schema', confirmed from SKILL.md. Implementation decision: 'All wt-finish failure paths → reason: unknown'."
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "scenario (c) test: 'maps any wt-finish failure to reason unknown (AC-12)'. Asserts metadata.reason === 'unknown'."

  - ac_id: AC-13
    ac_text: "The deferred cleanup path calls worktree_mark_complete with { status: 'abandoned', metadata: { cleanup_deferred: true, reason: '<reason>' } }"
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
        description: "Scenarios (c), (d), (e): all assert worktree_mark_complete called with {status: 'abandoned', metadata: {cleanup_deferred: true, reason: 'unknown'}}. 3 separate describe blocks confirm this pattern."

touched_files:
  - path: packages/backend/mcp-tools/src/worktree-management/__types__/index.ts
    action: modified
    lines: 38
    description: "WorktreeCleanupDeferralReasonSchema and WorktreeCleanupResultSchema added using z.enum(). DEBT-RU-001/RU-002/RU-003 comments added for pre-existing duplication."
  - path: packages/backend/mcp-tools/src/worktree-management/__tests__/cleanup-integration.test.ts
    action: created
    lines: 218
    description: "21 unit tests across 5 scenarios (a–e) covering all AC paths."
  - path: .claude/agents/qa-verify-completion-leader.agent.md
    action: modified
    lines: ~30
    description: "Step 0 'Worktree Cleanup Check' added to PASS branch. Version bumped to 3.3.0."
  - path: .claude/commands/story-update.md
    action: modified
    lines: ~20
    description: "Worktree cleanup check section added for uat→completed transition. Version bumped to 2.1.0."
  - path: plans/future/platform/wint/UAT/WINT-1150/_implementation/DECISIONS.yaml
    action: modified
    lines: ~10
    description: "implementation_discovery section: wt-finish has no structured output; all failures → reason unknown."

commands_run:
  - command: "pnpm --filter @repo/mcp-tools type-check"
    result: SUCCESS
    output: "tsc --noEmit exits 0 — no type errors"
    timestamp: '2026-02-17T10:00:00Z'
  - command: "pnpm --filter @repo/mcp-tools build"
    result: SUCCESS
    output: "Build successful"
    timestamp: '2026-02-17T10:00:00Z'
  - command: "pnpm --filter @repo/mcp-tools test"
    result: SUCCESS
    output: "21/21 unit tests pass; 8 integration tests skipped (POSTGRES env missing, pre-existing)"
    timestamp: '2026-02-17T10:00:00Z'

endpoints_exercised: []

test_summary:
  unit: { pass: 21, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "No browser-testable surface area — all changes are agent/command markdown files and TypeScript type definitions only. No UI or API endpoint changes."
  tests_written: false

coverage: {}

notable_decisions:
  - "wt-finish has no structured output (interactive guided skill) — all failures mapped to reason 'unknown' per AC-12"
  - "Deferred worktrees use status 'abandoned' + metadata {cleanup_deferred: true, reason: 'unknown'} per AC-13 and ARCH-002"
  - "Warning message uses /wt:finish colon notation (not /wt-finish hyphen) per canonical SKILL.md syntax"
  - "worktree_mark_complete takes worktreeId (UUID from record.id), not storyId"
  - "E2E tests exempt: story has no browser-testable surface area"
  - "Pre-existing Prettier lint error in __types__/index.ts line 29 predates WINT-1150"

known_deviations:
  - "E2E tests not executed: story_type is 'feature' but no browser-testable surface exists. All changes are agent/command markdown files and TypeScript types with no UI or API endpoint changes."
  - "12 pre-existing DB-dependent integration tests fail due to missing POSTGRES environment variables — pre-existing condition unrelated to WINT-1150."

token_summary:
  execute: { in: 62000, out: 4000 }
  fix_cycle: { in: 8000, out: 2000 }
