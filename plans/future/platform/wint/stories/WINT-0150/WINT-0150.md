---
id: WINT-0150
title: "Create doc-sync Skill for Database-Driven Documentation Sync"
status: uat
priority: P2
phase: 0
epic: wint
created_at: "2026-02-16"
elaboration_started_at: "2026-02-16"
points: 5
experiment_variant: control
touches_backend: false
touches_frontend: false
touches_database: true
touches_infra: false
story_type: feature
depends_on: []
blocks: ["WINT-0160"]
---

# WINT-0150: Create doc-sync Skill for Database-Driven Documentation Sync

## Context

The existing doc-sync skill (`.claude/skills/doc-sync/skill.md`) successfully synchronizes workflow documentation by reading `.agent.md` and `.command.md` files and updating `docs/workflow/` and top-level docs (AGENTS.md, COMMANDS.md, SKILLS.md). However, the WINT epic introduces database-driven workflow tracking, storing agent metadata, phase status, and component state in `postgres-knowledgebase` tables (workflow.phases, workflow.components, etc.).

**Problem:**

During the WINT migration (Phases 1-9), the workflow will operate in a **hybrid mode**: some metadata will exist in files, some in the database, and eventually all in the database. The current doc-sync skill cannot query the database, so it cannot maintain documentation accuracy during and after the migration.

**Grounded in Reality Baseline (2026-02-13):**

- Existing doc-sync skill with proven 7-phase workflow pattern
- 61 active agents across 4 tiers (orchestrators, leaders, workers, reference)
- Established documentation structure: AGENTS.md, COMMANDS.md, SKILLS.md, docs/workflow/
- WINT Phase 0 bootstrap in progress (database schemas, MCP tools)
- No conflicts with other active stories
- Phase 0 work is untracked, prerequisite for all subsequent phases

## Goal

Extend the doc-sync skill to support **dual-source synchronization**:

1. **Continue reading from files** - `.agent.md`, `.command.md`, `.skill.md` frontmatter
2. **Add database queries** - Query `workflow.phases`, `workflow.components`, and other WINT tables via postgres-knowledgebase MCP tools
3. **Merge sources** - Combine file-based and database metadata to build a complete view
4. **Update all targets** - AGENTS.md, COMMANDS.md, SKILLS.md, and docs/workflow/ files

The skill will use the existing 7-phase workflow pattern but enhance Phase 2 (Frontmatter Parsing) to include database queries and Phase 3 (Section Mapping) to handle database-sourced metadata.

## Non-Goals

**Explicitly Out of Scope:**

1. **Automatic database writes** - Skill is read-only for database; updates to database are handled by other WINT stories (e.g., WINT-0090 story management MCP tools)
2. **Real-time sync** - No watch mode or continuous sync; remains on-demand or pre-commit hook triggered
3. **Migration logic** - Does not migrate data from files to database; migration is handled by WINT-1030, WINT-1080, etc.
4. **Schema validation** - Does not validate database schema correctness; assumes schemas are already applied
5. **MCP tool creation** - Does not create new MCP tools; uses existing postgres-knowledgebase MCP tools created by WINT stories

**Protected Features Not to Touch:**

- Existing doc-sync.agent.md implementation (extend, don't replace)
- Pre-commit hook integration pattern
- SYNC-REPORT.md format (extend, don't break)
- Existing 7-phase workflow structure (enhance Phase 2/3, keep rest)

**Deferred Work:**

- **Phase 2+ enhancements** - Context cache queries (WINT-2030-2060), telemetry queries (WINT-3020-3050), ML pipeline queries (WINT-5040-5100) are Phase 2+ features
- **Watch mode** - Future enhancement documented in existing doc-sync agent
- **Mermaid-cli integration** - Future enhancement already documented

## Scope

### Files Modified

- `.claude/skills/doc-sync/skill.md` - Add database query documentation to Phase 2
- `.claude/agents/doc-sync.agent.md` - Add postgres-knowledgebase MCP tools to frontmatter

### Files Created

None - extends existing skill and agent files

### Packages Affected

- **postgres-knowledgebase MCP server** (read-only queries) - Dependency, not modified by this story
- **@repo/logger** - For logging database query attempts and failures

### Integration Points

- postgres-knowledgebase MCP tools (when available) for workflow.phases and workflow.components queries
- Existing Bash/Read/Grep/Glob/Edit tools for file operations
- Pre-commit hook integration (unchanged)

## Acceptance Criteria

- [ ] **AC-1: Database Query Integration**
  - Skill queries `workflow.phases` table for phase status and completion timestamps
  - Skill queries `workflow.components` table for agent/command/skill metadata
  - Queries are optional - skill degrades gracefully if database is unavailable
  - Database metadata merges with file frontmatter (database overrides if present)

- [ ] **AC-2: Extended Section Mapping**
  - Phase 3 mapping logic handles both file-sourced and database-sourced agents
  - Section mapping respects new WINT phase structure (Phase 0-9)
  - Unknown patterns flagged in manual_review_needed section of SYNC-REPORT.md

- [ ] **AC-3: AGENTS.md Updates**
  - Agent counts reflect current state (file + database agents)
  - Agent hierarchy table updated with WINT workflow agents
  - Model assignments reflect database overrides if present

- [ ] **AC-4: COMMANDS.md Updates**
  - Command reference includes new WINT workflow commands
  - Workflow diagram updated if new command relationships detected
  - Command-to-agent mappings reflect database state

- [ ] **AC-5: SKILLS.md Updates**
  - Skill registry includes doc-sync skill itself (self-documenting)
  - Skill descriptions reflect database-aware capabilities
  - WINT-related skills added to appropriate category

- [ ] **AC-6: docs/workflow/ Updates**
  - `phases.md` updated with WINT Phase 0-9 structure
  - `agent-system.md` updated with WINT agents if database-driven
  - Mermaid diagrams regenerated from spawns field OR database relationships

- [ ] **AC-7: Backward Compatibility**
  - Skill runs successfully with database unavailable (file-only mode)
  - Existing --check-only and --force flags work unchanged
  - SYNC-REPORT.md format unchanged, adds database query section if used

- [ ] **AC-8: Documentation**
  - Skill documentation (`.claude/skills/doc-sync/skill.md`) updated with database query examples
  - Phase 2 (Frontmatter Parsing) section documents database queries
  - New examples show database + file hybrid sync scenarios

- [ ] **AC-9: Database Query Timeout Handling**
  - MCP tool timeouts log timeout error and fall back to file-only mode
  - SYNC-REPORT.md distinguishes timeout vs connection failure in database query status
  - Timeout threshold configurable (default: 30 seconds)
  _Added by autonomous elaboration_

- [ ] **AC-10: Spawn Validation for Database-Only Agents**
  - Phase 5 (Mermaid Diagram Regeneration) validates spawned agent existence in files OR database
  - Missing spawned agents flagged in SYNC-REPORT.md manual_review_needed section
  - Database query used to check agent existence before including in diagram
  _Added by autonomous elaboration_

## Reuse Plan

### Components to Reuse

1. **Existing doc-sync skill structure** - Keep all 7 phases, enhance Phase 2 and Phase 3
2. **doc-sync.agent.md** - Worker agent implementation pattern (haiku model, Read/Grep/Glob/Write/Edit/Bash tools)
3. **Frontmatter parsing logic** - sed-based YAML extraction (reuse as-is)
4. **Version bump logic** - Major/Minor/Patch determination (reuse as-is)
5. **Error handling patterns** - Invalid YAML → skip file, add to manual_review_needed (reuse as-is)

### Patterns to Follow

1. **Dual-source pattern** - Query database first, fall back to files if unavailable
2. **Graceful degradation** - Database queries optional, skill works without them
3. **Conservative updates** - Use Edit tool for surgical changes to preserve formatting
4. **Comprehensive reporting** - Extend SYNC-REPORT.md with database query status

### Packages to Leverage

- `@repo/logger` for logging database query attempts and failures
- postgres-knowledgebase MCP tools (when available) for database queries
- Existing Bash/Read/Grep/Glob tools for file operations

## Architecture Notes

### 7-Phase Workflow (Extended)

**Phase 1: File Discovery** - No changes, continues to use git diff or timestamp fallback

**Phase 2: Frontmatter Parsing (ENHANCED)**

- Step 2.1: Parse YAML frontmatter from `.agent.md`, `.command.md`, `.skill.md` files (existing)
- **Step 2.2 (NEW): Query database** - If postgres-knowledgebase available:
  ```sql
  -- Query workflow.phases for phase metadata
  SELECT phase_id, status, completed_at, description FROM workflow.phases;

  -- Query workflow.components for agent/command/skill metadata
  SELECT component_id, component_type, name, model, version, metadata
  FROM workflow.components
  WHERE component_type IN ('agent', 'command', 'skill');
  ```
- **Step 2.3 (NEW): Merge sources** - Database overrides file frontmatter if both present
- **Step 2.4 (NEW): Log database status** - Track whether database was queried, any errors

**Phase 3: Section Mapping (ENHANCED)**

- Existing pattern-based routing (pm-*, elab-*, dev-* → phases.md sections)
- **NEW: Handle database-sourced agents** - Components from database get mapped to sections
- **NEW: WINT phase structure** - Recognize Phase 0-9 patterns
- Unknown patterns flagged for manual review

**Phase 4-7: No changes** - Documentation Updates, Mermaid Diagrams, Changelog, Report Generation remain unchanged

### Error Handling Strategy

| Scenario | Behavior |
|----------|----------|
| Database unavailable | Log warning, continue with file-only mode |
| Invalid database response | Log error, skip database data, use files only |
| MCP tool timeout | Log timeout, fall back to files |
| File AND database both fail | Add to manual_review_needed, continue |
| Conflicting metadata | Database wins, log conflict to SYNC-REPORT.md |

### Data Merge Logic

```typescript
// Pseudocode for merge logic
function mergeMetadata(fileData, dbData) {
  if (!dbData) return fileData; // Database unavailable

  return {
    ...fileData,
    ...dbData, // Database overrides file
    _source: dbData ? 'database' : 'file',
    _conflict: detectConflict(fileData, dbData)
  };
}
```

## Infrastructure Notes

### Dependencies

**Soft Dependency (graceful degradation):**
- WINT-0070: workflow.phases and workflow.components tables
- WINT-0080: Seed data for workflow tables
- postgres-knowledgebase MCP server with workflow query tools

**Hard Dependency:**
- Existing doc-sync skill and agent files
- Pre-commit hook infrastructure
- Git for change detection

### MCP Tools Required

From postgres-knowledgebase MCP server (read-only):

- `workflow_get_phases` - Query all phases or filter by status
- `workflow_get_components` - Query agents/commands/skills with metadata
- (These tools may be created by WINT-0080 or later stories)

### Tool Access for doc-sync Agent

Add to `.claude/agents/doc-sync.agent.md` frontmatter:

```yaml
tools:
  - Read
  - Grep
  - Glob
  - Write
  - Edit
  - Bash
  - mcp__postgres-knowledgebase__workflow_get_phases  # NEW
  - mcp__postgres-knowledgebase__workflow_get_components  # NEW
```

### Performance Considerations

**Token Budget (per run):**
- Input: ~10,000-15,000 tokens (file parsing ~5K-10K, database queries add ~5K)
- Output: ~3,000-6,000 tokens (SYNC-REPORT.md + updated docs)
- Total: ~13,000-21,000 tokens (haiku model, acceptable)

**Query Performance:**
- Initial workflow tables are small (<100 rows per table)
- Add indexes on frequently queried columns: `component_type`, `status`
- Future optimization if tables grow large (Phase 7+)

## Test Plan

### Test Scenarios

#### 1. Database Available - Hybrid Mode

**Setup:**
- postgres-knowledgebase running with populated workflow.phases and workflow.components
- Some agents in files, some agents in database, some in both (to test merge)

**Test Steps:**
1. Run `/doc-sync`
2. Verify SYNC-REPORT.md shows "Database queried: Yes"
3. Verify AGENTS.md includes both file-sourced and database-sourced agents
4. Verify conflicting metadata resolved (database wins)

**Expected Outcome:**
- Documentation reflects merged view of files + database
- No errors or warnings
- SYNC-REPORT.md lists database query success

#### 2. Database Unavailable - File-Only Mode

**Setup:**
- postgres-knowledgebase stopped or unavailable
- All metadata in `.agent.md` files only

**Test Steps:**
1. Run `/doc-sync`
2. Verify SYNC-REPORT.md shows "Database queried: No (unavailable)"
3. Verify AGENTS.md updated from files only
4. Verify no errors, just warning logged

**Expected Outcome:**
- Skill completes successfully
- Documentation updated from files
- Graceful degradation message in report

#### 3. Database Only - No Files

**Setup:**
- Remove some `.agent.md` files
- Populate workflow.components with metadata for those agents

**Test Steps:**
1. Run `/doc-sync`
2. Verify database-only agents appear in AGENTS.md
3. Verify section mapping works for database-sourced agents

**Expected Outcome:**
- Database agents documented correctly
- No missing agent errors

#### 4. Merge Conflict - Database Overrides

**Setup:**
- `.agent.md` file says `version: 1.0.0`
- Database says `version: 1.1.0` for same agent

**Test Steps:**
1. Run `/doc-sync`
2. Verify AGENTS.md shows version 1.1.0 (database wins)
3. Verify SYNC-REPORT.md logs the conflict

**Expected Outcome:**
- Database version used in documentation
- Conflict logged for manual review

#### 5. Pre-commit Hook Integration

**Setup:**
- Update a `.agent.md` file
- Run `git commit` (triggers pre-commit hook with `--check-only`)

**Test Steps:**
1. Hook runs `/doc-sync --check-only`
2. Verify hook checks documentation currency
3. Verify hook exits with appropriate code (0 = current, 1 = stale)

**Expected Outcome:**
- Pre-commit hook works unchanged
- Database queries work in check-only mode

#### 6. Error Handling - Invalid Database Response

**Setup:**
- Mock postgres-knowledgebase to return invalid JSON or malformed data

**Test Steps:**
1. Run `/doc-sync`
2. Verify skill logs error
3. Verify skill falls back to file-only mode
4. Verify SYNC-REPORT.md shows database error

**Expected Outcome:**
- Skill completes with warning
- File-based documentation generated
- Error logged but not fatal

#### 7. WINT Phase Structure Mapping

**Setup:**
- Create test agents with `phase: 0`, `phase: 1`, ..., `phase: 9` in database

**Test Steps:**
1. Run `/doc-sync`
2. Verify `phases.md` updated with Phase 0-9 sections
3. Verify agents correctly mapped to their phase sections

**Expected Outcome:**
- New WINT phase structure recognized
- Agents correctly categorized by phase

#### 8. Documentation Self-Reference

**Setup:**
- Ensure doc-sync skill itself has metadata

**Test Steps:**
1. Run `/doc-sync`
2. Verify SKILLS.md includes doc-sync with database-aware description

**Expected Outcome:**
- doc-sync skill documents itself
- Description mentions database capabilities

### Test Artifacts

**Fixtures Required:**

1. **sample-db-state.sql** - Seed data for workflow.phases and workflow.components
2. **file-only-scenario/** - Directory with `.agent.md` files, no database
3. **conflict-scenario/** - Files + database with conflicting versions
4. **pre-commit-test.sh** - Script to test hook integration

**Manual Verification:**

- Review SYNC-REPORT.md for completeness and clarity
- Review updated skill.md for database query documentation
- Review AGENTS.md/COMMANDS.md/SKILLS.md for accuracy

### Acceptance Criteria Validation

| AC | Test Scenario | Validation Method |
|----|---------------|-------------------|
| AC-1 | 1, 2, 3, 4, 6 | Verify database queries work and degrade gracefully |
| AC-2 | 7 | Verify WINT phase mapping works |
| AC-3 | 1, 3 | Verify AGENTS.md reflects merged state |
| AC-4 | 1 | Verify COMMANDS.md updated correctly |
| AC-5 | 8 | Verify SKILLS.md includes doc-sync |
| AC-6 | 7 | Verify phases.md updated with WINT structure |
| AC-7 | 2, 5 | Verify backward compatibility |
| AC-8 | Manual | Review skill.md documentation updates |

### Test Environment

**Prerequisites:**
- postgres-knowledgebase running locally
- WINT-0070 migration applied (workflow.phases, workflow.components tables)
- WINT-0080 seed data loaded (optional, but helpful)
- Existing doc-sync skill and agent files

**Test Data Generation:**

```bash
# Populate test database
psql -d postgres-knowledgebase -f test/fixtures/sample-db-state.sql

# Verify tables exist
psql -d postgres-knowledgebase -c "SELECT COUNT(*) FROM workflow.phases;"
psql -d postgres-knowledgebase -c "SELECT COUNT(*) FROM workflow.components;"
```

**Cleanup:**

```bash
# Restore original documentation
git restore docs/AGENTS.md docs/COMMANDS.md docs/SKILLS.md docs/workflow/

# Clear test database
psql -d postgres-knowledgebase -c "TRUNCATE workflow.phases, workflow.components;"
```

## Dev Feasibility

### Complexity Assessment

**Overall Complexity: Low-Medium**

This story extends a proven pattern (existing doc-sync skill) with database queries. No new workflow phases or complex orchestration required.

### Implementation Approach

**Phase 1: Extend Agent Metadata**

Add postgres-knowledgebase MCP tools to `.claude/agents/doc-sync.agent.md` frontmatter.

**Phase 2: Enhance Phase 2 Logic**

Update skill.md Phase 2 section to include:
1. Database availability check
2. Query workflow.phases and workflow.components
3. Merge database + file metadata
4. Log database query status

**Phase 3: Update Documentation**

Extend `.claude/skills/doc-sync/skill.md` with:
- Database query examples
- Error handling scenarios
- Merge logic explanation
- Hybrid sync examples

**Phase 4: Test Scenarios**

Implement 8 test scenarios (see Test Plan) to verify:
- File-only mode works (backward compatibility)
- Database-only mode works (future state)
- Hybrid mode merges correctly (migration state)
- Pre-commit hook unchanged

**Phase 5: Pre-commit Hook Verification**

Ensure `--check-only` mode works with database queries (graceful degradation if database unavailable).

### Dependencies

**Logical Dependency (not blocking):**
- WINT-0070: workflow.phases and workflow.components tables
- WINT-0080: Seed data for workflow tables
- postgres-knowledgebase MCP server with workflow query tools

**Graceful Degradation:**
If WINT-0070/0080 are incomplete, skill continues to work in file-only mode. No hard blocking dependency.

### Integration Points

**Read-only integration with postgres-knowledgebase:**
- MCP tools: `workflow_get_phases`, `workflow_get_components`
- Connection handling: Use existing MCP client pattern
- Error handling: Catch connection timeouts, invalid responses

**File system integration:**
- Existing Read/Grep/Glob tools for .agent.md/.command.md/.skill.md discovery
- Edit tool for surgical documentation updates
- Bash for git diff-based change detection

### Risks and Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Database schema changes during Phases 1-9 | Medium | Medium | Document query structure, version skill.md if schema evolves |
| Query performance degrades as tables grow | Low | Low | Add indexes on component_type, status columns |
| Merge conflicts difficult to debug | Low | Medium | Log all conflicts to SYNC-REPORT.md with clear explanation |
| MCP tool API changes | Low | High | Pin to postgres-knowledgebase MCP server version, test before upgrade |

### Token Budget

**Per Run Estimate:**
- Input: ~10,000-15,000 tokens
  - Existing file parsing: ~5,000-10,000 tokens
  - Database queries add: ~5,000 tokens (metadata is compact)
- Output: ~3,000-6,000 tokens
  - SYNC-REPORT.md: ~1,000-2,000 tokens
  - Updated docs: ~2,000-4,000 tokens
- **Total: ~13,000-21,000 tokens (haiku model)**

This is within acceptable range for haiku model performance goals.

### Timeline Estimate

**Story Points: 5**

- Phase 1 (Agent metadata): 0.5 points
- Phase 2 (Enhance logic): 2.0 points (core implementation)
- Phase 3 (Documentation): 1.0 points
- Phase 4 (Testing): 1.0 points
- Phase 5 (Pre-commit hook): 0.5 points

**Estimated Duration:** 2-3 days for experienced developer familiar with existing doc-sync pattern.

## Reality Baseline

**Baseline Date:** 2026-02-13

**Relevant Existing Features:**

1. **doc-sync skill** - `.claude/skills/doc-sync/skill.md`
   - 7-phase workflow: File Discovery, Frontmatter Parsing, Section Mapping, Documentation Updates, Mermaid Diagram Regeneration, Changelog Entry Drafting, Report Generation
   - Git diff-based change detection with timestamp fallback
   - Pre-commit hook integration with `--check-only` flag
   - Conservative update strategy using Edit tool

2. **doc-sync agent** - `.claude/agents/doc-sync.agent.md`
   - Haiku model for performance
   - Tools: Read, Grep, Glob, Write, Edit, Bash
   - Worker agent that implements doc-sync skill logic

3. **Documentation targets**
   - `docs/AGENTS.md` - 61 active agents (2 orchestrators, 30 leaders, 25 workers, 2 reference, 2 archived)
   - `docs/COMMANDS.md` - Full command reference with workflow diagrams
   - `docs/SKILLS.md` - 11 skills (8 worktree management, 3 code review/quality)
   - `docs/workflow/phases.md` - Phase-by-phase workflow documentation
   - `docs/workflow/agent-system.md` - Agent architecture and expert intelligence framework

4. **WINT Phase 0 context**
   - Database schemas defined in WINT-0010 (core, context_cache, telemetry, ml, graph, workflow)
   - workflow.phases and workflow.components tables created in WINT-0070 (pending)
   - MCP tools for story management in WINT-0090 (ready-for-qa)
   - Session management MCP tools in WINT-0110 (completed)

**Active In-Progress Work:** None - no conflicts with other stories

**Constraints to Respect:**

- Zod-first types (REQUIRED) - no TypeScript interfaces
- Functional components only, named exports
- No barrel files - import directly from source
- `@repo/logger` for logging, `@repo/ui` for UI components
- Prettier: no semicolons, single quotes, trailing commas, 100 char width
- Drizzle ORM for schema definitions
- No direct database access - use MCP tools

**Gaps from Baseline:**
- No ADR-LOG.md found (ADR infrastructure not yet established for WINT epic)
- No KB lessons available (Phase 0 bootstrap in progress, KB infrastructure from WINT-0010/WINT-0120 are prerequisites)
- postgres-knowledgebase MCP server workflow query tools not yet created (deferred to WINT-0080 or later)

---

**Generated:** 2026-02-16
**Seed:** /Users/michaelmenard/Development/monorepo/plans/future/platform/wint/WINT-0150/_pm/STORY-SEED.md
**Experiment Variant:** control
**No Active Experiments:** All experiments paused, story assigned to control group

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-16_

### MVP Gaps Resolved

| # | Finding | Resolution | AC Added |
|---|---------|------------|----------|
| 5 | No handling for database query timeout scenarios in Error Handling Strategy table | Add as AC | AC-9 |
| 9 | Agent frontmatter `spawns` field may reference database-sourced agents that don't have .agent.md files | Add as AC | AC-10 |

### Non-Blocking Items (Implementation Notes)

| # | Finding | Category | Implementation Note |
|---|---------|----------|-------------------|
| 1 | No explicit versioning strategy for dual-source metadata when BOTH file and database have version fields | edge-case | Version resolution: use database version if present, else file version (database overrides per merge strategy) |
| 4 | SYNC-REPORT.md format extension for database query status is not fully specified | documentation | SYNC-REPORT.md database section format: 'Database queried: Yes/No', query success/failure, row counts, merge conflicts detected |
| 6 | Test scenario 6 (Error Handling) uses 'mock postgres-knowledgebase' but no mocking strategy documented | test-infra | Test mocking strategy: Use actual database with injected test data for integration test fidelity (NOT MCP server mocks) |
| 8 | No rollback strategy if database queries corrupt SYNC-REPORT.md or documentation | safety | Rollback strategy: Use `git restore docs/` if sync fails - doc-sync uses git diff so restoration is safe |
| 10 | No explicit handling for WINT Phase 0-9 structure in existing Section Mapping table | documentation | Section Mapping extension: Add WINT phase patterns (wint-*.agent.md → docs/workflow/phases.md Phase 0-9 sections) to Phase 3 mapping table |

### Summary

- **ACs added:** 2 (AC-9: Database Query Timeout Handling, AC-10: Spawn Validation for Database-Only Agents)
- **Implementation notes added:** 5 (for skill.md documentation during implementation phase)
- **KB write requests generated:** 17 (see KB-WRITE-REQUESTS.yaml for deferred items)
- **Mode:** autonomous
- **Verdict:** PASS - Story ready to proceed to implementation
