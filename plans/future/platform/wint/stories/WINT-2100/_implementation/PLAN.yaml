schema: 1
story_id: "WINT-2100"
timestamp: "2026-02-21T18:05:00Z"

summary: "Create session-manager.agent.md — haiku worker agent for session lifecycle orchestration"

deliverable:
  - path: ".claude/agents/session-manager.agent.md"
    action: create
    description: "Haiku worker agent managing session lifecycle (create, update, complete, cleanup)"

phases:
  - id: 1
    name: "Create agent file with frontmatter and all sections"
    description: |
      Single-step creation of the complete agent file following
      turn-count-metrics-agent.agent.md structural conventions.
      All 10 ACs addressed in one file write.

references_read:
  - path: ".claude/agents/turn-count-metrics-agent.agent.md"
    purpose: "Structural exemplar for frontmatter, sections, output format"
  - path: "packages/backend/mcp-tools/src/session-management/SESSION-MANAGEMENT-TOOLS.md"
    purpose: "MCP tool API reference and lifecycle patterns"
  - path: "packages/backend/mcp-tools/src/session-management/__types__/index.ts"
    purpose: "Zod schema definitions for all 5 tools"
  - path: ".claude/skills/session-create/SKILL.md"
    purpose: "Session creation skill interface (WINT-2090)"
  - path: ".claude/skills/session-inherit/SKILL.md"
    purpose: "Session inheritance skill interface (WINT-2090)"

ac_mapping:
  AC-1: "Frontmatter with type:worker, model:haiku, spawned_by, etc."
  AC-2: "Phase 1 — session creation via session-create skill, null-return handling"
  AC-3: "Phase 1 — leaked-session detection via sessionQuery pre-check"
  AC-4: "Phase 2 — session update with incremental mode, throw handling"
  AC-5: "Phase 3 — session completion with idempotent guard"
  AC-6: "Phase 4 — cleanup with mandatory dryRun:true first"
  AC-7: "Output format section — structured YAML completion signal"
  AC-8: "Frontmatter spawned_by lists context-warmer agent"
  AC-9: "Non-negotiables section with 3 required prohibitions"
  AC-10: "LangGraph porting interface contract section"

e2e_gate: "exempt"
e2e_exemption_reason: "Documentation-only story — no UI, no API, no code changes"

risks: []
