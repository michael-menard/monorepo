---
id: WINT-1140
title: "Integrate Worktree Creation into dev-implement-story"
status: uat
priority: high
points: 3
type: feature
phase: 1
epic: wint
depends_on:
  - WINT-1130
blocks:
  - WINT-1160
  - WINT-1120
experiment_variant: "control"
created: "2026-02-16"
updated_at: "2026-02-17T19:10:00Z"
story_dir: plans/future/platform/wint/UAT/WINT-1140
story_file: plans/future/platform/wint/UAT/WINT-1140/WINT-1140.md
touches_backend: false
touches_frontend: false
touches_database: true
touches_infra: false
predictions:
  split_risk: 0.7
  review_cycles: 2
  token_estimate: 100000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-16T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
---

# WINT-1140: Integrate Worktree Creation into dev-implement-story

## Context

The dev-implement-story orchestrator command (v8.1.0) manages the full story implementation lifecycle but currently operates without git worktree isolation. WINT-1130 delivered 4 MCP tools for database-backed worktree tracking (`worktree_register`, `worktree_get_by_story`, `worktree_list_active`, `worktree_mark_complete`) and the 8 wt-* skills handle git operations. These two layers are currently unconnected.

When a developer runs `/dev-implement-story plans/future/platform/wint WINT-1140`, the implementation work happens in the current working directory/branch rather than an isolated worktree. This means:
1. Multiple concurrent sessions can work on the same story in the same branch context
2. There is no database record of which session/branch is handling a story
3. Resuming a story implementation (after a crash or context reset) may create a new branch instead of returning to the existing one

WINT-1130 is in UAT as of 2026-02-16 (QA verified PASS). Integration testing for this story requires WINT-1130 MCP tools to be confirmed live in the MCP server. Command doc implementation can proceed in parallel.

## Goal

Add a worktree pre-flight step (Step 1.3) to the dev-implement-story orchestrator that automatically creates, registers, and resumes git worktrees for each story implementation. Every story gets isolated branch context. Session continuity is preserved via CHECKPOINT.yaml.

## Non-Goals

- Modifying any existing wt-* skills (wt-new, wt-finish, wt-status, wt-cleanup, wt-list, wt-prune, wt-switch, wt-sync) — these are called unchanged
- Implementing the full parallel work conflict prevention UI — that is WINT-1160's scope
- Automatic worktree cleanup at story completion — that is WINT-1150's scope
- Batch worktree operations — that is WINT-1170's scope
- Changes to any worker agents (dev-setup-leader, dev-plan-leader, dev-execute-leader, dev-proof-leader) — the worktree pre-flight lives in the orchestrator only
- New MCP tools — WINT-1130 provided all needed tools
- Changes to EVIDENCE.yaml schema, REVIEW.yaml, or PLAN.yaml

## Scope

### Primary Artifacts
- `.claude/commands/dev-implement-story.md` — add Step 1.3 (Worktree Pre-flight) to the orchestrator step sequence
- `packages/backend/orchestrator/src/artifacts/` — check and update CHECKPOINT.yaml Zod schema to include `worktree_id: z.string().uuid().optional()`

### MCP Tools Called (read-only from WINT-1130)
- `worktree_register(storyId, worktreePath, branchName)` → returns `{ worktree_id }` or null
- `worktree_get_by_story(storyId)` → returns active worktree record or null

### Skills Invoked (unchanged)
- `/wt:new` — creates a new git worktree
- `/wt:switch` — switches to an existing git worktree

### No New Packages
No new packages, no new MCP tools, no new wt-* skills required.

## Acceptance Criteria

- [ ] **AC-1**: When `/dev-implement-story` is invoked for a story with no active worktree in the database, a new worktree is automatically created via `/wt:new` before Phase 0 (dev-setup-leader) begins
- [ ] **AC-2**: After creating a worktree, `worktree_register` is called with the story's database UUID, the new worktree path, and the branch name; the returned `worktree_id` is stored in CHECKPOINT.yaml
- [ ] **AC-3**: When `/dev-implement-story` is invoked for a story whose CHECKPOINT.yaml already contains a `worktree_id` that matches an active DB record with the same path, the command switches to the existing worktree via `/wt:switch` instead of creating a new one
- [ ] **AC-4**: When `/dev-implement-story` detects an active worktree in the database for the story but no matching CHECKPOINT.yaml (different session), the command shows a warning and presents three options: (a) switch to existing worktree, (b) take over (mark old worktree abandoned and create new), (c) abort
- [ ] **AC-5**: The `--skip-worktree` flag bypasses all worktree pre-flight steps and proceeds directly to Phase 0 (escape hatch for edge cases / environments without git worktree support)
- [ ] **AC-6**: The worktree pre-flight step is inserted as Step 1.3 in the orchestrator sequence — after Step 1 (Initialize) and before Step 2 (Detect Phase) — and is clearly documented in the command
- [ ] **AC-7**: The `--gen` flag flow also receives worktree pre-flight (Step 1.3 runs after Step 1.5 story generation, before Phase 0)
- [ ] **AC-8**: If `worktree_register` returns null (conflict or MCP tool failure), the command logs a warning and asks the user to confirm before proceeding without a registered worktree (graceful degradation)
- [ ] **AC-9**: The autonomy level (`--autonomous` flag) governs the confirmation prompt in AC-4: `conservative` always asks; `moderate` and `aggressive` auto-select option (a) (switch to existing) without prompting if the existing worktree is on the same machine
- [ ] **AC-10**: During the setup phase, the implementer must read `wt-switch/SKILL.md` to determine the actual invocation interface — if `/wt:switch` is interactive-only (no path argument), Step 1.3 in the command doc must describe it as a guided/assisted switch step rather than a silent auto-switch; AC-3 and AC-4a language must accurately reflect the interactive pattern
  _Added by autonomous elaboration_
- [ ] **AC-11**: During the setup phase, the implementer must read `wt-new/SKILL.md` to determine the actual invocation interface — if `/wt:new` cannot accept branch name as a parameter (prompts interactively), Step 1.3 in the command doc must present new-worktree creation as a guided step; AC-1, AC-2, and AC-9 autonomous mode behavior must be adjusted to describe an assisted-creation pattern rather than a silent one
  _Added by autonomous elaboration_

## Reuse Plan

- **Skills**: `/wt:new` (create worktree), `/wt:switch` (switch to existing) — invoked unchanged as sub-commands
- **MCP Tools**: `worktree_register`, `worktree_get_by_story` from WINT-1130 — same call pattern as `story_get_status`, `story_update_status` from WINT-0090
- **CHECKPOINT.yaml**: Extend with `worktree_id` field for session continuity — follows existing orchestrator checkpoint pattern
- **Decision handling**: `.claude/agents/_shared/decision-handling.md` and `.claude/agents/_shared/autonomy-tiers.md` for the confirmation prompt (AC-4, AC-9)
- **Step insertion pattern**: dev-implement-story already uses numbered steps (Step 1, Step 1.5, Step 2, etc.); Step 1.3 inserts cleanly between Initialize and Detect Phase

### MCP Tool Call Pattern
```
// Null-check resilience (from WINT-1130 lesson)
const existing = await worktree_get_by_story({ storyId })
if (existing !== null) {
  // handle existing worktree scenarios (AC-3, AC-4)
} else {
  // create new worktree (AC-1, AC-2)
  const result = await worktree_register({ storyId, worktreePath, branchName })
  if (result === null) {
    // graceful degradation (AC-8)
  }
}
```

## Architecture Notes

### Step 1.3 Insertion Point
The orchestrator command currently has:
- Step 1: Initialize (read story, validate inputs)
- Step 1.5: Generate Story (if `--gen` flag)
- Step 2: Detect Phase (read CHECKPOINT.yaml)
- ...

Step 1.3 inserts between Step 1 (or Step 1.5 if `--gen`) and Step 2:
- Standard flow: Step 1 → **Step 1.3** → Step 2
- Gen flow: Step 1 → Step 1.5 → **Step 1.3** → Step 2

### Pre-flight Decision Tree
```
Step 1.3 — Worktree Pre-flight
  IF --skip-worktree flag:
    Log warning about no tracking
    Proceed to Step 2 (skip all below)

  Read CHECKPOINT.yaml for existing worktree_id
  Call worktree_get_by_story(storyId)

  CASE: DB returns null (no active worktree)
    Call /wt:new
    Extract worktreePath, branchName from wt-new output
    Call worktree_register(storyId, worktreePath, branchName)
    IF result is null: warn + confirm (AC-8)
    ELSE: write worktree_id to CHECKPOINT.yaml
    Proceed to Step 2

  CASE: DB returns record AND CHECKPOINT.yaml.worktree_id matches
    Call /wt:switch to existing path
    Proceed to Step 2 (no registration needed)

  CASE: DB returns record AND CHECKPOINT.yaml.worktree_id mismatch (or no checkpoint)
    Show 3-option warning (AC-4)
    IF conservative (or default): prompt user
    IF moderate/aggressive: auto-select (a) + log
    HANDLE (a): /wt:switch to existing, update CHECKPOINT.yaml
    HANDLE (b): mark old as abandoned, /wt:new, register new, update CHECKPOINT.yaml
    HANDLE (c): abort
```

### Autonomy Level Integration
The `--autonomous` flag governs the confirmation flow per `.claude/agents/_shared/autonomy-tiers.md`:
- `conservative`: always prompt (default behavior)
- `moderate`: auto-select option (a) for same-machine worktree conflicts
- `aggressive`: auto-select option (a) without condition

### CHECKPOINT.yaml Schema Extension
```typescript
// In packages/backend/orchestrator/src/artifacts/CHECKPOINT schema (if Zod-defined)
const CheckpointSchema = z.object({
  // ... existing fields ...
  worktree_id: z.string().uuid().optional(),  // NEW: added by WINT-1140
})
```

## Infrastructure Notes

No infrastructure changes. This story modifies:
1. A markdown command doc (`.claude/commands/dev-implement-story.md`)
2. Potentially a Zod schema for CHECKPOINT.yaml validation

No Lambda functions, no database migrations, no CDN changes, no auth changes (ADR-001 through ADR-004 not applicable).

## Test Plan

### Scope
- No HTTP endpoints
- No browser UI (no Playwright)
- Data touched: CHECKPOINT.yaml (worktree_id field), core.worktrees table (via MCP tools)
- Integration tests serve as E2E equivalent per ADR-006

### Pre-flight Test Matrix (3 Required Scenarios)

| Scenario | worktree_get_by_story | CHECKPOINT.yaml | Expected Action |
|----------|-----------------------|-----------------|-----------------|
| No worktree | returns null | no worktree_id | Create new via /wt:new + register |
| Same-session resume | returns active record | matching worktree_id | Switch via /wt:switch, no new creation |
| Different-session | returns active record | no/mismatched worktree_id | Show 3-option warning (or auto-select if autonomous) |

### Happy Path Tests

**Test 1 — New Story (AC-1, AC-2)**: Run dev-implement-story for a story with no active worktree in DB.
- Expected: /wt:new called, worktree_register called, CHECKPOINT.yaml contains worktree_id
- Evidence: DB row in core.worktrees (status=active), CHECKPOINT.yaml diff, git worktree list

**Test 2 — Resume (AC-3)**: Re-run dev-implement-story for a story with matching worktree in DB and CHECKPOINT.yaml.
- Expected: /wt:switch called, no new worktree created, no new DB row
- Evidence: No new DB row, command output shows "Resuming in existing worktree"

**Test 3 — Skip Flag (AC-5)**: Run with --skip-worktree.
- Expected: No MCP calls, no DB row, warning message printed
- Evidence: DB has no new row, CHECKPOINT.yaml unchanged

**Test 4 — Gen Flag (AC-7)**: Run with --gen flag.
- Expected: Step 1.5 runs, then Step 1.3, then Phase 0
- Evidence: Worktree created after story generation, CHECKPOINT.yaml has worktree_id

### Error Cases

**Error Test 1 — worktree_register returns null (AC-8)**: Simulate MCP failure.
- Expected: Warning logged + user prompt + graceful proceed or abort
- Evidence: No worktree_id in CHECKPOINT.yaml if proceeding; command exits cleanly if aborting

**Error Test 2 — Different-session, user aborts (AC-4c)**: Present 3-option prompt, user selects (c).
- Expected: Command exits cleanly, no changes to DB or filesystem
- Evidence: No new DB rows, no CHECKPOINT.yaml changes, clean exit message

### Edge Cases

**EC-1 — Different-session + user switches (AC-4a)**: /wt:switch called with existing path, CHECKPOINT.yaml updated
**EC-2 — Different-session + take over (AC-4b)**: Old DB record marked abandoned, new worktree created
**EC-3 — Autonomous moderate (AC-9)**: Auto-selects option (a), logs selection without prompting
**EC-4 — Conservative always prompts (AC-9)**: Prompt shown even if only one obvious option

### Prerequisite for Integration Tests
Confirm WINT-1130 MCP tools (`worktree_register`, `worktree_get_by_story`) are callable from MCP server before running integration tests. The command doc can be written and reviewed independently.

### ADR Compliance
- ADR-005: No mocking of `worktree_register` in integration tests — use real DB
- ADR-006: No Playwright needed (no UI surface)

Full test plan: `/Users/michaelmenard/Development/monorepo/plans/future/platform/wint/backlog/WINT-1140/_pm/TEST-PLAN.md`

## UI/UX Notes

This story has no React/browser UI surface. UX is command-line/agent interaction only.

### Critical CLI Messaging Requirements

**Pre-flight Warning (AC-4)**:
```
Warning: An active worktree already exists for WINT-XXXX.
  Worktree path: /path/to/worktree
  Branch: feature/WINT-XXXX
  Registered: 2026-02-14T10:00:00Z

This worktree was registered by a different session.
Choose an action:
  (a) Switch to the existing worktree and continue there
  (b) Take over — mark existing as abandoned, create a new worktree
  (c) Abort

Enter choice [a/b/c]:
```

**Skip-Worktree Note (AC-5)**:
```
Note: Worktree pre-flight bypassed (--skip-worktree). No database tracking will occur for this session.
```

**Auto-Selection Log (AC-9)**:
```
[autonomous] Detected existing worktree for WINT-XXXX. Auto-selected: switch to existing (autonomous=moderate).
  Worktree path: /path/to/worktree
```

**Step Progress Style**: Match existing dev-implement-story output style — brief, emoji-free, single-line status messages.

Full UI/UX notes: `/Users/michaelmenard/Development/monorepo/plans/future/platform/wint/backlog/WINT-1140/_pm/UIUX-NOTES.md`

## Reality Baseline

- **Seed generated**: 2026-02-16 (baseline: 2026-02-13)
- **WINT-1130 status**: UAT, QA verified PASS 2026-02-16 — MCP tools implemented and tested, deployment confirmation required before integration testing
- **dev-implement-story version**: v8.1.0 — structured step sequence with clear Step 1.3 insertion point
- **wt-new skill**: exists at `.claude/skills/wt-new/SKILL.md`
- **wt-switch skill**: exists at `.claude/skills/wt-switch/SKILL.md` — interface must be verified during implementation setup
- **CHECKPOINT.yaml schema**: check `packages/backend/orchestrator/src/artifacts/` for Zod schema before adding worktree_id field
- **No blocking conflicts**: 1 warning (WINT-1130 deployment dependency, non-blocking for command doc authoring)
- **Sibling stories to coordinate**: WINT-1150 (cleanup) and WINT-1160 (conflict prevention) — must not duplicate worktree_mark_complete logic; WINT-1140 only handles creation/detection

## Dev Feasibility Summary

- **Feasible**: yes
- **Confidence**: high
- **Complexity**: Low-Medium (markdown doc update + CHECKPOINT schema extension + MCP calls)
- **Estimated story points**: 3
- **Primary risk**: wt-switch interface verification (resolve in setup phase by reading wt-switch/SKILL.md)
- **Secondary risk**: WINT-1130 deployment gate on integration tests (non-blocking for implementation)

Full feasibility review: `/Users/michaelmenard/Development/monorepo/plans/future/platform/wint/backlog/WINT-1140/_pm/DEV-FEASIBILITY.md`

---

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-16_

### MVP Gaps Resolved

| # | Finding | Resolution | AC Added |
|---|---------|------------|----------|
| 1 | wt-switch interface assumption mismatch (interactive-only per SKILL.md, no path parameter) | Formalized verification requirement for setup phase; implementer must read wt-switch/SKILL.md and adjust Step 1.3 command doc to reflect actual interface (guided/assisted pattern if confirmed interactive-only) | AC-10 |
| 2 | wt-new interface assumption mismatch (interactive prompts for base/feature branch, no parameter interface) | Formalized verification requirement for setup phase; implementer must read wt-new/SKILL.md and adjust Step 1.3 and AC-1/AC-2/AC-9 language to reflect guided-creation pattern if confirmed interactive-only | AC-11 |

### Non-Blocking Items (Logged to KB)

| # | Finding | Category | Notes |
|---|---------|----------|-------|
| 1 | Edge case: CHECKPOINT.yaml contains a worktree_id that references a DB record in merged or abandoned state (not active) | edge-case | Non-blocking. Deferred to future enhancement. |
| 2 | Edge case: Worktree path on disk no longer exists but DB record is active (orphaned DB record) | edge-case | Non-blocking. Deferred to WINT-1150. |
| 3 | Edge case: worktree_register returns conflict error vs generic MCP failure — both treated as null in AC-8 | error-handling | Non-blocking. Requires WINT-1130 structured error returns. |
| 4 | No test for --gen + --skip-worktree combined flags (AC-5 + AC-7) | test-coverage | Non-blocking test coverage gap. |
| 5 | UX polish: 3-option warning (AC-4) could show elapsed time since registration | ux-polish | Non-blocking UX enhancement. |
| 6 | UX polish: Step 1.3 could log worktree path and branch name to CHECKPOINT.yaml for debugging | ux-polish | Non-blocking UX/DX enhancement. |
| 7 | Performance: worktree_get_by_story called unconditionally even when --skip-worktree is set | performance | Non-blocking performance note. |
| 8 | Observability: No telemetry logging for worktree pre-flight outcomes | observability | Non-blocking. Flagged for WINT-3020/WINT-3070 scope. |
| 9 | Integration: AC-9 autonomous auto-select only handles same-machine detection; cross-machine not addressed | integration | Non-blocking. Cross-machine handling deferred to WINT-1160. |
| 10 | Accessibility: 3-option prompt (AC-4) uses a/b/c only; numbered aliases 1/2/3 not provided | accessibility | Non-blocking minor UX polish. |

### Summary

- ACs added: 2 (AC-10, AC-11)
- KB entries queued: 10
- Mode: autonomous
- Verdict: CONDITIONAL PASS — Ready to proceed to implementation with documented verification requirements in AC-10 and AC-11.
