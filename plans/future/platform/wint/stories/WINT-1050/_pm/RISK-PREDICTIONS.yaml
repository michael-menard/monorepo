# Risk Predictions: WINT-1050
# Generated by: pm-story-risk-predictor (WKFL-007)
# Date: 2026-02-17

predictions:
  split_risk: 0.2
  review_cycles: 1
  token_estimate: 80000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-17T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"

# Calculation notes:
# AC count: 10 → base_risk = (10 - 3) * 0.1 = 0.7
# BUT: scope is docs-only (single markdown file). No backend/frontend/database source changes.
# Complexity adjustment: -0.5 for docs-only scope (single file, command spec, no TypeScript)
# Adjusted split_risk: 0.2 (low — single file change, well-defined scope)
#
# review_cycles: 1 (docs-only, no code review gate, elaboration review handles it)
#
# token_estimate: ~80,000 tokens (command spec rewrite, single file, small diff)
# No similar stories found in KB (heuristics-only mode)
# WKFL-006 patterns: unavailable (heuristics-only mode)
#
# confidence: low (no similar stories, no patterns — heuristics only)
