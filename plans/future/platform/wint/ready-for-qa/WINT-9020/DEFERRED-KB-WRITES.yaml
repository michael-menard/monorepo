---
# Deferred KB writes for WINT-9020
# KB was unavailable during story generation (2026-02-18)
# These writes should be retried when postgres-knowledgebase is available

deferred_writes:
  - type: stories_table_upsert
    priority: high
    retry_after: "2026-02-18T00:00:00Z"
    payload:
      story_id: "WINT-9020"
      feature: "wint"
      title: "Create doc-sync LangGraph Node (Native 7-Phase Implementation)"
      story_dir: "plans/future/platform/wint/backlog/WINT-9020"
      story_file: "plans/future/platform/wint/backlog/WINT-9020/WINT-9020.md"
      story_type: "feature"
      points: 5
      priority: "medium"
      state: "backlog"
      touches_backend: true
      touches_frontend: false
      touches_database: false
      touches_infra: false
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'WINT-9020',
        'wint',
        'Create doc-sync LangGraph Node (Native 7-Phase Implementation)',
        'plans/future/platform/wint/backlog/WINT-9020',
        'plans/future/platform/wint/backlog/WINT-9020/WINT-9020.md',
        'feature',
        5,
        'medium',
        'backlog',
        true,
        false,
        false,
        false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;
