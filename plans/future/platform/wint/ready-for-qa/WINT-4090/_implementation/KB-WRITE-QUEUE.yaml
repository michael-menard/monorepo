# KB Write Queue for WINT-4090
# Generated by elab-autonomous-decider
# To be processed by kb-writer agent or orchestration system

story_id: WINT-4090
generated_at: "2026-02-18T00:00:00Z"

# 10 non-blocking findings queued for KB writes
# 0 findings were promoted to ACs (no MVP-critical gaps)
kb_write_requests:

  # Non-blocking gaps from FUTURE-OPPORTUNITIES.md (5 items)
  - request_id: "wint-4090-gap-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] Warning Count Ambiguity in AC-2 Graceful Degradation**
        - **Finding**: AC-2 says "warning count incremented" but does not define what constitutes a warning vs. a skipped optional input vs. a soft degradation
        - **Impact**: Low - convention is clear: one warning per missing optional input
        - **Effort**: Low - define `warnings: string[]` in output schema; clarify that each missing optional input adds one warning entry; document in agent file under Completion Signals
        - **Recommendation**: Implementer convention: count one warning per missing optional input; document in Completion Signals section as: "Each missing optional input contributes +1 to the warning count"
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "warning-count"
        - "graceful-degradation"
        - "evidence-judge"
        - "wint-4090"

  - request_id: "wint-4090-gap-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] ac-verdict.json Idempotency Behavior Not Specified**
        - **Finding**: No explicit instruction on what happens when `ac-verdict.json` already exists from a prior run — does a second run overwrite or append?
        - **Impact**: Low - overwrite-on-rerun is the safe default for agent artifacts
        - **Effort**: Low - specify overwrite semantics in the agent file (same pattern as scope-defender); add a note in LangGraph Porting Notes
        - **Recommendation**: Add to agent Non-Negotiables: "Overwrite existing ac-verdict.json on each run; do not append or merge"
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "idempotency"
        - "ac-verdict-json"
        - "evidence-judge"
        - "wint-4090"

  - request_id: "wint-4090-gap-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] ac-verdict.json Missing schema_version Field**
        - **Finding**: No `schema_version` field in ac-verdict.json output — WINT-4150 will need to distinguish v1 from future schema versions and consumers cannot detect version drift
        - **Impact**: Low - version drift between producer and consumer will be silent until a parsing failure occurs
        - **Effort**: Low - WINT-4150 should add `schema_version: 1` when standardizing; implementer may pre-add as an optional field
        - **Recommendation**: Add `schema_version: 1` as an optional field at implementation time; defer formal versioning semantics to WINT-4150 which owns the schema standardization contract
      additional_tags:
        - "non-blocking"
        - "observability"
        - "schema-versioning"
        - "ac-verdict-json"
        - "evidence-judge"
        - "wint-4090"
        - "wint-4150"

  - request_id: "wint-4090-gap-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] evidence-judge Agent Missing spawned_by / triggers Frontmatter**
        - **Finding**: The agent file has no `spawned_by` or `triggers` frontmatter fields; other agents use `spawned_by` for traceability. WINT-4120 will integrate evidence-judge into the QA workflow but this linking is deferred
        - **Impact**: Low - agent is discoverable from WINT-4120 story documentation; frontmatter field is a convenience
        - **Effort**: Low - add `spawned_by: []` as a placeholder in frontmatter with a note that WINT-4120 will populate it
        - **Recommendation**: Add `spawned_by: []` placeholder at implementation time; WINT-4120 should update this field when integrating the agent into the QA workflow
      additional_tags:
        - "non-blocking"
        - "edge-case"
        - "agent-frontmatter"
        - "discoverability"
        - "evidence-judge"
        - "wint-4090"
        - "wint-4120"

  - request_id: "wint-4090-gap-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] WINT-9050 depends_on Field Contains Likely PM Typo**
        - **Finding**: The stories.index.md entry for WINT-9050 lists `Depends On: WINT-4070` (cohesion-prosecutor) but the description says it ports the evidence-judge to LangGraph — it should depend on WINT-4090 (evidence-judge), not WINT-4070
        - **Impact**: Medium - if not corrected, WINT-9050 may be elaborated before its dependency (WINT-4090) is complete, causing rework
        - **Effort**: Low - PM corrects WINT-9050 depends_on field before WINT-9050 elaboration begins
        - **Recommendation**: PM should update WINT-9050 depends_on to WINT-4090 before WINT-9050 enters elaboration; non-blocking for WINT-4090 implementation
      additional_tags:
        - "non-blocking"
        - "data-integrity"
        - "pm-action-required"
        - "dependency-graph"
        - "evidence-judge"
        - "wint-4090"
        - "wint-9050"

  # Enhancement opportunities from FUTURE-OPPORTUNITIES.md (5 items)
  - request_id: "wint-4090-enh-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] Human Summary Output Format and Location Unspecified**
        - **Finding**: The agent produces a machine-readable `ac-verdict.json` but the format and storage location of the human-readable summary is not specified — could be inline in agent response or a companion `ac-verdict-summary.md` artifact
        - **Impact**: Low - ambiguity could lead to inconsistent implementations across agent versions
        - **Effort**: Low - define summary output in agent Output section
        - **Recommendation**: Define a `## Human Summary` section in the completion output (inline, not a file) to keep artifact surface minimal; defer formal artifact definition to WINT-4150
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "output-format"
        - "evidence-judge"
        - "wint-4090"
        - "wint-4150"

  - request_id: "wint-4090-enh-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] Evidence Type Taxonomy Extension Candidates**
        - **Finding**: The current evidence type taxonomy (test, command, e2e, http) could be extended with: `log` (structured log output), `screenshot` (E2E visual assertion), `migration` (DB state artifact before/after)
        - **Impact**: Low - current taxonomy covers the primary evidence types; extensions add coverage for advanced workflows
        - **Effort**: Medium - requires schema extension in WINT-4150 and update to evidence strength classification rules
        - **Recommendation**: Defer to WINT-4150 schema standardization; document log, screenshot, migration as candidate types in that story's FUTURE-OPPORTUNITIES
      additional_tags:
        - "non-blocking"
        - "future-proofing"
        - "evidence-taxonomy"
        - "schema-extension"
        - "evidence-judge"
        - "wint-4090"
        - "wint-4150"

  - request_id: "wint-4090-enh-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] Subjective Language Blocklist Could Be Extended**
        - **Finding**: The current blocklist (appears, seems, should, looks) could be expanded to include: possibly, probably, might, expect, assume — all indicate uncertainty that weakens evidence quality
        - **Impact**: Low - expanded blocklist catches more vague evidence descriptions
        - **Effort**: Low - extend the list in ST-3 as a non-breaking addition
        - **Recommendation**: Implementer may expand during ST-3 without violating story scope; additions are additive and do not change ACCEPT/CHALLENGE/REJECT decision logic
      additional_tags:
        - "non-blocking"
        - "ux-polish"
        - "evidence-evaluation"
        - "subjective-language"
        - "evidence-judge"
        - "wint-4090"

  - request_id: "wint-4090-enh-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] ST-6 Test Fixtures as Candidate for Shared Phase 4 Regression Harness**
        - **Finding**: ST-6 creates canonical EVIDENCE.yaml fixtures with expected ac-verdict.json outputs in `_implementation/test-fixtures/`; these could be promoted to a shared regression harness reusable across Phase 4 agents and WINT-9050 LangGraph port
        - **Impact**: Medium - reduces test setup cost for WINT-9050 and future evidence-judge iterations
        - **Effort**: Medium - requires extracting fixtures to a shared path (e.g., `tests/fixtures/evidence-judge/`) with clear naming conventions
        - **Recommendation**: Tag ST-6 fixture creation as a candidate for shared harness promotion; defer formal promotion to WINT-4150 or WINT-9050 when the LangGraph port begins
      additional_tags:
        - "non-blocking"
        - "integration"
        - "test-harness"
        - "evidence-judge"
        - "wint-4090"
        - "wint-9050"
        - "wint-4150"

  - request_id: "wint-4090-enh-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-4090"
      category: "future-opportunities"
      content: |
        **[WINT-4090] proof_required Should Be Required for CHALLENGE/REJECT Verdicts**
        - **Finding**: `proof_required` field in ac-verdict.json is documented as optional, but for CHALLENGE and REJECT verdicts it is the primary actionable output for the implementer — making it optional reduces the utility of the verdict
        - **Impact**: Low - implementers may not provide proof_required, leaving the verdict less actionable
        - **Effort**: Low - make proof_required required (non-nullable) for CHALLENGE and REJECT verdicts in the ST-1 schema design
        - **Recommendation**: Consider making proof_required required for CHALLENGE and REJECT verdicts at ST-1 schema design time; this is a non-breaking addition to the provisional schema before WINT-4150 standardizes it
      additional_tags:
        - "non-blocking"
        - "future-proofing"
        - "schema-constraint"
        - "ac-verdict-json"
        - "evidence-judge"
        - "wint-4090"
        - "wint-4150"

summary:
  total_requests: 10
  gaps: 5
  enhancements: 5
  note: "0 findings were promoted to ACs — no MVP-critical gaps found. All 10 findings are non-blocking."

notes: |
  All KB write requests are for genuinely non-blocking findings from the elaboration phase.
  These represent future improvement opportunities that do not impact MVP delivery of WINT-4090.

  No MVP-critical gaps were found. No ACs were added to WINT-4090.md.

  Categories:
  - Edge Cases: 4 (warning count ambiguity, idempotency, spawned_by traceability, WINT-9050 depends_on typo)
  - Observability: 1 (schema_version for downstream consumers)
  - UX Polish: 2 (human summary format, subjective language blocklist expansion)
  - Future-Proofing: 2 (evidence type taxonomy, proof_required constraint tightening)
  - Integration / Test Harness: 1 (ST-6 fixtures as shared regression harness candidate)
  - Data Integrity (PM action): 1 (WINT-9050 depends_on correction)
