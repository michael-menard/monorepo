# KB Write Queue for WINT-9020
# Generated by elab-autonomous-decider
# To be processed by kb-writer agent or orchestration system

story_id: WINT-9020
generated_at: "2026-02-20T00:00:00Z"

# 8 non-blocking findings queued for KB writes
# (4 gaps promoted to ACs: AC-18, AC-19, AC-20, AC-21)
kb_write_requests:

  # ---- Future Opportunities (8 items from FUTURE-OPPORTUNITIES.md) ----

  - request_id: "wint-9020-fo-1"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Expose databaseStatus in DocSyncResultSchema for Downstream Graph Routing**
        - **Finding**: `DocSyncResultSchema` does not include a `databaseStatus` field. The database query status (`success | timeout | connection_failed | unavailable`) is written to SYNC-REPORT.md but unavailable to downstream LangGraph nodes without parsing the report file.
        - **Impact**: Medium — WINT-9060 (batch-coordinator graph) may need to branch on DB availability for retry routing; without the field this requires report parsing
        - **Effort**: Small — add `databaseStatus: z.enum(['success', 'timeout', 'connection_failed', 'unavailable']).optional()` to `DocSyncResultSchema`, update `parseSyncReport()`, add test
        - **Recommendation**: Add during WINT-9060 design phase if graph needs to branch on DB availability. If graph only needs sync success/failure, the field is not needed.
      additional_tags:
        - "non-blocking"
        - "schema-enhancement"
        - "doc-sync"
        - "langgraph"
        - "wint-9060"
        - "wint-9020"

  - request_id: "wint-9020-fo-2"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Formal DocSyncMcpBridge Interface in @repo/workflow-logic**
        - **Finding**: MCP tool functions are passed as optional config parameters in `DocSyncConfigSchema`. This creates a loose contract — any consumer can pass any function signature without a formal interface.
        - **Impact**: Low for single consumer (WINT-9020). Medium when a second LangGraph node needs postgres-knowledgebase MCP tools — the injection contract becomes a shared concern.
        - **Effort**: Small-to-medium — define `DocSyncMcpBridgeSchema` in `@repo/workflow-logic`, update all consumers
        - **Recommendation**: Formalize `DocSyncMcpBridgeSchema` as a shared Zod schema in `@repo/workflow-logic` when a second LangGraph node needs postgres-knowledgebase MCP tools. Not justified for a single consumer.
      additional_tags:
        - "non-blocking"
        - "architecture-pattern"
        - "doc-sync"
        - "mcp-injection"
        - "workflow-logic"
        - "wint-9020"

  - request_id: "wint-9020-fo-3"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] AST-Based Markdown Editing for Phase 4 (Replace Regex with remark)**
        - **Finding**: Phase 4 uses regex-based surgical table row updates in `docs/workflow/phases.md` and `docs/workflow/README.md`. This approach is fragile to table format changes, new columns, or documentation restructuring.
        - **Impact**: Medium — regex approach will break if documentation table structure changes; `remark` + `remark-gfm` provides format-agnostic table editing via AST
        - **Effort**: Medium — add `remark`/`remark-gfm` dependency to orchestrator; rewrite Phase 4 table manipulation; update tests to use real document fixtures
        - **Recommendation**: Replace regex table manipulation with `remark` AST library when SYNC-REPORT.md reports recurring table update failures, or when documentation structure changes and regex approach breaks. Not urgent for initial implementation.
      additional_tags:
        - "non-blocking"
        - "technical-debt"
        - "doc-sync"
        - "phase-4"
        - "remark"
        - "markdown-ast"
        - "wint-9020"

  - request_id: "wint-9020-fo-4"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] nodes/sync/ Domain Convention for Future Sync Nodes**
        - **Finding**: When a second file is added to `nodes/sync/`, the distinction between `nodes/sync/` and `nodes/workflow/` will require a documented convention. Currently `nodes/workflow/` contains a mix of workflow automation and sync-style nodes.
        - **Impact**: Trivial — affects only directory organization and onboarding clarity
        - **Effort**: Trivial — comment/documentation only
        - **Recommendation**: Define convention when the second `nodes/sync/` file is created: `nodes/sync/` for state-synchronization nodes (doc-sync, agent-registry-sync, kb-sync); `nodes/workflow/` for workflow-state-advancement nodes (checkpoint, stage-movement, decision-callback, index-node). Document in `nodes/sync/index.ts` header comment.
      additional_tags:
        - "non-blocking"
        - "convention"
        - "doc-sync"
        - "nodes-sync"
        - "architecture"
        - "wint-9020"

  - request_id: "wint-9020-fo-5"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Story-Scoped SYNC-REPORT.md Path to Prevent Concurrent Write Collisions**
        - **Finding**: The default `reportPath` is `SYNC-REPORT.md` in `workingDir`. If WINT-9060 triggers parallel doc-sync runs for multiple stories, concurrent runs will overwrite each other's SYNC-REPORT.md.
        - **Impact**: Low for WINT-9020 (single-run context). Medium for WINT-9060 concurrent batch runs.
        - **Effort**: Trivial for WINT-9020 (reportPath is already configurable via config). Minor for WINT-9060 (set reportPath per-run in graph config).
        - **Recommendation**: Before WINT-9060 runs concurrent doc-sync nodes, configure per-run `reportPath` values: `<workingDir>/SYNC-REPORT-<storyId>-<timestamp>.md`. The mitigation is already available via existing config — WINT-9060 just needs to exercise it.
      additional_tags:
        - "non-blocking"
        - "concurrency-safety"
        - "doc-sync"
        - "report-path"
        - "wint-9060"
        - "wint-9020"

  - request_id: "wint-9020-fo-6"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Remove nodes/workflow/doc-sync.ts Subprocess Wrapper (Post-WINT-9060)**
        - **Finding**: The deprecated subprocess wrapper at `nodes/workflow/doc-sync.ts` remains in the codebase after WINT-9020 (AC-14 explicitly retains it). It is re-exported under the same names as the new node, creating a dual-export surface.
        - **Impact**: Low while the deprecation is fresh; increases maintenance surface if left for multiple release cycles
        - **Effort**: Small — file deletion + import cleanup in `nodes/workflow/index.ts` and `nodes/index.ts`; remove subprocess-specific tests
        - **Recommendation**: Schedule cleanup story after WINT-9060 UAT PASS confirms batch-coordinator graph uses `nodes/sync/doc-sync.ts`. Steps: (1) search all imports of `nodes/workflow/doc-sync`, (2) remove file, (3) update `nodes/workflow/index.ts`, (4) remove `nodes/workflow/__tests__/doc-sync.test.ts`.
      additional_tags:
        - "non-blocking"
        - "technical-debt"
        - "doc-sync"
        - "cleanup"
        - "wint-9060"
        - "wint-9020"

  - request_id: "wint-9020-fo-7"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Database-Sourced Documentation Generation for DB-Managed Agents (Phase 4 Extension)**
        - **Finding**: Phase 2 merges file frontmatter with DB metadata. A natural extension: Phase 4 could generate documentation rows for agents that exist in the DB but have no corresponding file in `.claude/agents/` — i.e., programmatically-managed "database-only" components.
        - **Impact**: Medium — relevant when `workflow.components` table contains agents managed programmatically rather than via `.agent.md` files (expected Phase 7+ concern)
        - **Effort**: Medium — Phase 2 DB merge logic extension, Phase 4 new path, test additions
        - **Recommendation**: Implement when `workflow.components` database table contains agents without corresponding `.agent.md` files. SKILL.md Step 2.3 hints at this path. Not applicable for MVP WINT-9020 delivery.
      additional_tags:
        - "non-blocking"
        - "feature-extension"
        - "doc-sync"
        - "phase-4"
        - "database-sourced"
        - "future-work"
        - "wint-9020"

  - request_id: "wint-9020-fo-8"
    kb_write_request:
      entry_type: finding
      source_stage: elab
      story_id: "WINT-9020"
      category: "future-opportunities"
      content: |
        **[WINT-9020] Real-Time Sync Trigger via LangGraph Event System (Architecture Evolution)**
        - **Finding**: The doc-sync node is currently invoked on-demand as an explicit graph step. A more powerful model: wire it as a reactive trigger that fires automatically after any agent file write within a LangGraph graph run — a conditional edge pattern.
        - **Impact**: High (architectural) — changes how doc-sync is wired into all future graphs; transparent to orchestration logic
        - **Effort**: Large — requires graph-level event routing design, conditional edge implementation; likely a dedicated story
        - **Recommendation**: Implement in Phase 9+ orchestration work after WINT-9060 establishes the base graph pattern. Pattern: `if (state.agentFilesModified) → docSyncNode → next_step` conditional edge in graph definition.
      additional_tags:
        - "non-blocking"
        - "architecture-evolution"
        - "doc-sync"
        - "langgraph"
        - "event-driven"
        - "wint-9060"
        - "wint-9020"

summary:
  total_requests: 8
  gaps_promoted_to_ac: 4
  kb_entries_queued: 8
  ac_numbers_added: [18, 19, 20, 21]
  note: "4 MVP-critical gaps promoted to ACs (AC-18 through AC-21). 8 FO items queued for KB writes."
