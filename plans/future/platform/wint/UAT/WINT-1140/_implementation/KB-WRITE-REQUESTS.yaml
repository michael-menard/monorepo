# KB Write Requests for WINT-1140
# Generated by elab-autonomous-decider
# These requests should be processed by kb-writer agent

story_id: "WINT-1140"
generated_at: "2026-02-16T22:00:00Z"
total_requests: 10

requests:
  # NON-BLOCKING GAPS

  - request_id: 1
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "edge-case"
    content: |
      **[WINT-1140] Edge Case: Stale worktree_id pointing to non-active DB record**

      - **Finding**: CHECKPOINT.yaml may contain a worktree_id that references a DB record in merged or abandoned state (not active)
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Add a check: if worktree_id found in CHECKPOINT.yaml but DB record status is not active, treat as "no active worktree" and create a new one. Log a warning.
      - **Context**: Current AC-3 only handles matching active DB records. A stale worktree_id pointing to a non-active record could cause confusing behavior during story resume.
    additional_tags:
      - "wint"
      - "worktree"
      - "checkpoint"
      - "stale-reference"
      - "edge-case"

  - request_id: 2
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "edge-case"
    content: |
      **[WINT-1140] Edge Case: Orphaned DB record — worktree path no longer exists on disk**

      - **Finding**: Worktree path on disk no longer exists but DB record is active (orphaned DB record from a crashed session with no cleanup)
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Add a pre-flight path-existence check before calling /wt:switch to surface a clearer error message instead of a confusing wt-switch failure. Full cleanup deferred to WINT-1150.
      - **Context**: Deferred to WINT-1150 (cleanup) but WINT-1140 could add path-existence pre-check as an improvement to error messaging.
    additional_tags:
      - "wint"
      - "worktree"
      - "orphaned-record"
      - "cleanup"
      - "wint-1150"
      - "edge-case"

  - request_id: 3
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "edge-case"
    content: |
      **[WINT-1140] Edge Case: worktree_register conflict error vs generic MCP failure not distinguished**

      - **Finding**: worktree_register can return a conflict error (duplicate path) vs a generic MCP failure — AC-8 treats both as "null"
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Future iteration could distinguish conflict vs failure in the graceful degradation message to give the user more actionable information. Requires worktree_register to return structured errors (WINT-1130 scope).
      - **Context**: Better error differentiation would make AC-8 degradation messages more actionable. Blocked on WINT-1130 returning structured error types.
    additional_tags:
      - "wint"
      - "worktree"
      - "error-handling"
      - "structured-errors"
      - "wint-1130"
      - "edge-case"

  - request_id: 4
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "edge-case"
    content: |
      **[WINT-1140] Edge Case: Combined --gen + --skip-worktree flags not tested**

      - **Finding**: No test for --gen + --skip-worktree combined flags (AC-5 + AC-7)
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Test plan covers --gen and --skip-worktree independently but not in combination. Add a matrix entry for combined flag behavior.
      - **Context**: The interaction of story generation (--gen) with worktree bypass (--skip-worktree) is an untested combination that could surface unexpected behavior.
    additional_tags:
      - "wint"
      - "worktree"
      - "testing"
      - "combined-flags"
      - "test-matrix"
      - "edge-case"

  # ENHANCEMENT OPPORTUNITIES

  - request_id: 5
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "ux-polish"
    content: |
      **[WINT-1140] UX Enhancement: Human-readable timestamp in 3-option warning (AC-4)**

      - **Finding**: UX polish: The 3-option warning (AC-4) could include time elapsed since the worktree was registered, not just the registered timestamp
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Show "Registered 2 days ago" in addition to or instead of raw ISO datetime. More human-readable for decision-making.
      - **Context**: Current AC-4 shows ISO datetime. Relative time display (e.g., "2 days ago") is more intuitive for users deciding whether to switch or take over an existing worktree.
    additional_tags:
      - "wint"
      - "worktree"
      - "ux"
      - "relative-time"
      - "cli-output"

  - request_id: 6
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "ux-polish"
    content: |
      **[WINT-1140] UX Enhancement: Extended CHECKPOINT.yaml fields for debugging**

      - **Finding**: UX polish: Step 1.3 could log the worktree path and branch name to CHECKPOINT.yaml as additional fields beyond worktree_id
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Add worktree_path and worktree_branch as optional CHECKPOINT.yaml fields. Would allow quick inspection of which worktree/branch is active without querying the DB.
      - **Context**: Currently only worktree_id is stored in CHECKPOINT.yaml. Storing path and branch alongside it would simplify debugging and session inspection.
    additional_tags:
      - "wint"
      - "worktree"
      - "checkpoint"
      - "debugging"
      - "developer-experience"

  - request_id: 7
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "performance"
    content: |
      **[WINT-1140] Performance Enhancement: Short-circuit worktree_get_by_story when --skip-worktree is set**

      - **Finding**: Performance: worktree_get_by_story is called unconditionally on every dev-implement-story invocation, even for short dry-run or re-resume flows
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: AC-5 should explicitly state no MCP calls are made (not just no DB rows created) when --skip-worktree is set. Add short-circuit logic to avoid the MCP call entirely.
      - **Context**: Currently the story describes --skip-worktree as bypassing DB row creation but does not explicitly prevent the worktree_get_by_story lookup. Avoiding the call entirely saves a round-trip.
    additional_tags:
      - "wint"
      - "worktree"
      - "performance"
      - "short-circuit"
      - "mcp-calls"

  - request_id: 8
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "observability"
    content: |
      **[WINT-1140] Observability: Telemetry logging for worktree pre-flight outcomes**

      - **Finding**: No telemetry logging for worktree pre-flight outcomes (new create, resume, take-over, abort)
      - **Impact**: Medium | **Effort**: Low
      - **Recommendation**: Flag for WINT-3070 scope — when dev-implement-story is instrumented for telemetry, include the worktree pre-flight step outcome (create/resume/takeover/abort/skip).
      - **Context**: WINT-3020 will add telemetry logging generally. The worktree pre-flight step should be included in WINT-3070 scope to track worktree lifecycle events.
    additional_tags:
      - "wint"
      - "worktree"
      - "telemetry"
      - "observability"
      - "wint-3020"
      - "wint-3070"

  - request_id: 9
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "integration"
    content: |
      **[WINT-1140] Integration: Cross-machine worktree detection not addressed**

      - **Finding**: The autonomy auto-select for moderate/aggressive (AC-9) only handles same-machine detection. Cross-machine detection (different hostname in DB record) is not addressed.
      - **Impact**: Low | **Effort**: Medium
      - **Recommendation**: Future: add machine identifier to worktree_register call. Cross-machine conflict handling is a WINT-1160 concern (conflict prevention UI).
      - **Context**: Current AC-9 auto-selects option (a) for same-machine conflicts. Cross-machine conflicts (different hostname) need machine ID tracking in worktree_register — deferred to WINT-1160.
    additional_tags:
      - "wint"
      - "worktree"
      - "cross-machine"
      - "conflict-detection"
      - "wint-1160"

  - request_id: 10
    entry_type: finding
    source_stage: elab
    story_id: "WINT-1140"
    category: "ux-polish"
    content: |
      **[WINT-1140] UX Enhancement: Numbered aliases for 3-option prompt (AC-4)**

      - **Finding**: Accessibility of output: The 3-option prompt (AC-4) uses lowercase a/b/c. A future iteration could add numbered options (1/2/3) as aliases.
      - **Impact**: Low | **Effort**: Low
      - **Recommendation**: Add 1/2/3 as aliases for a/b/c in the confirmation prompt input to reduce typo risk and improve accessibility. Minor UX polish.
      - **Context**: CLI prompts with letter options (a/b/c) are prone to typos and may be less accessible than numbered options. Aliases would not change current behavior.
    additional_tags:
      - "wint"
      - "worktree"
      - "ux"
      - "accessibility"
      - "cli-input"

summary:
  total_requests: 10
  by_category:
    edge-case: 4
    ux-polish: 3
    performance: 1
    observability: 1
    integration: 1
  by_impact:
    low: 9
    medium: 1
    high: 0
  by_effort:
    low: 9
    medium: 1
    high: 0

notes: |
  All KB write requests are for non-blocking findings from FUTURE-OPPORTUNITIES.md.
  MVP-critical gaps (wt-switch interactive-only, wt-new interactive-only) were resolved as AC-10 and AC-11 additions to the story.

  These findings should be logged to KB for future prioritization and reference.
  Cross-machine conflict handling is explicitly deferred to WINT-1160.
  Telemetry instrumentation is deferred to WINT-3020/WINT-3070.
  Orphaned-record cleanup is deferred to WINT-1150.
