schema: 1
story_id: WINT-2050
version: 1
timestamp: '2026-03-04T18:25:00Z'

acceptance_criteria:
  - ac_id: AC-1
    ac_text: ADR-LOG.md reader extracts active ADRs into structured architecture/active-adrs pack
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "extractAdrEntries unit test parses ADR entries; HP-1 integration test confirms active-adrs written with packType=architecture, content.adrs is array, content.summary is string"
      - type: command
        command: pnpm --filter @repo/mcp-tools test
        result: PASS

  - ac_id: AC-2
    ac_text: KB lessons queried per domain area (backend, frontend, testing, workflow) — 4 lessons_learned packs written
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "HP-2 integration test verifies lessons-backend/frontend/testing/workflow packs written with packType=lessons_learned using mock kbQueryFn with fixture entries"
      - type: command
        command: pnpm --filter @repo/mcp-tools test
        result: PASS

  - ac_id: AC-3
    ac_text: blockers-known pack written from constraint/blocker KB entries
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "HP-3 integration test confirms blockers-known pack exists with content.blockers array and content.summary string"

  - ac_id: AC-4
    ac_text: All packs use TTL 2592000 (30 days) — idempotent upsert (no duplicate rows on second run)
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "HP-3/HP-4 TTL test confirms expiresAt within 60s of 30-day window; HP-5 idempotency test confirms COUNT before = COUNT after second run"

  - ac_id: AC-5
    ac_text: Resilient write loop — single pack failure does not abort; EC-1 KB unavailable still writes ADR pack; EC-2 empty KB writes empty lessons packs
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "EC-1 unit test: failingKbQuery — ADR pack attempted, no exception thrown, succeeded+failed=attempted; EC-3 unit test: second put returns null → failed incremented; EC-2 integration: empty KB writes 6 packs with empty lessons arrays"

  - ac_id: AC-6
    ac_text: Content capped at 8000 chars (JSON.stringify); all packs include summary field
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "ED-1 integration test verifies all pack content < 8000 chars; ED-1 high-volume stress test with 200 entries still caps correctly; extractAdrPack unit test confirms 8000-char cap triggers trimming"

  - ac_id: AC-7
    ac_text: Script runnable via pnpm tsx — JSDoc documents DATABASE_URL (5432) and KB_DATABASE_URL (5433); exits 0
    status: PASS
    evidence_items:
      - type: command
        command: "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lego_dev pnpm tsx packages/backend/mcp-tools/src/scripts/populate-domain-kb.ts"
        result: "EXIT 0 — 6 packs written: active-adrs, lessons-backend, lessons-frontend, lessons-testing, lessons-workflow, blockers-known. KB_DATABASE_URL fallback handled gracefully."
      - type: test
        path: packages/backend/mcp-tools/src/scripts/populate-domain-kb.ts
        description: "JSDoc at top of file documents DATABASE_URL (port 5432) and KB_DATABASE_URL (port 5433) requirements; isMain check and process.exit pattern identical to populate-project-context.ts"

  - ac_id: AC-8
    ac_text: All 6 expected packs present after full run — active-adrs, lessons-backend, lessons-frontend, lessons-testing, lessons-workflow, blockers-known
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "HP-6 integration test selects all 6 pack_keys and confirms COUNT=6; result.attempted=6, result.succeeded=6, result.failed=0"

  - ac_id: AC-9
    ac_text: kbQueryFn is injectable — tests pass mock, no real port 5433 connection attempted
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "AC-9 unit test: populateDomainKb({ kbQueryFn: mockKb }) — mockKb called once, no pg import to port 5433; all integration tests pass mockKbQuery fixture"

  - ac_id: AC-10
    ac_text: Only packType values 'architecture' and 'lessons_learned' used — no other enum values
    status: PASS
    evidence_items:
      - type: test
        path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
        description: "ED-2 integration test SELECT DISTINCT pack_type returns only ['architecture', 'lessons_learned']; type check confirms source only uses these two packType literals"

touched_files:
  - path: packages/backend/mcp-tools/src/scripts/populate-domain-kb.ts
    action: created
    lines: 430
    description: "Main populate script — reads ADR-LOG.md, queries KB via injectable kbQueryFn, writes 6 context packs to wint.context_packs via contextCachePut()"
  - path: packages/backend/mcp-tools/src/scripts/__tests__/populate-domain-kb.test.ts
    action: created
    lines: 280
    description: "Unit + integration tests — 21 tests covering all ACs: mocked contextCachePut, injectable kbQueryFn, real DB at port 5432"

commands_run:
  - command: pnpm --filter @repo/mcp-tools check-types
    result: SUCCESS
    output: Type check passes with 0 errors
    timestamp: '2026-03-04T18:20:00Z'
  - command: pnpm --filter @repo/mcp-tools test
    result: SUCCESS
    output: "350 tests passed (36 test files) — 21 new tests in populate-domain-kb.test.ts"
    timestamp: '2026-03-04T18:22:31Z'
  - command: pnpm --filter @repo/mcp-tools build
    result: SUCCESS
    output: Build complete
    timestamp: '2026-03-04T18:23:00Z'
  - command: "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lego_dev pnpm tsx packages/backend/mcp-tools/src/scripts/populate-domain-kb.ts"
    result: SUCCESS
    output: "6 packs written, exit 0: active-adrs, lessons-backend, lessons-frontend, lessons-testing, lessons-workflow, blockers-known"
    timestamp: '2026-03-04T18:24:00Z'

endpoints_exercised: []

test_summary:
  unit: { pass: 350, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "No UI surface (touches_frontend: false, endpoints: [], ui_touched: false). Backend populate script only — no HTTP routes, no browser flows. Integration tests against real lego_dev DB cover the data layer."
  config: null
  project: null
  mode: null
  tests_written: false
  results:
    total: 0
    passed: 0
    failed: 0
    skipped: 0

coverage:
  lines: null
  branches: null

notable_decisions:
  - "kbQueryFn injectable pattern: default implementation uses dynamic pg import (transitive dep via @repo/db) with @ts-expect-error to avoid requiring @types/pg in mcp-tools devDependencies"
  - "KB query fallback: when KB_DATABASE_URL not set or query fails, all 6 packs still written with empty lessons/blockers arrays — graceful degradation"
  - "Content cap via capContent(): trims array fields from the end until JSON.stringify < 8000 chars — prevents oversized JSONB writes"
  - "Only 2 files created — populate-domain-kb.ts and test — no existing files modified per story constraint"
  - "Test file uses mockKbQuery (vi.fn() fixture) for all integration tests — no real port 5433 connection needed in CI"

known_deviations:
  - "E2E tests exempt: story has no HTTP endpoints, no frontend surface, and ui_touched: false. Integration tests against real lego_dev DB at port 5432 are the appropriate test layer."
  - "defaultKbQueryFn uses @ts-expect-error for dynamic pg import — pg is a transitive runtime dep (via @repo/db) but @types/pg is not in mcp-tools devDependencies. This is acceptable since kbQueryFn is always mocked in tests."
