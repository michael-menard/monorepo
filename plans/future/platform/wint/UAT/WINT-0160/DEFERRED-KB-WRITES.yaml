---
# Deferred KB Writes for WINT-0160
# Generated: 2026-02-17
# Updated: 2026-02-17 (elab-autonomous-decider added future-opportunity findings)
# Reason: KB MCP tools not available during story generation
# Retry: Run kb_write_story when postgres-knowledgebase is accessible

writes:
  - table: stories
    operation: upsert
    conflict_key: story_id
    data:
      story_id: WINT-0160
      feature: wint
      title: "Validate and Harden doc-sync Agent for Production Readiness"
      story_dir: "plans/future/platform/wint/elaboration/WINT-0160"
      story_file: "plans/future/platform/wint/elaboration/WINT-0160/WINT-0160.md"
      story_type: chore
      points: 2
      priority: medium
      state: elaboration
      touches_backend: false
      touches_frontend: false
      touches_database: false
      touches_infra: false
      created_at: "2026-02-17"

  # --- Future-Opportunity Findings from Elaboration (elab-autonomous-decider) ---

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - edge-case
      - integration
    data:
      title: "MCP Tool Name Discrepancy: query- vs get- prefix requires live server verification"
      content: |
        - **Gap**: WINT-0150 architecture notes used 'workflow_get_phases'/'workflow_get_components'
          but the agent was updated with 'query-workflow-phases'/'query-workflow-components' names.
          The query- vs get- prefix divergence needs live server verification when the
          postgres-knowledgebase MCP server is accessible (gated on WINT-0080 UAT).
        - **Impact**: High | **Effort**: Low
        - **Recommendation**: During AC-1 implementation, verify both agent frontmatter and
          SKILL.md mcp_tools_available field against the registered server tool names. Update
          both files if names are incorrect. If server unavailable, add attestation note per
          AC-1 workaround and re-verify when WINT-0080 passes UAT.
      related_story: WINT-0080
      related_ac: AC-1

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - edge-case
      - documentation
    data:
      title: "SKILL.md version history missing 1.1.0 entry for WINT-0150 changes"
      content: |
        - **Gap**: The SKILL.md version history shows only 1.0.0 (2026-02-07). WINT-0150
          added database query integration (Steps 2.2-2.4, hybrid sync examples, database
          status tracking) but no 1.1.0 or 1.0.1 entry was added to reflect these changes.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Add a version history entry (e.g., 1.1.0 - 2026-02-16) to
          SKILL.md during WINT-0160 implementation as a completeness fix.

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - edge-case
      - documentation
    data:
      title: "SKILL.md mcp_tools_available may drift from agent frontmatter if AC-1 correction only targets agent"
      content: |
        - **Gap**: AC-1 explicitly targets agent frontmatter mcp_tools field. If tool names
          are wrong and the developer updates only the agent, SKILL.md mcp_tools_available
          will be out of sync.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Scope AC-1 correction to cover both files simultaneously —
          agent frontmatter mcp_tools AND SKILL.md mcp_tools_available.

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - edge-case
      - documentation
    data:
      title: "No formal attestation record for MCP tool names if server unavailable at implementation time"
      content: |
        - **Gap**: If the postgres-knowledgebase server is unavailable during WINT-0160
          implementation, AC-1 closes with an 'assumed correct' attestation but there is
          no formal record of when/why this attestation was made.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Consider adding a note to SKILL.md or a SYNC-REPORT template
          stating tool names were added by WINT-0150 and await live server verification
          (gated on WINT-0080 UAT passing).

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - documentation
      - future-work
    data:
      title: "doc-sync.agent.md Future Enhancements section lacks backlog linkage"
      content: |
        - **Gap**: The 'Future Enhancements' section (watch mode, configuration file,
          mermaid-cli, PR creation, intelligent diff) was written before WINT-0150 and
          does not distinguish which items are tracked in the WINT backlog vs. truly
          untracked future ideas.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: During WINT-0160 or a subsequent cleanup story, link 'Watch
          mode' to the WINT backlog or note it as a Phase 2+ item to prevent future workers
          from re-inventing already-tracked items.

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - documentation
      - edge-case
    data:
      title: "--check-only --force flag interaction precedence undocumented in agent file"
      content: |
        - **Gap**: The --force flag interaction with --check-only is documented in SKILL.md
          ('check-only takes precedence') but the agent file does not mention this precedence.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Add a note to the agent's Input table that '--check-only --force'
          processes all files without modifying (check-only takes precedence). Keeps agent
          self-documenting.

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - integration
      - enhancement
    data:
      title: "WINT-9020 would benefit from phase-by-phase MCP tool mapping in LangGraph Porting Notes"
      content: |
        - **Enhancement**: The AC-6 LangGraph Porting Notes section scopes MCP tools as a
          flat list. A phase-by-phase MCP tool mapping would be more actionable for
          WINT-9020 implementers (e.g., Phase 2 uses query-workflow-phases and
          query-workflow-components; other phases use no MCP tools).
        - **Impact**: Medium | **Effort**: Low
        - **Recommendation**: When writing AC-6, include an optional subsection mapping
          MCP tools to phases. This makes the porting guide more actionable for WINT-9020.
      related_story: WINT-9020

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - observability
      - documentation
    data:
      title: "doc-sync.agent.md lacks inline version history / Agent Changelog section"
      content: |
        - **Gap**: doc-sync.agent.md does not document its own version history inline
          (unlike some other WINT agents that have a 'Changelog' section in the body).
          When WINT-0170 integration note and LangGraph Porting Notes are added in
          WINT-0160, there will be no inline record of when they were added.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Consider adding a brief 'Agent Changelog' section:
          v1.0.0 (initial), v1.1.0 (WINT-0150 database query support), v1.2.0
          (WINT-0160 hardening + porting contract). Mirrors best practice in other WINT agents.

  - table: findings
    operation: insert
    entry_type: finding
    source_stage: elab
    story_id: WINT-0160
    category: future-opportunities
    tags:
      - non-blocking
      - ux-polish
      - style
    data:
      title: "SKILL.md pre-commit hook template uses emoji in echo statements"
      content: |
        - **Gap**: The SKILL.md pre-commit hook integration example uses emoji (checkmarks,
          X marks) in the bash script echo statements. Per project guidelines (CLAUDE.md),
          the codebase avoids emojis. This is a minor style inconsistency in the hook template.
        - **Impact**: Low | **Effort**: Low
        - **Recommendation**: Clean up hook template in SKILL.md to remove emoji from echo
          statements during WINT-0160 or next touch of the file.
