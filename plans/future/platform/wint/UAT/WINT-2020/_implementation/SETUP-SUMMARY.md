# WINT-2020 Setup Summary

**Story:** Create Context Pack Sidecar
**Phase:** Setup (Phase 0)
**Iteration:** 0
**Timestamp:** 2026-03-03T17:15:00Z

## Setup Status

✅ **SETUP COMPLETE**

### Actions Completed

1. **Story Status Updated**
   - From: `ready-to-work`
   - To: `in-progress`
   - Location: `plans/future/platform/wint/in-progress/WINT-2020/`

2. **Checkpoint Artifact Created**
   - File: `_implementation/CHECKPOINT.yaml`
   - Phase: setup
   - Iteration: 0
   - Max iterations: 3

3. **Scope Artifact Created**
   - File: `_implementation/SCOPE.yaml`
   - Touches: backend, packages, db, contracts
   - Risk flags: performance (high priority)
   - Summary: "Create context pack sidecar with cache-first assembly and MCP tool wrapper to optimize token usage"

4. **Story Frontmatter Updated**
   - Status: in-progress
   - Updated_at: 2026-03-03T17:15:00Z

## Story Context

**Type:** Feature
**Points:** 5
**Priority:** P1
**Phase:** 2

**Depends On:**
- WINT-2010 (role-pack sidecar) — provides structural pattern

**Scope Summary:**
- Creates new sidecar package: `packages/backend/sidecars/context-pack/`
- Implements cache-first context assembly with token budget enforcement
- Adds MCP tool wrapper for context_pack_get
- Modifies: `packages/backend/mcp-tools/`, `apps/api/knowledge-base/`, `packages/backend/database-schema/`

## Elaboration Status

✅ Elaboration: CONDITIONAL_PASS
- 2 gaps found and resolved
- 3 opportunities identified and logged
- 12 acceptance criteria with coherent decomposition
- Estimated token budget: 200,000+

## Key Constraints (from CLAUDE.md)

- Use Zod schemas for all types (required)
- No barrel files
- Use @repo/logger, not console
- Minimum 45% test coverage
- Named exports preferred
- Import UI components from @repo/ui (not individual paths)

## Next Steps

### Phase 1: Core Implementation (ST-1 through ST-3)
1. Review WINT-2010 role-pack sidecar pattern for structural guidance
2. Create `packages/backend/sidecars/context-pack/` directory structure
3. Implement `assembleContextPack` function with:
   - KB search integration (kb_search)
   - Cache get/put (contextCacheGet, contextCachePut)
   - Token estimation and trimming (AC-1, AC-5)
   - Repository snippet loading (AC-4)
   - Error handling and logging (AC-11)

### Phase 2: HTTP Handler & MCP Wrapper (ST-4, ST-5)
4. Implement POST /context-pack HTTP route handler
5. Implement context_pack_get MCP tool wrapper
6. Wire caching, TTL, and error responses

### Phase 3: Testing (ST-6, ST-7)
7. Unit tests: Zod validation, token budgets, cache keys, trimming logic
8. Integration tests: Real postgres-knowledgebase on port 5433
9. Test fixtures: validContextPacksRow, mockKbSearchResult, oversizedContextPack

### Phase 4: Verification
10. Verify all 12 ACs are satisfied
11. Run full test suite (minimum 45% coverage)
12. Type-check and lint all code

## Risk Notes

**Critical Dependency:**
- WINT-2010 must reach UAT before ST-5 (MCP tool) can begin
- If WINT-2010's pattern differs significantly, ST-5 decision (direct import vs HTTP call) may need revision

**Architectural Decision (Resolved):**
- MCP tool imports assembleContextPack directly from sidecar package
- Uni-directional dependency: mcp-tools → sidecars/context-pack
- Avoids bidirectional coupling

**Performance Risk:**
- Token estimation uses char-count/4 approximation (may underestimate)
- Cache-miss race condition mitigated with ON CONFLICT DO UPDATE
- Target cache hit latency: <100ms (AC-12)

**Integration Test Infrastructure:**
- Requires postgres-knowledgebase on port 5433 (per ADR-005)
- Test may be flaky due to concurrent race condition testing

## Elaboration Artifacts

- **ELAB.yaml:** Full elaboration audit (scope alignment, risk disclosure, ACs, test plan)
- **CLAUDE.md:** Project guidelines for code style, testing, and TypeScript patterns

---

Generated by: dev-setup-leader
Mode: implement (gen_mode: false)
