---
# Deferred Knowledge Base Writes for WINT-1100
# To be processed when KB becomes available
generated_at: "2026-02-15T21:15:00Z"
story_id: WINT-1100
---

deferred_writes:
  - operation: insert_or_update
    table: stories
    data:
      story_id: WINT-1100
      feature: wint
      title: "Create Shared TypeScript Types for Unified WINT Schema"
      story_dir: plans/future/platform/wint/WINT-1100
      story_file: plans/future/platform/wint/WINT-1100/WINT-1100.md
      story_type: feature
      points: 5
      priority: medium
      state: backlog
      touches_backend: true
      touches_frontend: false
      touches_database: false
      touches_infra: false
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'WINT-1100',
        'wint',
        'Create Shared TypeScript Types for Unified WINT Schema',
        'plans/future/platform/wint/WINT-1100',
        'plans/future/platform/wint/WINT-1100/WINT-1100.md',
        'feature',
        5,
        'medium',
        'backlog',
        true,
        false,
        false,
        false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;

retry_strategy:
  max_attempts: 3
  backoff_seconds: 60
  fallback: log_warning
