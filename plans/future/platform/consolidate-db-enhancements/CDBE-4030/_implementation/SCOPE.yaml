schema: 1
story_id: CDBE-4030
timestamp: "2026-03-19T02:57:35Z"
phase: setup
iteration: 0
gen_mode: false
touches:
  backend: true
  frontend: false
  packages: false
  db: true
  contracts: false
  ui: false
  infra: true
touched_paths_globs:
  - "apps/api/knowledge-base/src/**"
  - "apps/api/knowledge-base/src/db/migrations/**"
risk_flags:
  auth: false
  payments: false
  migrations: true
  external_apis: true
  security: false
  performance: true
summary: "Persistent background worker for embedding generation on knowledge tables with NOTIFY/LISTEN + polling fallback"
elaboration: completed
constraints:
  - "Use Zod schemas for all types — never use TypeScript interfaces"
  - "No barrel files (index.ts re-exports)"
  - "Use @repo/logger, not console.log"
  - "Minimum 45% test coverage"
  - "Named exports preferred"
  - "NOTIFY/LISTEN requires dedicated pg.Client, not connection pool"
  - "Reuse EmbeddingClient via createEmbeddingClient()"
  - "Use processBatchWithSplitting() for backfill batch processing"
  - "Include DB safety preamble (DO block) in all new migrations"
  - "pnpm db:generate fails (WINT-0040) — author migrations manually"
  - "Target DB: knowledgebase (port 5433)"
  - "No wint.* or kbar.* schema references"
  - "CDBE-4020 must merge before ST-1 implementation begins (blocker)"
