# Deferred KB Writes
# These writes failed because the KB was unavailable at story generation time.
# Retry when KB connection is available.

generated: "2026-02-20"
story_id: TELE-0010

writes:
  - table: stories
    operation: upsert
    data:
      story_id: TELE-0010
      feature: telemetry
      title: "Docker Telemetry Stack — Validate and Complete Local Observability Foundation"
      story_dir: plans/future/platform/telemetry/backlog/TELE-0010
      story_file: plans/future/platform/telemetry/backlog/TELE-0010/TELE-0010.md
      story_type: tech_debt
      points: 2
      priority: high
      state: backlog
      touches_backend: false
      touches_frontend: false
      touches_database: false
      touches_infra: true
    failure_reason: "psql not found; KNOWLEDGEBASE_URL not set"
    retry_sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'TELE-0010', 'telemetry',
        'Docker Telemetry Stack — Validate and Complete Local Observability Foundation',
        'plans/future/platform/telemetry/backlog/TELE-0010',
        'plans/future/platform/telemetry/backlog/TELE-0010/TELE-0010.md',
        'tech_debt', 2, 'high', 'backlog',
        false, false, false, true
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;

# TTL stamp — added by import-deferred-writes
processed_at: "2026-03-08T23:55:28.324Z"
imported: true
