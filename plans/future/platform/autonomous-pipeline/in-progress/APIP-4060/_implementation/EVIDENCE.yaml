schema: 1
story_id: "APIP-4060"
version: 1
timestamp: "2026-03-01T21:00:00Z"

acceptance_criteria:
  - ac_id: "AC-1"
    ac_text: "runKbFreshnessCheck(deps, config) function exists and accepts injectable deps ({ db, fs?, embeddingClient? }) and config"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "KbFreshnessCheckDeps interface with db, fs?, embeddingClient?, auditLogger, kbUpdate, batchQuery. runKbFreshnessCheck(deps, config) function signature implemented."

  - ac_id: "AC-2"
    ac_text: "Function queries KB entries where createdAt < now - staleDays and archived = false, processed in batches"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "calculateCutoffDate(staleDays) + batchQuery injection. Batch loop uses cutoff, limit, offset matching AC-14 direct Drizzle pattern."
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-5 test: batch processing respects batchSize — passes"

  - ac_id: "AC-3"
    ac_text: "For each candidate entry with a file path reference, function checks whether that file exists"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "FILE_PATH_REGEX = /(apps|packages|src)/[^\\s'\"`.,\\]]+\\.(ts|tsx|js|json|md)/g. extractFirstFilePath() + fsModule.existsSync(filePath)"
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "extractFirstFilePath tests + HP-2/HP-3/HP-4 test coverage — all pass"

  - ac_id: "AC-4"
    ac_text: "Entries referencing confirmed non-existent file paths are auto-archived via kb_update({ id, archived: true, archivedAt })"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-2 test: calls kbUpdate with archived:true and AuditLogger.logUpdate for each archived entry — PASS"

  - ac_id: "AC-5"
    ac_text: "Entries exceeding age threshold without non-existent file reference are flagged with stale-candidate tag"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-3 test: calls kbUpdate with stale-candidate tag (not archived) — PASS. Duplicate tag check also passes."

  - ac_id: "AC-6"
    ac_text: "In dry-run mode, no mutations are made — returns counts of what would be archived/flagged"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-1 test: returns counts without any kbUpdate calls, dry_run:true — PASS"

  - ac_id: "AC-7"
    ac_text: "Returns { archived_count, flagged_count, skipped_count, entries_scanned, duration_ms, dry_run, batches_processed, truncated }"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "KbFreshnessResult interface + return value with all 8 required fields including truncated boolean"
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-6 test: KbFreshnessResultSchema.parse(result) succeeds — PASS"

  - ac_id: "AC-8"
    ac_text: "KbFreshnessResultSchema exists in __types__/index.ts and validates the return value"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/__types__/index.ts"
        description: "KbFreshnessResultSchema with all 8 fields defined with z.object()"
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-6 schema validation test — PASS"

  - ac_id: "AC-9"
    ac_text: "KbFreshnessConfigSchema exists in __types__/index.ts with defaults (staleDays: 90, dryRun: false, batchSize: 500)"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/__types__/index.ts"
        description: "KbFreshnessConfigSchema with staleDays:90, dryRun:false, batchSize:500, maxDurationMs:300000 defaults"
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-7 test: KbFreshnessConfigSchema.parse({ dryRun: true }) applies defaults — PASS"

  - ac_id: "AC-10"
    ac_text: "Unit tests exist covering: dry-run, non-existent paths archived, existent paths flagged, no-reference skipped, batchSize respected"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "20 tests: HP-1 through HP-7 (happy path), EC-1 through EC-3 (error cases), ED-1 through ED-3 (edge cases) — all 20 PASS"
      - type: command
        command: "pnpm --filter @repo/orchestrator test"
        result: "20/20 freshness tests PASS"

  - ac_id: "AC-11"
    ac_text: "Function is exported from freshness/index.ts as named export (no barrel file)"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "export async function runKbFreshnessCheck — named export, no barrel file"

  - ac_id: "AC-12"
    ac_text: "All archival mutations wrapped in try/catch — per-entry failure does not abort batch"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "EC-1 test: continues processing when kbUpdate throws on entry 3 — PASS. skipped_count includes failed entry."

  - ac_id: "AC-13"
    ac_text: "Function is time-bounded: halts at batch boundary when duration_ms > maxDurationMs, returns truncated:true"
    status: PASS
    evidence_items:
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "ED-1 test: returns truncated:true and partial counts when maxDurationMs exceeded — PASS"

  - ac_id: "AC-14"
    ac_text: "Batch scan uses direct Drizzle queries (not kb_list) — batchQuery injection is authoritative"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "FreshnessBatchQueryFn type documents: db.select().from(knowledgeEntries).where(and(lt(createdAt, cutoff), eq(archived, false))).limit(batchSize).offset(offset). Fully injectable — no kb_list usage."

  - ac_id: "AC-15"
    ac_text: "In-memory batch entry serves as previousEntry for AuditLogger.logUpdate — no separate pre-mutation fetch"
    status: PASS
    evidence_items:
      - type: code
        path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
        description: "await deps.auditLogger.logUpdate(entry.id, entry, newEntry) — entry is from batch, newEntry is kb_update return value"
      - type: test
        path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
        description: "HP-2 test verifies AuditLogger.logUpdate called 3 times with in-memory entries — PASS"

touched_files:
  - path: "packages/backend/orchestrator/src/nodes/freshness/__types__/index.ts"
    action: created
    lines: 73
    description: "KbFreshnessConfigSchema and KbFreshnessResultSchema Zod schemas with defaults"
  - path: "packages/backend/orchestrator/src/nodes/freshness/index.ts"
    action: created
    lines: 371
    description: "runKbFreshnessCheck function — batch scan, archive/flag mutations, dry-run, time truncation, full dep injection"
  - path: "packages/backend/orchestrator/src/nodes/freshness/__tests__/freshness.test.ts"
    action: created
    lines: 505
    description: "20 unit tests covering all AC-10 scenarios plus edge cases"

commands_run:
  - command: "pnpm --filter @repo/logger build"
    result: SUCCESS
    output: "Logger built to dist/"
    timestamp: "2026-03-01T21:00:00Z"
  - command: "pnpm --filter @repo/orchestrator check-types"
    result: SUCCESS
    output: "Zero freshness-specific type errors. 44 pre-existing errors in unrelated files (drizzle-orm, @repo/database-schema, @repo/workflow-logic)."
    timestamp: "2026-03-01T21:05:00Z"
  - command: "pnpm --filter @repo/orchestrator test"
    result: SUCCESS
    output: "20/20 freshness tests PASS. 4 pre-existing failures in structurer-affinity.test.ts (unrelated APIP-3050 feature)."
    timestamp: "2026-03-01T21:10:00Z"

endpoints_exercised: []

test_summary:
  unit: { pass: 20, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "No API endpoints, no UI, no browser-observable surface. Pure backend library function. Cron scheduling is APIP-3090's responsibility. story_type: backend-only with strategy: unit."
  config: null
  project: null
  mode: null
  tests_written: false
  results:
    total: 0
    passed: 0
    failed: 0
    skipped: 0
  failed_tests: []
  config_issues: []
  videos: []
  screenshots: []

coverage:
  lines: null
  branches: null
  notes: "Unit tests cover all 15 ACs across 20 test cases (HP-1-7, EC-1-3, ED-1-3)"

notable_decisions:
  - "Used full dependency injection (batchQuery, kbUpdate, auditLogger, fs, embeddingClient) to avoid build dependency on @repo/knowledge-base (not built in worktree). Production callers pass actual implementations."
  - "FILE_PATH_REGEX uses global flag with lastIndex reset on each call to avoid stateful regex issues across entries."
  - "stale-candidate tag deduplication: check before appending to avoid duplicate tags on re-runs."
  - "AuditLogger failures soft-failed with try/catch — matches AuditLogger.softFail behavior from retention-policy.ts pattern."
  - "FreshnessBatchQueryFn abstraction documents the equivalent direct Drizzle query (AC-14) in JSDoc."

known_deviations:
  - "E2E tests exempt: no browser-testable surface. runKbFreshnessCheck is a pure async function called by a cron scheduler (APIP-3090)."
  - "44 pre-existing type errors in @repo/orchestrator exist in unrelated files (drizzle-orm, @repo/database-schema, @repo/workflow-logic missing from build). These predate this story and are not caused by the freshness node."
  - "4 pre-existing test failures in structurer-affinity.test.ts (APIP-3050 feature) — unrelated to AC-10."

token_summary:
  setup: { in: 0, out: 0 }
  plan: { in: 0, out: 0 }
  execute: { in: 45000, out: 18000 }
