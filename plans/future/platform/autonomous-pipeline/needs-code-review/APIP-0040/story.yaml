id: "APIP-0040"
title: "Model Router v1 with Rate Limiting and Token Budgets"
status: needs-code-review
priority: medium
phase: 0
feature: "Extend existing model providers with rate limiting (token bucket per provider), token budgets per story, DB-backed model assignment config (wint.model_assignments), and Ollama → OpenRouter → Claude escalation chain"
goal: "Enable cost-controlled, provider-aware model dispatch with configurable escalation — no code changes required to swap models."
depends_on: ["APIP-0010"]
endpoints: []
infrastructure:
  - "Aurora PostgreSQL migration: wint.model_assignments table"
risk_notes: "Token bucket accuracy under concurrency; escalation chain must not silently swallow errors; budget cap hard-stop logic needs circuit breaker integration."
sizing_warning: false
created: "2026-02-25T00:00:00Z"
