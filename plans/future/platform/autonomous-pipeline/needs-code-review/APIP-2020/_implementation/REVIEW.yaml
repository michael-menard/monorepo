schema: 1
story_id: APIP-2020
iteration: 1
verdict: FAIL
timestamp: "2026-03-01T04:30:00Z"

findings:
  # Critical Issues - Code Style Violations
  - severity: error
    category: style
    file: apps/api/lego-api/server.ts
    line: 119
    message: "Uses console.error instead of @repo/logger per CLAUDE.md requirement"
    fix: "Import logger from @repo/logger and use logger.error() instead of console.error()"

  - severity: error
    category: style
    file: apps/api/lego-api/server.ts
    line: 138
    message: "Uses console.log instead of @repo/logger per CLAUDE.md requirement"
    fix: "Import logger from @repo/logger and use logger.info() instead of console.log()"

  # Schema Validation Issues
  - severity: warning
    category: style
    file: apps/web/app-dashboard/src/components/PipelineViewPanel/index.tsx
    line: 36
    message: "Schema uses z.function() for onRetry prop but callback types are not validated. z.function() does not enforce function signature."
    fix: "Remove z.function() from schema or use z.any() for callbacks, ensuring type safety via TypeScript types instead"

  - severity: warning
    category: style
    file: apps/web/app-dashboard/src/components/CostPanel/index.tsx
    line: 34
    message: "Schema uses z.function() for onRetry prop but callback types are not validated"
    fix: "Remove z.function() from schema or use z.any() for callbacks"

  - severity: warning
    category: style
    file: apps/web/app-dashboard/src/components/BlockedQueuePanel/index.tsx
    line: 33
    message: "Schema uses z.function() for onRetry prop but callback types are not validated"
    fix: "Remove z.function() from schema or use z.any() for callbacks"

  # Code Duplication Issues
  - severity: warning
    category: style
    file: apps/api/lego-api/domains/monitor/adapters/repositories.ts
    line: 29
    message: "Schema definitions (PipelineStorySchema, etc.) duplicate those in pipeline-read-service.ts. Creates maintenance burden and type inconsistency risk."
    fix: "Export schemas from PipelineReadService and import them in repositories.ts to maintain single source of truth"

  - severity: warning
    category: style
    file: apps/web/app-dashboard/src/hooks/usePipelineMonitor.ts
    line: 23
    message: "Zod schemas duplicate those in pipeline-read-service.ts and repositories.ts. Three copies of same schema definitions."
    fix: "Consider creating a shared @repo/schemas or @repo/types package to avoid duplication across backend and frontend"

  - severity: info
    category: style
    file: apps/web/app-dashboard/src/pages/monitor-page.tsx
    line: 24
    message: "MonitorPagePropsSchema defined but className prop not validated in component usage (unused schema validation)"
    fix: "Either use MonitorPagePropsSchema.parse() on props or remove unused schema definition"

# Positive Findings
strengths:
  - "All new files properly use Zod schemas for type definitions (z.infer<> pattern)"
  - "No semicolons found in new code (conforms to CLAUDE.md no-semicolons rule)"
  - "No @repo/ui individual path imports found (all from @repo/app-component-library)"
  - "No barrel files created - imports are from source files"
  - "Proper named exports throughout (no default exports in components)"
  - "Good use of aria-label, aria-live, and table captions for accessibility (AC-8)"
  - "Loading skeletons use animate-pulse as required (AC-7)"
  - "Error states implemented with retry buttons (AC-7)"
  - "All three component panels follow consistent structure (AC-1, AC-2, AC-3)"
  - "Backend uses @repo/logger consistently throughout domain"
  - "Proper use of Hono pattern from health domain reference"
  - "SQL queries use proper index optimization (state_idx, story_id_idx)"
  - "PipelineReadService abstracts queries for reuse (AC-13)"
  - "Auth middleware properly applied via monitor.use('*', auth) (AC-11)"
  - "Testing documented in EVIDENCE.yaml: 836 unit tests pass, 7 E2E tests pass"
  - "All acceptance criteria appear addressed in code (AC-1 through AC-12)"

# Architectural Notes
architecture_observations:
  - "PipelineReadService in packages/backend/orchestrator creates shared contract for APIP-5005 CLI"
  - "Monitor domain follows exact health domain pattern (routes.ts -> service -> repository)"
  - "Frontend hook (usePipelineMonitor) implements polling only per AC-12 deferral"
  - "SSE deferred to APIP-2025 as documented in code comments"
  - "Database schema adaptation (enum casting ::text) is necessary and documented"
  - "Three-panel composition (pipeline-view, cost-summary, blocked-queue) well-separated"

ranked_patches:
  - rank: 1
    severity: error
    file: apps/api/lego-api/server.ts
    issue: "Two console calls not using @repo/logger"
    impact: "Violates CLAUDE.md requirement and creates inconsistent logging across codebase"
    lines: [119, 138]
    fix_effort: "5 minutes"

  - rank: 2
    severity: warning
    file: "apps/api/lego-api/domains/monitor/adapters/repositories.ts"
    issue: "Schema duplication across backend and frontend"
    impact: "Three copies of same Zod schemas create maintenance burden; schema changes require updates in 3 places"
    lines: [29, 40, 50, 59]
    fix_effort: "30 minutes - extract to shared package or re-export from pipeline-read-service"

  - rank: 3
    severity: warning
    file: "apps/web/app-dashboard/src/components/*.tsx"
    issue: "z.function() used in component prop schemas"
    impact: "Function parameters not properly validated by Zod; type safety relies only on TypeScript"
    lines: [36, 34, 33]
    fix_effort: "15 minutes - remove z.function() and rely on explicit TypeScript function types"

test_coverage:
  unit_tests: "836 tests pass across orchestrator, lego-api, app-dashboard"
  e2e_tests: "7 Playwright tests pass (API endpoint validation)"
  coverage_assessment: "Test coverage appears comprehensive for acceptance criteria"
  gaps: "No explicit test files for monitor domain adapters/services in lego-api (assumes tested via integration tests)"

verdict_details:
  summary: "Implementation is largely complete and functional, but contains style violations that block merge per CLAUDE.md quality gates"
  blocking_issues: 2
  warnings: 4
  info_items: 1
  quality_gate_status: "FAIL - console usage violates @repo/logger requirement"
  recommendation: "Fix blocking console.error and console.log calls in server.ts, then re-run code review. Schema duplication should be addressed in follow-up PR for technical debt."

notes: |
  The code quality is generally high with good architectural patterns and comprehensive test coverage.
  The failing verdict is purely due to the two console.* calls in the modified server.ts file.
  Once those are replaced with logger calls, the code will pass all CLAUDE.md quality gates.

  The schema duplication across three locations (backend service, backend repository, frontend hook)
  is not a blocker but should be tracked as technical debt (APIP-XXXX) for consolidation into a
  shared types package or monorepo export.
