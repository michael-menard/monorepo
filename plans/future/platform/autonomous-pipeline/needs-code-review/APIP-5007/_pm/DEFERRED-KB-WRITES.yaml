deferred_at: "2026-02-25"
reason: "KB MCP tool unavailable during story generation (CLI access not configured)"
retry_instructions: "Run INSERT via mcp__postgres-knowledgebase__query tool in next KB-connected session"

stories_table_insert:
  story_id: "APIP-5007"
  feature: "autonomous-pipeline"
  title: "Database Schema Versioning and Migration Strategy for APIP Pipeline"
  story_dir: "plans/future/platform/autonomous-pipeline/backlog/APIP-5007"
  story_file: "plans/future/platform/autonomous-pipeline/backlog/APIP-5007/APIP-5007.md"
  story_type: "tech_debt"
  points: 5
  priority: "medium"
  state: "backlog"
  touches_backend: false
  touches_frontend: false
  touches_database: true
  touches_infra: true

sql: |
  INSERT INTO stories (
    story_id, feature, title, story_dir, story_file, story_type,
    points, priority, state, touches_backend, touches_frontend,
    touches_database, touches_infra
  ) VALUES (
    'APIP-5007',
    'autonomous-pipeline',
    'Database Schema Versioning and Migration Strategy for APIP Pipeline',
    'plans/future/platform/autonomous-pipeline/backlog/APIP-5007',
    'plans/future/platform/autonomous-pipeline/backlog/APIP-5007/APIP-5007.md',
    'tech_debt',
    5,
    'medium',
    'backlog',
    false,
    false,
    true,
    true
  )
  ON CONFLICT (story_id) DO UPDATE SET
    title = EXCLUDED.title,
    story_dir = EXCLUDED.story_dir,
    story_file = EXCLUDED.story_file,
    story_type = EXCLUDED.story_type,
    points = EXCLUDED.points,
    priority = EXCLUDED.priority,
    state = EXCLUDED.state,
    touches_backend = EXCLUDED.touches_backend,
    touches_frontend = EXCLUDED.touches_frontend,
    touches_database = EXCLUDED.touches_database,
    touches_infra = EXCLUDED.touches_infra;
