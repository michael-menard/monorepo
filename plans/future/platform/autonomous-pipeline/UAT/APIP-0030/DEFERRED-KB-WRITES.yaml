---
# Deferred KB writes — created because KB was unavailable at story generation time
# Retry by running the INSERT below when KB is accessible
created: "2026-02-25T00:00:00Z"
story_id: APIP-0030
reason: "KB MCP unavailable during story generation"
---

writes:
  - table: stories
    operation: upsert
    data:
      story_id: "APIP-0030"
      feature: "autonomous-pipeline"
      title: "LangGraph Platform Docker Deployment"
      story_dir: "plans/future/platform/autonomous-pipeline/backlog/APIP-0030"
      story_file: "plans/future/platform/autonomous-pipeline/backlog/APIP-0030/APIP-0030.md"
      story_type: "feature"
      points: 3
      priority: "medium"
      state: "backlog"
      touches_backend: false
      touches_frontend: false
      touches_database: true
      touches_infra: true
    sql: |
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'APIP-0030', 'autonomous-pipeline', 'LangGraph Platform Docker Deployment',
        'plans/future/platform/autonomous-pipeline/backlog/APIP-0030',
        'plans/future/platform/autonomous-pipeline/backlog/APIP-0030/APIP-0030.md',
        'feature', 3, 'medium', 'backlog', false, false, true, true
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;

# TTL stamp — added by import-deferred-writes
processed_at: "2026-03-08T23:55:28.324Z"
imported: true
