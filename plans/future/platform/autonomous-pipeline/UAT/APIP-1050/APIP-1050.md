---
id: APIP-1050
title: "Review Graph with Parallel Fan-Out Workers"
status: uat
priority: medium
phase: 1
epic: autonomous-pipeline
experiment_variant: "control"
story_type: feature
points: 8
touches_backend: true
touches_frontend: false
touches_database: false
touches_infra: false
depends_on:
  - APIP-1030
  - APIP-0040
created: "2026-02-25"
updated: "2026-02-25"

predictions:
  split_risk: 0.9
  review_cycles: 4
  token_estimate: 750000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-25T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"

pm_artifacts:
  test_plan:
    strategy: unit+integration
    scope:
      endpoints: []
      ui_touched: false
      data_touched: false
    happy_path_tests:
      - { id: HP-1, title: "Single worker returns PASS for clean code" }
      - { id: HP-2, title: "All 10 workers pass → REVIEW.yaml PASS verdict" }
      - { id: HP-3, title: "Dispatcher fans out to all enabled workers via Send API" }
      - { id: HP-4, title: "Fan-in aggregation uses addWorkerResult and generateRankedPatches helpers" }
      - { id: HP-5, title: "REVIEW.yaml written to correct story feature directory path" }
      - { id: HP-6, title: "Security worker passes budget check and calls Claude via model router" }
      - { id: HP-7, title: "Thread ID follows convention storyId:review:attempt" }
      - { id: HP-8, title: "changeSpecId mapped correctly in RankedPatch when file matches ChangeSpec" }
    error_cases:
      - { id: EC-1, title: "One worker fails → overall verdict is FAIL" }
      - { id: EC-2, title: "Worker timeout → FAIL finding with timeout message" }
      - { id: EC-3, title: "Security worker returns FAIL when token budget exceeded" }
      - { id: EC-4, title: "Security worker circuit breaker opens on Claude unavailability" }
      - { id: EC-5, title: "Disabled worker appears in workers_skipped, does not affect verdict" }
      - { id: EC-6, title: "Fan-in handles partial results if one worker hangs past deadline" }
      - { id: EC-7, title: "changeSpecId is null when finding file does not match any ChangeSpec target" }
    edge_cases:
      - { id: ED-1, title: "Zero enabled workers → PASS verdict (vacuous truth)" }
      - { id: ED-2, title: "Empty changeSpecs array — workers still run" }
      - { id: ED-3, title: "LLM review worker gracefully handles malformed model response" }
      - { id: ED-4, title: "ReviewWorkerNameSchema enum covers all 10 workers" }
      - { id: ED-5, title: "RankedPatchSchema includes changeSpecId field with null default" }
      - { id: ED-6, title: "Carried-forward worker does not affect PASS/FAIL" }
    risks:
      - "APIP-0040 not yet built — LLM workers require mock model router override"
      - "APIP-1030 not yet built — no real worktree; tests use mock worktreePath"
      - "LangGraph Send API parallel behavior must be verified against library version"
      - "Security worker Claude calls are expensive — no real Claude calls in CI"
      - "Fan-in timing test is inherently flaky — use fake timers"

  dev_feasibility:
    feasible: true
    confidence: high
    rationale: "ReviewSchema artifact, helpers, NodeCircuitBreaker, createToolNode factory, and YAML artifact writer are all already implemented. LangGraph Send API is established. Story is large (17 ACs) but cleanly decomposable into 5 subtasks. APIP-0040 dependency mitigated with injectable mock override."
    complexity: high
    change_surface:
      areas:
        - "packages/backend/orchestrator/src/nodes/review/ (new directory)"
        - "packages/backend/orchestrator/src/graphs/review.ts (new file)"
        - "packages/backend/orchestrator/src/artifacts/review.ts (extend RankedPatchSchema)"
      endpoints: []
      deploy_touchpoints:
        - "New LangGraph graph compiled and registered on dedicated local server"
    risks:
      - finding: "APIP-0040 not yet built — LLM-based workers cannot dispatch real model calls"
        mitigation: "Inject modelRouterOverride in worker config; unit and integration tests use mock"
      - finding: "LangGraph Send API for true parallel fan-out not yet used in this codebase"
        mitigation: "Create graph skeleton first, verify fan-out routing before implementing workers"
      - finding: "Fan-in deadlock risk if one worker hangs"
        mitigation: "Each worker enforces individual timeoutMs via Promise.race before writing to state"
      - finding: "Extending RankedPatchSchema is a shared schema change"
        mitigation: "changeSpecId has default(null) so existing parse calls continue to work"
    missing_requirements:
      - "runReview() ReviewGraphResult shape must be formalized as supervisor dispatch contract"
      - "ChangeSpec schema type (APIP-1020 spike) must be finalized before workers can filter by ChangeSpec"
    evidence_expectations:
      - "graph.compile() succeeds (pnpm check-types --filter @repo/orchestrator)"
      - "vitest run passes with >80% coverage on all worker files"
      - "REVIEW.yaml written after runReview() completes"
      - "Security worker never invokes Claude when circuit breaker open or budget exceeded"
---

# APIP-1050: Review Graph with Parallel Fan-Out Workers

## Context

The autonomous pipeline's implementation graph (APIP-1030) produces committed code changes in a git worktree. Before those changes can be merged, they must pass a comprehensive multi-dimensional code review checking correctness, style, type safety, security, and code quality across 10 independent dimensions simultaneously.

The `ReviewSchema` artifact schema is already fully defined in `packages/backend/orchestrator/src/artifacts/review.ts` with `FindingSchema`, `WorkerResultSchema`, `RankedPatchSchema`, and helper functions (`createReview`, `addWorkerResult`, `carryForwardWorker`, `generateRankedPatches`) — but no graph node currently uses it. This story wires that schema into a new Review Graph.

The 10 review workers are partitioned by model tier to control cost:
- **Static analysis via Ollama (local, free)**: lint, style, syntax, typecheck, build
- **LLM review via OpenRouter (cheap)**: react patterns, typescript idioms, reusability, accessibility
- **Security review via Claude (expensive, always Claude)**: SAST, dependency scanning, secret detection

This story is on the critical path: APIP-1030 → **APIP-1050** → APIP-1060 → APIP-1070.

**Baseline**: 2026-02-13. No Review Graph exists anywhere in the codebase. No `REVIEW.yaml` artifact writer is wired into any graph. The 10-worker decomposition is documented in `story.yaml` but has no implementation.

---

## Goal

Build the **Review Graph** — a LangGraph worker graph that fans out to 10 review workers in parallel using the Send API, collects their results via a fan-in aggregation node, produces a PASS/FAIL verdict, ranks findings for targeted re-implementation, and writes a `REVIEW.yaml` artifact to the story's feature directory.

---

## Non-Goals

- Building the model router (APIP-0040) — this story consumes it but does not implement it
- Building the implementation graph (APIP-1030) — this story depends on it but does not implement it
- Defining the ChangeSpec schema (APIP-1020) — this story receives ChangeSpecs as input but does not define them
- Operator CLI visibility for review results — APIP-5005
- Monitor dashboard showing review status — APIP-2020
- QA Graph acting on the review verdict — APIP-1060
- Merge Graph — APIP-1070
- Security scanning at infrastructure level (container hardening, network boundary) — APIP-0030 / APIP-5003
- Full auth/authz for LangGraph Platform — deferred to Phase 2
- Writing review results to Aurora DB — output is `REVIEW.yaml` only; DB persistence deferred
- Playwright E2E tests — `frontend_impacted: false`, ADR-006 does not apply

---

## Scope

**New files:**
- `packages/backend/orchestrator/src/nodes/review/types.ts` — `ReviewWorkerNameSchema` enum, shared types
- `packages/backend/orchestrator/src/nodes/review/workers/review-lint.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-style.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-syntax.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-typecheck.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-build.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-react.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-typescript.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-reusability.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-accessibility.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-security.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/__tests__/review-{name}.test.ts` (×10)
- `packages/backend/orchestrator/src/graphs/review.ts`
- `packages/backend/orchestrator/src/graphs/__tests__/review.test.ts`

**Modified files:**
- `packages/backend/orchestrator/src/artifacts/review.ts` — extend `RankedPatchSchema` with `changeSpecId: z.string().nullable().default(null)`

**No changes to:**
- `packages/backend/database-schema/` or `@repo/db` client
- Any Lambda handler
- Any Aurora migration
- Any UI package

---

## Acceptance Criteria

- [ ] **AC-1**: A `ReviewWorkerNameSchema = z.enum(['lint', 'style', 'syntax', 'typecheck', 'build', 'react', 'typescript', 'reusability', 'accessibility', 'security'])` Zod schema exists in `packages/backend/orchestrator/src/nodes/review/types.ts` as the canonical worker name enum used by all workers and the graph.

- [ ] **AC-2**: Each of the 10 review workers exists as a separate file in `packages/backend/orchestrator/src/nodes/review/workers/review-{name}.ts` with: (a) a `Review{Name}ConfigSchema` Zod schema (with `enabled: z.boolean().default(true)`, `timeoutMs: z.number().positive().default(60000)`, and worker-specific options), (b) a `createReview{Name}Node(config)` factory function using `createToolNode('review_{name}', fn)`, (c) the node function returning a `WorkerResult` (imported from `artifacts/review.ts`), (d) injectable tool runner for testability.

- [ ] **AC-3**: Static analysis workers (lint, style, syntax, typecheck, build) invoke real CLI tools when a `worktreePath` is available: ESLint for lint, Prettier check for style, TypeScript compiler for syntax/typecheck, and `pnpm build` for build. Each tool's exit code and output parse into `FindingSchema` entries. Tool invocation is injectable (`toolRunner?: (cmd: string, cwd: string) => Promise<{ exitCode: number; stdout: string; stderr: string }>`).

- [ ] **AC-4**: LLM review workers (react, typescript, reusability, accessibility) dispatch model calls through the model router (APIP-0040) using the `OpenRouter` tier. Each worker constructs a structured prompt from the changed files (limited to files in the ChangeSpec), parses the model response into `FindingSchema[]`, and returns a `WorkerResult`. Workers accept `modelRouterOverride` in config for testability.

- [ ] **AC-5**: The security worker (`review-security.ts`) always uses Claude (highest tier) via the model router. Before dispatching the Claude call, it checks the per-story token budget via the model router's budget gate — if budget is exceeded, it returns a `WorkerResult` with `verdict: 'FAIL'` and a single finding `{ message: 'Security review skipped: token budget exceeded', severity: 'error', auto_fixable: false }` rather than throwing an error. A `NodeCircuitBreaker` wraps the Claude call.

- [ ] **AC-6**: A `ReviewGraphStateAnnotation` Zod-annotated LangGraph state exists with fields: `storyId`, `worktreePath`, `changeSpecs` (array of ChangeSpec IDs from APIP-1020), `iteration`, `workerResults: Annotation<WorkerResult[]>` (append reducer), `workerNames: Annotation<string[]>` (append reducer), `aggregatedReview`, `reviewComplete`, `reviewVerdict` (`'PASS' | 'FAIL' | null`), `config`, `errors`, `warnings`.

- [ ] **AC-7**: A `dispatcher` node fans out to all enabled workers using LangGraph's `Send` API: `return enabledWorkers.map(name => new Send('review_worker_' + name, { storyId, worktreePath, changeSpecs, workerName: name }))`. Workers that are disabled in config are added to `workers_skipped` immediately without dispatching.

- [ ] **AC-8**: Each worker node executes within its configured `timeoutMs`. On timeout, the worker returns `{ verdict: 'FAIL', skipped: false, errors: 1, warnings: 0, findings: [{ file: '', message: 'Worker timed out after {timeoutMs}ms', severity: 'error', auto_fixable: false }], duration_ms: timeoutMs }`.

- [ ] **AC-9**: A `fan_in` aggregation node runs after all worker nodes complete. It calls `createReview(storyId, iteration)` once, then calls `addWorkerResult(review, workerName, result)` for each worker result in `state.workerResults`, then calls `generateRankedPatches(review)` to produce the `ranked_patches` list. The final `review.verdict` is FAIL if any non-skipped worker returned FAIL; PASS only if all non-skipped workers passed.

- [ ] **AC-10**: The `fan_in` node adds a `changeSpecId: z.string().nullable().default(null)` field to each `RankedPatch` by matching the finding's `file` path against the ChangeSpec file targets. If no ChangeSpec matches the file, `changeSpecId` is null. This requires extending `RankedPatchSchema` in `artifacts/review.ts` with the optional `changeSpecId` field.

- [ ] **AC-11**: The `fan_in` node writes the final `Review` object as `REVIEW.yaml` to the story's feature directory using the existing YAML artifact writer pattern (`packages/backend/orchestrator/src/persistence/yaml-artifact-writer.ts`). The file path is `plans/future/{epic}/{story_id}/REVIEW.yaml` (or the equivalent resolved path for the story's feature directory).

- [ ] **AC-12**: A `createReviewGraph(config)` factory function creates and compiles the `StateGraph` with: START → dispatcher → [all 10 worker nodes in parallel via Send] → fan_in → complete → END. The graph is exported as a compiled LangGraph graph.

- [ ] **AC-13**: A `runReview(storyId, worktreePath, changeSpecs, config)` entry point function invokes the compiled graph, awaits completion, and returns a `ReviewGraphResult` with `{ storyId, verdict, review, durationMs, errors }`. This is the function the supervisor (APIP-0020) calls.

- [ ] **AC-14**: When all workers pass, `runReview()` returns `verdict: 'PASS'` and the implementation graph proceeds to merge. When any worker fails, `runReview()` returns `verdict: 'FAIL'` and the ranked patches are available in the `REVIEW.yaml` for the implementation graph to use for targeted re-implementation.

- [ ] **AC-15**: Unit tests exist in `packages/backend/orchestrator/src/nodes/review/workers/__tests__/review-{name}.test.ts` for each worker, covering: (a) happy path with clean code returning PASS, (b) finding detection returning FAIL with correct `FindingSchema` entries, (c) timeout returning FAIL with timeout finding, (d) tool runner injection (mock returns controlled output), (e) disabled worker returns skipped result.

- [ ] **AC-16**: Graph integration tests exist in `packages/backend/orchestrator/src/graphs/__tests__/review.test.ts` covering: (a) `graph.compile()` succeeds, (b) all 10 workers reachable from dispatcher, (c) fan-in aggregation collects all worker results, (d) PASS verdict when all workers pass, (e) FAIL verdict when any one worker fails, (f) disabled workers appear in `workers_skipped`.

- [ ] **AC-17**: The APIP ADR-001 thread ID convention is documented in the graph source: `const threadId = \`${storyId}:review:${attempt}\`` — LangGraph checkpoint uses this thread ID for crash recovery.

- [ ] **AC-18**: A `ReviewGraphResultSchema` Zod schema is exported from `packages/backend/orchestrator/src/graphs/review.ts` with fields `{ storyId: z.string(), verdict: z.enum(['PASS', 'FAIL']), review: ReviewSchema, durationMs: z.number(), errors: z.array(z.string()) }`. The `runReview()` function returns `z.infer<typeof ReviewGraphResultSchema>`. This schema is the formal contract consumed by the supervisor (APIP-0020).
_Added by autonomous elaboration_

---

## Reuse Plan

- **No UI components.**
- **Patterns**:
  - `createToolNode('review_{name}', fn)` factory for each worker (from `node-factory.ts`)
  - `Annotation.Root()` with overwrite reducer for most fields; append reducer for `workerResults` and `workerNames`
  - LangGraph `Send` API for true parallel fan-out (map-reduce pattern)
  - `NodeCircuitBreaker` wrapping Claude call in security worker
  - `@repo/logger` structured logging: `{ storyId, worker, verdict, errorCount, durationMs }`
- **Packages**:
  - `packages/backend/orchestrator/src/artifacts/review.ts` — import `ReviewSchema`, `WorkerResultSchema`, `FindingSchema`, `RankedPatchSchema`, `createReview()`, `addWorkerResult()`, `carryForwardWorker()`, `generateRankedPatches()` — do NOT reimplement
  - `packages/backend/orchestrator/src/runner/node-factory.ts` — `createToolNode`
  - `packages/backend/orchestrator/src/runner/circuit-breaker.ts` — `NodeCircuitBreaker` for security worker
  - `packages/backend/orchestrator/src/persistence/yaml-artifact-writer.ts` — REVIEW.yaml write
  - `@repo/logger` — structured logging

---

## Architecture Notes

### Worker Structure (one file per worker)

Each worker file follows the `delta-detect.ts` canonical pattern:

```typescript
// review-lint.ts
import { z } from 'zod'
import { createToolNode } from '../../runner/node-factory'
import { WorkerResult, FindingSchema } from '../../../artifacts/review'
import { logger } from '@repo/logger'

const ReviewLintConfigSchema = z.object({
  enabled: z.boolean().default(true),
  timeoutMs: z.number().positive().default(60000),
  toolRunner: z.function().optional(), // injectable for tests
})

export function createReviewLintNode(config: z.infer<typeof ReviewLintConfigSchema>) {
  return createToolNode('review_lint', async (state) => {
    // ...
  })
}
```

### Parallel Fan-Out via Send API

```typescript
// dispatcher node
function dispatcher(state: ReviewGraphState) {
  const enabledWorkers = state.config.workers.filter(name =>
    state.config.workerConfigs[name]?.enabled !== false
  )
  return enabledWorkers.map(name =>
    new Send(`review_worker_${name}`, {
      storyId: state.storyId,
      worktreePath: state.worktreePath,
      changeSpecs: state.changeSpecs,
      workerName: name,
    })
  )
}
```

### Append Reducer for Worker Results

```typescript
workerResults: Annotation<WorkerResult[]>({
  reducer: (current, update) => [...current, ...update],
  default: () => [],
})
```

### Fan-In Aggregation

```typescript
async function fanIn(state: ReviewGraphState) {
  const review = createReview(state.storyId, state.iteration)
  for (const { workerName, result } of state.workerResults) {
    addWorkerResult(review, workerName, result)
  }
  generateRankedPatches(review)
  // Map changeSpecId by file path
  for (const patch of review.ranked_patches) {
    patch.changeSpecId = matchChangeSpec(patch.file, state.changeSpecs) ?? null
  }
  await yamlArtifactWriter.write(reviewYamlPath, review)
  return { aggregatedReview: review, reviewVerdict: review.verdict, reviewComplete: true }
}
```

### Thread ID Convention

```typescript
const threadId = `${storyId}:review:${attempt}`
// Used as LangGraph checkpoint thread ID for crash recovery
// Per APIP ADR-001 Decision 2
```

### PASS/FAIL Decision Rule (resolves QA-004)

1. All **enabled** workers are blocking by default — any FAIL = overall FAIL
2. Timed-out workers contribute a FAIL finding (`message: 'Worker timed out after {timeoutMs}ms'`)
3. A `criticalWorkers: WorkerName[]` config field designates workers whose failure is always blocking (even if advisory future config is added)
4. Skipped workers (disabled or carried forward) do **not** affect PASS/FAIL
5. Zero enabled workers → PASS (vacuous truth)

---

## Infrastructure Notes

- All review workers run on the dedicated local server (APIP ADR-001 Decision 4). No Lambda.
- Claude calls go outbound from server; Ollama runs locally on server; OpenRouter calls go outbound.
- No Aurora DB writes. Output is `REVIEW.yaml` only.
- LangGraph checkpoint uses thread ID `{storyId}:review:{attempt}` for crash recovery.
- Server must be provisioned (APIP-5006) before integration testing against real worktrees.

---

## Security & Cost Notes (DECISIONS.yaml)

- **SEC-003**: APIP-1050's security worker IS the SAST/security scanning gate. A separate "Security Scanning Gate" story (NEW-009) is deferred pending confirmation.
- **COST-CONTROL-001**: Security worker always uses Claude — model router circuit breaker enforces per-story token hard cap before this worker is invoked. Budget tracking per story must be active. If budget exceeded, security worker returns FAIL gracefully (no exception thrown).
- **ENG-001**: `RankedPatch` extended with `changeSpecId` field (nullable) to support targeted re-implementation in APIP-1030's retry loop.

---

## Test Plan

Full test plan in `_pm/test-plan.yaml`. Key requirements:

### Unit Tests (per worker)
- File: `nodes/review/workers/__tests__/review-{name}.test.ts`
- Coverage: >80% per worker file
- Cases: clean code → PASS; dirty code → FAIL with findings; timeout → FAIL; disabled → skipped; mock tool runner injection
- **Do NOT invoke real ESLint/TypeScript compiler/Claude in unit tests**

### Graph Integration Tests
- File: `graphs/__tests__/review.test.ts`
- Cases: compile succeeds; all 10 workers reachable; fan-in collects all results; PASS/FAIL verdict correct; disabled workers in `workers_skipped`
- Fan-in aggregation logic: **100% branch coverage**
- Security worker budget-exceeded → FAIL (not exception): critical correctness requirement

### Special Cases
- Security worker: FAIL (not throw) on budget exceeded (COST-CONTROL-001)
- Fan-in deadlock: fan-in reads from append-reducer state, not direct worker await
- No Playwright E2E (`frontend_impacted: false`)
- No UAT (internal pipeline tooling)

---

## Reality Baseline

- **As of 2026-02-13**: `ReviewSchema` artifact fully defined in `artifacts/review.ts` with all helper functions. No graph implementation. No `REVIEW.yaml` artifact written anywhere.
- **APIP-1030**: Not yet built (backlog). Produces the git worktree that this story's workers analyze.
- **APIP-0040**: Created (story file generated). Model router not yet implemented. LLM workers use injectable mock override.
- **APIP-0020**: In Elaboration. Supervisor must be aware of `runReview()` entry point contract.
- **APIP-5006**: In Elaboration. Server must be provisioned before integration testing.
- **Protected**: Do NOT modify `packages/backend/database-schema/` or `@repo/db` client.

---

## Canonical References

| Pattern | File | Why |
|---------|------|-----|
| Existing REVIEW.yaml artifact schema | `packages/backend/orchestrator/src/artifacts/review.ts` | This IS the review output contract. `ReviewSchema`, `WorkerResultSchema`, `FindingSchema`, `RankedPatchSchema`, and all helper functions are already defined. The fan-in aggregation node must use `createReview()`, `addWorkerResult()`, and `generateRankedPatches()` from this file — not reimplement them. |
| Node factory + createToolNode pattern | `packages/backend/orchestrator/src/nodes/elaboration/delta-detect.ts` | Canonical pattern for a new orchestrator node — Zod schemas at top, pure functions, `createToolNode` factory, exported result types, tests in `__tests__/`. Each review worker file should mirror this structure exactly. |
| Graph with parallel fanout + merge | `packages/backend/orchestrator/src/graphs/story-creation.ts` | Shows `StateGraph` construction, `Annotation.Root()` state schema, sequential fanout nodes, merge node, conditional edge functions, and `graph.compile()`. Review graph uses the same pattern but with LangGraph `Send` API for true parallelism. |
| Circuit breaker for node protection | `packages/backend/orchestrator/src/runner/circuit-breaker.ts` | `NodeCircuitBreaker` class with `canExecute()`, `recordSuccess()`, `recordFailure()` — wrap the Claude security worker invocation with this to prevent cost runaway if Claude is unavailable. |

---

## Subtasks

### ST-1: Graph skeleton — ReviewGraphStateAnnotation, createReviewGraph(), runReview(), dispatcher, fan-in stub, REVIEW.yaml write

**Goal**: Establish the compiled LangGraph review graph with correct state annotation, dispatcher node using Send API, empty fan-in stub, and YAML artifact write. No real workers yet — stub workers return PASS.

**Files to read**:
- `packages/backend/orchestrator/src/graphs/story-creation.ts`
- `packages/backend/orchestrator/src/artifacts/review.ts`
- `packages/backend/orchestrator/src/persistence/yaml-artifact-writer.ts`

**Files to create/modify**:
- `packages/backend/orchestrator/src/graphs/review.ts` (new)
- `packages/backend/orchestrator/src/nodes/review/types.ts` (new)

**ACs**: AC-1, AC-6, AC-7, AC-12, AC-13, AC-14, AC-17

**Depends on**: none

**Verification**: `pnpm check-types --filter @repo/orchestrator && vitest run graphs/review.test.ts`

**Estimated tokens**: 18,000

---

### ST-2: Static analysis workers — lint, style, syntax, typecheck, build + unit tests

**Goal**: Implement 5 Ollama-tier workers (no LLM, real CLI tools with injectable toolRunner). Each worker: ConfigSchema, factory function, node returning WorkerResult, unit tests for PASS/FAIL/timeout/disabled.

**Files to read**:
- `packages/backend/orchestrator/src/nodes/elaboration/delta-detect.ts`
- `packages/backend/orchestrator/src/runner/node-factory.ts`
- `packages/backend/orchestrator/src/artifacts/review.ts`

**Files to create**:
- `packages/backend/orchestrator/src/nodes/review/workers/review-lint.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-style.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-syntax.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-typecheck.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-build.ts`

**ACs**: AC-2, AC-3, AC-8, AC-15

**Depends on**: ST-1

**Verification**: `vitest run nodes/review/workers/__tests__/review-lint.test.ts` (and style, syntax, typecheck, build)

**Estimated tokens**: 32,000

---

### ST-3: LLM review workers — react, typescript, reusability, accessibility + model router integration + unit tests

**Goal**: Implement 4 OpenRouter-tier workers that dispatch model calls through model router (APIP-0040 or mock override). Each worker: ConfigSchema with modelRouterOverride, factory, prompt construction from ChangeSpec files, FindingSchema[] parsing from model response, unit tests.

**Files to read**:
- `packages/backend/orchestrator/src/nodes/elaboration/delta-detect.ts`
- `packages/backend/orchestrator/src/runner/node-factory.ts`
- `packages/backend/orchestrator/src/artifacts/review.ts`

**Files to create**:
- `packages/backend/orchestrator/src/nodes/review/workers/review-react.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-typescript.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-reusability.ts`
- `packages/backend/orchestrator/src/nodes/review/workers/review-accessibility.ts`

**ACs**: AC-2, AC-4, AC-8, AC-15

**Depends on**: ST-1

**Verification**: `vitest run nodes/review/workers/__tests__/review-react.test.ts` (and typescript, reusability, accessibility)

**Estimated tokens**: 28,000

---

### ST-4: Security worker — Claude integration, circuit breaker, budget gate + unit tests

**Goal**: Implement the Claude-tier security worker with NodeCircuitBreaker wrapping, per-story token budget check via model router before dispatch, graceful FAIL on budget exceeded, and injectable model router override for tests.

**Files to read**:
- `packages/backend/orchestrator/src/runner/circuit-breaker.ts`
- `packages/backend/orchestrator/src/runner/node-factory.ts`
- `packages/backend/orchestrator/src/artifacts/review.ts`

**Files to create**:
- `packages/backend/orchestrator/src/nodes/review/workers/review-security.ts`

**ACs**: AC-2, AC-5, AC-8, AC-15

**Depends on**: ST-1

**Verification**: `vitest run nodes/review/workers/__tests__/review-security.test.ts`

**Estimated tokens**: 14,000

---

### ST-5: Full fan-in aggregation, changeSpecId mapping, RankedPatchSchema extension, and graph integration tests

**Goal**: Complete the fan-in node with full addWorkerResult loop, generateRankedPatches call, and changeSpecId file-path matching against ChangeSpecs. Extend RankedPatchSchema with changeSpecId field. Wire all 10 workers into graph. Write comprehensive graph integration tests covering all AC-16 scenarios.

**Files to read**:
- `packages/backend/orchestrator/src/artifacts/review.ts`
- `packages/backend/orchestrator/src/graphs/review.ts`
- `packages/backend/orchestrator/src/persistence/yaml-artifact-writer.ts`

**Files to modify/create**:
- `packages/backend/orchestrator/src/artifacts/review.ts` (extend RankedPatchSchema)
- `packages/backend/orchestrator/src/graphs/review.ts` (complete fan-in)
- `packages/backend/orchestrator/src/graphs/__tests__/review.test.ts` (new)

**ACs**: AC-9, AC-10, AC-11, AC-16

**Depends on**: ST-2, ST-3, ST-4 (all 10 workers must exist before full graph wiring and integration tests)

**Verification**: `vitest run graphs/__tests__/review.test.ts && pnpm check-types --filter @repo/orchestrator`

**Estimated tokens**: 22,000

---

## QA Notes

```yaml
qa_notes:
  verdict: CONDITIONAL_PASS
  decided_at: "2026-02-25T12:05:00Z"
  mode: autonomous
  acs_added: 1
  gaps_resolved: 3
  opportunities_logged: 4
  notes:
    - "AC-18 added: ReviewGraphResultSchema Zod schema for supervisor contract"
    - "ST-5 dependency corrected to ST-2 + ST-3 + ST-4"
    - "criticalWorkers config deferred as out-of-scope (future enhancement)"
    - "Story sizing is large (18 ACs) but well-decomposed into 5 subtasks"
```
