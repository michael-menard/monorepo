---
id: APIP-3040
title: "Learning-Aware Model Router"
status: failed-code-review
priority: medium
phase: 3
experiment_variant: "control"
points: 5
story_type: feature
depends_on:
  - APIP-3020
  - APIP-0040
touches_backend: true
touches_frontend: false
touches_database: true
touches_infra: false
created: "2026-02-25T00:00:00Z"
generated: "2026-02-25T00:00:00Z"

predictions:
  split_risk: 0.9
  review_cycles: 2
  token_estimate: 180000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-25T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"

pm_artifacts:
  test_plan:
    strategy: unit+integration
    scope:
      endpoints: []
      ui_touched: false
      data_touched: true
      packages_touched:
        - packages/backend/orchestrator/src/pipeline/model-router.ts
        - packages/backend/orchestrator/src/pipeline/__types__/index.ts
        - packages/backend/database-schema/src/schema/
    happy_path_tests:
      - id: HP-1
        title: "Affinity routing selected when qualifying high-confidence profile exists"
      - id: HP-2
        title: "wint.model_assignments DB override takes precedence over affinity match"
      - id: HP-3
        title: "Integration test — affinity routing with real test PostgreSQL fixture rows (AC-11)"
      - id: HP-4
        title: "Cache populated on first dispatch and reused on second dispatch"
    error_cases:
      - id: EC-1
        title: "DB query failure — graceful fallback to static escalation chain (AC-13)"
      - id: EC-2
        title: "success_rate below threshold — fallback to static chain (AC-4)"
      - id: EC-3
        title: "confidence_level 'none' — fallback to static chain (AC-4, AC-10c)"
      - id: EC-4
        title: "confidence_level 'low' — fallback to static chain (AC-4, AC-10c)"
      - id: EC-5
        title: "No matching affinity profile — fallback to static chain cold-start (AC-4, AC-10b)"
    edge_cases:
      - id: ED-1
        title: "change_type and file_type not provided — default to 'unknown' (AC-8, AC-10g)"
      - id: ED-2
        title: "invalidateAffinityCache() causes re-query on next dispatch (AC-6, AC-10f)"
      - id: ED-3
        title: "Regression — existing APIP-0040 unit tests all pass (AC-12)"
      - id: ED-4
        title: "Cheapest qualifying model selected when multiple profiles exist (cost-first ordering)"
    risks:
      - id: TR-1
        description: "Integration test requires APIP-3020 wint.model_affinity migration — skip @integration if not applied"
        severity: medium
      - id: TR-2
        description: "APIP-0040 regression test suite gated on v1 being in main"
        severity: high
      - id: TR-3
        description: "Cache spy call count tests are fragile if lazy-load implementation changes"
        severity: low

  dev_feasibility:
    feasible: true
    confidence: high
    rationale: >
      Well-scoped additive extension to PipelineModelRouter. Three-tier routing precedence,
      cache architecture, and Drizzle/Zod patterns are all established in the codebase.
      Primary risks are gating dependencies (APIP-0040, APIP-3020), not design unknowns.
    complexity: medium
    change_surface:
      areas:
        - packages/backend/orchestrator/src/pipeline/model-router.ts
        - packages/backend/orchestrator/src/pipeline/__types__/index.ts
        - packages/backend/database-schema/src/schema/ (read wint.model_affinity — APIP-3020)
      endpoints: []
      deploy_touchpoints:
        - "wint.model_affinity table must exist (requires APIP-3020 migration)"
        - "PipelineModelRouter singleton re-initialized after merge (cache starts empty)"
    risks:
      - finding: "APIP-0040 not yet merged — PipelineModelRouter v1 interface may change at code review"
        mitigation: "Gate implementation on APIP-0040 merge; elaboration can proceed"
      - finding: "APIP-3020 not yet implemented — wint.model_affinity and Drizzle schema types unavailable"
        mitigation: "Inject affinityReader abstraction; unit tests use mock; integration tests tagged @integration"
      - finding: "Provider derivation from model string requires getProviderForModel() — format dependency"
        mitigation: "Reuse existing getProviderForModel() unchanged; guard on derivation failure with fallback"
    missing_requirements: []
    evidence_expectations:
      - "pnpm check-types --filter @repo/orchestrator passes"
      - "All unit tests (AC-10 sub-cases a-g) pass without real DB"
      - "Existing APIP-0040 test suite passes with zero regressions (AC-12)"
      - "@integration test passes against APIP-5001 test DB with wint.model_affinity fixture rows"
      - "logger.info routing decision log contains all required fields for each dispatch call"
---

# APIP-3040: Learning-Aware Model Router

## Context

The autonomous pipeline currently dispatches all LLM model calls through `PipelineModelRouter` v1 (defined by APIP-0040): a static Ollama → OpenRouter → Anthropic escalation chain with operator-level `wint.model_assignments` DB overrides. This is safe and cost-conservative but cost-inefficient — every change attempt uses the same fallback hierarchy regardless of how well a given model has historically performed for a specific change type and file type.

APIP-3020 delivers the `wint.model_affinity` table, continuously populated by the Pattern Miner cron. Each `(model, change_type, file_type)` combination accumulates an empirical `success_rate` and `confidence_level` based on pipeline telemetry from APIP-3010. Once a profile has sufficient sample size (`confidence_level IN ('medium', 'high')`), the router can bypass the static chain and route directly to the cheapest model with a success rate above the configured threshold.

APIP-3040 adds that learning layer. No `wint.model_affinity` table or affinity-guided routing exists in the codebase today. APIP-0040 must merge and APIP-3020 must complete before APIP-3040 can begin implementation.

## Goal

Upgrade `PipelineModelRouter` to read `wint.model_affinity` profiles on each dispatch call and route each atomic change to the cheapest model with `success_rate >= affinitySuccessRateThreshold` (default 85%) and `confidence_level IN ('medium', 'high')` for the specific `(change_type, file_type)` combination — bypassing the static escalation chain when affinity evidence is strong enough, while preserving the static chain as a mandatory fallback during cold-start and low-confidence periods.

## Non-Goals

- Implementing the `wint.model_affinity` table or Pattern Miner cron — that is APIP-3020
- Implementing the cold-start exploration budget mechanism — that is APIP-3070 (APIP-3040 defines the fallback contract; APIP-3070 adds exploration logic on top)
- Implementing affinity-guided diff planning — that is APIP-3030
- Implementing affinity-guided story structuring — that is APIP-3050
- Implementing the bake-off engine for model experiments — that is APIP-3060
- Adding UI or operator CLI visibility for routing decisions — that is APIP-5005
- Online learning or real-time profile updates during the change loop — profiles are only updated by the Pattern Miner cron (out-of-band)
- Modifying any file in `packages/backend/orchestrator/src/models/` (protected by WINT/MODL UAT)
- Changing the `wint.model_assignments` table schema or query logic defined by APIP-0040
- Per-story or per-ChangeSpec telemetry writes — that is APIP-3010
- Dynamic threshold adjustment — threshold is configurable but not auto-tuned in this story
- Adding HTTP endpoints — no API surface

## Scope

### Packages Modified

| Package | File | Change Type |
|---------|------|-------------|
| `packages/backend/orchestrator` | `src/pipeline/model-router.ts` | Additive extension |
| `packages/backend/orchestrator` | `src/pipeline/__types__/index.ts` | Additive Zod schema fields |
| `packages/backend/database-schema` | `src/schema/` (APIP-3020 types) | Read-only import |

### Files Protected (Do Not Modify)

- `packages/backend/orchestrator/src/models/` — entire directory (WINT/MODL UAT protected)
- `packages/backend/orchestrator/src/pipeline/token-bucket.ts` — rate limiter, unchanged
- `packages/backend/orchestrator/src/pipeline/budget-accumulator.ts` — budget accumulator, unchanged

### Three-Tier Routing Precedence

```
Tier 1: wint.model_assignments DB override (operator manual control — always wins)
    ↓ (no override)
Tier 2: wint.model_affinity affinity-guided selection
         (confidence_level IN ('medium','high') AND success_rate >= affinitySuccessRateThreshold)
         → select cheapest qualifying model (ORDER BY avg_cost_usd ASC, success_rate DESC)
    ↓ (no qualifying profile, DB failure, cold-start)
Tier 3: Static Ollama → OpenRouter → Anthropic escalation chain (fallback)
```

## Acceptance Criteria

- [ ] **AC-1**: `PipelineModelRouter.dispatch()` accepts `change_type` and `file_type` fields (from the ChangeSpec, via `TaskContract` or a new `PipelineDispatchOptionsSchema` extension) and uses them to perform an affinity lookup against `wint.model_affinity` before invoking the static escalation chain.

- [ ] **AC-2**: A `queryAffinityProfile(changeType: string, fileType: string): Promise<AffinityProfile | null>` method (or equivalent internal function) queries `wint.model_affinity` for all rows matching the `(change_type, file_type)` combination, filtered to `confidence_level IN ('medium', 'high')` and ordered by `success_rate DESC, cost_per_token ASC` (cheapest high-confidence model first). Returns `null` if no qualifying profile exists.

- [ ] **AC-3**: When `queryAffinityProfile` returns a qualifying row with `success_rate >= affinitySuccessRateThreshold`, `PipelineModelRouter` selects that model and provider, bypassing the static escalation chain (unless a `wint.model_assignments` DB override is in effect for the matching `agent_pattern`). `wint.model_assignments` overrides take absolute precedence.

- [ ] **AC-4**: When no qualifying affinity profile exists (cold-start, `confidence_level = 'none'` or `'low'`, no matching row, `success_rate < threshold`), `PipelineModelRouter` falls back to the static Ollama → OpenRouter → Anthropic escalation chain exactly as implemented in APIP-0040 v1. No error is thrown or logged for this fallback — it is the expected path during ramp-up.

- [ ] **AC-5**: The success-rate routing threshold is stored as a named constant `affinitySuccessRateThreshold` in `PipelineModelRouterConfig` (Zod-validated). Default value: `0.85`. The threshold must not be hardcoded inline. Config injection allows tests to set a different threshold without code changes.

- [ ] **AC-6**: Affinity profiles are cached in memory after first load. The cache is keyed by `(change_type, file_type)` and populated lazily on first dispatch for a given combination. An `invalidateAffinityCache()` method is exposed on `PipelineModelRouter` (matching the pattern of `invalidateAssignmentsCache()` from APIP-0040) for the Pattern Miner cron to call after each aggregation run.

- [ ] **AC-7**: Every `dispatch()` call logs a structured routing decision record via `@repo/logger` with domain `'pipeline_model_router'` and fields: `{ source: 'affinity' | 'db_assignment' | 'fallback', model, provider, change_type, file_type, confidence_level: string | null, success_rate: number | null, sample_size: number | null }`. This enables post-hoc analysis of routing attribution.

- [ ] **AC-8**: The existing `PipelineModelRouterFactory` singleton pattern from APIP-0040 is preserved. No breaking changes to the `dispatch()` call signature — `change_type` and `file_type` are optional fields (defaulting to `'unknown'` if not provided) so that graph nodes not yet ChangeSpec-aware can still call the router without modification.

- [ ] **AC-9**: A `PipelineModelRouterConfig` Zod schema update (additive) includes: `affinitySuccessRateThreshold: z.number().min(0).max(1).default(0.85)` and `affinityMinSampleSize: z.number().int().min(1).default(20)` (the minimum sample_size threshold before affinity is considered). No TypeScript interfaces.

- [ ] **AC-10**: Unit tests (Vitest) cover: (a) affinity routing selected when qualifying profile exists with correct confidence and success_rate, (b) fallback to static chain when no profile exists (cold-start), (c) fallback to static chain when confidence_level is 'none' or 'low', (d) fallback to static chain when success_rate < threshold, (e) `wint.model_assignments` DB override taking precedence over an affinity match, (f) `invalidateAffinityCache()` causes re-query on next dispatch, (g) `change_type`/`file_type` defaulting to `'unknown'` when not provided.

- [ ] **AC-11**: Integration test (Vitest, real test PostgreSQL — APIP-5001 test DB) with `wint.model_affinity` fixture rows pre-inserted: verifies that a dispatch call with a matching `(change_type, file_type)` combination routes to the model from the fixture row (not the static chain default). Fixture rows must include at least one high-confidence row and one low-confidence row to verify fallback. Tagged `@integration`.

- [ ] **AC-12**: All existing APIP-0040 unit tests continue to pass. No file in `packages/backend/orchestrator/src/models/` is modified. Existing `PipelineModelRouter` behaviors (rate limiting, budget accumulation, escalation chain, `BudgetExhaustedError`, `ProviderChainExhaustedError`) are unaffected.

- [ ] **AC-13**: `PipelineModelRouter.dispatch()` handles `wint.model_affinity` DB query failure gracefully — if the affinity query throws (e.g., DB unavailable), the router logs a warning and falls through to the static escalation chain without re-throwing. Budget enforcement and rate limiting continue to operate on the fallback path.

- [ ] **AC-14**: _(Elaboration fix)_ The affinity query sort order is `ORDER BY avg_cost_usd ASC, success_rate DESC` (cheapest qualifying model first, highest success_rate as tiebreaker). The column name is `avg_cost_usd` (not `cost_per_token`). This is the authoritative ordering — AC-2's inline description contains a reversed sort order and incorrect column name that must be read as superseded by this AC and the Architecture Notes SQL. Implementors MUST follow AC-14 + Architecture Notes SQL for the sort order, not AC-2's inline text.
_Added by autonomous elaboration_

- [ ] **AC-15**: _(Elaboration fix)_ The affinity data source is injected via an `AffinityReaderSchema` (Zod-typed callable) defined in `pipeline/__types__/index.ts` and passed as an optional field in `PipelineModelRouterConfig` (defaulting to the real Drizzle implementation). This enables unit test mocking via `vi.fn()` without module-level `vi.mock()` patching. ST-1 must define `AffinityReaderSchema` alongside `AffinityProfileSchema`. ST-2 must consume it as the query source.
_Added by autonomous elaboration_

## Reuse Plan

- **Components**: None (no UI)
- **Extend in-place**: `PipelineModelRouter` in `packages/backend/orchestrator/src/pipeline/model-router.ts` — additive affinity layer preserving all v1 functionality
- **Pattern reuse**: Follow `wint.model_assignments` DB-read + in-memory cache + `invalidateCache()` pattern for affinity profile cache
- **Types reuse**: `ModelAffinitySelectSchema` from APIP-3020; `BudgetExhaustedError`, `ProviderChainExhaustedError`, `TaskContractSchema` from APIP-0040 (unchanged)
- **Infrastructure reuse**: `getProviderForModel()` from `packages/backend/orchestrator/src/config/llm-provider.ts` for provider derivation from model string
- **Logging reuse**: `@repo/logger` throughout — domain `'pipeline_model_router'`

## Architecture Notes

### Affinity Query SQL

```sql
SELECT model, success_rate, avg_cost_usd, confidence_level, sample_size
FROM wint.model_affinity
WHERE change_type = $1 AND file_type = $2
  AND confidence_level IN ('medium', 'high')
  AND success_rate >= $3
ORDER BY avg_cost_usd ASC, success_rate DESC
LIMIT 1;
```

The cheapest qualifying model wins. If multiple models tie on cost, prefer higher success_rate.

### Cache Structure

```typescript
// Key: `${changeType}:${fileType}`, Value: AffinityProfile | null
// null means "queried but no qualifying profile exists" — avoid re-querying
private affinityCache: Map<string, AffinityProfile | null> = new Map()
```

TTL is not time-based — managed exclusively by `invalidateAffinityCache()` which clears the entire Map. Called by the Pattern Miner cron after each aggregation run.

### Config Schema Extensions (Additive)

```typescript
// In PipelineModelRouterConfig Zod schema:
affinitySuccessRateThreshold: z.number().min(0).max(1).default(0.85),
affinityMinSampleSize: z.number().int().min(1).default(20),
```

### Dispatch Options Extension (Additive)

```typescript
// In PipelineDispatchOptionsSchema:
changeType: z.string().default('unknown'),
fileType: z.string().default('unknown'),
```

### Routing Decision Log Shape

```typescript
logger.info('routing_decision', {
  source: 'affinity' | 'db_assignment' | 'fallback',
  model: string,
  provider: string,
  change_type: string,
  file_type: string,
  confidence_level: string | null,
  success_rate: number | null,
  sample_size: number | null,
})
```

### Singleton Impact

`PipelineModelRouterFactory.getInstance()` creates the singleton on first call. The affinity cache is an instance-level `Map` initialized as empty. No DB query on construction — lazy-load on first dispatch for each `(change_type, file_type)` combination.

### Provider Derivation

`wint.model_affinity` stores `model` as a prefixed string (e.g., `'ollama/qwen2.5-coder:7b'`, `'openrouter/gpt-4o-mini'`). Parse `provider` from the model string prefix using `getProviderForModel()` from `packages/backend/orchestrator/src/config/llm-provider.ts`. If derivation fails, log warning and fall through to static chain.

### Graph Config Injection Pattern

Per APIP ADR-001 Decision 2, `PipelineModelRouter` is called within graph nodes via graph config injection — not directly by the supervisor. This pattern must be preserved. Nodes call `router.dispatch()` through the injected config reference. APIP-3040 does not change this wiring — it only changes what `dispatch()` does internally.

## Infrastructure Notes

- **No new infrastructure**: `PipelineModelRouter` runs on the dedicated local server (APIP ADR-001 Decision 4). In-memory affinity cache is valid for single-process architecture.
- **No HTTP endpoints**: No APIGW or Lambda changes.
- **DB migration dependency**: `wint.model_affinity` table is created by APIP-3020's migration. APIP-3040 does not define or run any migrations. If additional DB indexes are needed for the affinity query, follow APIP-5007 migration naming convention (standalone SQL migration file).

## Test Plan

See `_pm/test-plan.yaml` for full test specifications. Summary:

### Unit Tests (Vitest, mocked DB — no real database required)

| Test | AC Coverage |
|------|-------------|
| HP-1: Affinity routing selected (high-confidence profile) | AC-1, AC-2, AC-3, AC-7 |
| HP-2: DB override takes precedence over affinity | AC-3, AC-7 |
| HP-4: Cache populated on first dispatch, reused on second | AC-6 |
| EC-1: DB query failure → graceful fallback + logger.warn | AC-13 |
| EC-2: success_rate below threshold → fallback | AC-4 |
| EC-3: confidence_level 'none' → fallback | AC-4, AC-10c |
| EC-4: confidence_level 'low' → fallback | AC-4, AC-10c |
| EC-5: No matching profile (cold-start) → fallback | AC-4, AC-10b |
| ED-1: change_type/file_type default to 'unknown' | AC-8, AC-10g |
| ED-2: invalidateAffinityCache() → re-query on next dispatch | AC-6, AC-10f |
| ED-3: Existing APIP-0040 tests pass (regression) | AC-12 |
| ED-4: Cheapest model wins when multiple profiles exist | AC-2, AC-3 |

### Integration Test (@integration tag — requires APIP-5001 test DB + APIP-3020 migration)

| Test | AC Coverage |
|------|-------------|
| HP-3: Real DB affinity routing with fixture rows | AC-11 |

Per ADR-005, integration test must use real PostgreSQL — no in-memory SQLite substitute.

## Reality Baseline

- `PipelineModelRouter` v1 does not yet exist in main — it is defined by APIP-0040 (currently at `needs-code-review`).
- `wint.model_affinity` table does not exist — it is created by APIP-3020 (currently in `backlog`).
- `wint.model_assignments` table and `ModelRouterFactory` static tier router do exist in main.
- `@repo/logger`, `@repo/db`, Drizzle ORM patterns, and `pgSchema('wint')` namespace are all established.
- **Hard gates**: APIP-0040 merge → APIP-3020 complete → APIP-3040 implementation begin.

## Canonical References

Files that demonstrate the patterns this story should follow:

| Pattern | File | Why |
|---------|------|-----|
| `PipelineModelRouter` DB-backed config + caching pattern | `packages/backend/orchestrator/src/pipeline/model-router.ts` | The exact file APIP-3040 extends; shows `wint.model_assignments` DB read on init, in-memory cache, `invalidateAssignmentsCache()` pattern that the affinity query must mirror |
| Zod-first module with singleton factory + logger | `packages/backend/orchestrator/src/models/unified-interface.ts` | Canonical singleton factory pattern and Zod schema structure to follow for any new types added in APIP-3040 |
| `wint` schema table with numeric/enum columns | `packages/backend/database-schema/src/schema/telemetry.ts` | Pattern for reading Drizzle ORM `wint`-namespace tables with typed Zod results; mirrors what reading `wint.model_affinity` rows will look like |
| LangGraph node injection via graph config | `packages/backend/orchestrator/src/nodes/elaboration/index.ts` | Pattern for injecting `PipelineModelRouter` as a graph config dependency so nodes call `router.dispatch()` — prevents hard imports and enables unit test mocking |

## Subtasks

Derived from `_pm/dev-feasibility.yaml`. Every AC is covered by at least one subtask.

| Subtask | Goal | ACs | Depends On | Files Modified |
|---------|------|-----|-----------|----------------|
| ST-1 | Extend PipelineModelRouterConfig Zod schema with affinity threshold constants | AC-5, AC-8, AC-9 | none | `pipeline/__types__/index.ts` |
| ST-2 | Add in-memory affinity profile cache and queryAffinityProfile() method | AC-2, AC-6 | ST-1 | `pipeline/model-router.ts` |
| ST-3 | Implement three-tier routing precedence in dispatch() | AC-1, AC-3, AC-4, AC-8, AC-13 | ST-2 | `pipeline/model-router.ts` |
| ST-4 | Add structured routing decision logging to dispatch() | AC-7 | ST-3 | `pipeline/model-router.ts` |
| ST-5 | Write unit tests for all AC-10 sub-cases (a-g) | AC-10, AC-13 | ST-4 | `pipeline/__tests__/model-router.test.ts` |
| ST-6 | Write integration test with real test PostgreSQL fixture rows | AC-11 | ST-5 | `pipeline/__tests__/model-router.integration.test.ts` |

### AC-Subtask Coverage Check

| AC | Covered By |
|----|-----------|
| AC-1 | ST-3 |
| AC-2 | ST-2 |
| AC-3 | ST-3 |
| AC-4 | ST-3 |
| AC-5 | ST-1 |
| AC-6 | ST-2 |
| AC-7 | ST-4 |
| AC-8 | ST-1, ST-3 |
| AC-9 | ST-1 |
| AC-10 | ST-5 |
| AC-11 | ST-6 |
| AC-12 | ST-5 (regression run) |
| AC-13 | ST-3, ST-5 |
