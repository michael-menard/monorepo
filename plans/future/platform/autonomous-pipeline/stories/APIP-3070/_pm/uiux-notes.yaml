agent: pm-uiux-recommendations
story_id: APIP-3070
generated: "2026-02-25"
skipped: true
skip_reason: "No UI impact. APIP-3070 is entirely headless — cold-start detection, exploration budget, and profile seeding are internal pipeline routing mechanisms invisible to end users. ADR-006 E2E Playwright requirement does not apply (frontend_impacted: false)."

observability_notes:
  - "Primary operator-visible output is structured log entries via @repo/logger with new fields: source: 'cold_start' | 'exploration'"
  - "These log fields extend the existing APIP-3040 routing log and should feed APIP-5005 (Minimal Operator CLI) for cold-start → affinity transition observability"
  - "Consider a 'pipeline maturity level' concept in APIP-5005: fraction of (change_type, file_type) combinations with confidence_level = 'high' as a percentage progress indicator"
  - "The importAffinitySeeds() utility could benefit from a CLI command in APIP-5005 (e.g., pipeline seed-affinity --file seeds.yaml) deferred to that story"

deferred_to: ["APIP-5005"]
