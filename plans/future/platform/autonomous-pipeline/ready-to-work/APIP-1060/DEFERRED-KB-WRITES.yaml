deferred_at: "2026-02-25T00:00:00Z"
reason: "KB unavailable in pm-story-generation-leader context (no pg client accessible)"
retry_when: "Next session with KB MCP tool available"

writes:
  - table: stories
    operation: upsert
    data:
      story_id: APIP-1060
      feature: autonomous-pipeline
      title: "QA Graph with Autonomous Verdict"
      story_dir: plans/future/platform/autonomous-pipeline/backlog/APIP-1060
      story_file: plans/future/platform/autonomous-pipeline/backlog/APIP-1060/APIP-1060.md
      story_type: feature
      points: 8
      priority: high
      state: backlog
      touches_backend: true
      touches_frontend: false
      touches_database: false
      touches_infra: false
    sql: >
      INSERT INTO stories (
        story_id, feature, title, story_dir, story_file, story_type,
        points, priority, state, touches_backend, touches_frontend,
        touches_database, touches_infra
      ) VALUES (
        'APIP-1060', 'autonomous-pipeline', 'QA Graph with Autonomous Verdict',
        'plans/future/platform/autonomous-pipeline/backlog/APIP-1060',
        'plans/future/platform/autonomous-pipeline/backlog/APIP-1060/APIP-1060.md',
        'feature', 8, 'high', 'backlog', true, false, false, false
      )
      ON CONFLICT (story_id) DO UPDATE SET
        title = EXCLUDED.title,
        story_dir = EXCLUDED.story_dir,
        story_file = EXCLUDED.story_file,
        story_type = EXCLUDED.story_type,
        points = EXCLUDED.points,
        priority = EXCLUDED.priority,
        state = EXCLUDED.state,
        touches_backend = EXCLUDED.touches_backend,
        touches_frontend = EXCLUDED.touches_frontend,
        touches_database = EXCLUDED.touches_database,
        touches_infra = EXCLUDED.touches_infra;
