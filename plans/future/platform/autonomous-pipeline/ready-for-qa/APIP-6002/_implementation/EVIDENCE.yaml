schema: 1
story_id: APIP-6002
version: 1
timestamp: "2026-03-03T20:00:00Z"

acceptance_criteria:
  - ac_id: AC-1
    ac_text: "detect_stuck_stories() function exists, scans all stage dirs, returns stuck stories with pattern"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "detect_stuck_stories() function at line 810 — scans in-progress, needs-code-review, failed-code-review, ready-for-qa, failed-qa; returns STORY_ID<tab>STAGE<tab>PATTERN<tab>MINUTES per stuck story"

  - ac_id: AC-2
    ac_text: "recover_story() function exists, takes story_id and pattern, applies recovery action, writes STUCK-RECOVERY.yaml"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "recover_story() at line 880 — takes STORY_ID, STAGE, PATTERN, MINUTES, TAG; writes STUCK-RECOVERY.yaml with schema, story_id, stage_at_detection, pattern, minutes_stuck, action, notes fields"

  - ac_id: AC-3
    ac_text: "exhausted-retries recovery: resets retry counter, moves failed-code-review→needs-code-review or failed-qa→ready-for-qa with max 1 additional recovery attempt per run"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 909-926 in recover_story(): exhausted-retries case moves failed-code-review→needs-code-review and failed-qa→ready-for-qa. Lines 1060-1063 in run_stuck_recovery_sweep(): MAX_RECOVERY_ATTEMPTS cap enforced (default=1)"

  - ac_id: AC-4
    ac_text: "no-evidence-no-commits recovery: moves story to ready-to-work, cleans up stale worktree if present"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 929-943 in recover_story(): no-evidence-no-commits case calls git worktree remove --force, git branch -D, git worktree prune on WORKTREE_BASE/STORY_ID, then move_story_to ready-to-work"

  - ac_id: AC-5
    ac_text: "missing-review-bouncing recovery: writes RECOVERY-NOTE.yaml with diagnosis, moves to needs-code-review with force-review flag"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 945-960 in recover_story(): missing-review-bouncing case writes RECOVERY-NOTE.yaml with diagnosis, recovery_action, force_review=true; then moves story to needs-code-review"

  - ac_id: AC-6
    ac_text: "STUCK-ALERT.yaml written for stories stuck >8 hours with full diagnosis"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 1010-1033: STUCK_ALERT_SECONDS=28800 (8 hours). When SECONDS_STUCK >= threshold, writes STUCK-ALERT.yaml with alert_type, stage, pattern, minutes_stuck, missing_artifacts, recovery_action, recovery_notes, human_review_recommended=true"

  - ac_id: AC-7
    ac_text: "detect_stuck_stories() sweep called at end of main process loop"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 2020-2021: run_stuck_recovery_sweep() called after 'wait' in the parallel batch loop (post-batch, before collect results). Comment: '# ── Post-batch stuck recovery sweep (AC-7)'"

  - ac_id: AC-8
    ac_text: "--detect-stuck flag added; running with that flag invokes only detect_stuck_stories() and recover_story() without processing stories"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Line 118: '--detect-stuck) DETECT_STUCK=true; shift ;;' in arg parser. Lines 1950-1960: standalone mode block — prints header, calls run_stuck_recovery_sweep, exits 0 before any story processing begins"

  - ac_id: AC-9
    ac_text: "Idempotency: detect_stuck_stories() checks for existing STUCK-RECOVERY.yaml before writing; running twice does not duplicate artifacts"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Lines 898-902 in recover_story(): idempotency guard checks 'if [[ -f \"$RECOVERY_FILE\" ]]' and returns 0 with RECOVERY SKIP message if STUCK-RECOVERY.yaml already exists. RECOVERY_FILE is IMPL_DIR/STUCK-RECOVERY.yaml — once written it blocks all subsequent recovery writes for that story"

  - ac_id: AC-10
    ac_text: "All recovery actions emit structured log line: RECOVERY: {story_id} pattern={pattern} action={action}"
    status: PASS
    evidence_items:
      - type: code
        path: "scripts/implement-stories.sh"
        description: "Line 995: 'echo \"$TAG RECOVERY: $STORY_ID pattern=\\${PATTERN} action=\\${ACTION}\"' — emitted after the action is determined but before writing the artifact, covers all code paths including no-action cases"

touched_files:
  - path: "scripts/implement-stories.sh"
    action: modified
    lines: 2112
    description: "Added detect_stuck_stories(), recover_story(), run_stuck_recovery_sweep(), get_mtime_epoch(), minutes_since_modified() functions; DETECT_STUCK, MAX_RECOVERY_ATTEMPTS, STUCK_THRESHOLD_SECONDS, STUCK_ALERT_SECONDS variables; --detect-stuck arg; --detect-stuck standalone mode; post-batch sweep call"

commands_run:
  - command: "bash -n scripts/implement-stories.sh"
    result: SUCCESS
    output: "Syntax OK"
    timestamp: "2026-03-03T20:00:00Z"

endpoints_exercised: []

notable_decisions:
  - "All functions added inline to implement-stories.sh per ARCH-001 decision — no separate lib file"
  - "macOS stat -f %m used with GNU stat -c %Y fallback per ARCH-002 decision"
  - "STUCK-RECOVERY.yaml and STUCK-ALERT.yaml written to _implementation/ per ARCH-003 decision"
  - "STUCK_THRESHOLD_SECONDS=14400 (4hr) chosen as conservative minimum; STUCK_ALERT_SECONDS=28800 (8hr) for human alerts"
  - "MAX_RECOVERY_ATTEMPTS=1 cap per pipeline run prevents infinite recovery loops"
  - "Sweep runs after wait in main batch loop — after all parallel workers complete, before result collection"
  - "--detect-stuck standalone mode exits before filtering/running stories — pure sweep mode"

known_deviations:
  - "shellcheck not run — tool not installed on this machine (listed as required: false in plan)"

test_summary:
  unit: { pass: 0, fail: 0 }
  e2e: { pass: 0, fail: 0 }

e2e_tests:
  status: exempt
  exempt_reason: "story_type: feature but story is infra-only bash script — no frontend or API endpoints, no E2E surface"
  tests_written: false
  results:
    total: 0
    passed: 0
    failed: 0
    skipped: 0

coverage:
  lines: null
  branches: null

token_summary:
  execute: { in: 45000, out: 12000 }
