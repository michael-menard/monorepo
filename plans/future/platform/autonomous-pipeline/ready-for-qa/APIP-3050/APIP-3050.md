---
id: APIP-3050
title: "Story Structurer Feedback (Affinity-Guided)"
status: ready-to-work
priority: medium
phase: 3
experiment_variant: "control"
depends_on:
  - APIP-1010
  - APIP-3020
points: 5
story_type: feature
touches_backend: true
touches_frontend: false
touches_database: false
touches_infra: false
created: "2026-02-25"
feature: autonomous-pipeline
story_dir: plans/future/platform/autonomous-pipeline/ready-to-work/APIP-3050

pm_artifacts:
  test_plan:
    strategy: unit+integration
    scope:
      endpoints: []
      ui_touched: false
      data_touched: true
    happy_path_tests:
      - id: HP-1
        title: "Affinity-guided pattern selection applies correctly for high-confidence profile"
        setup: "StructurerConfig with affinityEnabled:true, minAffinityConfidence:'low', maxAffinityWeight:1.0. Mock DB returns profile (model='haiku', change_type='create', file_type='tsx', confidence_level='high', success_rate=0.92, sample_size=45)."
        action: "Call readAffinityProfiles() with changeOutline containing TSX create-type items."
        expected: "ChangeOutlineItem.extensions.preferredChangePattern === 'create-new-file'."
        evidence: "Unit test asserts extensions object shape matches AffinityGuidanceSchema fields."
      - id: HP-2
        title: "Config extension adds affinityEnabled and minAffinityConfidence with correct defaults"
        setup: "Parse StructurerConfigSchema with no overrides."
        action: "Parse empty object through updated StructurerConfigSchema."
        expected: "affinityEnabled defaults to true; minAffinityConfidence defaults to 'low'; maxAffinityWeight defaults to 0.5."
        evidence: "Unit test: StructurerConfigSchema.parse({}) returns expected defaults."
      - id: HP-3
        title: "Integration: real DB fixture produces expected extensions"
        setup: "APIP-5001 test DB with wint.model_affinity row inserted for haiku/create/tsx with confidence 'high'."
        action: "Run Structurer with real DB injection against outline with TSX create-type items."
        expected: "extensions.preferredChangePattern === 'create-new-file' for matching items."
        evidence: "@integration tagged Vitest test asserts extensions match fixture data."
    error_cases:
      - id: EC-1
        title: "DB unavailable: Structurer falls back gracefully and logs warn"
        expected: "Error caught; logger.warn called; fallbackUsed:true; no extensions set; elaboration continues."
        evidence: "Spy on logger.warn; assert called with storyId and error."
      - id: EC-2
        title: "affinityEnabled:false — no DB call made"
        expected: "DB never called. Output identical to APIP-1010 Structurer baseline."
        evidence: "Spy on db.query; assert never called."
      - id: EC-3
        title: "minAffinityConfidence gate: 'low' confidence not applied when minimum is 'medium'"
        expected: "Profile not applied; no preferredChangePattern on output."
        evidence: "Unit test with confidence below threshold."
    edge_cases:
      - id: ED-1
        title: "Cold-start: no rows returned"
        expected: "fallbackUsed:true; logger.info with cold-start reason; no extensions set."
        evidence: "Spy on logger.info; assert called."
      - id: ED-2
        title: "APIP-1010 regression: all existing Structurer tests pass unchanged"
        expected: "Zero regressions in structurer test suite."
        evidence: "CI output shows 0 failures."
    risks:
      - "Integration tests require APIP-3020 to be merged and wint.model_affinity table to exist"
      - "APIP-1010 must merge before implementation begins"
      - "Model identifier format must match APIP-0040 exactly"

  dev_feasibility:
    feasible: true
    confidence: high
    complexity: medium
    rationale: "Well-scoped extension to existing Structurer node. Patterns from delta-detect.ts and metrics.ts DB injection provide clear implementation guides. Hard gates: APIP-1010 and APIP-3020 must merge first."
    change_surface:
      areas:
        - packages/backend/orchestrator/src/nodes/elaboration/structurer.ts
        - packages/backend/orchestrator/src/nodes/elaboration/affinity-reader.ts
        - packages/backend/orchestrator/src/nodes/elaboration/index.ts
        - packages/backend/orchestrator/src/graphs/elaboration.ts
      endpoints: []
      deploy_touchpoints:
        - "No migration required — APIP-3020 owns wint.model_affinity table"
        - "pnpm check-types --filter packages/backend/orchestrator must pass"
    risks:
      - finding: "Hard dependency on APIP-1010 (structurer.ts does not exist yet)"
        mitigation: "Gate implementation on APIP-1010 merge."
      - finding: "Hard dependency on APIP-3020 (wint.model_affinity table not built)"
        mitigation: "Write against APIP-3020 finalized spec; integration tests gate on APIP-3020 completion."
      - finding: "Model identifier format must match APIP-0040 exactly"
        mitigation: "Confirm format before writing affinity query; document in code comment."

predictions:
  split_risk: 0.7
  review_cycles: 3
  token_estimate: 180000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-25T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
---

# APIP-3050: Story Structurer Feedback (Affinity-Guided)

## Context

After the autonomous pipeline's learning system (Phase 3) begins accumulating per-model affinity profiles in `wint.model_affinity` (APIP-3020), the change pattern preferences encoded in those profiles should feed back into the story structuring stage — before implementation even begins.

The Structurer node (APIP-1010) currently uses heuristic complexity estimation only: it identifies what changes are needed, but it does not know which models will execute those changes or which change patterns those models handle most reliably.

APIP-3050 extends the Structurer node to read affinity profiles from `wint.model_affinity` and use them to bias the `preferredChangePattern` field in each `ChangeOutlineItem.extensions`. For example: if the affinity data shows that a given model has a 90% success rate on `create-new-file` changes to TypeScript files but only 40% on `modify-complex-existing`, the Structurer should prefer decompositions that favor the former pattern — splitting "modify the UserProfile component to add sub-components" into "create new SubProfileCard component" + "import SubProfileCard into UserProfile" — before the diff planner ever runs.

This is purely an optimization overlay on top of APIP-1010's heuristic path. The elaboration pipeline must never be blocked by the affinity system — affinity guidance is an optimization overlay, not a hard dependency.

**Baseline status (2026-02-13):** No `structurer.ts` file exists in `packages/backend/orchestrator/src/nodes/elaboration/` — APIP-1010 (the story that builds the Structurer node) is currently In Progress. No affinity profile reading capability exists anywhere in the elaboration graph. `wint.model_affinity` table does not exist (APIP-3020 backlog).

## Goal

Feed learned model strengths back into the story structuring stage by extending the Structurer node to read affinity profiles from `wint.model_affinity` and bias `ChangeOutlineItem.extensions.preferredChangePattern` accordingly — reducing implementation escalation cost before a single line of code is written.

## Non-Goals

- Modifying the diff planner (APIP-1030) or any graph downstream of elaboration
- Implementing the affinity profile table or Pattern Miner cron (APIP-3020)
- Implementing the change telemetry writer (APIP-3010)
- Learning-Aware Diff Planner (APIP-3030) or Learning-Aware Model Router (APIP-3040)
- Automated story splitting based on affinity data
- Any UI / operator dashboard changes (APIP-2020 / APIP-5005)
- Writing to `wint.model_affinity` from the Structurer — read-only; writes are APIP-3020's Pattern Miner
- Modifying `packages/backend/database-schema/` directly — no new tables in this story
- Defining the model identifier convention — that is APIP-0040's domain; APIP-3050 consumes it
- Modifying `FinalAcceptanceCriterionSchema` or `SynthesizedStory`

## Scope

**Packages touched:**
- `packages/backend/orchestrator` — primary target (Structurer node extension)
- `packages/backend/database-schema` — read-only (`ModelAffinitySelectSchema` from APIP-3020)

**Files modified (post APIP-1010 and APIP-3020 merges):**
- `packages/backend/orchestrator/src/nodes/elaboration/structurer.ts` — extend config schema and node implementation
- `packages/backend/orchestrator/src/nodes/elaboration/affinity-reader.ts` — new file (readAffinityProfiles function)
- `packages/backend/orchestrator/src/nodes/elaboration/index.ts` — export new schemas
- `packages/backend/orchestrator/src/graphs/elaboration.ts` — extend ElaborationConfigSchema with affinityConfig sub-object

**No HTTP endpoints. No database schema changes.**

## Acceptance Criteria

- [ ] **AC-1**: `StructurerConfigSchema` (from APIP-1010) is extended with two new optional fields: `affinityEnabled: z.boolean().default(true)` and `minAffinityConfidence: z.enum(['none', 'low', 'medium', 'high']).default('low')`. No existing callers are affected — both fields have defaults.

- [ ] **AC-2**: A `readAffinityProfiles(changeOutline, db, config)` function (or equivalent) is added to `structurer.ts` (or a new `affinity-reader.ts` helper). Given a `ChangeOutlineItem[]` and an injected `db: Db`, it queries `wint.model_affinity` for each `(model, changeType, fileType)` combination in the outline. It returns a map of `changeOutlineItemId → AffinityGuidance | null`. The function is wrapped in try/catch: on DB error, it logs `logger.warn()` and returns an all-null map (silently falls back).

- [ ] **AC-3**: `AffinityGuidanceSchema` is a new Zod schema with fields: `changeOutlineItemId` (string), `preferredPattern` (enum: `'create-new-file' | 'modify-existing' | 'split-component' | 'no-preference'`), `confidenceLevel` (from APIP-3020's `confidenceLevelEnum`: `'none' | 'low' | 'medium' | 'high'`), `successRate` (number, 0–1), `sampleSize` (integer). All types are `z.infer<typeof AffinityGuidanceSchema>` — no TypeScript interfaces.

- [ ] **AC-4**: When `affinityEnabled === true` and a profile exists with `confidence_level >= minAffinityConfidence`, the Structurer sets `ChangeOutlineItem.extensions.preferredChangePattern` to the affinity-recommended pattern. When no profile exists, or `confidence_level` is below the minimum threshold, `extensions.preferredChangePattern` is omitted (or set to `'no-preference'`).

- [ ] **AC-5**: The affinity-guided path never modifies `complexity`, `estimatedAtomicChanges`, `filePath`, `changeType`, `description`, or `relatedAcIds` on a `ChangeOutlineItem`. Only `extensions.preferredChangePattern` (and optionally `extensions.affinityMeta: { confidenceLevel, successRate, sampleSize }`) is set by affinity guidance. Structural fields remain heuristic-driven.

- [ ] **AC-6**: A configurable `maxAffinityWeight: z.number().min(0).max(1).default(0.5)` parameter in `StructurerConfig` controls how strongly affinity data influences pattern selection. At weight 1.0, affinity fully determines the pattern; at weight 0.0, affinity is ignored (equivalent to `affinityEnabled: false`). This guard rail prevents over-optimization and ensures human-readable structural quality.

- [ ] **AC-7**: When `affinityEnabled === false`, the Structurer behaves identically to the APIP-1010 implementation — no affinity reads, no `extensions.preferredChangePattern` set, no DB access. This is verified by a unit test that asserts no DB call is made when `affinityEnabled: false`.

- [ ] **AC-8**: When `affinityEnabled === true` but the DB is unavailable (connection error, timeout), the Structurer: (a) catches the error, (b) calls `logger.warn({ storyId, error }, 'affinity profile read failed — using plain heuristic')`, (c) completes with `fallbackUsed: true` on `StructurerResult`, (d) produces a `changeOutline` with no `extensions.preferredChangePattern` fields. The elaboration is NOT blocked.

- [ ] **AC-9**: When `affinityEnabled === true` but no affinity rows exist for any `(model, changeType, fileType)` combination in the outline (cold-start state — APIP-3020 not yet populated), the Structurer completes successfully with `fallbackUsed: true`, logs `logger.info({ storyId, reason: 'cold-start — no affinity data' }, 'affinity guidance skipped')`, and returns the outline with no `extensions.preferredChangePattern` fields.

- [ ] **AC-10**: The DB instance is injected into the Structurer node via `config.configurable.db` — not imported directly. Unit tests mock this injection. Integration tests provide a real test DB instance with `wint.model_affinity` fixture rows.

- [ ] **AC-11**: Unit tests cover: (a) affinity-guided pattern selection with mock affinity data producing expected `extensions.preferredChangePattern`; (b) cold-start path (no rows returned → `fallbackUsed: true`, no extensions set); (c) DB error path (thrown → `fallbackUsed: true`, `logger.warn` called); (d) `affinityEnabled: false` (no DB call made); (e) `minAffinityConfidence: 'high'` gate (rows with `'low'` confidence are not applied); (f) `maxAffinityWeight: 0.0` (no patterns applied despite data present).

- [ ] **AC-12**: Integration test (against APIP-5001 test DB with `wint.model_affinity` fixture rows) verifies: inserting profiles for `(model='haiku', change_type='create', file_type='tsx', confidence='high', success_rate=0.92)` results in `ChangeOutlineItem.extensions.preferredChangePattern = 'create-new-file'` for matching TSX create-type items in the outline.

- [ ] **AC-13**: All existing APIP-1010 Structurer tests pass unchanged after APIP-3050 modifications. New fields added to `StructurerConfigSchema` are optional with defaults — no existing test fixtures need updating.

- [ ] **AC-14**: New schemas (`AffinityGuidanceSchema`) and updated `StructurerConfigSchema` are exported from `packages/backend/orchestrator/src/nodes/elaboration/index.ts`. `pnpm check-types --filter packages/backend/orchestrator` passes with zero errors.

## Reuse Plan

- **Patterns to extend**: `StructurerConfigSchema` with optional sub-fields and defaults; `createToolNode` + `config.configurable.db` injection for DB access (metrics.ts pattern); `ChangeOutlineItem.extensions` escape hatch for `preferredChangePattern`; `logger.warn` + `fallbackUsed: true` for graceful degradation
- **File structure to follow**: `delta-detect.ts` — canonical elaboration node file (Zod schemas at top, pure logic functions, factory, exported types)
- **DB injection pattern**: `metrics.ts` — shows how a LangGraph graph accepts a DB connection via config for read queries
- **Schema pattern**: `packages/backend/orchestrator/src/artifacts/story.ts` — canonical Zod artifact schema structure
- **Type**: `ModelAffinitySelectSchema` from APIP-3020 — used for typing profile lookup result
- **Packages**: `@repo/logger` for all logging

## Architecture Notes

- **Layer**: APIP ADR-001 Decision 2 — affinity-guided Structurer is a LangGraph node (correct layer). Supervisor does not interact with affinity data.
- **Server**: APIP ADR-001 Decision 4 — all components run on the dedicated local server. Affinity profile reads via direct Aurora PostgreSQL connection on the dedicated server. DB injected via graph config.
- **Non-blocking**: Affinity guidance is an optimization overlay — the elaboration pipeline must never be blocked by the affinity system. try/catch + logger.warn + fallbackUsed:true is the contract.
- **Extensions escape hatch**: APIP-1010 added `extensions: z.record(z.unknown()).optional()` to `ChangeOutlineItemSchema` specifically for APIP-3050. Use `extensions.preferredChangePattern` — do NOT add a top-level field until APIP-1020 spike confirms the schema.
- **Config pattern**: Add `affinityConfig` as a nested sub-object following `deltaDetectionConfig` / `escapeHatchConfig` pattern in `ElaborationConfigSchema`.
- **DB injection in Structurer**: `createToolNode('structurer', async (state: GraphState, config?) => { const s = state as ElaborationState; const db = config?.configurable?.db ?? null; ... })` — same TypeScript constraint from APIP-1010 GAP-004 applies.
- **Affinity query**: Run once per distinct `(changeType, fileType)` pair — not once per item.
- **maxAffinityWeight guard rail**: affinity guidance applies to change pattern selection only, never to structural analysis (file count, complexity tier, split threshold).
- **Protected areas**: `packages/backend/database-schema/` additive only; `@repo/db` API surface must not change; `packages/backend/orchestrator/src/models/` must not be modified.

## Infrastructure Notes

No new infrastructure. No Lambda. No new database tables. Reads from `wint.model_affinity` (APIP-3020) via injected DB connection on the dedicated local server.

## Seed Requirements

- APIP-1010 must be merged and `structurer.ts`, `StructurerConfigSchema`, `ChangeOutlineItemSchema`, and `createStructurerNode` must all be available before implementation begins.
- APIP-3020 must be merged and `wint.model_affinity`, its Drizzle schema, and `ModelAffinitySelectSchema` must be available before the affinity query can be validated.
- Model identifier format must be confirmed with APIP-0040 before writing the affinity query (e.g., `'claude-haiku-4-5'` vs `'haiku'`). Document the convention in a code comment in `affinity-reader.ts`.

## Test Plan

**Strategy**: Unit + Integration (no E2E — ADR-006)

**Unit tests (Vitest, mocked DB):**
- HP-1: Affinity-guided pattern selection with high-confidence mock profile → extensions.preferredChangePattern set correctly
- HP-2: StructurerConfigSchema.parse({}) returns correct defaults (affinityEnabled:true, minAffinityConfidence:'low', maxAffinityWeight:0.5)
- HP-3: AffinityGuidanceSchema validates correct shape
- HP-5: Structural fields unchanged after affinity guidance applied
- EC-1: DB connection error → logger.warn called, fallbackUsed:true, no extensions set
- EC-2: affinityEnabled:false → DB never called, output matches APIP-1010 baseline
- EC-3: minAffinityConfidence gate → low-confidence rows ignored when minimum is 'medium'
- EC-4: maxAffinityWeight:0.0 → no patterns applied despite data present
- EC-5: confidence_level:'none' → treated as no data, fallback used
- EC-6: null DB injection → warn + fallback
- ED-1: Cold-start (empty rows) → fallbackUsed:true, logger.info with cold-start reason
- ED-2: All items same (changeType, fileType) → single DB query only

**Integration tests (Vitest + APIP-5001 test DB, @integration tag):**
- HP-4: Real DB fixture (haiku/create/tsx/high/0.92) → extensions.preferredChangePattern === 'create-new-file'
- ED-3: Empty wint.model_affinity table → cold-start fallback

**Regression:**
- ED-4: Full pnpm test --filter packages/backend/orchestrator → zero regressions from APIP-1010 Structurer suite

**Type check:**
- ED-5: pnpm check-types --filter packages/backend/orchestrator → 0 errors

## Reality Baseline

- Baseline date: 2026-02-13
- `structurer.ts` does not exist — APIP-1010 In Progress
- `wint.model_affinity` table does not exist — APIP-3020 backlog
- No affinity-guided heuristics exist anywhere in the elaboration graph
- `createToolNode` factory: `packages/backend/orchestrator/src/runner/node-factory.ts` — exists
- `delta-detect.ts` canonical pattern: `packages/backend/orchestrator/src/nodes/elaboration/delta-detect.ts` — exists
- `metrics.ts` DB injection pattern: `packages/backend/orchestrator/src/graphs/metrics.ts` — exists
- `ElaborationConfigSchema`: `packages/backend/orchestrator/src/graphs/elaboration.ts` — exists with `deltaDetectionConfig`, `deltaReviewConfig`, `escapeHatchConfig` sub-objects

## Canonical References

| Pattern | File | Why |
|---------|------|-----|
| Elaboration node extension (Zod + createToolNode) | `/Users/michaelmenard/Development/monorepo/packages/backend/orchestrator/src/nodes/elaboration/delta-detect.ts` | Canonical pattern for an elaboration node file: Zod schemas at top, pure logic functions, `createToolNode` factory, exported types. Any new `affinity-reader.ts` or extension to `structurer.ts` should mirror this file's structure exactly. |
| Graph config sub-object with DB injection | `/Users/michaelmenard/Development/monorepo/packages/backend/orchestrator/src/graphs/metrics.ts` | Only existing graph that accepts a DB connection via config for read operations. Affinity profile reads from Structurer require the same injection pattern — `db` passed via `config.configurable`, not imported directly. |
| Graph state + config extension | `/Users/michaelmenard/Development/monorepo/packages/backend/orchestrator/src/graphs/elaboration.ts` | Shows how to add an optional config sub-object to `ElaborationConfigSchema` with `.default({})`. The `affinityConfig` sub-object follows the same `deltaDetectionConfig` / `escapeHatchConfig` pattern already present. |
| Zod-first schema pattern | `/Users/michaelmenard/Development/monorepo/packages/backend/orchestrator/src/artifacts/story.ts` | Canonical Zod artifact schema structure — any new `AffinityGuidanceSchema` or extensions to existing schemas must follow this naming and structural pattern. No TypeScript interfaces. |

## Subtasks

### ST-1: Extend StructurerConfigSchema with affinity config sub-object
- **Goal**: Add `affinityConfig` nested sub-object to `StructurerConfigSchema` with `affinityEnabled`, `minAffinityConfidence`, `maxAffinityWeight` fields — all with defaults. No breaking change.
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/structurer.ts`
- **ACs covered**: AC-1, AC-6
- **Depends on**: none (gates on APIP-1010 merge)
- **Verification**: `pnpm check-types --filter packages/backend/orchestrator`
- **Estimated tokens**: 4,000

### ST-2: Create AffinityGuidanceSchema and readAffinityProfiles() function
- **Goal**: Add `AffinityGuidanceSchema` (Zod, no interfaces). Implement `readAffinityProfiles(changeOutline, db, config)` in `affinity-reader.ts` following `delta-detect.ts` file structure. Function queries `wint.model_affinity` once per distinct `(changeType, fileType)` pair. Returns map of `itemId → AffinityGuidance | null`. Wrapped in try/catch with `logger.warn` fallback.
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/affinity-reader.ts` (new file)
- **ACs covered**: AC-2, AC-3
- **Depends on**: ST-1
- **Verification**: `pnpm check-types --filter packages/backend/orchestrator`
- **Estimated tokens**: 8,000

### ST-3: Integrate affinity guidance into Structurer node implementation
- **Goal**: Extend `createStructurerNode` (APIP-1010) to call `readAffinityProfiles()` when `affinityEnabled:true` and apply returned `AffinityGuidance` to `ChangeOutlineItem.extensions.preferredChangePattern`. DB injected via `config.configurable.db`. Handle null db → warn + fallback. Apply `maxAffinityWeight` blending. Set `fallbackUsed:true` on `StructurerResult` when affinity skipped.
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/structurer.ts`
- **ACs covered**: AC-4, AC-5, AC-8, AC-9, AC-10
- **Depends on**: ST-2
- **Verification**: `pnpm check-types --filter packages/backend/orchestrator`
- **Estimated tokens**: 10,000

### ST-4: Export new schemas from elaboration index
- **Goal**: Export `AffinityGuidanceSchema` and `AffinityConfigSchema` from `packages/backend/orchestrator/src/nodes/elaboration/index.ts`. Verify pnpm check-types passes.
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/index.ts`
- **ACs covered**: AC-14
- **Depends on**: ST-3
- **Verification**: `pnpm check-types --filter packages/backend/orchestrator`
- **Estimated tokens**: 2,000

### ST-5: Write unit tests for affinity-reader and Structurer integration
- **Goal**: Vitest unit tests covering all 6 AC-11 scenarios plus structural field invariants (AC-5), APIP-1010 baseline parity (AC-7), and regression guard (AC-13).
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/__tests__/affinity-reader.test.ts` (new), `packages/backend/orchestrator/src/nodes/elaboration/__tests__/structurer-affinity.test.ts` (new)
- **ACs covered**: AC-7, AC-11, AC-13
- **Depends on**: ST-3
- **Verification**: `pnpm test --filter packages/backend/orchestrator`
- **Estimated tokens**: 12,000

### ST-6: Write integration test against APIP-5001 test DB
- **Goal**: Vitest `@integration` test. Insert fixture row for haiku/create/tsx/high/0.92. Run Structurer with real DB. Assert `extensions.preferredChangePattern === 'create-new-file'`. Verify cold-start behavior. Gates on APIP-3020 completion.
- **Files to modify**: `packages/backend/orchestrator/src/nodes/elaboration/__tests__/structurer-affinity.integration.test.ts` (new)
- **ACs covered**: AC-12
- **Depends on**: ST-5
- **Verification**: `pnpm test --filter packages/backend/orchestrator` (integration tag gated on APIP-3020)
- **Estimated tokens**: 8,000
