---
id: APIP-3060
title: "Bake-Off Engine for Model Experiments"
status: ready-for-qa
priority: medium
phase: 3
epic: autonomous-pipeline
story_type: feature
points: 8
experiment_variant: "control"
depends_on:
  - APIP-3020
  - APIP-3010
touches_backend: true
touches_frontend: false
touches_database: true
touches_infra: false
created: "2026-02-25T00:00:00Z"
updated: "2026-03-01T04:01:00Z"
pm_artifacts:
  test_plan:
    strategy: unit+integration
    scope:
      endpoints: []
      ui_touched: false
      data_touched: true
    happy_path_tests:
      - id: HP-1
        title: "Bake-off runs and promotes challenger winner when significance reached"
        setup: "Insert one active wint.model_experiments row with min_sample_per_arm=50. Insert 60 telemetry rows per arm: control 50% success, challenger 60% success (delta=0.10 >= MIN_ABSOLUTE_DELTA=0.05)."
        action: "Call runBakeOff(config) against real lego_dev PostgreSQL DB (port 5432)"
        expected: "wint.model_affinity upserted with challenger_model as winner. Experiment marked status='concluded', winner set, concluded_at IS NOT NULL. Structured log emitted."
        evidence: "SELECT winner, status, concluded_at FROM wint.model_experiments; SELECT preferred_model FROM wint.model_affinity"
      - id: HP-2
        title: "Bake-off does not promote when delta is below MIN_ABSOLUTE_DELTA"
        setup: "60 rows per arm, delta = 0.02 < 0.05."
        action: "Call runBakeOff(config)"
        expected: "Experiment remains status='active'. wint.model_affinity NOT updated."
        evidence: "SELECT status FROM wint.model_experiments — must still be 'active'"
      - id: HP-3
        title: "Bake-off promotes control when control beats challenger"
        setup: "60 rows per arm: control 70%, challenger 60%. Delta=0.10. Control wins."
        action: "Call runBakeOff(config)"
        expected: "wint.model_affinity upserted with control_model. Experiment marked concluded with winner=control_model."
        evidence: "SELECT winner FROM wint.model_experiments"
      - id: HP-4
        title: "No-op when no active experiments"
        setup: "No rows in wint.model_experiments with status='active'"
        action: "Call runBakeOff(config)"
        expected: "No DB writes. Log emitted with { active_experiments: 0, durationMs: N }."
        evidence: "Inspect DB and log output"
      - id: HP-5
        title: "Multiple simultaneous active experiments processed independently"
        setup: "Two active experiments with different change_type/file_type. Experiment A: challenger wins. Experiment B: insignificant delta."
        action: "Call runBakeOff(config)"
        expected: "Experiment A concluded with winner. Experiment B remains active."
        evidence: "SELECT status, winner FROM wint.model_experiments for each"
    error_cases:
      - id: EC-1
        title: "Experiment expires by max_window_rows without reaching significance"
        setup: "Active experiment with max_window_rows=100. Insert 51 rows per arm. Delta=0.03 < 0.05."
        action: "Call runBakeOff(config)"
        expected: "status='expired', winner=null, concluded_at IS NOT NULL. model_affinity NOT updated. Log: status='expired', reason='window_exceeded'."
        evidence: "SELECT status, winner FROM wint.model_experiments"
      - id: EC-2
        title: "Experiment expires by max_window_days without reaching significance"
        setup: "Active experiment with max_window_days=7, started_at=NOW()-8 days."
        action: "Call runBakeOff(config)"
        expected: "status='expired'. model_affinity NOT updated."
        evidence: "SELECT status FROM wint.model_experiments"
      - id: EC-3
        title: "Additive migration runs safely when change_telemetry does not exist"
        setup: "Fresh schema without wint.change_telemetry"
        action: "Run experiment_id FK migration"
        expected: "Migration completes without error (ALTER TABLE IF EXISTS guard)"
        evidence: "No SQL error; idempotency check passes"
    edge_cases:
      - id: ED-1
        title: "Significance check at exact MIN_SAMPLE_PER_ARM boundary (50 rows per arm)"
        setup: "Exactly 50 rows per arm, delta=0.10"
        action: "Run significance check (unit test)"
        expected: "Significance IS reached (boundary is inclusive)"
        evidence: "Unit test: isSignificant({samples: 50, delta: 0.10}) === true"
      - id: ED-2
        title: "Significance check at exactly MIN_ABSOLUTE_DELTA (0.05)"
        setup: "60 rows per arm, delta=0.05"
        action: "Unit test"
        expected: "Significance IS reached (boundary inclusive)"
        evidence: "Unit test: isSignificant({delta: 0.05}) === true"
      - id: ED-3
        title: "One arm has zero rows"
        setup: "Active experiment, 0 telemetry rows for challenger arm"
        action: "Call runBakeOff(config)"
        expected: "Significance not reached. Experiment stays active. No crash or NaN in success_rate."
        evidence: "Experiment status remains 'active'"
      - id: ED-4
        title: "Migration idempotency — run both migrations twice"
        action: "Apply both migrations a second time"
        expected: "No SQL errors (IF NOT EXISTS / IF EXISTS guards)"
        evidence: "No errors on second run"
    fixture_definitions:
      - name: active_experiment_fixture
        type: SQL INSERT
        description: "wint.model_experiments row with status='active', min_sample_per_arm=50, max_window_rows=500"
      - name: telemetry_rows_control_arm
        type: SQL INSERT batch
        description: "N rows in wint.change_telemetry with experiment_id set and model=control_model, outcome='success'/'failure' to hit target success rate"
      - name: telemetry_rows_challenger_arm
        type: SQL INSERT batch
        description: "N rows in wint.change_telemetry with experiment_id set and model=challenger_model"
    risks:
      - "Integration tests blocked until APIP-3010 (change_telemetry) and APIP-3020 (model_affinity) are complete"
      - "Migration sequence number may shift per APIP-5007 — glob for correct filename during implementation"
      - "wint schema DB is at port 5432 (lego_dev), not port 5433 (KB DB)"
  dev_feasibility:
    feasible: true
    confidence: high
    rationale: "All required patterns exist in the codebase. Scoped to schema, migrations, graph skeleton, significance logic, winner promotion, and tests — all implementable with existing tooling. Integration test gate behind APIP-3010+APIP-3020 is addressable via phased subtasks."
    complexity: high
    change_surface:
      areas:
        - packages/backend/database-schema/src/schema/wint.ts
        - packages/backend/orchestrator/src/graphs/bake-off-engine.ts
        - apps/api/knowledge-base/src/db/migrations/ (new migration: model_experiments)
        - apps/api/knowledge-base/src/db/migrations/ (new migration: change_telemetry experiment_id FK)
        - packages/backend/orchestrator/src/graphs/__tests__/bake-off-engine.test.ts
        - packages/backend/orchestrator/src/graphs/__tests__/bake-off-engine.integration.test.ts
      endpoints: []
      deploy_touchpoints:
        - "Aurora PostgreSQL migration must run before bake-off cron activated (APIP-3090)"
        - "Additive FK migration must run after APIP-3010 migration"
    risks:
      - finding: "Integration tests cannot run until APIP-3010 + APIP-3020 both complete"
        mitigation: "Phase subtasks: schema/migration/unit-test group completable immediately; integration-test group gated. Tag integration tests @integration."
      - finding: "Additive experiment_id FK migration must guard against change_telemetry not existing"
        mitigation: "ALTER TABLE IF EXISTS wint.change_telemetry ADD COLUMN IF NOT EXISTS experiment_id ..."
      - finding: "Concurrent bake-off cron runs could race on experiment status transitions"
        mitigation: "Use ON CONFLICT DO UPDATE for model_affinity upsert and conditional UPDATE WHERE status='active' for experiment status. APIP-3090 enforces single-instance scheduling."
    evidence_expectations:
      - "createBakeOffGraph() compiles without TypeScript errors"
      - "Unit tests pass for significance, expiry, no-op, and BakeOffConfigSchema validation"
      - "Integration test: fixture experiment + telemetry rows run through runBakeOff() → model_affinity updated, experiment concluded"
      - "Both SQL migrations run idempotently"
      - "Existing packages/backend/database-schema/src/schema/__tests__/ tests all pass"
predictions:
  split_risk: 0.8
  review_cycles: 3
  token_estimate: 200000
  confidence: low
  similar_stories: []
  generated_at: "2026-02-25T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
  note: "Heuristics-only mode (no KB similar stories available). 14 ACs + database + backend scope = high split risk. Complex story with phased subtasks."
qa_notes:
  verdict: CONDITIONAL_PASS
  decided_at: "2026-02-25T00:00:00Z"
  mode: autonomous
  acs_added: 0
  gaps_resolved: 0
  opportunities_logged: 4
---

# APIP-3060: Bake-Off Engine for Model Experiments

## Context

The autonomous pipeline's learning system (Phase 3) accumulates per-`(model, change_type, file_type)` success rate data in `wint.model_affinity` via the Pattern Miner cron job (APIP-3020). The Pattern Miner tracks historical performance of models already in use, but there is no mechanism to introduce a new model or variant and objectively determine whether it outperforms the current winner for a given change type.

Without a controlled experiment framework, model changes are ad-hoc: a new model is either adopted globally (high risk) or never adopted (no improvement). The system cannot determine statistically whether a proposed model variant is genuinely better, or whether observed differences are noise.

This story implements the infrastructure to close that gap: a `wint.model_experiments` Aurora PostgreSQL table and a Bake-Off Engine LangGraph cron graph that provides a data-driven, cost-aware mechanism to safely validate model improvements and automatically incorporate them into learned routing decisions.

**Dependencies**: APIP-3010 (provides `wint.change_telemetry` schema and rows), APIP-3020 (provides `wint.model_affinity` table and `confidenceLevelEnum`). Integration tests are gated behind both; schema/migration/unit-test subtasks are completable immediately.

## Goal

Implement the `wint.model_experiments` table, SQL migrations, and Bake-Off Engine LangGraph cron graph so that the pipeline can run controlled two-arm (control vs challenger) model experiments, evaluate statistical significance from accumulated telemetry, promote winning models into learned affinity profiles, and gracefully expire inconclusive experiments — all without human intervention.

## Non-Goals

- **A/B split assignment of incoming requests to experiment arms** — that is APIP-3040 (Learning-Aware Model Router) responsibility; it reads active experiments and routes accordingly; this story adds only the `experiment_id` FK column to `wint.change_telemetry`
- **HTTP API or operator UI for creating/viewing experiments** — deferred to APIP-2020 (Monitor UI) and APIP-5005 (Operator CLI)
- **Multi-arm (N > 2) experiments** — strictly two-arm (control vs challenger) in this story
- **Bayesian significance testing or complex statistical methods** — simple frequentist threshold (minimum samples + minimum delta) is sufficient for Phase 3
- **Cron job scheduling infrastructure** — APIP-3090; this story delivers the graph entry point only
- **Modifying `wint.model_assignments`** (Phase 0 static routing rules) — protected and separate
- **Modifying `@repo/db` client API surface** — protected
- **Cold-start bootstrapping or exploration budgets** — APIP-3070
- **Populating `experiment_id` during routing** — APIP-3040 is responsible for populating it; this story adds the FK column only

## Scope

**Packages touched**:
- `packages/backend/database-schema/src/schema/wint.ts` — new table definition + Zod schemas
- `packages/backend/orchestrator/src/graphs/bake-off-engine.ts` — new cron graph
- `apps/api/knowledge-base/src/db/migrations/` — two new SQL migration files
- `packages/backend/orchestrator/src/graphs/__tests__/` — unit + integration tests

**No HTTP endpoints. No Lambda. No UI. Internal cron only.**

## Acceptance Criteria

- [ ] **AC-1**: A `wint.model_experiments` table is defined in `packages/backend/database-schema/src/schema/wint.ts` using `wintSchema.table()`; contains at minimum: `id` (UUID PK), `change_type` (text), `file_type` (text), `control_model` (text), `challenger_model` (text), `status` (pgEnum: `'active' | 'concluded' | 'expired'`), `winner` (text, nullable), `control_sample_size` (integer), `challenger_sample_size` (integer), `control_success_rate` (numeric, precision 5 scale 4), `challenger_success_rate` (numeric, precision 5 scale 4), `min_sample_per_arm` (integer), `max_window_rows` (integer, nullable), `max_window_days` (integer, nullable), `started_at` (timestamp), `concluded_at` (timestamp, nullable), `created_at` (timestamp), `updated_at` (timestamp)

- [ ] **AC-2**: An `experiment_id` foreign key column (nullable UUID referencing `wint.model_experiments.id`) is added to `wint.change_telemetry` via a separate additive migration so that telemetry rows written during an active experiment are tagged for isolation; `experiment_id` is null for rows outside any experiment

- [ ] **AC-3**: `ModelExperimentInsertSchema` and `ModelExperimentSelectSchema` are exported from `wint.ts` using drizzle-zod `createInsertSchema`/`createSelectSchema`; all types are `z.infer<typeof ...>` — no TypeScript interfaces

- [ ] **AC-4**: A SQL migration file creates `wint.model_experiments` with all columns and indexes inside a `BEGIN`/`COMMIT` transaction; includes: `CREATE TYPE wint.experiment_status AS ENUM ('active', 'concluded', 'expired')` before the table, `CREATE TABLE IF NOT EXISTS wint.model_experiments`, `CREATE UNIQUE INDEX` on `(change_type, file_type)` WHERE `status = 'active'` (only one active experiment per combination at a time), `CREATE INDEX` on `status` and `started_at`; migration is idempotent

- [ ] **AC-5**: A separate additive SQL migration adds `experiment_id UUID REFERENCES wint.model_experiments(id)` to `wint.change_telemetry`; migration uses `ALTER TABLE IF EXISTS` guard so it is safe to run on a fresh schema where `change_telemetry` may not yet exist

- [ ] **AC-6**: A `BakeOffEngine` LangGraph cron graph exists in `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`; it exports `createBakeOffGraph()` factory and `runBakeOff(config)` entry point following the `metrics.ts` / `pattern-miner.ts` pattern; `BakeOffConfigSchema` is defined as a Zod schema (no TypeScript interfaces)

- [ ] **AC-7**: The Bake-Off Engine queries `wint.model_experiments WHERE status = 'active'` on each run; for each active experiment it aggregates `wint.change_telemetry` rows tagged with `experiment_id` per arm (control vs challenger), computing `sample_size` and `success_rate` per arm

- [ ] **AC-8**: Significance decision uses configurable named-constant thresholds: `MIN_SAMPLE_PER_ARM` (default: 50), `MIN_ABSOLUTE_DELTA` (default: 0.05 i.e. 5 percentage points); an experiment is considered significant when both arms have `>= MIN_SAMPLE_PER_ARM` rows AND `|challenger_success_rate - control_success_rate| >= MIN_ABSOLUTE_DELTA`; thresholds are overridable via `BakeOffConfigSchema`

- [ ] **AC-9**: When significance is reached, the engine promotes the winning model: it upserts `wint.model_affinity` for the `(change_type, file_type)` combination with the winner's observed `success_rate` as the new ground truth, marks the experiment `status = 'concluded'`, sets `winner` and `concluded_at`, and logs the promotion with structured fields: `experimentId`, `changeType`, `fileType`, `winner`, `controlSuccessRate`, `challengerSuccessRate`, `durationMs`

- [ ] **AC-10**: When an experiment's `max_window_rows` or `max_window_days` is exceeded without reaching significance, the engine marks the experiment `status = 'expired'`, sets `concluded_at`, leaves `winner = null`, and does NOT modify `wint.model_affinity`; logs with `status: 'expired'`, `reason: 'window_exceeded'`

- [ ] **AC-11**: If no active experiments exist, the Bake-Off Engine runs successfully with no DB writes and logs `{ active_experiments: 0, durationMs: <N> }`

- [ ] **AC-12**: Unit tests cover: (a) significance threshold logic — both arms at minimum, winner determination (challenger wins / control wins / tie defaults to control when delta < MIN_ABSOLUTE_DELTA), (b) expiry logic — window exceeded by rows vs by days, (c) no-op behavior when no active experiments, (d) `BakeOffConfigSchema` Zod validation

- [ ] **AC-13**: Integration tests tagged `@integration` against real test PostgreSQL DB (`postgresql://postgres:postgres@localhost:5432/lego_dev`): insert experiment row and fixture telemetry rows; run `runBakeOff()`; assert `model_affinity` updated to winner; assert experiment marked concluded; verify expiry path with window-exceeded fixture; verify no-op with zero active experiments

- [ ] **AC-14**: All existing schema tests in `packages/backend/database-schema/src/schema/__tests__/` continue to pass; Bake-Off Engine does not modify existing WINT ML pipeline tables (`trainingData`, `mlModels`, `modelPredictions`, `modelMetrics`)

## Reuse Plan

| Component | Location | How Used |
|-----------|----------|----------|
| `wintSchema` (pgSchema) | `packages/backend/database-schema/src/schema/wint.ts` | Namespace for `wint.model_experiments` table definition |
| `StateGraph`, `Annotation`, `END`, `START` | `@langchain/langgraph` | Graph structure for `createBakeOffGraph()` |
| `createToolNode` | `packages/backend/orchestrator/src/runner/node-factory.ts` | Node factory for graph nodes |
| `updateState` | `packages/backend/orchestrator/src/runner/state-helpers.ts` | State propagation helper |
| `createInsertSchema`, `createSelectSchema` | `drizzle-zod` | Auto-generated Zod schemas for `model_experiments` |
| `@repo/logger` | `packages/core/logger` | All structured log output |
| `@repo/db` | `packages/backend/db/src/` | Aurora PostgreSQL connection (inject via config.configurable, do NOT import directly into graph nodes) |
| SQL migration template | `apps/api/knowledge-base/src/db/migrations/015_artifact_type_tables.sql` | `BEGIN`/`COMMIT` transaction pattern |

## Architecture Notes

- **Injection over import**: Do NOT import `db` directly into graph nodes — inject via `config.configurable` to maintain testability (same as Pattern Miner pattern)
- **Idempotent winner promotion**: Use Drizzle `INSERT ... ON CONFLICT DO UPDATE` for `model_affinity` upserts to handle concurrent invocations safely
- **Conditional status transition**: Use `UPDATE wint.model_experiments SET status='concluded' WHERE id=<id> AND status='active'` to prevent double-processing
- **pgEnum ordering**: `CREATE TYPE wint.experiment_status AS ENUM (...)` MUST precede `CREATE TABLE wint.model_experiments` in the migration SQL
- **Partial unique index**: `CREATE UNIQUE INDEX ... ON wint.model_experiments (change_type, file_type) WHERE status = 'active'` — Aurora PostgreSQL 15 supports partial unique indexes; verify in test DB
- **Named constants**: `MIN_SAMPLE_PER_ARM = 50`, `MIN_ABSOLUTE_DELTA = 0.05` — no magic numbers; overridable via `BakeOffConfigSchema`
- **No self-referencing FKs**: `model_experiments` references `model_affinity` — define the Drizzle FK referencing the correct table; the FK definition order matters per the Drizzle self-referencing lesson

## Infrastructure Notes

- Cron scheduling is NOT implemented in this story — that is APIP-3090
- The Bake-Off Engine runs on the dedicated LangGraph Platform server (ADR-001 Decision 4) — not AWS EventBridge or Lambda
- Migration files go in `apps/api/knowledge-base/src/db/migrations/` pending APIP-5007 authoritative migration runner decision — confirm sequence number before writing the file (do not hardcode; glob for current highest number)
- Integration tests target `postgresql://postgres:postgres@localhost:5432/lego_dev` (port 5432, wint schema) — NOT port 5433 (KB database per ARCH-001 lesson)

## Test Plan

**Strategy**: Unit + Integration (no E2E — no UI surface; ADR-006 skip condition applies)

**Unit tests** (no DB required, completable immediately):
- Significance threshold boundary testing (exactly at MIN_SAMPLE_PER_ARM, exactly at MIN_ABSOLUTE_DELTA)
- Winner determination: challenger wins, control wins, tie (both equal success rate → control preserved, experiment not concluded when delta < threshold)
- Expiry by max_window_rows exceeded
- Expiry by max_window_days exceeded
- No-op behavior when no active experiments
- `BakeOffConfigSchema` Zod validation (valid config passes, missing required fields throw ZodError)

**Integration tests** (`@integration`, gated on APIP-3010 + APIP-3020 completion):
- Full winner promotion path: insert fixture experiment + 60 telemetry rows per arm (challenger wins), run `runBakeOff()`, assert `model_affinity` updated with challenger model, assert experiment `status='concluded'`
- Expiry path: window-exceeded fixture (max_window_rows exceeded, delta insufficient), assert `status='expired'`, `winner=null`, `model_affinity` unchanged
- No-op path: zero active experiments, assert no DB writes, assert log output `{ active_experiments: 0 }`
- Multiple concurrent active experiments: two experiments processed independently in single run
- Migration idempotency: both migrations run twice, no errors

**Integration DB target**: `postgresql://postgres:postgres@localhost:5432/lego_dev`

**Regression guard**: Run all existing `packages/backend/database-schema/src/schema/__tests__/` tests after schema additions

## Reality Baseline

- **Baseline date**: 2026-02-13
- **Blocking conflicts**: 0 (non-blocking warnings only)
- **Warning — Dependency chain length**: 5 stories deep (APIP-3060 → APIP-3020 → APIP-3010 → APIP-1030 → APIP-1020 → APIP-1010). Subtasks are structured so schema/migration/unit-test work is completable immediately; integration tests gated on APIP-3010 + APIP-3020.
- **Warning — Statistical significance volume risk**: Low change throughput in early pipeline phases may make it difficult to reach MIN_SAMPLE_PER_ARM=50 within a reasonable window. Experiments that close their window without significance are expired gracefully (AC-10) with no winner promotion — this is the correct safe behavior.

## Canonical References

Files that demonstrate the patterns this story should follow:

| Pattern | File | Why |
|---------|------|-----|
| LangGraph cron/aggregation graph | `packages/backend/orchestrator/src/graphs/metrics.ts` | Sole existing cron-style LangGraph graph with `StateGraph` + `Annotation` + node composition + `run*()` entry point — bake-off engine directly follows this structure |
| Drizzle ORM table in `wint` schema with numeric columns and indexes | `packages/backend/database-schema/src/schema/telemetry.ts` | Shows `pgSchema` table definition with typed columns, multiple indexes, drizzle-zod schema generation — mirrors what `wint.model_experiments` needs |
| SQL migration with `BEGIN`/`COMMIT`, `CREATE TABLE`, `CREATE INDEX` | `apps/api/knowledge-base/src/db/migrations/015_artifact_type_tables.sql` | Authoritative migration structure for Aurora PostgreSQL — `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS` inside transaction |
| Zod-first artifact schema (no interfaces) | `packages/backend/orchestrator/src/artifacts/story.ts` | Canonical Zod-first pattern for config and result schemas — `z.object()` with `z.infer<>` type alias only |

## Subtasks

| ID | Title | ACs | Depends On | Est. Tokens |
|----|-------|-----|-----------|-------------|
| ST-1 | Define wint.model_experiments Drizzle ORM table and Zod schemas in wint.ts | AC-1, AC-3 | none | 8,000 |
| ST-2 | Write SQL migration for wint.model_experiments table and experiment_status enum | AC-4 | ST-1 | 6,000 |
| ST-3 | Write additive SQL migration for experiment_id FK on wint.change_telemetry | AC-2, AC-5 | ST-2 | 4,000 |
| ST-4 | Implement BakeOffEngine LangGraph graph skeleton in bake-off-engine.ts | AC-6, AC-11 | ST-1 | 10,000 |
| ST-5 | Implement significance decision and active experiment query logic | AC-7, AC-8 | ST-4 | 10,000 |
| ST-6 | Implement winner promotion and experiment expiry logic | AC-9, AC-10 | ST-5 | 10,000 |
| ST-7 | Write unit tests for significance logic, expiry, no-op, and schema validation | AC-12, AC-14 | ST-6 | 10,000 |
| ST-8 | Write integration tests against real lego_dev PostgreSQL DB (gated on APIP-3010 + APIP-3020) | AC-13, AC-14 | ST-7 | 10,000 |

### ST-1: Define wint.model_experiments Drizzle ORM table and Zod schemas in wint.ts
**Goal**: Add `wint.model_experiments` table definition using `wintSchema.table()`, `experimentStatusEnum` pgEnum, and export `ModelExperimentInsertSchema`/`ModelExperimentSelectSchema` via drizzle-zod `createInsertSchema`/`createSelectSchema`.
**Files to read**: `packages/backend/database-schema/src/schema/wint.ts`, `packages/backend/database-schema/src/schema/telemetry.ts`
**Files to modify**: `packages/backend/database-schema/src/schema/wint.ts`
**Verification**: `pnpm check-types --filter @repo/database-schema`

### ST-2: Write SQL migration for wint.model_experiments table and experiment_status enum
**Goal**: Create idempotent SQL migration inside `BEGIN`/`COMMIT` transaction: `CREATE TYPE wint.experiment_status AS ENUM` before table, `CREATE TABLE IF NOT EXISTS wint.model_experiments` with all required columns, `CREATE UNIQUE INDEX` on `(change_type, file_type) WHERE status='active'`, `CREATE INDEX` on `status` and `started_at`.
**Files to read**: `apps/api/knowledge-base/src/db/migrations/015_artifact_type_tables.sql`
**Files to modify**: `apps/api/knowledge-base/src/db/migrations/NNN_model_experiments.sql` (new file — glob for sequence number)
**Verification**: Run migration against lego_dev test DB at port 5432; run twice to verify idempotency.

### ST-3: Write additive SQL migration for experiment_id FK on wint.change_telemetry
**Goal**: Separate idempotent migration that adds `experiment_id UUID REFERENCES wint.model_experiments(id)` to `wint.change_telemetry` using `ALTER TABLE IF EXISTS` guard; safe to run before or after APIP-3010 migration.
**Files to read**: `apps/api/knowledge-base/src/db/migrations/015_artifact_type_tables.sql`
**Files to modify**: `apps/api/knowledge-base/src/db/migrations/NNN_change_telemetry_experiment_fk.sql` (new file)
**Verification**: Run against lego_dev; verify `ALTER TABLE IF EXISTS` is safe when `change_telemetry` does not exist.

### ST-4: Implement BakeOffEngine LangGraph graph skeleton in bake-off-engine.ts
**Goal**: Create `packages/backend/orchestrator/src/graphs/bake-off-engine.ts` exporting `createBakeOffGraph()` factory and `runBakeOff(config)` entry point following `metrics.ts` `StateGraph`+`Annotation`+node factory pattern; define `BakeOffConfigSchema` as Zod schema.
**Files to read**: `packages/backend/orchestrator/src/graphs/metrics.ts`, `packages/backend/orchestrator/src/runner/node-factory.ts`, `packages/backend/orchestrator/src/runner/state-helpers.ts`, `packages/backend/orchestrator/src/artifacts/story.ts`
**Files to modify**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts` (new file)
**Verification**: `pnpm check-types --filter @repo/orchestrator && createBakeOffGraph() compiles`

### ST-5: Implement significance decision and active experiment query logic
**Goal**: Add node that queries `wint.model_experiments WHERE status='active'`, aggregates `wint.change_telemetry` rows by `experiment_id` per arm (control vs challenger), computes `sample_size` and `success_rate`; implement `isSignificant()` pure function with `MIN_SAMPLE_PER_ARM=50` and `MIN_ABSOLUTE_DELTA=0.05` named constants; DB injected via `config.configurable`.
**Files to read**: `packages/backend/orchestrator/src/graphs/metrics.ts`, `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`
**Files to modify**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`
**Verification**: `pnpm check-types --filter @repo/orchestrator`

### ST-6: Implement winner promotion and experiment expiry logic
**Goal**: Winner promotion node: when significant, upsert `wint.model_affinity` with winner's success_rate, UPDATE experiment `status='concluded'`, `winner`, `concluded_at`. Expiry node: when `max_window_rows` or `max_window_days` exceeded without significance, UPDATE `status='expired'`, `concluded_at`, `winner=NULL` — do NOT touch `model_affinity`. Log all outcomes with `@repo/logger` structured fields.
**Files to read**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`, `packages/backend/database-schema/src/schema/wint.ts`
**Files to modify**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`
**Verification**: `pnpm check-types --filter @repo/orchestrator`

### ST-7: Write unit tests for significance logic, expiry, no-op, and schema validation
**Goal**: Create `bake-off-engine.test.ts` covering: significance threshold boundaries (exactly MIN_SAMPLE, exactly MIN_DELTA), winner determination (challenger wins / control wins / tie → control preserved when delta < threshold), expiry by rows vs days, no-op with zero active experiments, `BakeOffConfigSchema` Zod validation — all without DB access.
**Files to read**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`
**Files to modify**: `packages/backend/orchestrator/src/graphs/__tests__/bake-off-engine.test.ts` (new file)
**Verification**: `pnpm test --filter @repo/orchestrator bake-off-engine.test`

### ST-8: Write integration tests against real lego_dev PostgreSQL DB (gated on APIP-3010 + APIP-3020)
**Goal**: Create `bake-off-engine.integration.test.ts` tagged `@integration` targeting `postgresql://postgres:postgres@localhost:5432/lego_dev`. Tests: winner promotion path (fixture experiment + telemetry → `runBakeOff()` → model_affinity updated, experiment concluded), expiry path (window-exceeded fixture), no-op path. Verify existing schema tests unaffected.
**Files to read**: `packages/backend/orchestrator/src/graphs/bake-off-engine.ts`, `packages/backend/database-schema/src/schema/wint.ts`
**Files to modify**: `packages/backend/orchestrator/src/graphs/__tests__/bake-off-engine.integration.test.ts` (new file)
**Verification**: `pnpm test --filter @repo/orchestrator bake-off-engine.integration.test` (requires APIP-3010 + APIP-3020 complete)
