id: "APIP-3010"
title: "Change Telemetry Table and Instrumentation"
status: backlog
priority: medium
phase: 3
feature: "DB migration for wint.change_telemetry; instrument the implementation graph change loop to record every attempt (change_type, file_type, model, outcome, attempts, escalated_to, tokens, cost, duration) per ChangeSpec"
goal: "Capture granular per-change-attempt data as the raw input for the model affinity learning system."
depends_on: ["APIP-1030"]
endpoints: []
infrastructure:
  - "Aurora PostgreSQL migration: wint.change_telemetry table with indexes"
risk_notes: "High write volume at scale; telemetry must not block the change loop on write failure; schema must capture all fields defined in ChangeTelemetrySchema."
sizing_warning: false
created: "2026-02-25T00:00:00Z"
