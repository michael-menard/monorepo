---
id: KBAR-0030
title: "Story Sync Functions"
status: uat
priority: P1
epic: KBAR
depends_on:
  - KBAR-0020
story_points: 5
experiment_variant: control
surfaces:
  backend: true
  frontend: false
  database: true
  infrastructure: false
created_at: "2026-02-16T00:00:00Z"
updated_at: "2026-02-17T01:45:00Z"
---

# Story: KBAR-0030 — Story Sync Functions

## Context

KBAR-0010 created the foundational KBAR database schema with 11 tables to store story metadata, artifacts, and sync state. KBAR-0020 validated the schema with comprehensive tests. However, the database tables are currently empty, and there is no mechanism to synchronize story data between the filesystem (YAML files in `plans/future/platform/`) and the KBAR database.

Without sync functions, we cannot:
- Populate `kbar.stories` table from existing YAML story files
- Detect when filesystem stories have changed (checksum comparison)
- Update database when story files are modified by agents
- Sync story metadata bidirectionally (filesystem ↔ database)
- Track sync history and conflicts

The KBAR schema defines the following sync-related tables:
- `syncEvents`: Track filesystem→database sync operations
- `syncConflicts`: Log conflicts when both filesystem and database change
- `syncCheckpoints`: Checkpoint state for incremental syncs
- `artifacts.checksum`: SHA-256 hash for change detection
- `artifacts.syncStatus`: Enum tracking sync state (pending, in_progress, completed, failed, conflict)

These tables are unused without sync logic.

## Goal

Create a sync service with three core functions that enable bidirectional synchronization between filesystem story files and the KBAR database, with checksum-based change detection and conflict tracking.

## Non-Goals

**Deferred to Future Stories**:
- **Batch sync operations**: KBAR-0040 (Artifact Sync Functions) will handle bulk syncs
- **MCP tool wrapper**: KBAR-0050+ will expose sync functions as MCP tools
- **CLI commands**: KBAR-0050 (CLI Sync Commands) will provide CLI interface
- **Automated sync triggers**: KBAR-0060+ will add file watchers or cron jobs
- **Conflict resolution UI**: KBAR-0080+ may add manual conflict resolution
- **Artifact sync (non-story files)**: KBAR-0040 will handle PLAN.yaml, SCOPE.yaml, etc.
- **Index regeneration**: KBAR-0230 (DB-Driven Index Generation) handles index updates

**Protected Features** (Do Not Modify):
- KBAR schema structure (created in KBAR-0010, validated in KBAR-0020)
- WINT story management MCP tools (different schema, different use case)
- Existing story files in `plans/future/platform/` (read-only for this story)
- Artifact service (INFR-0020) - use its patterns, don't modify

## Scope

### New Package
- **`packages/backend/kbar-sync/`**: New backend package for KBAR sync functions
  - Dependencies: `@repo/database-schema`, `@repo/db`, `@repo/logger`, `drizzle-orm`, `yaml`
  - Exports: `syncStoryToDatabase`, `syncStoryFromDatabase`, `detectSyncConflicts`

### Database Tables (Read/Write)
- **`kbar.stories`**: Insert/update story metadata
- **`kbar.syncEvents`**: Insert sync event logs
- **`kbar.syncConflicts`**: Insert conflict records
- **`kbar.artifacts`**: Update `checksum`, `syncStatus`, `lastSyncedAt`

### Filesystem Access
- **Read**: YAML story files in `plans/future/platform/`
- **Write**: YAML story files (for sync from database to filesystem)

## Acceptance Criteria

- [ ] **AC-1**: Sync story from filesystem to database
  - Read YAML story file, parse frontmatter and content
  - Compute SHA-256 checksum of file content
  - Insert or update `kbar.stories` table with story metadata
  - Create `syncEvents` record with sync metadata
  - Update `artifacts.syncStatus` to 'completed' on success, 'failed' on error
  - Return sync result (status, checksum, timestamp)

- [ ] **AC-2**: Sync story from database to filesystem
  - Read story from `kbar.stories` table
  - Generate YAML frontmatter with all metadata fields
  - Write to filesystem using atomic file write (temp file + rename)
  - Create `syncEvents` record
  - Update `artifacts.lastSyncedAt` timestamp
  - Return sync result

- [ ] **AC-3**: Detect sync conflicts
  - Read story from both filesystem and database
  - Compare checksums and `updatedAt` timestamps
  - Detect conflicts (both changed since last sync)
  - Log conflict to `syncConflicts` table with metadata
  - Return conflict status with resolution options

- [ ] **AC-4**: Checksum-based change detection
  - Compute SHA-256 checksum for YAML file content
  - Store checksum in `artifacts.checksum` field
  - Skip sync if checksum unchanged (idempotency)
  - Log checksum mismatches for debugging

- [ ] **AC-5**: Zod validation for all inputs/outputs
  - Define `SyncStoryToDatabaseInputSchema`, `SyncStoryToDatabaseOutputSchema`
  - Define `SyncStoryFromDatabaseInputSchema`, `SyncStoryFromDatabaseOutputSchema`
  - Define `DetectSyncConflictsInputSchema`, `DetectSyncConflictsOutputSchema`
  - Validate all inputs before execution
  - Validate all outputs before returning

- [ ] **AC-6**: Error handling and logging
  - Catch all errors (filesystem, database, parsing)
  - Log errors with context (storyId, filePath, error message)
  - Return graceful error results (never throw to caller)
  - Update `artifacts.syncStatus` to 'failed' on error
  - Store error details in `syncEvents.metadata`

- [ ] **AC-7**: Sync event tracking
  - Create `syncEvents` record for every sync operation
  - Track `eventType` ('story_sync_to_db', 'story_sync_from_db', 'conflict_detected')
  - Track `status` (pending, in_progress, completed, failed)
  - Track timing (`startedAt`, `completedAt`, `durationMs`)
  - Store metadata (filesScanned, filesChanged, conflictsDetected)

- [ ] **AC-8**: Unit tests (>80% coverage)
  - Test successful sync to database
  - Test successful sync from database
  - Test conflict detection (both sides changed)
  - Test checksum-based skip (no changes)
  - Test error handling (file not found, parse error, DB error)
  - Test Zod validation (invalid inputs rejected)

## Reuse Plan

- **Schema Package**: `packages/backend/database-schema/` for KBAR schema tables
- **Database Client**: `packages/backend/db/` for Drizzle ORM queries
- **YAML Parsing**: `yaml` package for frontmatter/content parsing (same as Artifact Service)
- **Checksums**: Node.js `crypto.createHash('sha256')` for file hashing
- **File I/O**: Node.js `fs/promises` for async file operations
- **Story File Adapter Pattern**: Follow `story-file-adapter.ts` for YAML structure
- **MCP Tool Pattern**: Follow `story-management/` Zod validation and error handling
- **Repository Pattern**: Follow `story-repository.ts` for database encapsulation

## Architecture Notes

### Solution Direction

Create a sync service with three core functions:

1. **`syncStoryToDatabase(storyId, filePath)`**
   - Read YAML story file from filesystem
   - Parse frontmatter and content
   - Compute SHA-256 checksum
   - Compare checksum with database record (if exists)
   - Insert/update `kbar.stories` table if changed
   - Create `syncEvents` record
   - Update `artifacts.syncStatus` to 'completed'
   - Return sync result with status

2. **`syncStoryFromDatabase(storyId, filePath)`**
   - Read story from `kbar.stories` table
   - Generate YAML frontmatter + content
   - Compute SHA-256 checksum
   - Write to filesystem (atomic write via temp file)
   - Update `artifacts.lastSyncedAt` timestamp
   - Create `syncEvents` record
   - Return sync result

3. **`detectSyncConflicts(storyId)`**
   - Read both filesystem and database versions
   - Compare checksums and timestamps
   - Detect conflicts (both changed since last sync)
   - Log to `syncConflicts` table if conflict detected
   - Return conflict status with metadata

### Implementation Structure

```
packages/backend/kbar-sync/
  package.json
  tsconfig.json
  vitest.config.ts
  src/
    index.ts                        # Export all sync functions
    sync-story-to-database.ts       # AC-1: Filesystem → Database
    sync-story-from-database.ts     # AC-2: Database → Filesystem
    detect-sync-conflicts.ts        # AC-3: Conflict detection
    __types__/
      index.ts                      # Zod schemas (AC-5)
    __tests__/
      sync-story-to-database.test.ts
      sync-story-from-database.test.ts
      detect-sync-conflicts.test.ts
      integration.test.ts           # Testcontainers integration
```

### Key Design Decisions

1. **Conflict resolution strategy**: Log conflict, require manual resolution (safer, explicit)
2. **Sync direction precedence**: Filesystem is source of truth (database is cache)
3. **Checksum algorithm**: SHA-256 (strong, standard, matches KBAR-0010 schema design)
4. **Sync event granularity**: One event per story sync (better audit trail)

### Transaction Handling

- Wrap database operations in transactions with row-level locking
- Use Drizzle ORM transaction API: `db.transaction(async (tx) => { ... })`
- Check checksums inside transaction before update to prevent race conditions

### Error Handling Strategy

- All operations wrapped in try-catch
- Log all errors with `@repo/logger`
- Return graceful error results (never throw to caller)
- Update `syncStatus` to 'failed' on errors
- Store error details in `syncEvents.metadata`

## Test Plan

See: `_pm/TEST-PLAN.md`

**Summary**:
- **Unit tests**: Mock filesystem and database, test sync logic in isolation (>80% coverage)
- **Integration tests**: Use testcontainers for PostgreSQL, real YAML files in temp directories
- **Critical test cases**:
  - Sync unchanged story (checksum match) → skip operation
  - Sync changed story (checksum mismatch) → update database
  - Conflict detection (both changed) → log conflict, return conflict status
  - Filesystem error (file not found) → graceful error, log to syncEvents
  - Database error (constraint violation) → graceful error, update syncStatus to 'failed'
  - Malformed YAML → parse error, log to syncEvents with error details
- **Performance test**: Sync 100+ stories in <10s

## Dev Feasibility

See: `_pm/DEV-FEASIBILITY.md`

**Summary**:
- **Feasible for MVP**: Yes
- **Confidence**: High
- **Estimated effort**: 10-14 hours (2 days)

**Implementation phases**:
1. Setup (30 min): Create package, install dependencies
2. Schema definition (1 hour): Define Zod schemas
3. Core sync logic (4-5 hours): Implement 3 sync functions
4. Error handling (1-2 hours): Wrap operations, graceful errors
5. Testing (3-4 hours): Unit + integration tests, >80% coverage
6. Documentation (1 hour): JSDoc, README

**MVP-critical risks**:
1. Database transaction handling for concurrent syncs → Use row-level locking, test concurrency
2. YAML parsing errors on malformed files → Wrap yaml.parse() in try-catch, graceful errors
3. File system I/O errors → Catch all fs/promises errors, log to syncEvents
4. Checksum computation failures on large files → Add 5MB size limit, streaming for large files
5. Incomplete sync state tracking → Update syncStatus at start/end, log all events

## Risk Predictions

```yaml
predictions:
  split_risk: 0.3
  review_cycles: 2
  token_estimate: 120000
  confidence: medium
  similar_stories:
    - story_id: WINT-0090
      similarity_score: 0.85
      actual_cycles: 2
      actual_tokens: null
      split_occurred: false
    - story_id: INFR-0020
      similarity_score: 0.78
      actual_cycles: null
      actual_tokens: null
      split_occurred: false
    - story_id: LNGG-0010
      similarity_score: 0.72
      actual_cycles: null
      actual_tokens: null
      split_occurred: false
  generated_at: "2026-02-16T00:00:00Z"
  model: haiku
  wkfl_version: "007-v1"
  reasoning: |
    Split risk (0.3): 8 ACs indicate medium complexity. Backend-only service reduces risk.
    No frontend coordination or multi-domain dependencies. KBAR-0010/0020 provide stable
    foundation. Pattern reuse from WINT-0090, INFR-0020, LNGG-0010 lowers implementation risk.

    Review cycles (2): Infrastructure story with clear scope and reuse patterns. Database sync
    logic is straightforward. Expected 1 initial review + 1 follow-up for edge case handling.

    Token estimate (120K): Based on similar backend infrastructure stories. Lower than average
    due to strong pattern reuse and no UI work. Estimated breakdown: setup (10K), implementation
    (60K), testing (40K), documentation (10K).

    Confidence (medium): 3 similar stories identified but OUTCOME.yaml data incomplete. No
    WKFL-006 patterns available yet. Heuristic-based predictions with historical pattern matching.
```

## Reality Baseline

### Relevant Existing Features

| Feature | Location | Status | Relevance |
|---------|----------|--------|-----------|
| KBAR Schema (11 tables) | `packages/backend/database-schema/src/schema/kbar.ts` | Active (KBAR-0010) | Foundation tables for story sync functions |
| KBAR Schema Tests | `packages/backend/database-schema/src/schema/__tests__/kbar-schema.test.ts` | Active (KBAR-0020) | Comprehensive validation tests for KBAR schema |
| WINT Story Management Tools | `packages/backend/mcp-tools/src/story-management/` | Active (WINT-0090) | Pattern reference for MCP tool implementation |
| Story Repository | `packages/backend/orchestrator/src/db/story-repository.ts` | Active | Pattern for story CRUD operations |
| Artifact Service | `packages/backend/orchestrator/src/services/artifact-service.ts` | Active (INFR-0020) | YAML artifact read/write patterns |
| YAML Story File Adapter | `packages/backend/orchestrator/src/adapters/story-file-adapter.ts` | Active (LNGG-0010) | Pattern for story file parsing/writing |
| Drizzle ORM | `packages/backend/db/` | Active | Database client with connection pooling |

### Active In-Progress Work

| Story ID | Title | Status | Potential Overlap |
|----------|-------|--------|-------------------|
| WINT-0090 | Create Story Management MCP Tools | ready-to-work | No conflict - WINT uses `wint.stories` table, KBAR uses `kbar.stories` table |
| LNGG-0020 | Index Management Adapter | uat | No conflict - Different layer (index vs sync) |
| INFR-0020 | Artifact Writer/Reader Service | uat | Complementary - INFR-0020 provides YAML I/O, KBAR-0030 adds sync logic |

### Constraints to Respect

- **Protected Features**:
  - KBAR schema structure (created in KBAR-0010, validated in KBAR-0020)
  - WINT story management MCP tools (different schema namespace)
  - Existing artifact service patterns (YAML read/write)
- **Schema Isolation**: KBAR uses `kbar` PostgreSQL schema, WINT uses `wint` schema
- **Zod-first types**: All input/output must use Zod schemas with runtime validation
- **Pattern consistency**: Follow WINT-0090 MCP tool patterns (Zod validation, graceful errors)

### Patterns to Follow

From KBAR-0010/0020 and WINT-0090:
- **Zod schema validation**: Define input/output schemas, validate at entry points
- **Repository pattern**: Encapsulate database logic in repository classes
- **Checksum-based sync**: Use SHA-256 checksums for change detection
- **Graceful error handling**: Log errors, return null/empty results, never throw to caller
- **Type safety**: Use Drizzle ORM typed queries, Zod-inferred types
- **Idempotency**: Sync functions should be safe to run multiple times

From Artifact Service (INFR-0020):
- **YAML parsing**: Use `yaml` package for frontmatter + content parsing
- **File path resolution**: Resolve paths relative to workspace root
- **Atomic writes**: Write to temp file, then rename for atomicity

### Patterns to Avoid

- **Manual SQL strings**: Use Drizzle ORM query builder, not raw SQL
- **Missing error handling**: Every database operation can fail
- **Incomplete sync state**: Track sync status (pending, in_progress, completed, failed)
- **No conflict detection**: Check checksums before overwriting data
- **Forgetting indexes**: Use indexes defined in KBAR-0010 for efficient queries

## QA Discovery Notes (Auto-Generated)

_Added by Autonomous Elaboration on 2026-02-16_

### MVP Gaps Resolved

| # | Finding | Resolution | AC Added |
|---|---------|------------|----------|
| 1 | All core sync functions defined (filesystem→db, db→filesystem, conflict detection) | All ACs present and complete | N/A - all ACs in scope |

### Non-Blocking Items (Logged to KB)

| # | Finding | Category | Notes |
|---|---------|----------|-------|
| 1 | Batch sync operations | deferred | KBAR-0040 will handle bulk syncs |
| 2 | Automated sync triggers | deferred | KBAR-0060+ will add file watchers or cron jobs |
| 3 | Conflict resolution UI | deferred | KBAR-0080+ may add manual conflict resolution |
| 4 | MCP tool wrapper | deferred | KBAR-0050+ will expose sync functions as MCP tools |
| 5 | CLI commands | deferred | KBAR-0050 will provide CLI interface |
| 6 | Artifact sync (non-story files) | deferred | KBAR-0040 will handle PLAN.yaml, SCOPE.yaml, etc. |
| 7 | Index regeneration | deferred | KBAR-0230 handles index updates |
| 8 | Streaming for large files | enhancement | Add streaming in future if needed |
| 9 | Parallel sync operations | enhancement | Future optimization for batch operations |
| 10 | Sync metrics dashboard | enhancement | Future telemetry enhancement |
| 11 | Dry-run mode | enhancement | Future testing enhancement |
| 12 | Checksum caching | enhancement | Minor optimization |
| 13 | Symlink handling | edge-case | Add detection if encountered |
| 14 | Permission error handling | edge-case | Add EACCES/EPERM handling |
| 15 | Git integration | integration | Detect git status before sync |
| 16 | Webhook notifications | integration | Trigger on sync events |

### Summary

- Audit checks: 8/8 PASS
- MVP-critical gaps found: 0
- Non-blocking gaps: 8
- Enhancement opportunities: 8
- Mode: autonomous
- Verdict: PASS

## Review Findings

> **Review Date:** 2026-02-17T00:32:00Z
> **Reviewed By:** Claude Code
> **Gate:** CONCERNS (71 total findings: 11 high, 28 medium, 32 low)
> **Gate File:** docs/qa/gates/KBAR-0030-story-sync-functions.yml

### T-Shirt Size Estimate

**Recommended Size: M** (Confidence: high)

**Specialist Breakdown:**
| Specialist | Size | Rationale |
|------------|------|-----------|
| Requirements | M | 5-7 ACs with 89% test coverage, 2 gaps in skipped tests |
| Code Quality | M | Moderate complexity (score 6/10), duplicated utilities, type safety gaps |
| Security | L | High risk - 3 critical vulnerabilities (path traversal, symlink attacks) |
| Performance | L | N+1 query patterns, missing indexes affecting scalability |
| Accessibility | XS | Backend-only package, no UI components |
| Test Coverage | M | 16/18 passing, 2 critical tests skipped, transaction gaps |
| Tech Debt | M | Moderate burden - duplicated code, brittle mocks, missing docs |

**Size Distribution:** XS: 1, S: 0, **M: 4**, L: 2, XL: 0, XXL: 0

**Synthesis:**
Modal size is M (4/7 specialists). Security and Performance flagged as L due to path traversal vulnerabilities and N+1 queries. Overall recommendation: **M** with mandatory security hardening before production use. Despite high-risk security findings, this is a backend utility package for internal use (not user-facing), which reduces immediate exploit risk. Performance issues are real but not critical for infrequent sync operations.

---

### Summary

- **Files Analyzed:** 9 (3 core sync files + 3 test files + 3 support files)
- **Total Findings:** 71 (high: 11, medium: 28, low: 32)
- **Traceability:** 8/8 acceptance criteria addressed, 89% test coverage (16/18 tests)

### Required Checks

| Check | Status |
|-------|--------|
| Tests | PASS (16/18, 2 skipped) |
| Types | PASS |
| Lint  | SKIPPED |

### Requirements Traceability

**T-Shirt Size: M** | Coverage: 89% (16/18 tests)

- **[REQ-001] medium:** AC-4 idempotency test skipped - checksum matching logic not verified in unit tests due to crypto mocking complexity
  - **File:** src/__tests__/sync-story-to-database.test.ts:109
  - **Action:** Refactor computeChecksum for mockability or move to integration tests

- **[REQ-002] medium:** AC-3 no-conflict test skipped - checksums-match scenario not verified in unit tests
  - **File:** src/__tests__/detect-sync-conflicts.test.ts:44
  - **Action:** Use same crypto mocking solution as REQ-001

✓ All 8 acceptance criteria have implementation and tests
✓ Integration tests cover both skipped scenarios (idempotency working in real environment)

### Code Quality

**T-Shirt Size: M** | Complexity: 6/10 | Findings: 20

- **[QUAL-001] high:** Code duplication - computeChecksum function duplicated in sync-story-to-database.ts and detect-sync-conflicts.ts
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:29
  - **Action:** Extract to src/utils/checksum.ts shared module

- **[QUAL-002] high:** Repeated sync event error handling pattern (~15 lines duplicated across 3 files)
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:258-273
  - **Action:** Extract utility: updateSyncEventOnFailure(syncEventId, errorMessage)

- **[QUAL-003] high:** Repeated input validation pattern in all three sync functions
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:65-75
  - **Action:** Create reusable validateInput<T>(input, schema) helper

- **[QUAL-004] high:** Transaction callback typed as 'any' instead of proper Drizzle type (defeats type safety)
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:110
  - **Action:** Import and use proper Drizzle transaction type

- **[QUAL-005] medium:** Magic string event types with no central registry ('story_sync_to_db', 'story_sync_from_db', 'conflict_detected')
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:87
  - **Action:** Create constants file with EVENT_TYPES enum

- **[QUAL-006] medium:** Metadata structure inconsistency - syncEvents.metadata uses {triggeredBy, syncMode} without type safety
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:92
  - **Action:** Add Zod schema for SyncEventMetadata

_...15 additional code quality findings in gate file_

### Security

**T-Shirt Size: L** | Risk Level: high | Findings: 10

- **[SEC-001] high:** Path Traversal Vulnerability - filePath/outputPath parameters accept any string without validation against directory traversal (.., absolute paths escaping intended directories)
  - **File:** packages/backend/kbar-sync/src/__types__/index.ts:21, 53, 84
  - **CWE:** CWE-22 (Path Traversal)
  - **Action:** Add path.resolve() and path.relative() validation, reject paths with '..' or symlinks outside boundaries

- **[SEC-002] high:** Symlink Following Vulnerability - readFile() follows symlinks without restrictions, allowing read of sensitive files outside intended scope
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:100
  - **CWE:** CWE-59 (Improper Link Resolution Before File Access)
  - **Action:** Use fs.lstat() to detect symlinks, fs.realpath() to verify canonical path, or O_NOFOLLOW flag

- **[SEC-003] high:** Symlink Following on Write - rename() follows symlinks at target, could cause unintended file overwrites in arbitrary locations
  - **File:** packages/backend/kbar-sync/src/sync-story-from-database.ts:109
  - **CWE:** CWE-59
  - **Action:** Check if outputPath is symlink using fs.lstat() before writing, reject if target is symlink

- **[SEC-004] medium:** Insufficient error information in logs - file paths could expose sensitive information in log aggregation systems
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:255
  - **CWE:** CWE-532 (Insertion of Sensitive Information into Log File)
  - **Action:** Sanitize paths in logs (hash, truncate, or mask)

_...6 additional security findings in gate file_

### Performance

**T-Shirt Size: L** | Performance Risk: medium | Findings: 11

- **[PERF-001] high:** N+1 query pattern - artifact table queried twice with identical filters (lines 122-128, then 185-191)
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:122-128, 185-191
  - **Impact:** For 100+ stories, doubles artifact queries. ~20-30ms overhead per sync
  - **Action:** Store first artifact query result and reuse, eliminate duplicate

- **[PERF-002] high:** Duplicate SELECT queries on kbarStories when operations run sequentially
  - **File:** packages/backend/kbar-sync/src/detect-sync-conflicts.ts:122
  - **Impact:** ~15-20ms per artifact pair, 750-1000ms wasted on 50 stories with conflicts
  - **Action:** Return story data from sync operations or implement session-scope caching

- **[PERF-003] medium:** Missing composite index for artifacts (storyId, artifactType) lookup pattern
  - **File:** packages/backend/database-schema/src/schema/kbar.ts:293-296
  - **Impact:** Index scan + filter instead of direct lookup, 10-50ms at 1000+ artifacts
  - **Action:** Verify composite index order optimizes query pattern

- **[PERF-004] medium:** Inefficient sync event updates - 2-4 UPDATE queries per sync operation
  - **File:** packages/backend/kbar-sync/src/detect-sync-conflicts.ts:103-227
  - **Impact:** At 1000 stories/hour = 2000-4000 unnecessary UPDATEs, 50-100ms per sync
  - **Action:** Defer status updates, batch updates, or use async logging

_...7 additional performance findings in gate file_

### Accessibility

**T-Shirt Size: XS** | UI Complexity: none

✓ Backend-only package with no UI components or accessibility requirements

### Test Coverage

**T-Shirt Size: M** | Test Complexity: medium | Findings: 10

- **[TEST-001] high:** Critical skipped test for checksum-based idempotency (AC-4) - cannot verify without real SHA-256 hashes
  - **File:** packages/backend/kbar-sync/src/__tests__/sync-story-to-database.test.ts:109
  - **Action:** Refactor computeChecksum to accept injectable crypto function or move to integration tests

- **[TEST-002] high:** Skipped no-conflict test - critical success path not verified in unit tests
  - **File:** packages/backend/kbar-sync/src/__tests__/detect-sync-conflicts.test.ts:44
  - **Action:** Same solution as TEST-001

- **[TEST-003] medium:** Insufficient transaction error handling coverage - no tests for rollback, constraint violations, or partial failures
  - **File:** packages/backend/kbar-sync/src/__tests__/sync-story-to-database.test.ts
  - **Action:** Add tests for transaction rollback scenarios, duplicate storyId constraints, orphaned sync events

- **[TEST-004] medium:** Mock chains are fragile - nested db.insert().values().returning() hard to verify
  - **File:** All test files
  - **Action:** Consider drizzle test utils or prefer integration tests over deep mocks

_...6 additional test coverage findings in gate file_

### Technical Debt

**T-Shirt Size: M** | Maintenance Burden: medium | Findings: 18

- **[DEBT-001] medium:** Duplicated computeChecksum function (same as QUAL-001)
  - **File:** sync-story-to-database.ts, detect-sync-conflicts.ts
  - **Effort:** small
  - **Action:** Extract to shared utilities

- **[DEBT-002] high:** Type safety violation - Drizzle transaction callback typed as 'any' (defeats TypeScript)
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:110
  - **Effort:** small
  - **Action:** Import proper Drizzle transaction type

- **[DEBT-003] medium:** Unsafe metadata casting with 'as any' bypasses type validation
  - **File:** packages/backend/kbar-sync/src/sync-story-to-database.ts:154, 176
  - **Effort:** medium
  - **Action:** Create metadata Zod schema, use MetadataSchema.parse()

- **[DEBT-004] medium:** 2 skipped unit tests prevent idempotency verification
  - **File:** Test files
  - **Effort:** medium
  - **Action:** Refactor for mockability or move to integration layer

_...14 additional technical debt findings in gate file_

### Recommendation

⚠ **CONCERNS** - Multiple high-priority findings require attention before production use:

**Must Fix:**
1. Security hardening (SEC-001, SEC-002, SEC-003) - Path validation and symlink protection
2. Test gaps (TEST-001, TEST-002) - Complete idempotency verification
3. Performance optimizations (PERF-001, PERF-002) - Eliminate N+1 queries
4. Type safety (QUAL-004, DEBT-002) - Fix 'any' types in transactions

**Should Fix:**
5. Code duplication (QUAL-001-003) - Extract shared utilities
6. Metadata validation (DEBT-003) - Replace 'as any' with Zod schemas
7. Transaction testing (TEST-003) - Add error handling coverage

**Status:** Implementation is functionally complete with 89% test coverage and all ACs addressed. Core sync logic works correctly. Security and performance issues are real but mitigated by:
- Internal use only (not user-facing)
- Infrequent sync operations (not high-volume)
- Strong integration test coverage

**Next Steps:** Address security vulnerabilities and test gaps before production deployment.
