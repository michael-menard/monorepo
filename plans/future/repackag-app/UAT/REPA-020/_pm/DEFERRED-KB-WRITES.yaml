---
created_at: "2026-02-11"
retry_count: 0
reason: "KB write not available during story generation - queue for later persistence"
---

# Deferred Knowledge Base Writes for REPA-020

## Story Record

```sql
INSERT INTO stories (
  story_id,
  feature,
  title,
  story_dir,
  story_file,
  story_type,
  points,
  priority,
  state,
  touches_backend,
  touches_frontend,
  touches_database,
  touches_infra
) VALUES (
  'REPA-020',
  'repackag-app',
  'Create Domain Card Factories for @repo/gallery',
  'plans/future/repackag-app/backlog/REPA-020',
  'plans/future/repackag-app/backlog/REPA-020/REPA-020.md',
  'feature',
  3,
  'medium',
  'backlog',
  false,
  true,
  false,
  false
)
ON CONFLICT (story_id) DO UPDATE SET
  feature = EXCLUDED.feature,
  title = EXCLUDED.title,
  story_dir = EXCLUDED.story_dir,
  story_file = EXCLUDED.story_file,
  story_type = EXCLUDED.story_type,
  points = EXCLUDED.points,
  priority = EXCLUDED.priority,
  state = EXCLUDED.state,
  touches_backend = EXCLUDED.touches_backend,
  touches_frontend = EXCLUDED.touches_frontend,
  touches_database = EXCLUDED.touches_database,
  touches_infra = EXCLUDED.touches_infra;
```

## Retry Instructions

To persist this story to the knowledge base, run:
```bash
cat plans/future/repackag-app/backlog/REPA-020/_pm/DEFERRED-KB-WRITES.yaml | grep -A 30 "INSERT INTO" | psql <connection-string>
```

Or use the KB management tool when available.
