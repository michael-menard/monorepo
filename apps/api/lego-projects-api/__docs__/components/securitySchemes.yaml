cookieAuth:
  type: apiKey
  in: cookie
  name: token
  description: JWT authentication via httpOnly cookie named 'token'.

bearerAuth:
  type: http
  scheme: bearer
  bearerFormat: JWT
  description: Optional JWT via Authorization header using the Bearer scheme.
