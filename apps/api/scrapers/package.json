{
  "name": "@repo/scrape-worker",
  "version": "1.0.0",
  "description": "BullMQ-powered scrape queue worker — processes scrape jobs from multiple queues",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx --watch src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@repo/logger": "workspace:*",
    "@repo/s3-client": "workspace:*",
    "bullmq": "^5.74.1",
    "ioredis": "^5.4.2",
    "dotenv": "^16.3.1",
    "playwright": "^1.54.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
