schema: 1
story_id: WINT-7010
timestamp: '2026-03-21T00:00:00Z'
phase: setup
iteration: 0
gen_mode: false

title: Audit Agent Directory References

touches:
  backend: false
  frontend: false
  packages: false
  db: false
  contracts: false
  ui: false
  infra: false

touched_paths_globs:
  - '.claude/agents/**'
  - '.opencode/agents/**'
  - '.claude/skills/**'
  - 'scripts/**'

risk_flags:
  auth: false
  payments: false
  migrations: false
  external_apis: false
  security: false
  performance: false

summary: Audit all Claude agent files, opencode agents, shared includes, and skill files to produce comprehensive inventory of filesystem directory references for KB-only migration

elaboration: completed

acceptance_criteria_count: 10

key_acs:
  - AC1: Scan .claude/agents/ for all *.agent.md files and catalog directory references
  - AC2: Scan .opencode/agents/ for all *.md files and catalog directory references
  - AC3: Scan .claude/skills/ for all SKILL.md files and parse directory patterns
  - AC4: Identify all regex patterns and glob references used in agent/skill files
  - AC5: Produce structured JSON inventory with story_id, pattern, line_number, source_file
  - AC6: Categorize patterns: absolute_paths, relative_paths, glob_patterns, regex
  - AC7: Identify potential KB migration blockers (hardcoded paths, absolute references)
  - AC8: Generate migration risk report with severity levels
  - AC9: Document all shared include references and dependency chains
  - AC10: Output KB analysis artifact with findings, risk assessment, and recommendations

audit_only: true
artifact_output: kb_analysis
no_code_changes: true

notes: |
  - Story is audit-only: no code changes, no E2E testing required
  - Output is a structured KB analysis artifact for KB-only migration planning
  - Focus on identifying directory reference patterns that may block migration
  - All 10 ACs are analysis/reporting only
