schema: 1
story_id: PIPE-2050
timestamp: '2026-03-19T07:00:00Z'
phase: implementation
iteration: 0
status: complete

acceptance_criteria:
  - ac_id: AC-1
    status: PASS
    evidence_type: test
    summary: >-
      getEligibleReviewStories(limit) implemented in scheduler/index.ts.
      SQL CTE queries WHERE s.state = 'ready_for_qa' with same unresolved_deps CTE
      structure as getEligibleStories(). Unit test HP-5 passes:
      "HP-5: returns stories in ready_for_qa state from DB execute result".
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-2
    status: PASS
    evidence_type: test
    summary: >-
      dispatchReviewStory(story, attemptNumber) implemented. Calls kb_update_story_status
      advancing ready_for_qa → in_qa (canonical single-hop transition per story-state-machine.ts)
      BEFORE calling queue.add(). If KB advance returns updated:false, logs warning and returns
      early (F006 preservation). Enqueues ReviewJobDataSchema job. jobId: '{storyId}:review:{attempt}'.
      Unit tests HP-1 (ordering), ED-1 (jobId format), EC-1 (F006 skip) pass.
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-3
    status: PASS
    evidence_type: test
    summary: >-
      runOnce() extended with review dispatch branch. Calculates slotsRemaining after
      implementation dispatch, calls getEligibleReviewStories(), applies applyFinishBeforeNewStart
      and applyStrictFinishFilter with same config flags, dispatches up to slotsRemaining.
      Unit test HP-3 passes: "HP-3: dispatches review story when implementation slots are
      partially filled".
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-4
    status: PASS
    evidence_type: test
    summary: >-
      getEligibleQaStories(limit) implemented. SQL CTE queries WHERE s.state = 'in_qa'
      with same unresolved_deps structure. Dependency-blocked in_qa stories are excluded.
      Unit test HP-6 passes: "HP-6: returns stories in in_qa state from DB execute result".
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-5
    status: PASS
    evidence_type: test
    summary: >-
      dispatchQaStory(story, attemptNumber) implemented. F006 DEVIATION (ARCH-001/AC-14):
      does NOT call kb_update_story_status — no valid canonical intermediate state exists
      between in_qa and completed/failed_qa/blocked. BullMQ jobId deduplication
      ('{storyId}:qa:{attempt}') serves as idempotency fence. Enqueues QaJobDataSchema job.
      Unit test HP-2 passes: "HP-2: enqueues QA job WITHOUT calling kb_update_story_status".
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-6
    status: PASS
    evidence_type: test
    summary: >-
      runOnce() extended with QA dispatch branch after review. Calculates final slotsRemaining
      after implementation + review dispatch, dispatches QA stories up to remaining slots.
      Unit test HP-4 passes: "HP-4: dispatches QA story in same runOnce() cycle".
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-7
    status: PASS
    evidence_type: test
    summary: >-
      finishBeforeNewStart and strictFinishBeforeNewStart flags apply to all three dispatch
      stages uniformly. applyFinishBeforeNewStart() and applyStrictFinishFilter() called with
      review/QA eligible lists. Unit test ED-4 passes: "ED-4: strictFinishBeforeNewStart applies
      uniformly to review dispatch (AC-7)". Shared slot ceiling enforced across all 3 stages.
    files:
      - apps/api/pipeline/src/scheduler/index.ts
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-8
    status: PASS
    evidence_type: test
    summary: >-
      Unit test EC-1 passes: "EC-1: does NOT call queue.add when KB state advance returns
      updated:false (F006 preservation)". Also verified via HP-3 which confirms dispatchReviewStory
      is called during runOnce() when a slot is available after implementation dispatch.
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-9
    status: PASS
    evidence_type: test
    summary: >-
      Unit test HP-4 passes: "HP-4: dispatches QA story in same runOnce() cycle". Verifies
      dispatchQaStory is called with in_qa story when slots available after implementation
      and review stages.
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-10
    status: PASS
    evidence_type: test
    summary: >-
      Unit test EC-1 + HP-1 confirm dispatchReviewStory does NOT call queue.add when KB advance
      fails, and does call queue.add in correct order when KB advance succeeds. Exact mirror of
      PIPE-2040 AC-7 pattern.
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

  - ac_id: AC-11
    status: PASS
    evidence_type: test
    summary: >-
      Unit tests ED-1 (review jobId format) and QA jobId test pass. jobId formats:
      'STORY-001:review:1' and 'STORY-001:qa:1'. Retry format verified: 'PIPE-099:review:3',
      'PIPE-100:qa:3'. All 42 scheduler tests pass.
    test_file: apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts
    test_count: 42
    tests_passed: 42

  - ac_id: AC-12
    status: PASS
    evidence_type: command
    summary: >-
      pnpm --filter @repo/pipeline check-types exits 0. No TypeScript errors introduced.
      Verified by running tsc --noEmit on the main monorepo with updated scheduler/index.ts.
      All three new methods (getEligibleReviewStories, getEligibleQaStories, dispatchReviewStory,
      dispatchQaStory) type-check cleanly. ReviewJobDataSchema and QaJobDataSchema imported
      from @repo/pipeline-queue.
    command: pnpm --filter @repo/pipeline check-types

  - ac_id: AC-13
    status: PASS
    evidence_type: test
    summary: >-
      pnpm vitest run apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts: 42/42 tests
      pass. No regressions in existing tests. Pre-existing failures in other pipeline test suites
      (circuit-breakers, dispatch-router, supervisor-blockers, concurrency-controller) are caused
      by a pre-existing @repo/database-schema/schema/wint missing module issue — not introduced
      by this story.
    command: pnpm vitest run apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts
    test_count: 42
    tests_passed: 42

  - ac_id: AC-14
    status: PASS
    evidence_type: file
    summary: >-
      Architecture decision ARCH-001 documented prominently in dispatchQaStory() with multi-line
      comment block referencing PIPE-2050 AC-14, explaining the F006 deviation, the BullMQ jobId
      deduplication fence, and pointing to PIPE-2030 as the future fix. Unit test HP-2 explicitly
      asserts db.execute is NOT called during dispatchQaStory (confirming no KB advance).
    files:
      - apps/api/pipeline/src/scheduler/index.ts

files_modified:
  - apps/api/pipeline/src/scheduler/index.ts
  - apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts

test_summary:
  command: pnpm vitest run apps/api/pipeline/src/scheduler/__tests__/scheduler.test.ts
  total: 42
  passed: 42
  failed: 0
  pre_existing_failures: >-
    4 other pipeline test suites (circuit-breakers.test.ts, dispatch-router.test.ts,
    supervisor-blockers.test.ts, concurrency-controller.test.ts) fail due to pre-existing
    missing @repo/database-schema/schema/wint module dependency — not introduced by PIPE-2050.

architectural_decisions_implemented:
  - id: ARCH-001
    status: implemented
    notes: >-
      dispatchQaStory() skips KB state advance. F006 deviation documented in code comments.
      BullMQ jobId '{storyId}:qa:{attempt}' is idempotency fence. HP-2 test verifies no
      db.execute call during QA dispatch.
  - id: ARCH-002
    status: resolved_simpler
    notes: >-
      State machine check confirmed ready_for_qa → in_qa is a SINGLE valid hop.
      kb_update_story_status makes ONE call (not two as ARCH-002 warned).
      The two-hop concern (needs_code_review → ready_for_qa → in_qa) was for review
      DISPATCH trigger state. The story queries ready_for_qa and advances to in_qa directly.
