schema: 1
story_id: APRS-5040
phase: implementation
iteration: 0
timestamp: '2026-03-23T23:00:00Z'

acceptance_criteria:
  - ac_id: AC-1
    status: PASS
    evidence_type: test
    description: >-
      createCLIDecisionCallbackAdapter in adapters/plan-refinement/cli-decision-callback-adapter.ts
      satisfies the graph's DecisionCallback interface (ask(DecisionContext) returning
      {decision, confirmedFlowIds, rejectedFlowIds, reason}). Handles all 4 HiTL decisions:
      approve, edit, reject, defer. Type checked with zero errors.
    files:
      - packages/backend/orchestrator/src/adapters/plan-refinement/cli-decision-callback-adapter.ts
      - packages/backend/orchestrator/src/cli/run-plan-refinement.ts

  - ac_id: AC-2
    status: PASS
    evidence_type: test
    description: >-
      CLI prompt displays plan slug header, all flows with IDs and names, warnings,
      and errors. 4 inquirer choices (approve/edit/reject/defer). Approve flow populates
      confirmedFlowIds/rejectedFlowIds via checkbox prompt. Verified by 19 unit tests.
    files:
      - packages/backend/orchestrator/src/adapters/plan-refinement/cli-decision-callback-adapter.ts
      - packages/backend/orchestrator/src/adapters/plan-refinement/__tests__/cli-decision-callback-adapter.test.ts

  - ac_id: AC-3
    status: PASS
    evidence_type: test
    description: >-
      24 total tests passing: 19 unit tests (all 4 decision paths, prompt format, Ctrl+C handling)
      + 5 integration tests (graph.invoke() with bridged callback for all 4 HiTL outcomes
      + smoke test with stub planLoader). All tests pass in main monorepo.
    files:
      - packages/backend/orchestrator/src/adapters/plan-refinement/__tests__/cli-decision-callback-adapter.test.ts
      - packages/backend/orchestrator/src/adapters/plan-refinement/__tests__/cli-decision-callback-adapter.integration.test.ts

test_results:
  - command: pnpm test --filter @repo/orchestrator -- adapters/plan-refinement
    outcome: PASS
    details: '2 test files, 24 tests passed (19 unit + 5 integration)'

  - command: npx tsc --noEmit (grep plan-refinement|run-plan-refinement)
    outcome: PASS
    details: 'Zero type errors in all 4 new files'

  - command: pnpm eslint (source files only)
    outcome: PASS
    details: 'Zero lint errors on adapter and CLI entrypoint'

files_created:
  - path: packages/backend/orchestrator/src/adapters/plan-refinement/cli-decision-callback-adapter.ts
    action: created
    description: >-
      Bridge adapter creating graph-compatible DecisionCallback using inquirer directly.
      Handles approve (with flow checkbox), edit (no follow-up), reject (reason prompt),
      defer (reason prompt). Ctrl+C interruption handling for all prompts.

  - path: packages/backend/orchestrator/src/cli/run-plan-refinement.ts
    action: created
    description: >-
      CLI entrypoint following generate-stories.ts pattern. parseArgs (--plan-slug, --help),
      stub planLoader with INJECTION POINT comment, wires createPlanRefinementGraph with
      bridged callback, structured stdout output, exit codes 0/1.

  - path: packages/backend/orchestrator/src/adapters/plan-refinement/__tests__/cli-decision-callback-adapter.test.ts
    action: created
    description: >-
      19 unit tests covering all 4 decision paths, prompt format verification
      (plan slug, flow IDs, warnings, errors, 4 choices), and Ctrl+C interruption handling.

  - path: packages/backend/orchestrator/src/adapters/plan-refinement/__tests__/cli-decision-callback-adapter.integration.test.ts
    action: created
    description: >-
      5 integration tests wiring graph.invoke() with bridged CLI adapter + stub planLoader.
      Covers all 4 HiTL outcomes (approve, reject, defer, edit→approve) plus smoke test.
