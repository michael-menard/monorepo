{
  "name": "@repo/upload-types",
  "version": "0.0.1",
  "description": "Shared upload types and utilities for MOC upload features",
  "deprecated": "Moved to @repo/upload/types. See README for migration guide.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint --fix src/ --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "require": "./dist/index.js",
      "types": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ]
}
