{
  "name": "@repo/sidecar-rules-registry",
  "version": "1.0.0",
  "description": "Rules registry sidecar service — stores and retrieves enforceable rules with propose/promote lifecycle",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "start": "node dist/server.js",
    "seed:cohesion-rules": "node dist/seeds/cohesion-rules.js"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/sidecar-http-utils": "workspace:*",
    "drizzle-orm": "^0.44.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "25.2.1",
    "tsx": "4.19.3",
    "@vitest/coverage-v8": "2.1.9",
    "dotenv": "17.3.1",
    "typescript": "^5.8.0",
    "vitest": "^2.1.8"
  },
  "keywords": [
    "rules-registry",
    "sidecar",
    "mcp"
  ]
}
