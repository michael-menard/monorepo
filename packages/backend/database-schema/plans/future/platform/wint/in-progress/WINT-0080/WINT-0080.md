---
story_id: "WINT-0080"
title: "Seed Initial Workflow Data"
status: "in-progress"
created: "2026-02-16T20:00:00Z"
updated: "2026-02-16T20:00:00Z"
tags:
  - database
  - seed-data
  - reference-data
  - workflow
  - graph
acceptance_criteria: []
technical_notes: []
---

# WINT-0080: Seed Initial Workflow Data

Create idempotent seed data scripts that populate foundational reference tables with current workflow inventory:

1. **Workflow Phases**: Extract 8 phases from WINT stories index → `wint.phases` table
2. **CRUD Capabilities**: Seed 7 standard operations → `wint.capabilities` table
3. **Agent Metadata**: Parse all `.agent.md` files → `wint.agents` table
4. **Command Metadata**: Parse all command files → `wint.commands` table
5. **Skill Metadata**: Parse all skill directories → `wint.skills` table

All seed scripts will be transaction-safe, idempotent (safe to rerun), and validate data with Zod schemas before insertion.

## Implementation Notes

This story was generated using `--gen` flag and bypassed elaboration.

**Blocking issues resolved:**
- ✅ AC-11: Agent/command/skill table schemas added to wint.ts
- ✅ AC-12: Workflow.phases namespace clarified (wint.phases)
- ✅ AC-13: Schema namespace consistency enforced
- ✅ Migration 0025 applied successfully

**Dependencies:**
- WINT-0070 (Workflow Tracking Tables) - PENDING but schema exists
- WINT-0060 (Graph Relational Tables) - PENDING but capabilities table exists

**Key Requirements:**
- Seed scripts must be idempotent (INSERT ... ON CONFLICT DO NOTHING)
- All operations in database transactions
- Zod validation before insertion
- Parse ~143 agents, 33 commands, 14 skills, 8 phases, 7 capabilities

## Acceptance Criteria

The implementation will focus on:
1. Creating seed script infrastructure in `packages/backend/database-schema/src/seed/`
2. Implementing parsers for frontmatter and metadata extraction
3. Creating seeders for each table (phases, capabilities, agents, commands, skills)
4. Transaction management and idempotency guarantees
5. CLI entry point with `pnpm seed:wint` command
