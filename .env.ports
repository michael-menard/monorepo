# Port Configuration for Monorepo Services
# This file centralizes all port assignments for the development environment
# Copy this to .env.ports.local to override defaults for your local setup

# =============================================================================
# Application Service Ports
# =============================================================================

# Web Application (Frontend)
WEB_APP_PORT=3002

# Auth Service API
AUTH_API_PORT=9300

# LEGO Projects API
LEGO_API_PORT=9000

# Documentation Site (Docusaurus)
DOCS_PORT=3000

# =============================================================================
# Infrastructure Service Ports (Docker)
# These should generally not be changed as they're standard ports
# =============================================================================

# Database Ports
MONGODB_PORT=27017
POSTGRESQL_PORT=5432
REDIS_PORT=6379

# Search and Analytics
ELASTICSEARCH_PORT=9200

# Admin Tools
MONGO_EXPRESS_PORT=8081
PGADMIN_PORT=8082

# =============================================================================
# Development URLs (Auto-generated from ports above)
# =============================================================================

# Frontend URLs
WEB_APP_URL=http://localhost:${WEB_APP_PORT}
DOCS_URL=http://localhost:${DOCS_PORT}

# API URLs
AUTH_API_URL=http://localhost:${AUTH_API_PORT}
LEGO_API_URL=http://localhost:${LEGO_API_PORT}

# Infrastructure URLs
MONGODB_URL=mongodb://localhost:${MONGODB_PORT}
POSTGRESQL_URL=postgresql://localhost:${POSTGRESQL_PORT}
REDIS_URL=redis://localhost:${REDIS_PORT}
ELASTICSEARCH_URL=http://localhost:${ELASTICSEARCH_PORT}
MONGO_EXPRESS_URL=http://localhost:${MONGO_EXPRESS_PORT}
PGADMIN_URL=http://localhost:${PGADMIN_PORT}

# =============================================================================
# Port Arrays for Scripts (Bash-compatible)
# =============================================================================

# Application ports that need cleanup before starting
APP_PORTS="${WEB_APP_PORT} ${AUTH_API_PORT} ${LEGO_API_PORT}"

# All service ports for reference
ALL_PORTS="${WEB_APP_PORT} ${AUTH_API_PORT} ${LEGO_API_PORT} ${DOCS_PORT} ${MONGODB_PORT} ${POSTGRESQL_PORT} ${REDIS_PORT} ${ELASTICSEARCH_PORT} ${MONGO_EXPRESS_PORT} ${PGADMIN_PORT}"
