reusability:
  verdict: FAIL
  files_checked: 5
  errors: 1
  warnings: 0
  findings:
    - severity: error
      file: packages/backend/kbar-sync/scripts/__tests__/integration/sync-cli.integration.test.ts
      line: 1
      message: "Duplicated testcontainer setup code — identical PostgreSQL schema initialization exists in shared helper packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts. This file reimplements startKbarTestContainer, applyKbarSchema, createEnumSql, and createTempDir from scratch instead of importing from the shared module."
      rule: no-app-duplication
      auto_fixable: false
      existing_package: "@repo/kbar-sync (internal shared helper)"
      existing_path: "packages/backend/kbar-sync/src/__tests__/helpers/testcontainers.ts"
      recommendation: "Replace lines 18-169 with imports from testcontainers helper. Change: `import { startKbarTestContainer, stopKbarTestContainer, createTempDir, removeTempDir, type TestContainerContext } from '../../../src/__tests__/helpers/testcontainers.js'` and use startKbarTestContainer() instead of inline PostgreSqlContainer setup."
      duplicated_functions:
        - "PostgreSqlContainer initialization (lines 34-38)"
        - "pg.Client connection setup (lines 41-49)"
        - "Environment variable setup (lines 52-58)"
        - "Schema and table creation (lines 61-169)"
        - "Temporary directory creation (lines 173-174)"
      token_impact: "Eliminating this duplication would save ~150 lines of boilerplate code and reduce maintenance burden."
  summary: "Found 1 error: sync-cli.integration.test.ts duplicates testcontainer initialization code that already exists in a shared, documented helper module. The shared helper (testcontainers.ts) was created specifically for KBAR-0060 and is already used by kbar0060.integration.test.ts. Consolidation would improve code reusability and reduce duplication."
  tokens:
    in: 5000
    out: 1200
