reusability:
  verdict: FAIL
  files_checked: 1
  errors: 1
  warnings: 0
  findings:
    - severity: error
      file: apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools.test.ts
      line: 69
      message: "Duplicated test fixture factory — identical to artifact-tools-integration.test.ts. Extract to test-helpers.ts for shared use."
      rule: no-cross-file-duplication
      auto_fixable: false
      duplicated_in: "apps/api/knowledge-base/src/mcp-server/__tests__/artifact-tools-integration.test.ts (lines 68-99)"
      duplicated_functions:
        - createMockArtifact (lines 69-84)
        - createMockArtifactListItem (lines 86-100)
      recommendation: "Move both fixture factories to test-helpers.ts and import them in both test files. Example: export function createMockArtifact(overrides?: Partial<ArtifactResponse>): ArtifactResponse { ... }"
      impact: "Code duplication increases maintenance burden. Any bug fix or schema update requires changes in two places instead of one."
  tokens:
    in: 8500
    out: 350
