reusability:
  verdict: FAIL
  files_checked: 1
  errors: 2
  warnings: 1
  findings:
    - severity: error
      file: .claude/agents/pm-bootstrap-generation-leader.agent.md
      line: 82
      message: "Manual psql INSERT statement pattern — should use kb_upsert_plan or KB story MCP tool instead. This duplicates story insertion logic across multiple agents."
      rule: no-manual-db-operations
      auto_fixable: false
      existing_pattern: "kb_list_stories, kb_update_story_status, kb_write_artifact in shared KB integration pattern"
      existing_reference: ".claude/agents/_shared/kb-integration.md (lines 293-389)"
      recommendation: "Replace psql batch INSERT with kb_upsert_plan for plan inserts and use kb_write_artifact for story artifacts. Reference kb-integration.md section 'Story Artifact Read/Write (MANDATORY STANDARD)'."

    - severity: error
      file: .claude/agents/pm-bootstrap-generation-leader.agent.md
      line: 1
      message: "Missing kb_tools frontmatter declaration — agent does not declare KB tool dependencies but instructions reference context_cache_get and KB story inserts. Per kb-integration.md, all agents with KB interactions MUST declare tools in frontmatter."
      rule: missing-kb-tools-declaration
      auto_fixable: false
      existing_pattern: "kb_tools: [kb_write_artifact, kb_read_artifact, kb_list_artifacts] pattern used in other agents"
      existing_reference: ".claude/agents/_shared/kb-integration.md (lines 25-36, 357-368)"
      recommendation: "Add to frontmatter: kb_tools: [kb_write_artifact, kb_upsert_plan, kb_list_stories] and ensure narrative instructions align with tool capabilities."

    - severity: warning
      file: .claude/agents/pm-bootstrap-generation-leader.agent.md
      line: 175
      message: "Context Cache integration pattern (context_cache_get) documented but not declared as a tool dependency. Should clarify if this is a built-in or requires explicit MCP tool support."
      rule: ambiguous-tool-dependency
      auto_fixable: false
      existing_pattern: "pm-story-generation-leader.agent.md uses kb_search with explicit tool declaration"
      existing_reference: ".claude/agents/pm-story-generation-leader.agent.md (lines 12-14) and kb-integration.md (lines 40-65)"
      recommendation: "Either add context_cache_get to kb_tools if it's an MCP function, or document it as a built-in orchestrator capability separate from KB tools."

  tokens:
    in: 8500
    out: 1200
