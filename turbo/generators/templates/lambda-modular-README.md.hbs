# {{pascalCase name}} Lambda Function

{{description}}

## Overview

- **Domain:** {{domain}}
- **Authentication:** {{authType}}
- **Runtime:** Node.js 20.x
{{#if needsDatabase}}
- **Database:** PostgreSQL via @monorepo/db
{{/if}}

## Functionality

TODO: Describe what this Lambda function does

## API Endpoints

TODO: List the API Gateway routes that invoke this function

## Dependencies

{{#if needsDatabase}}
- `@monorepo/db` - Database client and schema
{{/if}}
- `aws-lambda` - AWS Lambda type definitions
- `uuid` - UUID generation

## Development

This function is part of the `lego-api-serverless` monorepo and deployed via SST.

### Local Testing

```bash
pnpm test
```

### Deployment

Deployed automatically via SST when changes are pushed to the main branch.
